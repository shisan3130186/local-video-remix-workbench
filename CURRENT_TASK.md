# CURRENT_TASK.md

> 最后更新：2026-07-21。只记录当前任务、真实中断点和下一步动作。

## 当前状态

“智剪 / SmartCut”第一版品牌、首页、三类工作区、AI配音可视入口和窗口/预览比例优化已于2026-07-21通过用户人工验收，并作为完整版本保存到本地Git；未推送GitHub。

## 本次完成范围

1. 首页三个入口分别进入`AI智能成片 / 批量混剪 / 视频工具`独立工作区。
2. 软件默认以居中`1320×860`窗口启动，允许缩放和最大化，但不再默认全屏；最小窗口为`1100×720`。
3. 首页主内容和新手引导栏重新调整宽度，避免2K全屏下内容过小、四周空白过多。
4. 工作台在常用窗口宽度下收窄左右栏，把主要空间让给中间预览。
5. AI工作区把视频预览提升为第一视觉中心；预览区最小高度提高，文案分镜区域缩短并内部滚动。
6. AI右侧设置改为`智能准备 / 声音与文字 / 画面与输出`三个分组，降低查找成本。
7. 批量混剪预览改为完整画面等比缩放，预览和组合规则约按35%/65%分配，不再遮挡右侧参数。
8. 视频工具预览改为完整画面；常用窗口和大屏下工具卡使用三列，全部分类更容易在一屏查看。
9. 左侧素材库、底部任务中心和所有原有业务事件继续复用。
10. AI工作区三栏比例按成熟视频编辑器的信息层级重排，竖屏预览区加高，黑色舞台使用约1.37:1比例随高度自动缩放；横屏和方形预览不受影响。

## 自动与桌面检查

- `corepack pnpm build`通过。
- `corepack pnpm tauri build --debug --no-bundle`通过。
- `启动验收.bat --check`通过。
- 最新竖屏预览收窄修改已再次通过前端生产构建、Tauri Debug构建和差异格式检查。
- 已使用独立临时预览程序核对真实新版窗口：1320像素宽窗口中，竖屏黑色舞台约643×480像素，视频高度明显增大，左右留黑比例与竞品截图按窗口缩放后的结果基本一致。
- 真实Tauri窗口已检查默认窗口比例、AI工作区、批量混剪和视频工具；竖屏视频按完整画面显示，AI预览区域明显增大。
- 2026-07-21用户确认人工验收通过。

## 固定边界

- 没有修改Rust VideoEngine、FFmpeg命令、AI/TTS/ASR协议、SQLite和项目快照结构。
- 没有复制竞品品牌、图标和具体文案，只参考左侧素材、中间预览文案、右侧分组设置的信息层级。
- `docs/icon-candidates/`是任务开始前已经存在的未跟踪目录，本次没有修改。
- 本轮只提交本地Git，暂不推送GitHub。

## 下一动作

等待用户确认是否进入第四阶段用户测试包准备。推荐顺序：内置精简版FFmpeg/FFprobe → 首次启动环境检测 → 崩溃日志与诊断信息 → 干净Windows安装测试。
