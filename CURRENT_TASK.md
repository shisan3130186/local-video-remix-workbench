# CURRENT_TASK.md

## 2026-08-06 竞品复刻第一阶段：UI 基准冻结

状态：已完成，进入共用工作台外壳逐页对齐。

本轮将用户提供的竞品拆解、两段演示视频、17 张关键帧和现有双尺寸验收图整理为 `docs/research/` 基准库，包含 UI 尺寸与三栏规格、页面结构、交互状态、资源台账、精确范围和验收矩阵。

后续开发必须以 `1440×900`、`1280×720` 两种尺寸和“空状态 / 有素材 / 任务中 / 结果或失败”四类状态逐页验收；原始演示视频继续只保留在 `E:\Workspace\竞品素材库`，不复制到项目或安装包。

## 2026-08-06 共用工作台外壳：顶栏工作区导航

状态：首个外壳子步骤完成，继续处理任务抽屉和底部任务区。

已将已有 `WorkspaceModeNav.vue` 挂载到工作台顶栏，支持 AI 成片、批量混剪和效率工具直接切换，并保持当前工作区状态、权限检查和抽屉状态清理逻辑不变。

验证：`corepack pnpm build` 通过；浏览器在 1440×900 与 1280×720 下检查导航、三种工作区切换、深色主题和横向溢出，均通过。

## 2026-08-06 共用工作台外壳：任务与结果抽屉

状态：首轮对齐完成，继续处理底部任务操作区。

已将任务日志、导出结果和批量生成结果从右下角浮层改为统一右侧全高抽屉；任务队列保持相同的顶栏下方进入方式。抽屉宽度按用途区分，浅色和深色主题沿用主题覆盖。

验证：`corepack pnpm build`、`git diff --check`通过；1280×720下验证结果抽屉、任务队列、深色主题和无横向溢出。

## 2026-08-06 验收结果

用户已完成重新启动和真实账号登录，验收通过。本轮待提交本机 Git，不推送 GitHub。

## 2026-08-06 启动脚本端口占用处理

已将 Tauri 开发启动前置命令改为检查 1420 端口：已有 Vite 服务时复用，没有服务时才启动，避免旧服务占用端口导致脚本报错。会员登录验签仍等待用户重新登录确认。

## 2026-08-06 会员登录故障处理

状态：已同步线上签名密钥配置，等待用户重新登录验收。

登录出现“账号状态签名校验失败”时，已确认桌面端内置公钥与本机发布公钥一致，并通过项目安全同步脚本将线上会员服务签名私钥重新同步到对应密钥对。未清除账号缓存、设备标识或 API Key；验收通过前不提交 Git。

## 2026-08-06：个人中心、会员兑换与 API/TTS 设置

状态：代码已完成，等待用户人工验收。

本轮完成：会员有效期显示精确到秒；兑换卡密前增加设备绑定确认；会员账号保存设备绑定状态并限制每月最多 2 次换绑；个人中心 API 设置收缩为火山引擎、Ark API Key、TTS API Key 三项可见内容；模型使用内置默认值，密钥继续使用 Windows 加密保存。

本轮验证：`http://127.0.0.1:1420` 可正常打开；1280×720 下页面无横向溢出；浅色与深色主题可往返切换；前端构建、会员服务测试和 Rust 检查已通过。登录服务未部署，无法在浏览器中进入真实已登录会员页面，需用户用真实账号验收个人中心和兑换确认弹窗。

未提交 Git，等待用户验收。

> 最后更新：2026-08-06。只记录当前状态、真实中断点和下一步动作。

## 当前状态

2026-08-06 完成项目瘦身：删除约22.78GB的 `src-tauri\target` 编译缓存，并删除可重新安装/生成的 `node_modules` 与 `dist`；保留源码、内置FFmpeg资源和测试结果。`.gitignore` 已修正为忽略真实的 `target/` 目录。重新开发前需运行 `corepack pnpm install`，重新构建前需运行项目固定构建命令。

AI 混剪竞品式工作流本轮开发完成，等待用户人工验收。已补齐自定义 / 文案 / 音频三种入口、独立任务输出目录、成片结果预览、任务目录打开和剪映草稿工程包整理；没有改动会员、首页其他功能或非 AI 混剪模块。

竞品式六卡片首页、效率工具、竖屏预览和三模式主题均保持不变。本轮正式功能使用权限检查已完成：个人中心显示真实到期日期和“有效 / 已过期”状态；卡密、AI API Key、TTS服务任一条件缺失时，真实Tauri桌面程序会阻止进入正式功能并显示“没有权限”提示。卡密管理器复制兑换码的剪贴板异常已修复（退避重试 + 失败弹窗手动复制兜底）。V0.5.0新电脑测试安装包已生成，用户在独立新电脑完成人工验收；本轮已提交本地Git，不推送GitHub。

## 已完成验证

- 国内公网会员全流程11项通过。
- 服务端TypeScript检查和8项单元测试通过。
- 前端生产构建通过。
- Rust格式、编译、严格Clippy和138项测试通过。
- 卡密管理器XAML加载、管理员接口连接、生成、列表和停用回归通过。
- V0.5.0安装包115.95MB，SHA256为`5b24f3cdd443260ac4b412bf5bb8662a66ccc8c4e13d4beb3e620c7c17a2b6eb`。
- 2026-08-05新版UI与权限门禁新电脑测试包115.97MB，SHA256为`bcef22fb5407cf7b31d3de5b9f09b1ab7220da2ed477ddff49477b1587d88316`；本机隔离安装、Windows GUI类型和内置资源检查通过。
- V3前端`corepack pnpm build`、Vue/TypeScript检查和`git diff --check`通过。
- 真实Tauri桌面程序已检查首页、AI成片、批量混剪、效率工具、视频效果、视频内容提炼和五项新增工具。
- 文案改写真实AI调用返回3个版本，并已验证“用于AI成片”后文案自动带入且在未导入素材时仍可见、可编辑。
- 批量改名真实完成3个文件；字幕识别得到7条中文字幕并成功导出校对后的SRT/TXT；大字报输出1080×1920 PNG；两张图片均成功生成1080×1920、30fps、5秒MP4。
- 1440×900与1280×720检查通过；导入、切片、AI成片和新增工具中的视频/图片预览区域均可见。
- 第二轮对照确认AI页为348px/弹性/304px三栏；1280×720下预览674×430，页面和右栏均无横向溢出。
- 第二轮对照图保存在`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台`。
- 测试结果统一保存在`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-08-04_竞品布局功能验收`。
- 真实`测试2.mp4`为1080×1920且无旋转元数据；1280×720和1440×900下预览均保持9:16并完整显示。
- 红框修复截图保存在`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-08-04_红框显示修复`。
- 主题选择会在本机记忆；跟随系统、浅色和深色模式均可切换，刷新后状态保持。
- 浅色主题已检查首页、AI工作台、二级语音设置弹窗和个人中心；视频预览画布继续保持黑色。
- 主题验收截图保存在`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-08-04_主题切换验收`。

## 固定边界

- 不修改或提交用户原有`docs/icon-candidates/`。
- 私钥、管理员令牌、密码、完整会话令牌和可用兑换码禁止提交Git。
- 本轮只提交本地Git，不推送GitHub。

## 本轮验证

- `corepack pnpm build` 通过。
- `cargo fmt -- --check`、`cargo check`、`cargo clippy --all-targets -- -D warnings` 通过。
- `cargo test` 通过，138 项测试全部通过。
- 1440×900 与 1280×720 本地界面检查通过；视频预览区域可见，三种 AI 混剪输入模式可见，页面无横向溢出。

## 当前任务

本轮 AI 混剪改造等待用户人工验收，重点检查：输入音频识别后带入文案、生成任务目录结构、批量结果预览、打开任务目录和剪映草稿导出。根目录一键启动脚本 `启动智剪.bat` 已修复为纯 ASCII 批处理内容，兼容 Windows cmd.exe。

三个新电脑或日常使用中反馈的 Bug 已全部修复，等待用户人工验收：

1. **音色试听失败**：Tauri WebView2 autoplay 策略阻止首次播放，已改为共享 audio 元素复用 + catch 块保留实例供重试。
2. **音色只有 25 个**：已扩充到 137 个真实火山音色 ID（覆盖中/英/日/西/印尼语；10 种场景分类）。
3. **任务进度侧边栏缺失**：已新增 `TaskQueuePanel.vue`（右侧 320px 可折叠面板），同时显示当前 activeTask 进度 + 最近 30 条历史任务状态。

`vue-tsc --noEmit` 通过零错误；用户人工验收后再决定提交策略（按规范仍只提交本地 Git，不推送 GitHub）。

## 本轮真实中断点

- 当前没有代码阻塞，最新Debug桌面程序正在运行，停在浅色新版六卡片首页，等待用户人工验收。
- 当前测试账号到期日期为`2026/08/01`，在`2026-08-05`显示红色“已过期”；本机AI Key和TTS已配置，因此点击正式功能时只提示缺少有效卡密。
- 点击权限提示“确定”后会进入个人信息/卡密兑换页；如果仅缺AI或TTS，则进入API Key页。
- 登录、卡密兑换、API配置、主题、新手教程和诊断等必要入口不会被权限门禁锁住。
- 真实Tauri桌面程序执行权限检查；浏览器开发预览不锁功能，避免前端开发调试被本机商业状态阻断。
- 一次误显示旧版首页并非源码回退，而是同一Tauri应用标识复用了旧实例。已经按真实进程路径确认当前窗口来自项目最新Debug程序，背后为新版六卡片首页。
- 文案改写、批量改名、字幕编辑、大字报、图片转视频均已完成真实输入和真实输出验证。
- 原V2批量五步流程和AI来源步骤栏已删除，替换为竞品式三栏直达工作台；真实预览、AI分镜、视频效果和二级设置均已回归。
- 使用`design-taste-frontend`与UI/UX规则完成交付前审查；补充了未导入素材时仍可见的成片文案草稿区，解决跨工具带入文案后用户看不到内容的问题。
- 浏览器视觉检查发现组件内CSS会在浅色模式下留下深色输入框和音色卡片，已使用主题最终覆盖层修复并复测通过。
- 2026-08-05进一步定位到旧版全局表单规则带`!important`，导致部分输入区只更新边框而背景仍为黑色；浅色主题现已用统一高优先级语义覆盖修复。
- 水印工具的操作按钮已改为浅色面板与青绿选中态，预览区继续深色；新手教程和诊断共用向导已覆盖侧栏、卡片、提示和按钮。
- 首页分类状态已提升到`App.vue`保存；文件批量改名、字幕识别、大字报设计和图片转视频逐项返回后均保持在效率工具页。
- 本轮验收截图保存在`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-08-05_主题与返回修复验收`。

## 暂停中的安装回归

- 当前电脑已检测到虚拟化底层正在运行，但没有Windows沙盒程序、Hyper-V管理工具或现成Windows虚拟机。
- 已在测试结果库创建`2026-07-24_V0.5.0干净Windows安装卸载回归`，包含沙盒配置、零基础说明、人工验收清单和结果记录。
- 沙盒配置会只读映射V0.5.0安装包和测试素材，并把成片、截图写回真实电脑的`sandbox-output`目录。
- 用户已于2026-07-24启用Windows沙盒并重启，沙盒进程已经成功启动。
- 当前等待用户在沙盒内完成人工主链路与卸载回归，尚不能标记为通过。

## 下一步

1. 用户在独立新电脑或日常使用中验证三个修复（音色试听 / 137 音色筛选和试听 / 任务队列侧边栏折叠和清空）。
2. 验收通过后检查Git差异与敏感信息，按规范只提交本轮修复 + 文档，不包含`.workbuddy/`与`docs/icon-candidates/`。
3. 只提交本地Git，不推送GitHub。

## 2026-08-06 音色库目录与试听兼容

状态：开发与自动检查完成，用户回来后统一验收。

已核对原目录实际只有 53 条，不是历史记录中的 136/137 条。本轮基于火山公开音色资料补齐为 **136 条去重 speaker ID**，覆盖 2.0、1.0、角色、客服和多语种音色；头像继续优先使用已有本地资源或官方 URL，加载失败时回退为本地渐变文字头像，不批量下载素材到安装包。

试听规则改为：优先播放官方样音；官方样音任意加载失败时自动调用已配置的豆包实时 TTS；同一音色试听只保存在本次窗口内存缓存；连续切换时以最后一次点击为准，旧请求不会抢回播放。

Rust 端会按 speaker ID 自动将 `*_uranus_bigtts` / `saturn_*` 走 `seed-tts-2.0`，将 `*_mars_bigtts`、`*_moon_bigtts` 和 `ICL_*` 走 `seed-tts-1.0`。正式配音与试听共用该规则，避免选择旧音色后导出失败。

本轮验证：目录静态校验 136 条且无重复；`corepack pnpm build`、`cargo fmt -- --check`、`cargo check`、`cargo clippy --all-targets -- -D warnings` 通过；`cargo test` 139 项通过；1440×900、1280×720 和浅/深色主页无横向溢出。浏览器空状态会按既有规则禁用音色选择，真实豆包合成和弹窗试听待用户带素材的桌面端统一验收。

下一步：继续共用工作台外壳的底部任务操作区和 AI 成片流程的竞品对齐；用户回来后集中验证音色试听、筛选、头像回退和真实导出。

## 2026-08-06 共用工作台外壳：底部任务操作区

状态：开发完成，继续进入 AI 成片流程对齐。

本轮完成：

- 将 `TaskControlBar.vue` 挂载到工作台底部，工作台固定为顶栏、三栏内容区、底部任务区三段布局。
- 底部任务区接入真实当前任务状态、阶段、进度百分比、素材/完成/失败数量和输出目录。
- 保留取消任务、失败重试、日志抽屉、导出结果抽屉、批量结果入口和临时文件清理。
- 移除右侧设置栏重复的“创建任务 / 任务日志 / 导出结果”区域，避免同一操作出现两套入口。

本轮验证：1280×720 页面检查通过；底部任务栏可见，右侧设置栏恢复完整高度，任务按钮与抽屉入口可见，无横向溢出。下一步继续处理 AI 成片的分镜结果、生成中状态和结果预览联动。

## 2026-08-06 AI 成片结果预览与任务终态

状态：开发完成，继续进入结果联动与生成中状态对齐。

本轮完成：

- 底部任务区在任务完成、失败、取消后保留任务名称与终态，不再直接回退到“准备生成视频”。
- 失败任务显示后端返回的错误信息；完成或取消时显示对应结果说明，清理临时文件反馈仍保持优先展示。
- 批量结果抽屉增加当前成片大预览，结果列表支持点选切换；保留打开任务目录和剪映草稿导出入口。

本轮验证：前端构建和 `git diff --check` 通过；1280×720 实际界面检查通过，结果抽屉空状态、任务底栏和主预览均无横向溢出，浏览器错误日志为空。

## 2026-08-06 AI 分镜区独立滚动

状态：开发完成，继续进入任务状态与 AI 生成过程联动。

本轮完成：

- 中栏文案/分镜区在内容增多时独立滚动，避免分镜列表、备选画面和生成按钮被固定工作台高度裁掉。
- 不改变上方视频预览的比例和可见面积，保持任务底栏固定在窗口底部。

本轮验证：前端构建和 `git diff --check` 通过；1280×720、1440×900 实际页面检查通过，页面宽度分别保持 1280px、1440px，无横向溢出。
