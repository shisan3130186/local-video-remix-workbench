# CURRENT_TASK.md

> 最后更新：2026-07-24。只记录当前任务、真实中断点和下一步动作。

## 当前状态

V0.4.1“后台视频命令不弹终端”修复已经完成代码开发、自动检查、正式安装包构建、隔离安装、真实项目恢复验证和2026-07-24用户人工验收。本轮随当前本地Git提交保存，不推送GitHub。

## 问题原因

智剪主程序已经是Windows桌面应用，但导入、读取信息、缩略图、切片、字幕、GPU检测和导出仍有多处直接启动FFmpeg/FFprobe。Windows会为这些后台子进程临时创建控制台窗口，因此出现反复闪黑窗。

## 本次修改

1. 在`video_engine/tool_paths.rs`新增统一`background_command`后台命令启动器。
2. Windows统一使用`CREATE_NO_WINDOW`标记启动FFmpeg/FFprobe；其他系统保持原行为。
3. 视频信息读取、音频检测、缩略图、切片任务、混剪探测、配音探测、字幕能力检查和GPU编码测试全部改用统一入口。
4. 保留现有任务进度、错误输出、任务取消和临时文件清理，不修改视频参数与混剪算法。
5. 版本升级为V0.4.1，避免测试用户误装仍会弹终端的V0.4.0安装包。

## 自动与桌面检查

- `cargo fmt -- --check`通过。
- `cargo check`通过。
- `cargo clippy --all-targets -- -D warnings`通过。
- `cargo test`通过，共129项测试，0失败。
- `corepack pnpm tauri build`通过。
- V0.4.1安装包大小115.84MB。
- SHA256：`b05e6b4d6db0c3b78b7c3987715141e6fb65e0370fdb18c94ed3b64dee039a31`。
- 隔离安装成功，主程序为Windows GUI，内置FFmpeg与隐私文件存在。
- 真实恢复包含7个原视频和16个切片的项目，连续读取视频信息与缩略图时未出现终端窗口。
- 未重新执行会覆盖现有片段的切片操作，留给用户人工验收。

## 人工验收入口

`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-07-22_V0.4.1后台命令无终端弹窗修复\智剪 SmartCut_0.4.1_x64-setup.exe`

人工重点检查：打开软件、导入视频、切片、导出过程中不再出现黑色终端窗口。

## 固定边界

- 未修改AI/TTS/ASR协议、SQLite、项目快照或FFmpeg视频参数。
- 未提交FFmpeg大型运行文件、安装包、密钥或本地配置。
- `docs/icon-candidates/`是用户原有未跟踪目录，本次不修改、不提交。

## 下一动作

V0.4.1提交后暂停开发。推荐下一步做“独立干净Windows电脑完整主链路与卸载回归”，但必须先找用户确认；之后再进入代码签名或会员商业化能力。
