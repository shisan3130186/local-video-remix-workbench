# CURRENT_TASK.md

> 最后更新：2026-07-18。只记录当前任务、真实中断点和下一动作。

## 当前唯一任务

“音频输入 + 火山云端ASR + 精简本地文案库联动”已经通过用户人工验收，正在完成本地Git收尾，不推GitHub。

## 本次已经完成

1. 独立“语音识别”入口支持本地音频和带声音视频。
2. 返回完整文字、句子级时间轴，并使用版本化缓存避免重复付费。
3. ASR结果新增“保存到文案库”和“保存并使用到当前文案”。
4. “文案与画面分镜”顶部新增“文案库”按钮。
5. 文案库列表只保留“使用这条文案”和“删除”两个动作。
6. 文案独立保存到Windows应用数据目录中的版本化JSON，不修改SQLite，不复制音视频文件。
7. 同一来源和相同文字重复保存时不会生成重复记录。
8. 使用文案前如果当前文案已有内容，会先明确询问是否替换。
9. 超过4000字的长文案仍可保存，但不会直接塞入当前AI分镜文案，避免接口超限。

## 自动检查

- `corepack pnpm build`：通过。
- `cargo fmt -- --check`：通过。
- `cargo check`：通过。
- `cargo clippy --all-targets -- -D warnings`：通过。
- `cargo test`：104项通过，0项失败。

## 人工验收结果

2026-07-18用户确认ASR真实识别、本地文案库保存和文案联动验收成功。

## 下一动作

提交本地Git，不推GitHub。提交后等待用户确认下一项功能，不直接继续开发。
