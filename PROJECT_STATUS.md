# PROJECT_STATUS.md

## 2026-06-21 补充记录：进入 V0.3.6 阶段验收与整理
状态：已确认进入 V0.3.6，不直接开发新功能，先做阶段验收。
本次目标：统一验收 V0.3 已完成能力，包括基础导入预览、固定切片、随机混剪、批量生成、画布比例、平滑混剪、基础视频效果、画中画、封面帧 / 片段预览、分类混剪、新手 / 高级双模式 UI。
执行原则：1. 先验收主链路。2. 再验收 V0.3 新增能力。3. 最后检查旧功能是否被破坏。4. 发现问题只做小步修复。5. 暂不开发 AI 自动分类、字幕识别、TTS、API Key、会员卡密、复杂时间线或新的大 UI 重构。6. 不绕过 VideoEngine 直接拼接 FFmpeg 命令。
当前下一步：按 `TODO_NEXT.md` 的 V0.3.6 验收顺序开始人工验收。

## 2026-06-21 补充记录：片段分类支持点击视频预览
状态：已由用户人工验收通过，准备提交 Git。
本次完成：1. 左侧高级模式的片段分类列表支持点击片段。2. 点击 `segment_000.mp4`、`segment_001.mp4` 等片段后，中间大视频预览区会直接切换播放该片段。3. 当前选中的片段行会高亮，方便用户知道正在看哪个片段。4. 分类下拉框继续保留，点击下拉框只修改分类，不会误触发片段切换。5. 点击左侧原视频时会回到原视频预览。6. 本次只修改 Vue 前端状态联动和 CSS，没有修改 Rust / FFmpeg / VideoEngine，也没有修改混剪生成逻辑。
验证情况：- `corepack pnpm build` 已通过。- 未运行 `cargo check`，因为本次没有修改 Rust / FFmpeg / VideoEngine。
人工验收结果：用户已确认功能可用，可以提交 Git。

## 2026-06-21 当前状态摘要

当前版本：
v0.3.5-dev

当前定位：
本地桌面端 AI 视频批量混剪工作台。

当前状态：
用户已多轮人工验收，现有主链路可正常使用。V0.3.5 分类混剪第一版已开发完成；本次已按参考视频和截图完成新手/高级双模式 UI 第一轮简化，等待用户手动验收。

当前已完成主链路：
1. 视频导入。
2. 视频预览。
3. 固定时长切片。
4. 随机片段抽取。
5. 片段拼接导出。
6. 批量生成混剪结果。
7. 画布比例和模糊背景。
8. 平滑混剪。
9. 基础视频效果。
10. 画中画。
11. 封面帧 / 片段预览图。
12. 分类混剪第一版。

当前唯一下一步：
手动验收新手/高级双模式 UI 和 V0.3.5 分类混剪第一版。

本阶段继续禁止：
1. 不做 AI 自动分类。
2. 不做字幕识别。
3. 不做 TTS 配音。
4. 不做 API Key 系统。
5. 不做会员卡密系统。
6. 不做 UI 大重构。
7. 不绕过 VideoEngine 直接拼接 FFmpeg 命令。

整理说明：
本摘要用于解决旧文档中 `v0.2.0-dev`、V0.3 多个验收记录和下一步任务混杂的问题。历史记录继续保留在下方，当前执行入口以 `TODO_NEXT.md` 为准。

## 2026-06-21 补充记录：新手/高级双模式 UI 第一轮

状态：已开发，等待用户手动验收。

本次完成：
1. 默认进入工作台新手模式，不再先停留在模块首页。
2. 新手模式只突出四步流程：导入素材 -> 预览视频 -> 开始处理 -> 查看结果。
3. 左侧改为清爽素材树，导入视频和导入文件夹保留为顶部小按钮，不再占用大面积。
4. 中间保留最大视频预览区，底部流程区压缩为简洁状态条。
5. 右侧默认只显示本地引擎状态、模式切换、新手流程、高级设置入口和结果入口。
6. 高级模式继续保留混剪设置、画布设置、视频效果、画中画、封面、字幕、音频、BGM、TTS、水印等入口。
7. 底部任务栏在新手模式只保留输出目录、处理状态和开始处理按钮；高级模式继续显示更多导出参数。
8. 本次没有修改 Rust / FFmpeg / VideoEngine，没有改视频处理逻辑。

验证情况：
- `corepack pnpm build` 已通过。
- 未运行 `cargo check`，因为本次没有修改 Rust / FFmpeg / VideoEngine。

待人工验收：
1. 默认打开是否足够简洁。
2. 左侧素材区是否不再挤占空间。
3. 中间预览区是否足够大。
4. 右侧是否不再堆满参数。
5. 点击“进入高级”后，旧功能入口是否仍然能找到。
6. 原有导入、预览、切片、混剪、批量生成、结果查看入口是否仍正常。

## 2026-06-21 补充记录：V0.3.5 分类混剪第一版

状态：已开发，等待用户手动验收。

本次完成：
1. 已切片片段支持手动选择分类。
2. 支持分类：开头钩子、产品展示、使用过程、细节特写、效果展示、结尾引导。
3. 分类信息保存在当前页面状态中，切换片段和抽取片段时不会丢失。
4. 新增“分类混剪”按钮。
5. 分类混剪按固定顺序抽取片段：开头钩子 -> 产品展示 -> 使用过程 -> 细节特写 -> 效果展示 -> 结尾引导。
6. 每个分类默认随机抽取 1 个片段。
7. 空分类会自动跳过，并写入任务日志。
8. 至少 2 个分类有片段时，会复用现有 VideoEngine 拼接导出能力生成 mp4。
9. 分类混剪结果会进入导出结果列表。
10. 继续保留画布比例、模糊背景、平滑混剪、基础视频效果、画中画、输出目录和任务日志。

验证情况：
- `corepack pnpm build` 已通过。
- `cargo check` 已通过。

待人工验收：
1. 切片后给不同片段选择不同分类。
2. 至少设置两个分类后点击“分类混剪”。
3. 检查任务日志是否显示每个分类抽中了哪个片段。
4. 检查空分类是否自动跳过。
5. 检查导出结果是否进入导出结果列表。
6. 复测随机抽取、拼接抽中片段、批量生成、画布比例、模糊背景、平滑混剪、基础视频效果和画中画。

## 2026-06-14 补充记录：V0.3.4 关键帧 / 封面帧 / 片段预览第一版

状态：已开发，等待用户手动验收。

本次完成：
1. 新增后端抽帧能力，通过 VideoEngine 封装 FFmpeg 生成 jpg 预览图。
2. 导入视频后自动尝试生成素材封面帧。
3. 切片成功后自动为片段生成预览图。
4. “视频封面 / 视频帧操作”二级设置面板支持输入秒数并设为当前封面帧。
5. 左侧素材列表显示视频封面，片段列表和已抽取片段显示预览图。
6. 中间信息区显示当前视频封面帧。
7. 不做复杂关键帧动画，不改字幕、TTS、会员、API Key。
8. 未修改混剪、基础效果、画中画导出链路。

验证情况：
- `corepack pnpm build` 已通过。
- `cargo check` 已通过。

待人工验收：
1. 导入视频后素材卡片是否出现封面图。
2. 切片后片段列表是否出现预览图。
3. 随机抽取后已抽取片段是否继续显示预览图。
4. 打开“视频封面”或“视频帧操作”，输入秒数后是否能生成并替换封面。
5. 原有导入、预览、切片、随机抽取、拼接、批量生成、画中画入口是否仍正常。

## 2026-06-14 补充记录：V0.3.3 画中画第一版

状态：已开发，等待用户手动验收。

前置验收：
1. V0.3.1 平滑混剪已由用户手动测试确认可用。
2. V0.3.2 基础视频效果已由用户手动测试确认可用。

本次完成：
1. 右侧画中画入口已接入二级设置窗口。
2. 支持选择叠加视频或图片作为画中画素材。
3. 支持启用 / 关闭画中画。
4. 支持位置：左上、右上、左下、右下、居中。
5. 支持大小比例：0.2 到 0.5。
6. 支持透明度：0 到 1。
7. 支持边距：0 到 240。
8. 画中画接入拼接抽中片段和批量生成混剪。
9. 继续保留画布比例、模糊背景、平滑混剪、基础视频效果、导出结果列表和任务日志。

验证情况：
- `corepack pnpm build` 已通过。
- `cargo check` 已通过。

待人工验收：
1. 不开启画中画时，原有拼接和批量生成仍正常。
2. 使用视频作为画中画素材导出。
3. 使用图片作为画中画素材导出。
4. 测试 5 个位置是否正确。
5. 测试大小比例、透明度、边距是否生效。
6. 与 16:9 横屏 + 模糊背景一起测试。
7. 与平滑混剪、基础视频效果一起测试。

## 2026-06-14 补充记录：V0.3.2 基础视频效果第一版

状态：已开发，等待用户手动验收。

前置验收：
1. V0.3.1 平滑混剪已由用户手动测试确认可用。

本次完成：
1. 拼接抽中片段和批量生成混剪支持基础视频效果参数。
2. 保留原有水平镜像，并新增垂直镜像。
3. 新增旋转：顺时针 90°、逆时针 90°、180°。
4. 新增亮度、对比度、饱和度调整。
5. 新增轻微缩放，范围限制为 1.00 到 1.20。
6. 继续保留已有视频变速能力，范围仍为 0.5x 到 2.0x。
7. 所有效果继续走 VideoEngine 统一封装，不在前端拼接 FFmpeg 命令。
8. 不破坏画布比例、模糊背景、平滑混剪、导出结果列表和任务日志入口。

验证情况：
- `corepack pnpm build` 已通过。
- `cargo check` 已通过。

待人工验收：
1. 单独测试垂直镜像。
2. 单独测试 90° / 180° 旋转。
3. 单独测试亮度、对比度、饱和度。
4. 单独测试 1.05x 或 1.10x 轻微缩放。
5. 测试这些效果与 16:9 横屏 + 模糊背景一起使用。
6. 测试这些效果与平滑混剪一起使用。

## 2026-06-14 补充记录：UI v3 高还原重构

状态：已开发，等待用户手动验收。

本次完成：
1. 首页保持模块卡片入口，不直接进入工作台。
2. 首页补齐图标占位、标题、说明文案、功能标签和状态按钮。
3. 顶部标题栏补齐用户图标、菜单图标和窗口控制区占位。
4. 工作台保持左素材、中预览、右功能入口、底部导出参数四区结构。
5. 中间预览区右上角增加视频裁剪、添加文本入口。
6. 视频文案区放在中间视频预览下方，包含文案 1、新增入口、大文本框、字幕模式和字数提示。
7. 左侧素材区补充文件夹树。
8. 右侧改为功能入口卡片，不展开复杂参数。
9. 底部导出栏补充进度条和日志、导出结果、批量结果入口。

验证情况：
- `corepack pnpm build` 已通过。

本次未修改：
1. 没有修改 Rust / FFmpeg 后端。
2. 没有开发新的视频处理功能。
3. 没有复制参考软件名称、Logo、品牌文案或图标素材。

## 2026-06-14 补充记录：UI v3 对标结构与尺寸定稿

状态：已开发，等待用户手动验收。

本次完成：
1. 首页改为模块入口结构，包含创作中心、效率工具、自动化三个分类。
2. 首页保留当前项目自己的软件名称，不复制竞品软件名、Logo、品牌文案和图标素材。
3. 工作台采用专业视频处理软件常见结构：左侧素材区、中间预览和核心操作区、右侧功能入口区、底部任务处理区。
4. 左侧栏约 340px，右侧栏约 320px，底部任务条约 104px，主预览区保留最大可用空间。
5. 右侧只保留功能入口，复杂参数继续进入 480px 到 640px 的二级设置窗口。
6. 底部任务区包含输出格式、分辨率、帧率、命名规则、线程、进度信息和开始处理入口。
7. 保留导入视频、导入文件夹、视频预览、切片、随机抽取、拼接抽中片段、批量生成、画布比例、模糊背景、平滑混剪、输出目录、导出结果列表、任务日志等入口。

验证情况：
- `corepack pnpm build` 已通过。

本次未修改：
1. 没有修改 Rust / FFmpeg 后端逻辑。
2. 没有新增真实字幕、TTS、会员、API Key、画中画或视频效果处理能力。
3. 没有复制竞品品牌资源。

## 2026-06-14 补充记录：UI v3 主工作台 + 二级设置窗口

状态：已开发，待用户手动验收。

完成：
1. 主窗口保持三栏结构：左侧素材和输出目录，中间视频预览和核心操作，右侧功能入口列表。
2. 右侧不再展开堆叠全部参数，改为“混剪设置、画布设置、视频效果、平滑转场、画中画、镜像旋转、视频变速、字幕设置、导出设置”等入口。
3. 点击入口后打开二级设置弹窗，参数区域内部滚动，不挤压主页面。
4. 任务日志、导出结果、批量生成结果改为底部浮层抽屉，需要时展开，不再占用固定底部高度。
5. 拆分前端组件：HomePage、MaterialPanel、PreviewPanel、RightToolPanel、ToolSettingModal、ExportResultDrawer、TaskLogDrawer、BatchResultDrawer。
6. 保留导入视频、导入文件夹、视频预览、切片、随机抽取、拼接抽中片段、批量生成、画布比例、模糊背景、平滑混剪、输出目录、导出结果列表和任务日志入口。

验证情况：
- `corepack pnpm build` 已通过。

本次未修改：
1. 没有修改 Rust / FFmpeg 后端逻辑。
2. 没有新增真实字幕、TTS、会员、API Key 或画中画处理能力。

## 2026-06-14 补充记录：V0.3.1 平滑混剪第一版

状态：已开发，待用户手动验收。

完成：
1. “拼接抽中片段”支持平滑混剪开关。
2. “批量生成混剪”支持平滑混剪开关。
3. 开启平滑混剪后，VideoEngine 会过滤小于 1.5 秒的过短片段。
4. 开启平滑混剪后，每个有效片段会先生成临时平滑片段，画面首尾各 0.2 秒淡入淡出。
5. 有原声音频的片段会同步做 0.2 秒音频淡入淡出。
6. 原有画布比例、模糊背景、水平镜像、变速和导出结果列表链路继续保留。

验证情况：
- `corepack pnpm build` 已通过。
- `cargo check` 已通过。

待人工验收：
1. 关闭平滑混剪时，原有拼接和批量生成仍正常。
2. 开启平滑混剪时，片段切换有淡入淡出效果。
3. 开启平滑混剪时，小于 1.5 秒的片段会被过滤，日志显示过滤数量。
4. 16:9 横屏 + 模糊背景仍正常。
5. 导出成功后仍进入导出结果列表。

## 2026-06-14 补充记录：V0.3 阶段开发顺序已重排

当前阶段：
V0.3 混剪质量增强与视频效果处理阶段。

当前已完成：
1. V0.1 基础视频闭环。
2. V0.2 基础混剪闭环。
3. UI v2 暗色专业工作台。
4. 画布比例 / 裁剪 / 模糊背景。
5. 16:9 横屏 + 模糊背景修复。
6. 导出结果列表管理。
7. CodeGraph MCP 已可用。

V0.3 开发顺序：
1. V0.3.1 淡入淡出 / 平滑混剪第一版。
2. V0.3.2 基础视频效果第一版。
3. V0.3.3 关键帧 / 封面帧 / 片段预览图第一版。
4. V0.3.4 分类混剪第一版。
5. V0.3.5 画中画第一版。
6. V0.3.6 V0.3 阶段验收与整理。

下一步最高优先级：
V0.3.1 淡入淡出 / 平滑混剪第一版。

原因：
分类混剪、批量生成、后续视频效果都会依赖底层拼接导出流程，必须先把成片切换质量做好。

本次只更新文档：
1. 没有修改 UI。
2. 没有修改视频处理逻辑。
3. 没有修改 Rust / FFmpeg 后端。

## 2026-06-14 补充记录：UI v2 主链路手动验收通过

状态：用户已确认通过。
验收结果：
1. UI v2 右侧参数栏遮挡混剪入口的问题已解决。
2. 导入、预览、切片、随机抽取、拼接、批量生成、导出主链路可以跑通。
3. 当前可以继续进入下一步功能开发或调试。

## 2026-06-14 补充记录：修复 UI v2 右侧参数栏遮挡混剪入口

状态：已修复，待用户手动验收。
完成：
1. 压缩右侧参数栏卡片间距、标题间距、输入框高度和按钮上边距。
2. 保留原有功能按钮尺寸，没有压缩混剪、批量生成、导出等关键操作按钮的可点击空间。
3. 本次只修改前端样式，没有修改 Rust、FFmpeg、VideoEngine 或视频处理 Service。
验证情况：
- `corepack pnpm build` 已通过。

## 历史版本记录

v0.2.0-dev

## 项目定位

本地桌面端 AI 视频批量混剪工作台。

## 历史阶段

阶段二：基础混剪能力开发中。

当前核心目标：

```text
视频切片 → 随机片段抽取 → 片段拼接 → 基础混剪导出
```

## 已完成

- [x] 首页 UI
- [x] 视频导入按钮
- [x] 基础页面布局
- [x] 项目整体开发框架规划
- [x] FFmpeg环境检测
- [x] 单视频/多视频文件导入
- [x] 视频信息读取
- [x] 文件夹导入
- [x] 视频预览
- [x] 输出目录选择
- [x] 基础导出任务
- [x] 基础导出日志
- [x] V0.1 主链路人工验收通过
- [x] 固定时长视频切片
- [x] 随机片段抽取
- [x] 片段拼接导出
- [x] 批量生成混剪结果
- [x] 基础水平镜像
- [x] 桌面软件工作台 UI 框架升级
- [x] 基础变速

## 历史阻塞

暂无。

## 历史禁止修改

- 不允许更换 Tauri
- 不允许重做首页
- 不允许新增会员系统
- 不允许开发 AI 内容理解
- 不允许开发 API Key 系统
- 不允许开发 TTS
- 不允许开发 ASR 字幕识别
- 不允许进行 UI 大重构

## 最近一次开发内容

日期：2026-06-12
完成：实现基础变速。用户可以在混剪参数中设置 0.5x 到 2.0x 的变速倍数，单次片段拼接和批量生成都会通过 VideoEngine 应用对应变速。
涉及文件：
- src-tauri/src/video_engine/mix.rs
- src-tauri/src/lib.rs
- src/services/videoMixService.ts
- src/App.vue
- PROJECT_STATUS.md
- TODO_NEXT.md
验证情况：`corepack pnpm build` 已通过；设置 `CARGO_HTTP_CHECK_REVOKE=false` 后 `cargo check` 已通过；已检查变速参数由页面传入，FFmpeg 滤镜仍只在 VideoEngine 中拼接。
人工验收：用户已确认基础变速测试完美通过。
遗留问题：暂无。

## 下一步建议

1. 继续开发 V0.2 裁剪比例
2. 裁剪比例先支持原画、9:16 竖屏、1:1 方屏、16:9 横屏
3. 保持裁剪逻辑通过 VideoEngine 封装，不让页面组件直接拼接 FFmpeg 命令
---

## 2026-06-12 补充记录：视频比例画布适配

状态：已开发，待用户人工验收。

本次实际实现的产品逻辑：

1. 不是自由裁剪。
2. 不是拖拽缩放裁剪框。
3. 选择 9:16、1:1、16:9 时，保留完整原视频画面。
4. 目标比例外的空白区域用黑边或模糊背景补齐。
5. 预览区使用目标比例画布展示，前景播放器仍保留原生 controls。
6. 基础导出、片段拼接导出、批量生成混剪结果都接入同一套画布参数。

涉及文件：

- src/App.vue
- src/styles.css
- src/services/videoMixService.ts
- src/services/videoRenderService.ts
- src-tauri/src/lib.rs
- src-tauri/src/video_engine/canvas.rs
- src-tauri/src/video_engine/render.rs
- src-tauri/src/video_engine/mix.rs

验证情况：

- `corepack pnpm build` 已通过。
- `cargo check` 已通过。
- `corepack pnpm tauri dev` 已启动；首次启动时发现旧开发进程占用 1420 端口，已结束旧进程后重新启动。

待人工验收：

1. 原画模式预览和导出不改变比例。
2. 9:16 黑边模式保留完整画面，导出为竖屏画布。
3. 9:16 模糊背景模式保留完整画面，导出为竖屏画布。
4. 1:1 和 16:9 模式导出比例正确。
5. 播放器底部播放、暂停、时长条仍可用。
6. 画布参数能与水平镜像、基础变速一起使用。

## 2026-06-12 补充记录：修复原画预览比例异常

状态：已修复，待用户复验。

问题：
导入 1080 x 1920 竖屏原始视频后，预览区没有按原视频比例居中显示，而是被横向大画布撑满，导致原画预览看起来被放大和裁切。

修复：

1. 为预览画布增加明确的比例数字变量。
2. 预览画布根据容器宽高自动计算最大可显示尺寸。
3. 原画模式使用原视频宽高比例。
4. 9:16、1:1、16:9 模式继续使用目标画布比例。
5. 未修改导出引擎和 FFmpeg 逻辑。

涉及文件：

- src/App.vue
- src/styles.css

验证情况：

- `corepack pnpm build` 已通过。

待复验：

1. 竖屏原画导入后，预览区按竖屏比例居中显示。
2. 横屏原画导入后，预览区按横屏比例显示。
3. 9:16、1:1、16:9 切换仍然正常。
4. 播放器底部 controls 仍然可用。

## 2026-06-12 补充记录：修复混剪导出模糊背景未生效

状态：已修复，用户人工测试已通过。

问题：
选择 16:9 横屏和模糊背景后，点击“拼接抽中片段”导出的视频仍然像竖屏居中黑边，模糊背景没有生效。

修复：

1. 已确认页面会把画布比例和背景方式传给 `VideoMixService`。
2. 已确认“拼接抽中片段”会调用混剪导出流程，不是基础导出流程。
3. 混剪导出已使用 `libx264 + aac` 重新编码，可应用 FFmpeg 滤镜。
4. 修复 VideoEngine 画布滤镜：模糊背景模式下，前景主体不再先 pad 成黑底整画布，避免覆盖背景层。
5. 混剪导出日志增加输出比例、输出分辨率、背景方式、是否应用到混剪导出。

涉及文件：

- src/App.vue
- src/services/videoMixService.ts
- src-tauri/src/video_engine/canvas.rs
- src-tauri/src/video_engine/mix.rs
- PROJECT_STATUS.md
- TODO_NEXT.md

验证情况：

- `corepack pnpm build` 已通过。
- `cargo check` 已通过。
- 用户已确认 16:9 横屏 + 模糊背景的“拼接抽中片段”导出完美通过人工测试。

## 下一步建议

1. 开发导出结果列表管理。
2. 支持从页面打开输出目录。
3. 只整理导出结果查看和目录打开能力，不新增字幕、TTS、会员、API Key 或 AI 功能。

## 2026-06-12 补充记录：导出结果列表管理第一版

状态：已开发，用户截图确认可用。

完成：

1. 基础导出成功后加入导出结果列表。
2. 拼接导出成功后加入导出结果列表。
3. 批量生成每条成功结果加入导出结果列表。
4. 结果区统一展示导出类型、文件名、时间和完整路径。

验证情况：

- `corepack pnpm build` 已通过。

## 2026-06-13 补充记录：UI v2 暗色专业工作台重构

状态：已开发，待用户验收。

完成：

1. 增加首页 / 功能入口页。
2. 首页保留项目名称，不使用竞品品牌名、Logo 和文案。
3. 已实现模块可以进入真实工作台，未实现模块显示“敬请期待”。
4. 工作台调整为深色三栏布局：左侧素材区、中间预览和结果区、右侧参数区、底部任务区。
5. 保留导入、预览、切片、随机抽取、拼接、批量生成、画布设置、输出目录、结果列表和任务日志能力。
6. 未修改 Rust、FFmpeg 或视频处理核心逻辑。

验证情况：

- `corepack pnpm build` 已通过。

## 2026-06-12 补充记录：打开输出目录和结果位置

状态：已开发，待用户验收。

完成：

1. 输出设置区增加“打开输出目录”。
2. 导出结果列表每条结果增加“打开位置”。
3. 后端通过 Tauri Command 调用系统文件管理器。

验证情况：

- `corepack pnpm build` 已通过。
- `cargo check` 已通过。

## 2026-06-12 补充记录：修复任务日志区域过短

状态：已修复，待用户验收。

完成：

1. 底部任务日志区域高度从固定短高度改为更大的自适应高度。
2. 日志列表取消过短的最大高度限制，改为占满任务日志面板可用空间。

验证情况：

- `corepack pnpm build` 已通过。
