# PROJECT_STATUS.md

## 2026-08-08 智能配置真实导出接线
状态：开发完成，等待用户验收。
- 前端智能方案、批量任务进度与 Rust VideoEngine 已完成接线。
- 当前智能自动应用范围：轻量调色、轻微变速、稳定缩放；需要外部素材的效果继续显示待补充状态。
- 真实视频导出已通过；Git 提交等待用户确认。

## 2026-08-08 视频效果处理主界面布局复刻（等待用户验收）

状态：布局开发和浏览器检查已完成，本轮未提交 Git。

- 已按参考图完成左中右三栏：素材导入与列表、视频预览与分割参数、视频效果与贴画水印入口。
- 已补齐详细使用指引、预览顶部直接入口和底部输出字段。
- 已验证 1440×900、1280×720、浅色主题；页面无横向溢出，预览区域和右侧效果列表保持可见。
- 当前仅完成主界面布局和本地交互状态，真实分割参数、输出分类和效果处理链路留到后续逐项开发。

## 2026-08-08 视频效果处理主界面布局复刻（等待用户验收）

状态：布局开发和浏览器检查已完成，本轮未提交 Git。

- 已按参考图完成左中右三栏：素材导入与列表、视频预览与分割参数、视频效果与贴画水印入口。
- 已补齐详细使用指引、预览顶部直接入口和底部输出字段。
- 已验证 1440×900、1280×720、浅色主题；页面无横向溢出，预览区域和右侧效果列表保持可见。
- 当前仅完成主界面布局和本地交互状态，真实分割参数、输出分类和效果处理链路留到后续逐项开发。

## 2026-08-08 竞品字体与桌面窗口尺寸对齐

状态：已完成开发和自动检查，等待用户重启桌面端验收；本轮不提交 Git。

- 工作台改为 Windows 中文字体优先的字体栈，收紧字号、字重、行高和数字显示。
- 降低卡片与按钮圆角、压缩工作区间距和控件高度，减少模板化 AI 视觉。
- Tauri 默认窗口调整为 1440×900，最小尺寸调整为 1280×720。
- 前端构建、Tauri 配置 JSON 校验、字体机械检查和差异检查通过。

## 2026-08-08 素材栏文字可读性修复

状态：开发完成，等待用户验收；本轮不提交 Git。

- 放大文件夹名、素材文件名、数量和时长的文字层级。
- 素材列表改为清晰的两行信息结构，增大缩略图和行高。
- 前端构建、字体扫描和差异检查通过。

## 2026-08-07 ECutAuto 对标批量实施：字幕与内容提炼闭环

状态：本轮相关代码已实现，等待统一集中验收；不提交 Git。

- 普通混剪字幕不再因后端占位校验直接失败：开启后会复用 ASS 字幕引擎，按当前 AI 文案、位置和字号生成字幕并参与画布/水印/画中画滤镜组合。
- 右侧文本/字幕样式已接入普通混剪参数；AI 配音链路继续使用独立的逐句字幕设置。
- 内容提炼工作台显示已完成提炼的主题，并新增“按提炼结果生成精华成片”，复用现有分类混剪和输出目录。
- 本轮验证：`corepack pnpm build`、`cargo fmt -- --check`、`cargo check`、`cargo clippy --all-targets -- -D warnings`、`cargo test`（142 项）通过。
- 尚待真实短视频导出、字幕画面效果、失败重试和独立 Windows 环境主链路验收。

## 2026-08-07 AI 成片文案区、头像与云端成片输出修复

状态：开发完成，等待用户集中验收；本轮不提交 Git。

- 底部文案区已扩大并保持持续可编辑，分镜完成后不再被画面卡片替换。
- 主工作台改为只展示后台画面匹配状态，不显示画面缩略图入口；上方真实视频预览保留。
- 右侧当前音色增加头像加载失败回退，浅色/深色主题均已检查。
- 带 AI 配音最终视频改写入任务目录的“视频成片”，MP3/字幕中间文件继续进入“配音与字幕”。
- `corepack pnpm build`、Rust格式/编译/Clippy/140项测试和两个窗口尺寸检查通过。

## 2026-08-06 人工验收结果

- 个人中心、会员兑换、API/TTS 设置、会员登录和开发启动均已人工验收通过。
- 本轮将只提交本机 Git，不推送 GitHub。

## 2026-08-06 启动与登录验收

- 开发启动已增加端口复用处理；线上会员服务已重新发布并同步签名密钥。
- 等待用户重新启动后验证登录。

## 2026-08-06 会员登录故障处理

- 已处理线上会员服务签名密钥配置不同步问题；桌面端内置公钥与本机发布公钥一致，线上服务可用。
- 等待真实账号重新登录人工验收；本轮尚未提交 Git。

## 2026-08-06：个人中心设置与设备绑定

状态：本轮开发完成，等待用户人工验收。

当前状态：个人中心显示秒级会员有效期和设备绑定状态；兑换卡密时必须确认绑定当前设备，服务端按设备哈希识别当前设备，并限制每月最多 2 次换绑。API 设置页已压缩为火山引擎、Ark API Key、TTS API Key，模型和语音资源使用软件统一配置。

验证状态：前端构建、会员服务 TypeScript 检查、会员服务测试、Rust 格式/编译/Clippy/测试均已通过；本地浏览器 1280×720 无横向溢出，浅色和深色主题往返通过。真实登录服务尚未部署，个人中心登录后页面需要用户人工验收。

# 2026-08-06：新增智剪一键启动脚本

状态：已完成。

项目根目录新增 `启动智剪.bat`。双击后会自动切换到项目目录，优先使用 Corepack + pnpm 启动 Tauri 开发版；如果环境中没有 Corepack，则依次尝试 pnpm 和 npx。脚本内容使用纯 ASCII，避免 Windows cmd.exe 读取中文 UTF-8 批处理时拆坏命令。

## 2026-08-05：新版UI与权限门禁新电脑测试包已生成（等待独立电脑验收）

状态：V0.5.0 NSIS安装包已包含新版六卡片首页、主题修复、真实到期日期和正式功能权限门禁。前端生产构建、Rust严格检查、138项测试、安装包构建及本机隔离安装均通过；验收前不提交Git，不推送GitHub。

安装包：`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-08-05_V0.5.0新版UI与权限门禁新电脑测试包\智剪 SmartCut_0.5.0_x64-setup.exe`。

大小115.97MB，SHA256为`bcef22fb5407cf7b31d3de5b9f09b1ab7220da2ed477ddff49477b1587d88316`。安装后主程序为Windows GUI，内置FFmpeg/FFprobe、隐私说明和许可证完整。下一步由用户在独立新电脑检查安装、新版首页、账号会员、API/TTS配置、权限提示、最短导出和卸载。

## 2026-08-05：会员到期状态与正式功能权限门禁（等待人工验收）

状态：个人中心真实到期日期、“有效 / 已过期”状态以及卡密、AI、TTS三项正式功能门禁已完成。前端生产构建、Debug桌面构建、差异检查和真实桌面程序检查通过；验收前不提交Git，不推送GitHub。

当前测试账号到期日期为`2026/08/01`，在`2026-08-05`正确显示红色“已过期”。本机AI Key和TTS已配置，点击正式功能时只提示缺少有效卡密；点击确定后进入个人信息/卡密兑换页。浅色和深色权限弹窗、个人中心状态及续费提示均正常。

一次视觉检查误看到旧首页，确认原因是相同Tauri应用标识复用了旧实例，并非源码或构建产物回退。当前运行窗口已按真实进程路径核对为项目最新Debug程序，新版六卡片首页保持正常。

## 2026-08-05：水印、教程、诊断主题和效率工具返回已修复（验收通过）

状态：浅色水印操作控件、新手教程、诊断窗口已统一；四个效率工具返回后保持在效率工具列表。正式构建、浏览器逐项回归、深浅主题往返检查和用户人工验收通过，本轮已提交本地Git，不推送GitHub。

## 2026-08-05：浅色主题残留黑色输入条已修复

状态：视频文案、批量改名、大字报文案和画中画弹窗的残留深色控件已统一修复；视频预览与设计画布继续保持黑色。浏览器实际视觉检查通过，已与本轮验收功能一起提交本地Git，不推送GitHub。

## 2026-08-04：三模式主题系统完成（等待统一验收）

状态：跟随系统、浅色和深色模式已接入首页、AI工作台、二级设置弹窗、语音模块与个人中心；选择记忆、系统主题监听、双尺寸视觉检查和前端自动构建均通过，验收前不提交Git。

浅色模式使用柔和灰白面板、清晰深色文字和低饱和青绿强调色；视频预览继续保持黑色。实际视觉检查中已补齐组件内样式造成的残留深色输入框、音色卡片和下拉框。

验证结果目录：`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-08-04_主题切换验收`。

## 2026-08-04：四个红框入口的竖屏预览已修复（等待验收）

视频混剪、分类混剪、视频效果处理和视频内容提炼已统一修复竖屏素材裁切。真实1080×1920素材在1280×720和1440×900窗口下均保持9:16完整显示，不再横向铺满后只露出画面上半段。自动构建和差异检查通过，验收前不提交Git。

验证结果目录：`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-08-04_红框显示修复`。

## 2026-08-04：竞品逐页高保真重构（等待统一验收）

状态：AI、视频混剪、分类混剪、视频效果、内容提炼、个人中心和效率工具已完成第二轮结构级重构；自动检查和1440×900、1280×720视觉对照通过，验收前不提交Git。

本次重点不是换色，而是删除旧步骤导航、顶部工作区切换和通用任务条，按竞品重建48px顶栏、348px/弹性/304px三栏、直接参数面板、紧凑弹窗和空状态。API Key主页面已精简，大字报模板已扩展到20套。

验证结果：前端生产构建通过；Rust格式、编译、严格Clippy通过；138项Rust测试全部通过；`git diff --check`通过；1280×720下AI预览674×430且页面无滚动溢出。

## 2026-08-04：竞品式V3工作台与五项新增工具（等待统一验收）

状态：V3首页、三工作区导航、现有核心功能接入和五项新增工具全部完成，自动检查、真实桌面程序和真实输出回归通过；验收前不提交Git。

本次完成：
1. 按竞品可见的信息架构重做创作中心、AI成片、批量混剪和效率工具布局，继续使用智剪自己的品牌、代码和图形资源。
2. 保留并回归AI分镜、批量混剪、视频效果、内容提炼、真实视频预览、字幕识别、配音、水印、会员、项目恢复和导出能力。
3. 新增文案改写、文件批量改名、字幕编辑与SRT/TXT导出、大字报设计、图片转视频五项可真实使用的功能。
4. 修正文案改写到AI成片的可见性：文案已自动传递，且在素材不足时仍显示可编辑草稿，不再让用户误以为串联失败。

真实验证：3个文件完成改名；7条中文字幕完成识别和校对导出；大字报输出1080×1920 PNG；两张图片分别输出1080×1920、30fps、5秒MP4；文案改写真实AI返回3个版本并成功带入AI成片。前端构建、Vue/TypeScript、Rust格式、编译、严格Clippy、138项测试、差异检查、1440×900和1280×720布局检查全部通过。

验收结果目录：`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-08-04_竞品布局功能验收`。

## 2026-07-24：智剪可视化卡密管理器（人工验收通过）

状态：独立管理员工具已完成开发、自动验证和用户确认；本轮提交本地Git，不推送GitHub。

边界：管理器位于会员服务开发目录，只供管理员本机使用，不打入普通用户安装包，也不上传到CloudBase函数代码包。管理员令牌继续使用Windows DPAPI加密文件。

启动入口：`E:\Workspace\Project_03_本地短视频批量混剪工作台\启动智剪卡密管理器.vbs`。

## 2026-07-24：V0.5.0国内账号会员系统第一版（人工验收通过）

状态：账号会员业务、腾讯云开发部署、国内公网回归、自动检查、正式安装包和用户确认均已完成；本轮提交本地Git，不推送GitHub。

本次完成：
1. 邮箱账号注册/登录、兑换码充值、会员有效期、改密退出其他设备和换机恢复。
2. CloudBase HTTP云函数、文档数据库和Windows安全缓存共同组成远程会员验证链路。
3. 服务端只保存密码哈希、会话哈希和兑换码哈希；私钥与管理令牌只在云函数环境变量和本机Windows加密文件中存在。
4. V0.5.0正式安装包编译写入国内默认接口地址和公钥，普通用户不需要打开终端。

验证：国内公网11项会员回归、服务端8项测试、前端构建、Rust严格检查和132项测试全部通过。正式安装包115.95MB，SHA256为`5b24f3cdd443260ac4b412bf5bb8662a66ccc8c4e13d4beb3e620c7c17a2b6eb`。

验收包：`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-07-24_V0.5.0国内会员系统用户测试包\智剪 SmartCut_0.5.0_x64-setup.exe`。

## 2026-07-24：V0.4.1后台视频命令隐藏终端窗口（人工验收通过）

状态：代码、129项测试、正式安装包、隔离安装、真实项目恢复验证和用户人工验收全部完成；提交本地Git，不推送GitHub。

本次完成：
1. Windows下FFmpeg/FFprobe统一通过`background_command`和`CREATE_NO_WINDOW`启动。
2. 覆盖视频信息、音频探测、缩略图、切片任务、混剪探测、配音探测、字幕与GPU能力检查。
3. V0.4.1正式安装包大小115.84MB，SHA256为`b05e6b4d6db0c3b78b7c3987715141e6fb65e0370fdb18c94ed3b64dee039a31`。
4. 真实恢复7个原视频和16个片段，后台读取过程未出现终端窗口。

人工验收结果：2026-07-24用户确认打开软件不再出现黑色终端窗口，验收通过。

## 2026-07-21：第四阶段V0.4.0普通用户测试包（人工验收通过）

状态：代码开发、自动检查、本机隔离安装、真实界面验证和用户人工验收全部完成；提交本地Git，不推送GitHub。

本次完成：
1. 安装包内置FFmpeg 8.1.1和FFprobe，并优先从安装目录读取。
2. 新增四步首次启动向导，支持从首页和顶部入口重复打开。
3. 新增AI/TTS/ASR配置状态、日志目录、法律文件目录和视频引擎路径诊断。
4. 新增启动日志、Rust panic崩溃日志和安全诊断报告。
5. 新增隐私说明、FFmpeg许可证、第三方组件说明和FFmpeg版本清单。
6. 版本升级为V0.4.0，NSIS使用当前用户安装模式和LZMA压缩。
7. 修复Windows PowerShell 5.1中文脚本编码与安装后FFmpeg内置标签判断。

验证：
- 前端161个模块生产构建通过。
- Rust格式、编译、严格Clippy和128项测试通过。
- 正式NSIS安装包生成成功，大小115.84MB，低于1GB限制。
- 标准测试结果库隔离安装成功，主程序、FFmpeg和隐私说明均存在，程序真实启动成功。
- 向导四页、AI/TTS/ASR状态、隐私说明、诊断报告和顶部诊断入口真实显示正常。
- 安装后主程序PE Subsystem为2（Windows GUI），启动只显示智剪窗口，不附带控制台。
- 项目恢复弹窗两个操作按钮真实显示正常，无白底白字。

安装包SHA256：`c9e61b72df85c0dec495ab5f7deb2755007795487e26a01db502870d07c433de`。

当前限制：未配置Windows代码签名；独立干净Windows电脑上的完整视频主链路尚待用户测试。

人工验收结果：2026-07-21用户确认通过，安装版无需终端即可运行，项目恢复弹窗两个按钮显示正常。

## 2026-07-20：智剪 SmartCut 首页与工作区真实分流（人工验收通过）

状态：代码开发、前端构建、Tauri Debug构建、真实桌面视觉检查和用户人工验收全部完成；提交本地Git，不推送GitHub。

本次完成：
1. 首页AI智能成片、批量混剪和视频工具三个入口进入不同工作区。
2. AI工作区继续使用文案驱动四步成片流程。
3. 批量工作区使用多素材、组合规则、样片和批量生成流程。
4. 视频工具使用独立工具中心，不显示完整成片流程和右侧全功能列表。
5. 右侧设置和底部主操作根据工作区变化，顶部支持直接切换工作区。
6. 左侧素材库和底层视频能力继续复用，未修改Rust、FFmpeg、VideoEngine、协议、数据库或项目快照。
7. 默认窗口调整为居中1320×860，保留缩放和最大化，避免2K屏幕默认铺满造成比例失衡。
8. AI中间预览提高为第一视觉中心，文案分镜区域内部滚动；右侧设置按三类任务分组。
9. 批量混剪和视频工具使用完整画面等比预览，批量规则和工具卡在窗口模式下完整显示。
10. AI工作区按竞品截图重排三栏与上下区域，竖屏舞台固定约1.37:1并随可用高度自动缩放，下面文案区固定为紧凑滚动区域，避免“大黑框里放一个小视频”；横屏和方形预览不变。

验证：
- `corepack pnpm build`通过。
- `corepack pnpm tauri build --debug --no-bundle`通过。
- 最新竖屏预览收窄修改再次通过前端生产构建、Tauri Debug构建和差异格式检查。
- 独立临时预览程序真实检查：1320像素宽窗口中竖屏舞台约643×480像素，视频高度和左右留黑比例已接近竞品按窗口缩放后的尺寸。
- 1320×860真实Tauri桌面窗口中，三类工作区中心结构、预览和功能范围均正常显示。

人工验收结果：2026-07-21用户确认品牌、工作区分流和最终竖屏预览比例可用，同意提交本地Git。

## 2026-07-20：水印工具第一版（人工验收通过）

状态：代码开发、自动检查、真实FFmpeg验证和用户人工验收全部通过；本次提交本地Git，不推GitHub。

本次完成：
1. 右侧“常用调整”新增一级“水印工具”入口，合并添加水印和处理原水印。
2. 支持开关、类型、左上/右上/居中/左下/右下位置、透明度和边距。
3. 文字水印支持中文、80字符限制、16～120字号和自定义文字颜色。
4. 图片水印支持PNG、JPG、JPEG、WEBP和BMP，宽度可设置为成片的8%～50%。
5. 水印应用到基础单视频导出、普通混剪、分类混剪、批量生成、AI无配音成片和AI配音成片。
6. 水印与画布、画面效果、画中画、BGM、平滑混剪和完整输出设置使用同一VideoEngine滤镜链路。
7. 文字水印通过临时UTF-8文件和Windows系统字体生成，避免中文、空格和特殊标点直接进入FFmpeg参数。
8. 旧项目快照缺少水印字段时自动使用关闭状态；图片水印文件失效时自动关闭并提示，不修改SQLite结构。
9. 基础导出参数收拢为结构化`BasicExportSettings`，避免新增功能继续扩大Tauri命令参数数量。
10. 原水印处理支持边缘裁剪、融合修复、区域模糊、马赛克和色块遮盖。
11. 支持处理强度、遮盖颜色和透明度；固定顺序为处理原水印、画中画、添加新水印。
12. FFprobe读取最终画面尺寸并处理90度旋转，避免融合修复区域越界。
13. 边缘裁剪移除顶部或底部水印带，再按原比例放大铺满；四个边角可选，居中位置禁用。
14. 新增移动水印关键帧跟踪，可在完整视频预览中拖动处理框并保存2～8个关键位置。
15. 支持关键位置载入、更新、删除、清空、轨迹预览，以及相邻关键位置之间线性移动。
16. 动态区域模糊、马赛克和色块遮盖支持转折轨迹，移动处理后仍可继续添加新水印。
17. 移动模式只允许原画画布的当前完整视频导出；清理结果需要重新导入后再进行混剪。
18. 第一版不引入OpenCV、AI模型或云端接口，不做自动识别和全自动目标追踪。

自动验证：
- `corepack pnpm build`通过。
- `cargo fmt -- --check`、`cargo check`和严格Clippy通过。
- `cargo test`共125项通过、0项失败；其中水印专项测试12项通过。
- 真实FFmpeg测试成功生成中文文字水印、透明图片水印和“画中画+图片水印+BGM”组合视频。
- 真实FFmpeg测试成功生成融合修复、区域模糊、马赛克、色块遮盖、横竖屏边缘裁剪和“处理旧水印+添加新水印”组合视频。
- 测试结果：`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-07-18_添加水印第一版自动测试`、`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-07-18_水印工具第一版自动测试`。
- 移动跟踪测试结果：`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-07-20_移动水印关键帧跟踪第一版自动测试`。

人工验收结果：移动水印处理框、关键位置轨迹和完整视频导出均通过，用户同意提交本地Git。

## 2026-07-18：可视化AI音色库第一版

状态：代码开发、自动检查和用户确认全部完成；已提交本地Git `4438287`，不推GitHub。

本次完成：
1. AI配音设置新增可视化音色选择卡片和独立音色库弹窗，保留高级手动音色ID入口。
2. 第一版收录25款真实火山TTS 2.0音色，覆盖中文、英语、日语、西班牙语和印尼语，并提供多语种快捷筛选。
3. 支持按音色名称、风格说明和音色ID搜索，支持语言与通用、口播解说、商品营销、角色演绎、年轻活力场景筛选。
4. 音色卡片展示男女声、支持语言、适用场景、风格说明、推荐状态和当前选择。
5. 使用火山公开官方样音试听，不消耗用户TTS生成额度；25个样音地址均已检查可访问。
6. 选择结果继续写入现有`speaker`字段，复用已验收的MP3生成和逐句配音视频链路，不修改Rust TTS协议、数据库或VideoEngine。
7. 弹窗支持Esc关闭、键盘焦点约束、关闭后焦点返回入口、明确的试听读屏名称和窄窗口单列布局。

自动验证：
- `corepack pnpm build`通过。
- `cargo fmt -- --check`通过。
- `cargo check`通过。
- `cargo clippy --all-targets -- -D warnings`通过。
- `cargo test`共104项通过、0项失败。
- 自动测试报告：`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-07-18_可视化AI音色库第一版自动测试\自动测试报告.md`。

最终确认：2026-07-18用户确认提交并继续下一项功能，音色库进入已提交基线；更多音色后续按账号权限继续扩充。

## 2026-07-18：ASR与本地文案库精简联动

状态：开发、自动验证和用户人工验收全部通过；提交本地Git，不推GitHub。

本次完成：
1. ASR完整文字可一键保存进本地文案库。
2. 可通过“保存并使用到当前文案”直接进入AI分镜流程。
3. 文案分镜顶部常驻文案库入口，不要求先打开高级模式。
4. 文案库抽屉只展示标题、保存时间、来源和文字摘要，操作仅保留使用、删除。
5. Rust后端负责版本化JSON读取、校验、去重、原子保存和删除。
6. 文案库最多保留500条，文件上限4MB，避免无限增长。
7. 当前文案替换前确认，长文案限制与现有AI分镜4000字边界一致。

自动验证：
- `corepack pnpm build`通过。
- `cargo fmt -- --check`通过。
- `cargo check`通过。
- `cargo clippy --all-targets -- -D warnings`通过。
- `cargo test`共104项通过、0项失败。
- 自动测试报告：`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-07-18_ASR与文案库联动自动测试\自动测试报告.md`。

人工验收：
- 2026-07-18用户确认ASR结果保存、文案库载入、保存并使用和持久化联动验收成功。


## 2026-07-18：音频输入与火山云端ASR第一版

状态：开发、自动验证和用户人工验收全部通过；与文案库联动一并提交本地Git，不推GitHub。

本次完成：
1. 增加独立语音识别工具，不把完整业务堆入`App.vue`或`ai_remix.rs`。
2. 音频与视频统一由Rust VideoEngine提取声音，转换为16kHz单声道24kbps MP3。
3. 调用火山`volc.bigasr.auc_turbo`极速识别接口，返回完整文字和句子起止毫秒时间。
4. 复用现有安全语音密钥；密钥仍由Windows当前账户加密，完整内容不回显、不进入日志。
5. 识别结果使用版本化JSON缓存，键由规范路径、文件大小、修改时间和模型版本共同生成。
6. 缓存命中发生在音频转换和网络请求之前，重复识别不重复消耗云端额度。
7. 视频无声音、超过2小时、压缩音频过大、静音、额度不足和服务繁忙均有独立中文提示。
8. 任务中心展示音频转换和云端识别阶段；取消后停止后续处理，临时网络错误最多受控尝试3次。
9. 前端结果支持复制完整文字，并按列表展示每句开始时间、结束时间和内容。

自动验证：
- `corepack pnpm build`通过。
- `cargo fmt -- --check`通过。
- `cargo check`通过。
- `cargo clippy --all-targets -- -D warnings`通过。
- `cargo test`共102项通过、0项失败。
- 自动测试报告：`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-07-18_云端ASR第一版自动测试\自动测试报告.md`。

人工验收：
- 2026-07-18用户确认真实语音识别结果显示成功。


## 2026-07-17：视频内容提炼与AI自动分类第一版

状态：开发、自动验证和用户人工验收全部通过；提交本地Git，不推GitHub。

本次完成：
1. 左侧素材库的视频片段页签增加一级内容提炼入口，不依赖高级模式。
2. 内容提炼复用每个片段已有的多帧客观描述，只对缺失描述的片段调用看图分析。
3. Rust AI接口输出主题、可见卖点、主体动作、标签和镜头类型，并严格限制为8类合法分类。
4. 最多并行处理2个片段，继续使用现有临时失败自动重试，单个失败不会丢失其他成功结果。
5. 选中片段可人工修改结构化结果；镜头类型继续复用现有分类混剪数据。
6. SQLite不迁移表结构，使用description字段保存带版本号的JSON；旧纯文字描述仍可读取。
7. 项目快照和素材库恢复会清理异常字段并恢复结构化结果。

自动验证：
- `corepack pnpm build`通过。
- `cargo fmt -- --check`通过。
- `cargo check`通过。
- `cargo clippy --all-targets -- -D warnings`通过。
- `cargo test`共96项通过、0项失败。
- 自动测试报告：`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-07-17_内容提炼与AI分类第一版自动测试\自动测试报告.md`。

人工验收：
- 2026-07-17用户确认内容提炼、自动分类、人工修改和缓存恢复验收成功。

## 2026-07-17：智能切片与多帧理解第一版

状态：开发、自动验证和用户人工验收全部通过；提交本地Git，不推GitHub。

本次完成：
1. 原固定时长切片保留，新增默认推荐的智能切片模式。
2. Rust VideoEngine先扫描画面变化，再按候选转场、最短片段和最长片段生成安全切点。
3. 三档灵敏度对应不同画面变化阈值，前端只传结构化设置，不拼FFmpeg命令。
4. 连续镜头通过最长时长补切；短连续视频在满足最短时长时自动二等分。
5. 每个片段最多抽取15%、50%、85%位置的3帧，AI综合理解主体、场景和动作变化。
6. 左侧片段列表使用中间代表图；SQLite继续保存代表图和最终AI描述，避免重复调用。
7. 新增可返回FFmpeg错误输出的任务运行接口，画面扫描仍支持真实进度和取消。
8. FFmpeg分段加入0.05秒关键帧容差，兼容当前FFmpeg 8.x的切点时间误差。

自动验证：
- `corepack pnpm build`通过。
- `cargo fmt -- --check`通过。
- `cargo clippy --all-targets -- -D warnings`通过。
- `cargo test`共94项通过、0项失败。
- 6秒连续视频智能兜底输出2段，时长约3.02秒和3.01秒。
- 合成9秒三场景视频识别3秒、6秒两个候选点，准确输出3个3秒片段。
- 开头、中间、结尾3张AI分析帧真实生成成功。
- 自动测试报告：`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-07-17_智能切片第一版自动测试\自动测试报告.md`。

人工验收：
- 首轮反馈精简模式找不到切换入口，已把“智能切片”提升为右侧常用调整一级按钮。
- 2026-07-17用户确认复验成功，智能切片与多帧理解第一版完成。

## 2026-07-17：素材库持久化第一版

状态：代码开发、自动检查和用户人工验收全部通过；提交本地Git，不推GitHub。

本次完成：
1. Rust新增SQLite素材库，数据库位于Windows当前用户应用数据目录，不复制视频和成片文件。
2. 素材库保存原视频元数据、封面路径、切片顺序、人工分类、缩略图路径和AI画面描述。
3. 数据库采用版本化表结构和事务保存，中途失败不会留下半份素材库。
4. 最近项目快照和SQLite素材库职责分离：最近项目负责编辑进度，SQLite负责长期素材索引。
5. 丢失的原视频和切片不会阻止其他素材恢复，界面会显示失效数量和原路径。
6. 失效原视频支持“重新定位”，选择新位置后自动更新素材库。
7. 已保存的AI画面描述会恢复到切片缓存，再次生成分镜时优先复用，减少AI调用。
8. 素材库界面显示正在读取、正在保存、已保存、恢复数量和错误反馈。
9. 人工验收发现“放弃上次记录”后SQLite立即自动载入，导致空白项目失效；现已改为“新建空白项目”，素材库只提示可用数量并由用户手动载入。

人工验收：
- 2026-07-17用户确认SQLite保存、空白项目、手动载入素材库和职责分离修复验收成功。
- 第一阶段“稳定生产基础”全部完成。

自动验证：
- `corepack pnpm build`通过。
- `cargo fmt -- --check`通过。
- `cargo check`通过。
- `cargo clippy --all-targets -- -D warnings`通过。
- `cargo test`共90项通过、0项失败；新增数据库往返、失效文件和覆盖保存3项测试。
- 自动测试报告：`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-07-17_素材库持久化第一版自动测试\自动测试报告.md`。

## 2026-07-17：完整输出设置第一版

状态：代码开发、自动检查、真实FFmpeg验证和用户人工验收全部通过；提交本地Git，不推GitHub。

本次完成：
1. 输出设置页新增固定MP4格式、跟随画布/720p/1080p、跟随源/24～60fps、省空间/标准/高清质量档位。
2. 新增自动、CPU、NVIDIA、Intel和AMD编码选择；不可用GPU在下拉框中禁用。
3. Rust通过320×240单帧实际编码测试判断GPU是否真正可用，不只检查FFmpeg编码器名称。
4. 自动模式按NVIDIA、Intel、AMD、CPU顺序选择实际测试通过的编码器。
5. 基础导出、随机混剪、分类混剪、批量生成、AI无配音和AI配音的最终成片统一应用输出设置。
6. 临时平滑片段和配音分镜继续使用CPU编码，避免中间文件因GPU兼容问题失败。
7. 横屏、竖屏、方屏和原画均保持比例；原画720p/1080p按方向自动选择最长边，禁止画面拉伸。
8. 输出日志显示实际分辨率、帧率、质量、编码器和目标视频码率。
9. 新设置进入最近项目自动保存；旧快照缺少字段时回退到跟随画布、跟随源帧率、标准质量和自动编码。

自动验证：
- `corepack pnpm build`通过。
- `cargo fmt -- --check`通过。
- `cargo check`及严格Clippy通过。
- `cargo test`共87项通过、0项失败。
- 当前电脑实际编码测试：CPU与NVIDIA通过，Intel QSV与AMD AMF不可用。
- 横屏720p/25fps测试结果：1280×720、25fps。
- 竖屏720p/25fps测试结果：720×1280、25fps。
- 质量档位测试文件：省空间898072字节、标准1635514字节、高清2297720字节。
- NVIDIA GPU测试结果：H.264、1280×720、30fps导出成功。
- 自动测试报告：`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-07-17_完整输出设置第一版自动测试\自动测试报告.md`。

人工验收：
- 2026-07-17用户确认验收成功。
- 验收前发现右侧“成片设置”遗漏可见入口，现已补齐“导出设置”按钮并重新通过前端正式构建。

## 2026-07-17：统一任务控制第一版

状态：代码开发、自动检查和用户人工验收全部通过；准备提交本地Git，不推GitHub。

本次完成：
1. 新增统一任务中心，底部任务栏显示当前任务、真实FFmpeg百分比、当前阶段和取消状态。
2. Rust统一用FFmpeg `-progress pipe:1`读取实际处理时间，切片、基础导出、普通混剪、AI无配音成片和AI配音成片均接入进度范围。
3. 点击“取消任务”后，Rust向当前FFmpeg进程发出终止请求；切片目录、导出半成品和混剪半成品会安全删除。
4. AI/TTS网络请求无法瞬间强杀时，取消标记会阻止后续句子、后续分镜或后续成片继续执行。
5. 随机批量、AI差异视频和AI配音视频保存失败条目；“重试失败项”只重新执行失败条目，成功结果不会重复生成。
6. 平滑混剪、配音分镜和拼接列表迁入本项目专用系统临时目录，正常、失败和取消均通过生命周期自动清理。
7. 软件启动和手动清理只删除超过24小时的本项目遗留目录，避免误删另一实例仍在使用的临时文件；素材、缩略图和最终成片不在清理范围。
8. 新增低Token项目入口 `CODEX_START_HERE.md` 和 `CURRENT_TASK.md`，详细历史文档改为按关键词读取。

自动验证：
- `corepack pnpm build`通过。
- `cargo fmt -- --check`通过。
- `cargo check`通过。
- `cargo clippy --all-targets -- -D warnings`通过。
- `cargo test`共81项通过、0项失败。
- 新增测试覆盖任务进度范围、任务取消状态、FFmpeg进度解析、专用临时目录和新鲜活动目录保护。
- 自动测试报告：`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-07-17_统一任务控制第一版自动测试\自动测试报告.md`。

待人工验收：真实长视频进度变化、FFmpeg处理中取消、批量失败项重试和临时文件清理按钮。

2026-07-17首轮人工验收：任务取消、真实进度和失败项重试通过；清理临时文件实际执行后只写入隐藏任务日志，主界面没有反馈。现已在底部任务摘要直接显示清理成功、无遗留文件或清理失败信息，前端正式构建通过，等待用户复验该提示。

最终人工验收：2026-07-17用户确认清理反馈复验通过，统一任务控制第一版全部验收完成。

## 2026-07-17：项目保存与失败恢复第一版

状态：代码开发、自动检查和用户人工验收全部通过；准备提交本地Git，不推GitHub。

本次完成：
1. 软件自动保存最近一个项目的素材信息、输出目录、切片、分类、文案、AI片段描述、人工调整后的分镜和主要成片设置。
2. 自动保存采用约1.2秒合并延迟，连续调整参数时不会每次点击都写磁盘。
3. 顶部状态显示正在读取、自动保存已开启、正在保存、已保存时间和保存失败。
4. 软件启动时如果存在上次项目，会显示“恢复上次项目 / 放弃上次记录”对话框。
5. 恢复前由Rust检查原始视频、切片、缩略图、BGM、画中画和输出目录是否仍存在。
6. 失效素材自动跳过；分镜引用失效时清空旧分镜要求重新生成；BGM或画中画失效时自动关闭。
7. 快照使用版本化JSON保存在当前Windows用户应用数据目录，不复制素材，不保存成片，不修改SQLite结构。
8. API Key、密码、凭证和secret字段由Rust强制拒绝写入项目快照；密钥继续只使用原Windows加密配置。
9. 损坏、过大或不兼容的快照不会覆盖当前工作台，用户可以清除记录后继续。

自动验证：
- `corepack pnpm build`通过。
- `cargo fmt -- --check`通过。
- `cargo check`通过。
- `cargo clippy --all-targets -- -D warnings`通过。
- `cargo test`共76项通过、0项失败。
- 新增4项Rust测试覆盖快照保存/读取/删除、失效路径识别、密钥字段拒绝和版本不兼容拒绝。
- 测试只使用系统临时目录，没有创建或覆盖真实用户项目快照。
- 自动测试报告：`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-07-17_项目保存与失败恢复第一版自动测试\自动测试报告.md`。

2026-07-17首轮人工验收修复：
1. 恢复对话框能够正确读取3个原视频和47个切片，但点击恢复时报 `structuredClone` 无法复制对象。
2. 根因是加载结果进入Vue响应式状态后成为Proxy代理对象，浏览器原生 `structuredClone` 不支持直接复制该对象。
3. 恢复设置改为JSON安全复制，符合项目快照本身只包含JSON数据的边界。
4. 修复后前端构建、Rust严格检查和76项测试再次全部通过，等待用户复验恢复按钮。
5. 2026-07-17用户复验通过：关闭并重新打开软件后可以成功恢复上次项目。

## 2026-07-17：AI/TTS临时失败自动重试

状态：代码开发、自动检查和用户人工验收全部通过；已提交本地Git `2db9154`，未推GitHub。

本次完成：
1. AI片段画面理解和固定短句画面匹配遇到临时网络错误时自动重试，最多3次。
2. TTS试听和逐句配音遇到临时网络错误时自动重试，逐句模式只重试当前失败句。
3. HTTP 408、425、429以及常见5xx服务故障允许受控重试；Key、权限、模型配置和明确额度耗尽直接返回。
4. 首次失败等待约1.2秒，第二次失败等待约2.5秒，避免立刻连续撞击服务限流。
5. 任务日志显示当前重试对象、次数、等待时间和安全错误说明；3次仍失败时明确显示最终原因。
6. AI画面理解保留已经成功的片段描述，再次点击只补仍然失败的片段。
7. 后端只把可恢复错误标记给前端，不记录或返回API Key、完整请求体和完整文案。

自动验证：
- `corepack pnpm build`通过。
- `cargo fmt -- --check`通过。
- `cargo check`通过。
- `cargo clippy --all-targets -- -D warnings`通过。
- `cargo test`共72项通过、0项失败。
- 新增测试覆盖临时429/503可重试、401/403不重试、额度/余额耗尽不重试和AI错误详情安全提取。
- 未调用真实AI/TTS密钥，真实限流、断网和服务抖动体验交由用户人工验收。
- 自动测试报告：`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-07-17_AI与TTS自动重试自动测试\自动测试报告.md`。
- 2026-07-17用户人工验收通过：AI/TTS自动重试功能符合使用预期。

## 2026-07-17：AI/TTS密钥本机安全保存

状态：代码开发、自动检查、真实桌面界面检查和用户人工验收全部完成。

本次完成：
1. 工作台右侧新增“API 密钥”入口，参考竞品的一次填写流程，但沿用项目现有深色工作台设计。
2. AI和TTS配置集中到独立功能模块，不继续堆入 `App.vue`。
3. Rust通过Windows数据保护机制加密整个配置文件，密文绑定当前Windows用户。
4. 前端提交新密钥后只接收脱敏状态，后端不会回传已保存密钥。
5. AI和TTS调用优先读取本机保存配置，再回退到原环境变量。
6. 默认AI Base URL、TTS资源ID和音色ID已经内置，用户主要填写两个Key和模型接入点ID。
7. 支持密钥显隐、留空保留旧密钥、配置状态和二次确认删除。
8. 保存后立即刷新TTS配置状态，软件重启后无需终端即可继续使用。

验证：
- 前端正式构建通过。
- Rust格式、编译和严格Clippy通过。
- Rust共67项测试通过、0项失败。
- Windows真实DPAPI加密/解密和加密文件往返通过，密文中不包含测试明文Key。
- 真实桌面应用视觉检查通过；无障碍树可以识别密钥入口、密码输入、显隐按钮和保存按钮。
- 测试未创建真实用户配置文件，也未使用或记录真实密钥。
- 2026-07-17用户人工验收通过：AI/TTS密钥入口可用，保存后无需每次通过终端配置。

安全边界：本机加密解决的是“无需重复输入”和“磁盘不明文保存”，不等于云端账户安全。用户仍需妥善保管火山账号，并在怀疑泄露时到平台撤销旧Key。

## 2026-07-17：多条AI差异视频生成

状态：代码开发、完整自动检查和用户人工验收已完成；项目继续保持测试中。

本次完成：
1. 用户可以设置一次生成1～10条视频，默认3条。
2. Rust差异方案生成器保留第一条当前主分镜，并为后续版本轮换备选画面。
3. 所有版本保持原文和分镜顺序；单条视频内部不重复片段，完全相同的版本不会重复输出。
4. 备选不足时返回实际唯一版本数，不用随机凑出重复视频。
5. 无配音和AI配音两条链路都按顺序生成；某一条失败后继续剩余任务。
6. AI配音只逐句合成一次，多个版本复用相同音频、字幕和配音时序。
7. 画布、平滑混剪、效果、画中画、原声、BGM、字幕、VideoEngine和结果列表继续复用现有稳定链路。
8. 界面显示生成数量、当前进度、成功数、失败数和备选不足提示。

自动验证：
- `corepack pnpm build`通过。
- `cargo fmt -- --check`通过。
- `cargo check`通过。
- `cargo clippy --all-targets -- -D warnings`通过。
- `cargo test`共61项通过、0项失败。
- 自动测试报告位于：`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-07-17_多条差异视频自动测试\自动测试报告.md`。
- 2026-07-17用户人工验收通过：一次生成多条AI差异视频已可用于继续真实测试。

后续仍需扩大样本的项目：
1. 测试素材库目前只有2个Project_03小样本，不满足至少3组、每组3～7个视频的稳定性回归标准。
2. 真实云端调用已经跑通，但仍需观察火山方舟额度、429限流和失败重试体验。
3. 仍需做更长文案、更多素材、更多生成条数的连续回归。

2026-07-17稳定性回归进展：
1. 本地基线复测通过：前端构建、Rust格式、编译、Clippy和67项Rust测试全部通过。
2. 已把3个真实服饰带货素材复制到标准测试素材库：`E:\Workspace\测试素材库\Project_03_本地短视频批量混剪工作台\2026-07-17_稳定性回归素材_服饰带货`。
3. `启动验收.bat` 可启动干净桌面验收窗口，FFmpeg显示就绪。
4. Codex自动化被Windows系统“选择文件夹”弹窗阻塞，未能自动完成素材导入；完整端到端回归仍需用户手动选择素材目录后继续，或后续增加测试辅助导入入口。
5. 回归报告已写入：`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-07-17_多条差异视频真实稳定性回归\真实稳定性回归报告.md`。

## 2026-07-16：修复切片后文案步骤不出现

状态：代码修复、前端构建检查和用户人工验收全部通过。

根因：
1. AI片段信息准备采用全有或全无规则，14个片段中只要1个预览图或时长失败，其他成功片段也不会写入 `aiPreparedSegments`。
2. 生成分镜前还有第二道“成功数量必须等于全部切片数量”的检查，会再次拒绝部分成功结果。
3. 核心流程只显示切片错误，没有显示AI片段准备错误，用户只能看到按钮退回“重新切片”。

修复：
1. 所有成功准备的片段立即保留，失败片段自动跳过。
2. 只要可用片段不少于2个，就显示文案输入并允许继续AI分镜。
3. 部分成功时显示绿色状态提醒，例如“13/14个片段准备完成，1个已跳过”。
4. 可用片段不足2个时显示红色阻断错误和任务日志入口提示。
5. 更新AI配音说明，删除已经过期的“长配音冻结尾帧”文案。

验证：`corepack pnpm build`通过。

## 2026-07-16：长文案本地断句与备选片段冲突自动清理

状态：代码修复、完整自动检查和本轮主链路人工验收全部通过。

真实问题：
1. AI偶尔不遵守“最长16个有效字符”，返回18字分镜，导致整份结果被拒绝。
2. AI会把一个片段同时作为某分镜主画面和另一分镜备选画面；这是可自动处理的轻微冲突，但旧校验会拒绝整份结果。

修复：
1. Rust先按标点和长度把原文自动拆成自然短句，通常6～14个有效字符，特殊尾句最多16个。
2. AI不再负责断句，也不再返回或改写文案，只按固定 `shotIndex` 选择主片段和备选片段。
3. AI返回顺序可以变化，Rust会按 `shotIndex` 恢复原文顺序。
4. 备选片段如果已被其他分镜作为主片段，Rust自动删除该备选项，不再报错。
5. 仍严格拒绝未知片段、未知分镜序号、遗漏分镜、重复主片段和重复分镜序号。

验证：
- `corepack pnpm build` 通过。
- `cargo fmt`、`cargo check`、`cargo clippy --all-targets -- -D warnings` 通过。
- Rust共55项测试通过；新增长文案自然断句、无标点断句、短文案拆成两个分镜、固定分镜顺序恢复和备选冲突自动清理测试。

## 2026-07-16：配音长于画面导致结尾长时间定格

状态：推荐方案已完成开发、自动检查和本轮主链路人工验收。

真实结果：
1. 用户成片总时长约15.016秒，视频轨约14.933秒，音频轨约15.016秒。
2. 底层人物画面从约10.8秒持续静止到结尾，约4.1秒。
3. 11.33秒和13.17秒的变化来自字幕切换，不是人物画面恢复运动。

原因：
当前每个AI分镜使用一个视频片段和一整段TTS配音。配音长于片段时，`narrated_mix.rs` 使用FFmpeg `tpad=stop_mode=clone` 复制最后一帧直到配音结束。字幕虽已自然分句，但没有把一个长配音分镜拆成多个画面分镜。

本次修复：
1. 软件在本地完整保留原文并拆成短分镜，目标每句6～14个有效字符，硬上限16个。
2. AI只按固定短句和预计说话时长选择主片段，并提供较长备选片段。
3. 前端把主片段、真实时长和备选片段一起交给TTS混剪流程。
4. VideoEngine按真实配音时长检查主画面；主画面过短时自动选择可安全适配的备选画面。
5. 只允许不超过8%的轻微减速和最多约0.8秒尾帧补齐；无法安全适配时明确报错，不再生成连续数秒定格的成片。
6. 保留原声时，原声音轨会跟随轻微减速同步处理。

自动验证：
- `corepack pnpm build` 通过。
- `cargo fmt -- --check`、`cargo check`、`cargo clippy --all-targets -- -D warnings` 通过。
- Rust当前共55项测试通过，覆盖本地断句、固定分镜匹配、备选冲突清理、长备选画面选择、轻微减速和长时间定格拒绝。

分析结果和原问题成片已归档到：`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-07-16_配音长于画面问题分析`。

## 2026-07-16：素材完整显示与切片入口第二轮修复

状态：两项代码修复、真实FFmpeg验证和人工复验全部通过。

根因：
1. 原始素材封面虽然在CSS中改为完整缩放，但当前运行窗口仍呈现旧的铺满裁切效果；只依赖界面样式不能保证旧缓存或覆盖规则下的完整显示。
2. 用户当前保存的输出路径指向已经不存在的旧切片目录；切片流程只检查路径是否为空，没有检查文件夹是否仍存在。
3. 全部切片失败后的错误只进入隐藏日志或设置弹窗，核心流程区没有展示，所以用户看到的是按钮点击后没有反应。

修复：
1. Rust缩略图引擎新增 `source` 和 `contain` 两种模式；原始素材封面直接生成320×240完整画面并留黑，AI片段预览继续保留原始比例。
2. 切片前通过Rust检查输出目录是否真实存在；没有目录或旧目录失效时自动弹出文件夹选择框。
3. 用户选择有效文件夹后直接继续切片，不需要再次点击按钮。
4. 切片和输出目录错误直接显示在核心流程按钮旁。

验证：
- 使用用户截图对应的真实竖屏 `5.mp4` 生成320×240完整封面，人物与上下画面均保留。
- 使用标准6秒测试视频按正式切片参数生成2个MP4片段。
- `corepack pnpm build` 通过。
- `cargo fmt -- --check`、`cargo check` 和 `cargo clippy --all-targets -- -D warnings` 通过。
- Rust全部47项测试通过，0项失败；其中新增2项缩略图模式测试。
- 测试结果：`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-07-16_素材显示与切片修复`。

人工验收结果（2026-07-16）：原始素材完整显示、失效目录重新选择和全部素材切片均通过；可以继续后续字幕与配音测试。

## 2026-07-15：自动字幕第一阶段

状态：首轮人工验收确认主要功能正常；根据验收截图完成字幕拥挤和原始素材封面裁切修复，等待针对这两项复验。

本次完成：
1. AI配音设置新增“自动生成字幕”开关，默认开启；关闭后生成的视频不带字幕。
2. 支持底部、中间、顶部三种字幕位置，以及小、标准、大三种字号。
3. 设置卡片提供实时字幕样式预览，关闭开关后位置和字号自动禁用。
4. 每个AI分镜会按逗号、句号等停顿符号拆成短字幕；没有标点的长文案按可读长度继续拆分，并在该句TTS配音时长内依次显示。
5. 新增独立Rust `video_engine/subtitle.rs`，集中ASS字幕生成、自然分句、按字符长度分配显示时间、中文转义、字号收缩、位置映射和FFmpeg路径转义。
6. 字幕在每个临时配音分镜中烧录，最终继续复用现有画布、效果、画中画、平滑混剪、原声混音、BGM和导出结果链路。
7. 生成前自动检查FFmpeg是否包含 `ass` 字幕滤镜；缺失时显示可理解的错误提示。
8. 临时ASS字幕文件保存在配音分镜临时目录，任务完成或失败后随目录清理。
9. 原始素材缩略图改为完整显示，竖屏封面不再被横向缩略框裁掉上下内容。

验证：
- `corepack pnpm build` 通过。
- `cargo fmt` 已执行。
- `cargo check` 通过。
- `cargo clippy --all-targets -- -D warnings` 通过。
- 修复后Rust共45项测试通过；字幕模块10项，补充自然分句、无标点长文案拆分、连续时间分配和纯标点输入校验。
- 真实FFmpeg测试通过：中文两行字幕成功烧录到横屏和竖屏3秒视频，输出包含正常视频轨和音频轨。
- 修复后再次生成5.2秒竖屏真实字幕视频，4条短字幕按顺序单行显示，没有同时堆叠。
- 界面检查通过：默认开关、位置、字号、预览和关闭后的禁用状态正常。
- 测试结果：`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-07-15_自动字幕第一阶段自动测试`。
- 修复测试结果：`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-07-15_字幕自然分句与素材封面修复`。

首轮人工验收反馈（2026-07-15）：自动字幕、配音和其他功能正常；长文案整段显示过于拥挤，原始素材竖屏封面显示不完整。两项均已完成定向修复，等待用户复验。

本阶段限制：只为AI配音视频生成分镜内短句字幕，不识别原视频真人语音，不做逐字变色、卡拉OK、复杂动画或字幕模板市场。

## 2026-07-15：TTS第二阶段逐句配音与画面同步第一版

状态：代码开发、自动检查、真实FFmpeg测试和完整人工验收全部通过。

本次完成：
1. AI配音设置新增“生成AI视频时加入逐句配音”开关；关闭时继续走原无配音AI混剪链路。
2. 根据当前人工调整后的AI分镜，依次为每句分镜文案调用火山TTS，并显示第几句生成进度。
3. 原片段声音在配音模式下默认静音；用户可打开“保留原视频声音”，并用0%～100%滑块调节音量，默认15%。
4. 配音比画面长时冻结最后一帧，配音比画面短时裁短画面，使每个分镜与对应语音同时结束。
5. 配音模式固定使用1.0x速度，避免配音变调；非1.0x时前后端都会明确拒绝。
6. 每句临时MP3写入系统临时目录，最终成功或失败后自动清理；中间配音视频同样使用独立临时目录。
7. 新增 `video_engine/narrated_mix.rs`，所有裁剪、尾帧冻结、静音、配音轨道和最终编码仍通过Rust VideoEngine与FFmpeg完成。
8. 最终继续复用现有画布、视频效果、画中画、平滑混剪、BGM、导出结果列表和任务日志。
9. 保留原声时会混入完整原声音轨，其中可能同时包含原人声、环境声和背景音乐；无音轨片段会自动退回纯AI配音。

验证：
- `corepack pnpm build` 通过。
- `cargo fmt -- --check` 通过。
- `cargo clippy --all-targets -- -D warnings` 通过。
- Rust单元测试35项全部通过，其中新增原声关闭、原声混合、无音轨回退和音量范围校验。
- 本地FFmpeg冒烟测试通过：1秒画面配2秒音频可冻结为2秒；3秒画面配1秒音频可裁为1秒。
- 自动测试结果：`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-07-15_TTS第二阶段自动测试`。
- 原声增强真实FFmpeg测试通过：有原声时可以按15%混合，无原声时可以自动生成纯AI配音视频；结果位于 `E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-07-15_TTS原声保留增强自动测试`。

人工验收结果（2026-07-15）：
1. 逐句AI配音、画面时长同步和最终视频生成通过。
2. 原声开关关闭时只保留AI配音。
3. 原声开关开启后可以按音量保留完整原视频声音。
4. 默认15%音量和音量滑块操作通过。
5. TTS第二阶段与原声增强正式验收完成，可以提交Git。

本阶段未实现字幕、声音克隆、人声与背景音乐分离、批量多音色、图片转视频、去水印、复杂时间轴或自动发布。下一步等待用户确认新的单功能阶段，推荐优先规划基于现有TTS时间信息的自动字幕第一版。

## 2026-07-14：火山引擎TTS语音合成第一阶段

状态：代码开发、自动检查和真实火山语音环境人工验收全部通过。

本次完成：
1. 新增 `src/features/tts`，集中TTS设置面板、状态、Tauri Service和类型。
2. 右侧成片设置新增“AI配音”入口，配音文案与当前AI混剪文案保持同步。
3. TTS面板显示是否配置、模型资源、音色ID、文案字数、生成状态、错误、MP3播放器、时长和输出路径。
4. Rust新增 `tts.rs`，调用火山引擎单向流式语音合成HTTP接口，将分块Base64音频合并后保存为本地MP3。
5. 火山语音凭证只从 `TTS_API_KEY` 环境变量读取；`TTS_RESOURCE_ID` 和 `TTS_SPEAKER`支持环境变量覆盖并提供默认值。
6. 后端保留火山返回的字词时间信息，为后续字幕和逐句同步预留数据，但本阶段不生成字幕。
7. TTS任务日志只记录字符数、成功文件名和安全错误，不记录Key、完整请求体、完整文案或音频Base64。
8. 修复火山正式成功码 `20000000` 被旧解析器误判为失败的问题；继续兼容 `code = 0` 的响应，并保留真实错误码拦截。

验证：
- `corepack pnpm build` 通过。
- `cargo fmt` 已执行。
- `cargo check` 通过。
- `cargo clippy --all-targets -- -D warnings` 通过。
- Rust单元测试26项全部通过，其中AI 15项、TTS 8项、FFmpeg工具路径3项。

人工验收结果（2026-07-15）：
1. 中文文案可以通过火山TTS生成本地MP3。
2. 软件内音频播放器可以正常加载和试听生成结果。
3. 界面可以显示文案字数、音频时长和输出路径。
4. 输出目录中可以找到生成的MP3，重新生成配音正常。
5. 火山正式成功码 `20000000` 不再被误判为服务拒绝。
6. 验收截图：`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-07-15_TTS第一阶段人工验收.png`。
7. 验收音频：`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-07-15_TTS第一阶段验收音频.mp3`。

本次未修改现有AI分镜请求、片段顺序、视频生成、原视频声音、BGM、字幕、FFmpeg或VideoEngine行为。该阶段验收完成后，项目已进入上方记录的TTS第二阶段。

## 2026-07-14：前端渐进式重构第五阶段

状态：代码拆分、自动检查和人工回归全部通过。

本次完成：
1. `ToolSettingModal.vue` 保留原有外部参数和事件接口，仅作为设置弹窗外壳与功能面板调度器。
2. 混剪、画布、平滑转场、画中画、BGM、封面、视频效果和导出设置拆入 `src/components/tool-settings`。
3. 设置弹窗从约654行减少到约174行，新增面板均控制在135行以内。
4. 原 `styles.css` 的3148行样式按连续区段拆入 `src/styles`，入口文件只保留7条按原顺序排列的导入。
5. 拆分前后生产构建生成的CSS文件指纹均为 `index-CXE9KrnC.css`，证明最终样式内容和顺序没有变化。
6. 设置弹窗补充标题关联，屏幕阅读器可以识别当前弹窗标题；页面可见内容和操作路径没有变化。

验证：
- `corepack pnpm build` 通过。
- `cargo fmt -- --check` 通过。
- `cargo clippy --all-targets -- -D warnings` 通过。
- Rust单元测试18项全部通过，其中AI 15项、FFmpeg工具路径3项。
- `git diff --check` 通过。

人工验收结果：设置弹窗、画布、平滑转场、画中画、BGM、视频效果、封面、导出设置、窗口布局和混剪生成回归全部通过。

本次未修改页面布局、颜色、尺寸、按钮文案、设置默认值、App对设置弹窗的调用接口、Rust Tauri命令、FFmpeg、AI请求或VideoEngine行为。下一步先确认首个新增业务功能，推荐优先规划TTS第一版；VideoEngine只在新功能需要时按需整理。

## 2026-07-13：前端渐进式重构第四阶段

状态：代码迁移、自动检查和人工回归全部通过。

本次完成：
1. 新增 `src/features/remix-export`，集中基础导出、随机抽取与拼接、分类混剪和随机批量生成。
2. 普通混剪和导出的处理中状态、错误、日志清理与结果记录从 `App.vue` 迁出。
3. 批量结果抽屉、基础导出Tauri调用和随机/分类抽取规则收拢到普通混剪功能目录。
4. AI混剪继续复用同一个“正在混剪”状态、结果记录和混剪结果日志格式。
5. `App.vue` 从第三阶段后的约1154行减少到约825行。

验证：
- `corepack pnpm build` 通过。
- `cargo fmt -- --check` 通过。
- `cargo clippy --all-targets -- -D warnings` 通过。
- Rust单元测试18项全部通过，其中AI 15项、FFmpeg工具路径3项。

人工验收结果：基础导出、随机混剪、分类混剪、批量生成、批量结果抽屉、成片设置、AI分镜与AI视频生成、任务日志和重新导入后的状态清理均通过。

本次未修改页面布局、CSS、随机规则、分类顺序、批量数量规则、成片设置、Rust Tauri命令、FFmpeg、AI请求或VideoEngine行为。下一阶段为设置弹窗和全局样式拆分，开始前先确认具体迁移范围。

## 2026-07-13：前端渐进式重构第三阶段

状态：代码迁移、自动检查和人工回归全部通过。

本次完成：
1. 新增 `src/features/materials`，集中素材面板、导入状态、封面管理、素材元数据读取和批量切片工作流。
2. 单视频导入、文件夹导入、素材选择、片段选择和素材封面状态从 `App.vue` 迁出。
3. 全部已导入视频依次切片、单个素材失败后继续、片段分类初始化和缩略图衔接从 `App.vue` 迁出。
4. 原 `videoImportService.ts` 和 `videoSplitService.ts` 收拢到素材功能目录。
5. `App.vue` 从第二阶段后的约 1381 行减少到约 1154 行。

验证：
- `corepack pnpm build` 通过。
- `cargo fmt -- --check` 通过。
- `cargo clippy --all-targets -- -D warnings` 通过。
- Rust 单元测试 18 项全部通过，其中 AI 15 项、FFmpeg 工具路径 3 项。

人工验收结果：单视频、多视频和文件夹导入、素材切换、全部素材切片、片段预览与分类、封面帧、AI分镜与生成、重新导入状态清理、随机混剪、分类混剪和批量生成均通过。

本次未修改页面布局、CSS、切片参数、切片顺序、Rust Tauri 命令、FFmpeg、AI请求或VideoEngine行为。下一阶段为普通混剪与导出模块整理，开始前先确认具体迁移范围。

## 2026-07-13：前端渐进式重构第二阶段

状态：第一阶段和第二阶段均已由用户人工验收通过。

本次完成：
1. 新增 `src/features/ai-remix`，集中 AI 混剪专用组件、类型、Tauri Service、片段工作流和状态控制。
2. 片段预览图与时长准备、图片描述缓存、分批画面理解、文案分镜规划、分镜调整和生成状态从 `App.vue` 迁出。
3. `App.vue` 只保留输出目录、成片设置、日志和生成结果等跨功能编排。
4. `AiRemixPlanner.vue` 和 `aiRemixService.ts` 移入 AI 功能目录，避免未来 TTS 等功能继续堆入公共目录。
5. `App.vue` 从第一阶段后的 1895 行减少到约 1381 行。

验证：
- `corepack pnpm build` 通过。
- `cargo fmt -- --check` 通过。
- `cargo clippy --all-targets -- -D warnings` 通过。
- Rust 单元测试 18 项全部通过，其中 AI 15 项、FFmpeg 工具路径 3 项。

人工验收结果：多视频导入与切片、片段预览、AI 图片理解、文案分镜、分镜调整、缓存复用、AI 视频生成、普通混剪、成片设置、日志和结果列表均通过。

本次未修改页面布局、CSS、火山引擎请求参数、提示词、Rust AI 后端、FFmpeg 或 VideoEngine 行为。下一阶段为素材与切片模块整理，开始前先确认具体迁移范围。

## 2026-07-13：代码质量与 Windows 安装包可行性审计

状态：审计、低风险结构整改和安装器构建验证完成，等待确定外部测试包的 FFmpeg 与 AI 凭证方案。

结论：
1. 项目不是必须重写的屎山，分层边界仍然清楚。
2. `App.vue` 2095 行、`styles.css` 3148 行、`mix.rs` 819 行，是后续需要小步拆分的技术债热点。
3. Windows NSIS 安装器已经成功生成，证明打包链路可行。
4. 当前安装器尚未内置 FFmpeg / FFprobe，AI 仍依赖环境变量，且安装包未签名，因此只适合内部技术测试，不适合直接发普通外部用户。

本次完成：
1. 补齐 `assets` 和 `tasks` 标准目录及用途说明。
2. 新增统一 FFmpeg / FFprobe 路径解析，支持显式路径、程序内置资源和系统 PATH。
3. 混剪导出参数收拢为 `RemixSettings`，消除 Rust 参数过多警告。
4. 新增安装包准备检查脚本、发布清单、基础功能分类和完整审计报告。
5. 成功生成 `本地短视频批量混剪工作台_0.3.4_x64-setup.exe`。

自动验证：
- `corepack pnpm build` 通过。
- `cargo fmt` 已执行。
- `cargo clippy --all-targets -- -D warnings` 通过。
- AI 15 项单元测试通过。
- FFmpeg 工具路径 3 项单元测试通过。
- `corepack pnpm tauri build --bundles nsis` 通过。

当前唯一阻塞：准备合法来源的 FFmpeg / FFprobe 分发文件，并在“用户自己的 Key 安全保存”和“自建服务端代理”之间确定 AI 凭证方案。

## 2026-07-14：关闭模型深度思考，修复纯文字分镜规划超时

状态：已完成修复、自动检查和真实火山环境人工验收。AI 智能混剪 MVP 主链路已经打通，可以进入第一轮测试。

定位结果：
1. 13 个片段的图片画面理解已经成功完成，当前失败点不是图片识别。
2. 超时发生在最后的“纯文字分镜规划阶段”。
3. 当前使用的 `doubao-seed-2-1-pro` 默认开启深度思考；纯文字分镜只是文案和已有画面描述的匹配，不需要深度推理。

本次修复：
1. 图片理解和纯文字分镜请求统一发送 `thinking.type = disabled`，明确关闭模型深度思考。
2. 纯文字分镜最大输出从 1600 tokens 调整为 800 tokens，减少无意义生成等待。
3. 纯文字分镜超时从 120 秒调整为 45 秒；图片理解仍保留 180 秒。
4. 新增请求体单元测试，防止后续修改时意外丢失关闭思考参数。

自动验证：
- `corepack pnpm build` 通过。
- `cargo fmt` 已执行。
- `cargo check` 通过。
- `cargo test ai_remix --lib` 通过，15 项测试全部成功。

人工验收结果：
1. 原 13 个片段可以完成图片画面理解。
2. 关闭模型深度思考后，纯文字分镜规划可以正常返回，不再超时。
3. AI 分镜能够正常展示，第一版智能混剪核心链路验收通过。

当前结论：MVP 已具备测试条件。本轮相关代码和文档已经保存 Git，未 push；下一步用不同素材和文案重复测试请求成功率、分镜可用率、人工调整次数和总出片耗时。

## 2026-07-13：AI 核心链路人工验收暂停记录

状态：部分通过，AI 分镜生成被超时阻塞；计划于 2026-07-14 继续排查。

人工验收已通过：
1. 可以一次导入多个视频。
2. “切片全部素材”可以处理全部已导入视频。
3. 不同来源视频生成的片段可以合并进入同一个素材池。

人工验收未通过：
1. 当前素材池共有 13 个片段，点击生成 AI 分镜后没有生成分镜，界面显示 0 个分镜。
2. 错误原文：`等待火山引擎返回结果超时。请检查网络后重试；如果仍然失败，请查看任务日志。（operation timed out）`
3. 错误截图：`E:\AI\Temp\codex-clipboard-77874e0d-bbda-4311-8abd-35db15ff33b8.png`

2026-07-14 第一件事：先查看或补充不含敏感信息的阶段日志，判断超时发生在“片段画面理解批次”还是“纯文字分镜规划”，再决定是否降低并发、缩小每批图片数或分别调整超时。调查前不得把问题直接归因于余额、API Key 或基础网络。

本次没有继续排查，没有提交 Git，也没有 push。

## 2026-07-11：AI 文案逐句分镜匹配第一版

状态：开发完成，等待真实方舟模型和真实素材人工验收。

本次完成：
1. AI 返回结构从全量 `orderedSegmentIds` 升级为逐句 `shots`。
2. 每个分镜包含对应文案、主片段和最多 3 个备选片段。
3. AI 可以舍弃无关片段，不再强制使用全部切片；不同分镜主片段不得重复。
4. Rust 严格拒绝少于 2 个分镜、未知片段、重复主片段、空文案和超过 3 个备选片段。
5. 前端按“文案句子 + 当前画面 + 备选画面”展示分镜。
6. 用户可以切换备选画面、上移、下移和删除分镜。
7. 人工调整后的主片段顺序继续复用现有 `concatSelectedSegments` 和 VideoEngine 导出。
8. 画布、平滑混剪、视频效果、画中画和 BGM 设置继续保留。

自动验证：
- `corepack pnpm build` 通过。
- `cargo fmt` 已执行，`cargo fmt -- --check` 的唯一差异已修正。
- `cargo check` 和 Rust 单元测试在依赖编译阶段被当前 Windows 环境拒绝执行新生成的 `erased-serde` 构建脚本，未进入项目代码编译或测试阶段。

本次未做：TTS、字幕、音频识别、镜头时长同步和 AI 批量多版本。

## 2026-07-11：修复 Windows 证书吊销检查导致 AI 无法连接

状态：已修复，等待用户重新启动软件验证真实方舟请求。

问题：软件请求方舟 `/chat/completions` 时在收到 HTTP 响应前失败，页面显示“无法连接 AI 服务”。

原因：当前 Windows 无法联网检查 HTTPS 证书吊销状态，系统 TLS 返回 `CRYPT_E_NO_REVOCATION_CHECK`。方舟域名、DNS 和接口本身均正常，使用跳过系统吊销查询的诊断请求可收到预期 `401` 响应。

修复：Rust AI 客户端改用 Mozilla 内置可信根证书，继续验证证书签名和域名，但不依赖 Windows 在线吊销检查；没有关闭 HTTPS 证书校验。

自动验证：AI 模块 8 项单元测试全部通过；`cargo fmt`、`cargo check` 和 `corepack pnpm build` 已通过。

## 2026-07-11：AI 文案智能混剪 MVP 第一版

状态：已完成开发和自动检查，等待真实火山引擎环境人工验收。

本次完成：
1. 切片完成后自动读取每个片段的真实时长，并复用现有 320px JPG 预览图。
2. Rust 新增火山引擎方舟多模态 AI 请求入口，API Key、Base URL 和模型仅从环境变量读取。
3. AI 请求只发送文案、片段编号、时长和预览图，不上传完整视频或本地文件路径。
4. 后端严格校验 `orderedSegmentIds` 必须完整、唯一且只包含已知片段编号。
5. 前端新增文案输入、AI 排序结果、上移、下移、删除和重新排序。
6. 人工调整后的顺序继续调用现有 `concatSelectedSegments` 和 `VideoEngine` 导出。
7. 画布、平滑混剪、视频效果、画中画、BGM 和导出结果列表继续保留。
8. 保留已有字幕接口草稿并统一传递禁用配置，不开发字幕 UI 或字幕烧录。

自动验证：`corepack pnpm build`、`cargo fmt`、`cargo check` 已通过；AI 排序校验单元测试 7 项全部通过。

待人工验收：
1. 配置 `AI_API_KEY`、`AI_BASE_URL`、`AI_MODEL` 后完成真实方舟请求。
2. 确认 AI 排序、人工调整和最终视频顺序一致。
3. 回归画布、平滑混剪、视频效果、画中画、BGM 和旧混剪链路。

本次未做：字幕、TTS、会员、复杂时间轴、完整视频上传、AI 自动发布和无关重构。

## 2026-06-22 补充记录：V0.4.2 BGM 淡入淡出第一版
状态：已开发，等待和 V0.4.1 一起人工验收。
本次完成：1. BGM 设置弹窗新增“BGM 淡入秒数”和“BGM 淡出秒数”。2. 默认淡入 0.5 秒、淡出 0.5 秒。3. 淡入淡出范围限制为 0 到 10 秒。4. 前端生成前会校验淡入淡出秒数，避免参数错误导致按钮看似无反应。5. Rust VideoEngine 会根据最终导出视频时长计算淡出起点，并通过 FFmpeg `afade` 只处理背景音乐。6. 本次继续复用现有拼接抽中片段、分类混剪和批量生成链路，没有新增网络素材、节拍卡点或多轨时间线。
验证情况：`corepack pnpm build` 已通过；设置 `CARGO_HTTP_CHECK_REVOKE=false` 后 `cargo check` 已通过；`cargo fmt` 已执行。
待人工验收：1. BGM 淡入秒数为 0.5 时，开头音乐不是突然进入。2. BGM 淡出秒数为 0.5 时，结尾音乐不是突然停止。3. 淡入淡出设为 0 时，BGM 不做渐变。4. 分类混剪和批量生成带 BGM 淡入淡出仍能导出。
本次未做：网络音乐、在线素材库、AI 自动配乐、歌词识别、节拍卡点、字幕识别、TTS、复杂多轨时间线。

## 2026-06-22 补充记录：V0.4.1 本地 BGM 第一版
状态：已开发，等待用户人工验收。
本次完成：1. 高级模式右侧新增“背景音乐”正式入口。2. BGM 设置弹窗支持开启 / 关闭 BGM。3. 支持选择本地音频文件，格式包括 mp3、wav、m4a、aac、flac、ogg。4. 支持调节原视频音量和 BGM 音量，范围为 0 到 2。5. 拼接抽中片段、分类混剪、批量生成都会把 BGM 参数传给 Rust VideoEngine。6. VideoEngine 统一封装 FFmpeg 音频混合，不让前端直接拼接 FFmpeg 命令。7. BGM 比视频短时通过循环适配，BGM 比视频长时通过最短流控制随视频结束。8. 开启 BGM 但未选择音频文件时，前端会直接提示错误，避免按钮看起来无反应。
验证情况：`corepack pnpm build` 已通过；`cargo fmt` 已执行；设置 `CARGO_HTTP_CHECK_REVOKE=false` 后 `cargo check` 已通过；已用本地预览页面确认高级模式能看到“背景音乐”入口，BGM 设置弹窗能显示“启用 BGM”“选择本地音乐”“原视频音量”“BGM 音量”。
待人工验收：1. 不开启 BGM 时，原有拼接、分类混剪、批量生成仍正常。2. 开启 BGM 并选择本地音乐后，导出视频可以听到背景音乐。3. 调低 / 调高原视频音量能生效。4. 调低 / 调高 BGM 音量能生效。5. 分类混剪带 BGM 可导出。6. 批量生成带 BGM 可导出。7. 任务日志能看到 BGM 是否开启。
本次未做：AI 自动配乐、在线音乐库、歌词识别、节拍卡点、字幕识别、TTS、API Key、会员卡密、复杂多轨时间线。

## 2026-06-22 补充记录：进入 V0.4 音频与 BGM 第一版规划
状态：V0.3.6 阶段已经由用户人工验收完成，并已提交 Git；当前正式进入 V0.4 开阶段规划。
当前阶段：V0.4 音频与 BGM 第一版。
本阶段目标：在不破坏现有混剪主链路的前提下，让用户可以给导出的混剪视频添加本地背景音乐。
最高优先级：先做本地 BGM 第一版，不做 AI 自动配乐、在线音乐库、歌词识别、节拍卡点、字幕识别、TTS、API Key、会员卡密或复杂多轨时间线。
建议第一步：读取现有拼接抽中片段、分类混剪、批量生成和 VideoEngine 导出链路，确认 BGM 参数应该从前端、Service 层一路传到 Rust VideoEngine。
预计涉及文件：前端主流程和设置入口、视频混剪 Service、Rust VideoEngine 混剪导出模块、项目文档。
验证方式：`corepack pnpm build`；如果修改 Rust / VideoEngine，则运行 `cargo check`；最后人工验收不开 BGM、开启 BGM、音量调节、分类混剪带 BGM、批量生成带 BGM。
边界：所有 FFmpeg 音频混合逻辑必须继续由 VideoEngine 封装，页面组件不得直接拼接 FFmpeg 命令。

## 2026-06-22 补充记录：补回平滑混剪高级入口
状态：已修复，并已由用户人工验收通过。
问题：V0.3.6 验收中发现，平滑混剪底层功能和二级设置弹窗仍然存在，但高级模式右侧设置入口没有展示出来，用户无法从主界面找到。
修复：在右侧高级模式的正式设置入口中补回“平滑混剪”，说明为“淡入淡出、过滤过短片段”。
范围：本次只恢复 UI 入口，没有修改混剪逻辑，没有修改 Rust / FFmpeg / VideoEngine。
人工验收结果：用户已确认平滑混剪入口和相关验收项完成。

## 2026-06-22 补充记录：修复画中画透明度超范围导致混剪看似无反应
状态：已修复，并已由用户人工验收通过。
问题：V0.3.6 验收中发现，先设置画布、画中画和视频效果后，再点击分类混剪或批量生成，按钮看起来没有反应。
原因：画中画透明度输入框虽然标了 0 到 1，但用户仍可手动输入 5。后端 VideoEngine 校验透明度只能在 0 到 1 之间，因此会拒绝生成；前端错误只进入任务日志，没有在主流程按钮附近直接显示，用户会误以为按钮失效。
修复：1. 前端生成前新增画中画参数校验。2. 画中画大小比例、透明度、边距输入会自动裁剪到合法范围。3. 分类混剪和批量生成失败时，会在中间流程区直接显示错误提示。4. 本次没有修改 Rust / FFmpeg / VideoEngine。
人工验收结果：用户已确认画中画参数提示、分类混剪、批量生成和平滑混剪相关验收完成。

## 2026-06-21 补充记录：进入 V0.3.6 阶段验收与整理
状态：已确认进入 V0.3.6，不直接开发新功能，先做阶段验收。
本次目标：统一验收 V0.3 已完成能力，包括基础导入预览、固定切片、随机混剪、批量生成、画布比例、平滑混剪、基础视频效果、画中画、封面帧 / 片段预览、分类混剪、新手 / 高级双模式 UI。
执行原则：1. 先验收主链路。2. 再验收 V0.3 新增能力。3. 最后检查旧功能是否被破坏。4. 发现问题只做小步修复。5. 暂不开发 AI 自动分类、字幕识别、TTS、API Key、会员卡密、复杂时间线或新的大 UI 重构。6. 不绕过 VideoEngine 直接拼接 FFmpeg 命令。
当前下一步：按 `TODO_NEXT.md` 的 V0.3.6 验收顺序开始人工验收。

## 2026-06-21 补充记录：片段分类支持点击视频预览
状态：已由用户人工验收通过，准备提交 Git。
本次完成：1. 左侧高级模式的片段分类列表支持点击片段。2. 点击 `segment_000.mp4`、`segment_001.mp4` 等片段后，中间大视频预览区会直接切换播放该片段。3. 当前选中的片段行会高亮，方便用户知道正在看哪个片段。4. 分类下拉框继续保留，点击下拉框只修改分类，不会误触发片段切换。5. 点击左侧原视频时会回到原视频预览。6. 本次只修改 Vue 前端状态联动和 CSS，没有修改 Rust / FFmpeg / VideoEngine，也没有修改混剪生成逻辑。
验证情况：- `corepack pnpm build` 已通过。- 未运行 `cargo check`，因为本次没有修改 Rust / FFmpeg / VideoEngine。
人工验收结果：用户已确认功能可用，可以提交 Git。

## 2026-06-21 当前状态摘要

当前版本：
v0.3.5-dev

当前定位：
本地桌面端 AI 视频批量混剪工作台。

当前状态：
用户已多轮人工验收，现有主链路可正常使用。V0.3.5 分类混剪第一版已开发完成；本次已按参考视频和截图完成新手/高级双模式 UI 第一轮简化，等待用户手动验收。

当前已完成主链路：
1. 视频导入。
2. 视频预览。
3. 固定时长切片。
4. 随机片段抽取。
5. 片段拼接导出。
6. 批量生成混剪结果。
7. 画布比例和模糊背景。
8. 平滑混剪。
9. 基础视频效果。
10. 画中画。
11. 封面帧 / 片段预览图。
12. 分类混剪第一版。

当前唯一下一步：
手动验收新手/高级双模式 UI 和 V0.3.5 分类混剪第一版。

本阶段继续禁止：
1. 不做 AI 自动分类。
2. 不做字幕识别。
3. 不做 TTS 配音。
4. 不做 API Key 系统。
5. 不做会员卡密系统。
6. 不做 UI 大重构。
7. 不绕过 VideoEngine 直接拼接 FFmpeg 命令。

整理说明：
本摘要用于解决旧文档中 `v0.2.0-dev`、V0.3 多个验收记录和下一步任务混杂的问题。历史记录继续保留在下方，当前执行入口以 `TODO_NEXT.md` 为准。

## 2026-06-21 补充记录：新手/高级双模式 UI 第一轮

状态：已开发，等待用户手动验收。

本次完成：
1. 默认进入工作台新手模式，不再先停留在模块首页。
2. 新手模式只突出四步流程：导入素材 -> 预览视频 -> 开始处理 -> 查看结果。
3. 左侧改为清爽素材树，导入视频和导入文件夹保留为顶部小按钮，不再占用大面积。
4. 中间保留最大视频预览区，底部流程区压缩为简洁状态条。
5. 右侧默认只显示本地引擎状态、模式切换、新手流程、高级设置入口和结果入口。
6. 高级模式继续保留混剪设置、画布设置、视频效果、画中画、封面、字幕、音频、BGM、TTS、水印等入口。
7. 底部任务栏在新手模式只保留输出目录、处理状态和开始处理按钮；高级模式继续显示更多导出参数。
8. 本次没有修改 Rust / FFmpeg / VideoEngine，没有改视频处理逻辑。

验证情况：
- `corepack pnpm build` 已通过。
- 未运行 `cargo check`，因为本次没有修改 Rust / FFmpeg / VideoEngine。

待人工验收：
1. 默认打开是否足够简洁。
2. 左侧素材区是否不再挤占空间。
3. 中间预览区是否足够大。
4. 右侧是否不再堆满参数。
5. 点击“进入高级”后，旧功能入口是否仍然能找到。
6. 原有导入、预览、切片、混剪、批量生成、结果查看入口是否仍正常。

## 2026-06-21 补充记录：V0.3.5 分类混剪第一版

状态：已开发，等待用户手动验收。

本次完成：
1. 已切片片段支持手动选择分类。
2. 支持分类：开头钩子、产品展示、使用过程、细节特写、效果展示、结尾引导。
3. 分类信息保存在当前页面状态中，切换片段和抽取片段时不会丢失。
4. 新增“分类混剪”按钮。
5. 分类混剪按固定顺序抽取片段：开头钩子 -> 产品展示 -> 使用过程 -> 细节特写 -> 效果展示 -> 结尾引导。
6. 每个分类默认随机抽取 1 个片段。
7. 空分类会自动跳过，并写入任务日志。
8. 至少 2 个分类有片段时，会复用现有 VideoEngine 拼接导出能力生成 mp4。
9. 分类混剪结果会进入导出结果列表。
10. 继续保留画布比例、模糊背景、平滑混剪、基础视频效果、画中画、输出目录和任务日志。

验证情况：
- `corepack pnpm build` 已通过。
- `cargo check` 已通过。

待人工验收：
1. 切片后给不同片段选择不同分类。
2. 至少设置两个分类后点击“分类混剪”。
3. 检查任务日志是否显示每个分类抽中了哪个片段。
4. 检查空分类是否自动跳过。
5. 检查导出结果是否进入导出结果列表。
6. 复测随机抽取、拼接抽中片段、批量生成、画布比例、模糊背景、平滑混剪、基础视频效果和画中画。

## 2026-06-14 补充记录：V0.3.4 关键帧 / 封面帧 / 片段预览第一版

状态：已开发，等待用户手动验收。

本次完成：
1. 新增后端抽帧能力，通过 VideoEngine 封装 FFmpeg 生成 jpg 预览图。
2. 导入视频后自动尝试生成素材封面帧。
3. 切片成功后自动为片段生成预览图。
4. “视频封面 / 视频帧操作”二级设置面板支持输入秒数并设为当前封面帧。
5. 左侧素材列表显示视频封面，片段列表和已抽取片段显示预览图。
6. 中间信息区显示当前视频封面帧。
7. 不做复杂关键帧动画，不改字幕、TTS、会员、API Key。
8. 未修改混剪、基础效果、画中画导出链路。

验证情况：
- `corepack pnpm build` 已通过。
- `cargo check` 已通过。

待人工验收：
1. 导入视频后素材卡片是否出现封面图。
2. 切片后片段列表是否出现预览图。
3. 随机抽取后已抽取片段是否继续显示预览图。
4. 打开“视频封面”或“视频帧操作”，输入秒数后是否能生成并替换封面。
5. 原有导入、预览、切片、随机抽取、拼接、批量生成、画中画入口是否仍正常。

## 2026-06-14 补充记录：V0.3.3 画中画第一版

状态：已开发，等待用户手动验收。

前置验收：
1. V0.3.1 平滑混剪已由用户手动测试确认可用。
2. V0.3.2 基础视频效果已由用户手动测试确认可用。

本次完成：
1. 右侧画中画入口已接入二级设置窗口。
2. 支持选择叠加视频或图片作为画中画素材。
3. 支持启用 / 关闭画中画。
4. 支持位置：左上、右上、左下、右下、居中。
5. 支持大小比例：0.2 到 0.5。
6. 支持透明度：0 到 1。
7. 支持边距：0 到 240。
8. 画中画接入拼接抽中片段和批量生成混剪。
9. 继续保留画布比例、模糊背景、平滑混剪、基础视频效果、导出结果列表和任务日志。

验证情况：
- `corepack pnpm build` 已通过。
- `cargo check` 已通过。

待人工验收：
1. 不开启画中画时，原有拼接和批量生成仍正常。
2. 使用视频作为画中画素材导出。
3. 使用图片作为画中画素材导出。
4. 测试 5 个位置是否正确。
5. 测试大小比例、透明度、边距是否生效。
6. 与 16:9 横屏 + 模糊背景一起测试。
7. 与平滑混剪、基础视频效果一起测试。

## 2026-06-14 补充记录：V0.3.2 基础视频效果第一版

状态：已开发，等待用户手动验收。

前置验收：
1. V0.3.1 平滑混剪已由用户手动测试确认可用。

本次完成：
1. 拼接抽中片段和批量生成混剪支持基础视频效果参数。
2. 保留原有水平镜像，并新增垂直镜像。
3. 新增旋转：顺时针 90°、逆时针 90°、180°。
4. 新增亮度、对比度、饱和度调整。
5. 新增轻微缩放，范围限制为 1.00 到 1.20。
6. 继续保留已有视频变速能力，范围仍为 0.5x 到 2.0x。
7. 所有效果继续走 VideoEngine 统一封装，不在前端拼接 FFmpeg 命令。
8. 不破坏画布比例、模糊背景、平滑混剪、导出结果列表和任务日志入口。

验证情况：
- `corepack pnpm build` 已通过。
- `cargo check` 已通过。

待人工验收：
1. 单独测试垂直镜像。
2. 单独测试 90° / 180° 旋转。
3. 单独测试亮度、对比度、饱和度。
4. 单独测试 1.05x 或 1.10x 轻微缩放。
5. 测试这些效果与 16:9 横屏 + 模糊背景一起使用。
6. 测试这些效果与平滑混剪一起使用。

## 2026-06-14 补充记录：UI v3 高还原重构

状态：已开发，等待用户手动验收。

本次完成：
1. 首页保持模块卡片入口，不直接进入工作台。
2. 首页补齐图标占位、标题、说明文案、功能标签和状态按钮。
3. 顶部标题栏补齐用户图标、菜单图标和窗口控制区占位。
4. 工作台保持左素材、中预览、右功能入口、底部导出参数四区结构。
5. 中间预览区右上角增加视频裁剪、添加文本入口。
6. 视频文案区放在中间视频预览下方，包含文案 1、新增入口、大文本框、字幕模式和字数提示。
7. 左侧素材区补充文件夹树。
8. 右侧改为功能入口卡片，不展开复杂参数。
9. 底部导出栏补充进度条和日志、导出结果、批量结果入口。

验证情况：
- `corepack pnpm build` 已通过。

本次未修改：
1. 没有修改 Rust / FFmpeg 后端。
2. 没有开发新的视频处理功能。
3. 没有复制参考软件名称、Logo、品牌文案或图标素材。

## 2026-06-14 补充记录：UI v3 对标结构与尺寸定稿

状态：已开发，等待用户手动验收。

本次完成：
1. 首页改为模块入口结构，包含创作中心、效率工具、自动化三个分类。
2. 首页保留当前项目自己的软件名称，不复制竞品软件名、Logo、品牌文案和图标素材。
3. 工作台采用专业视频处理软件常见结构：左侧素材区、中间预览和核心操作区、右侧功能入口区、底部任务处理区。
4. 左侧栏约 340px，右侧栏约 320px，底部任务条约 104px，主预览区保留最大可用空间。
5. 右侧只保留功能入口，复杂参数继续进入 480px 到 640px 的二级设置窗口。
6. 底部任务区包含输出格式、分辨率、帧率、命名规则、线程、进度信息和开始处理入口。
7. 保留导入视频、导入文件夹、视频预览、切片、随机抽取、拼接抽中片段、批量生成、画布比例、模糊背景、平滑混剪、输出目录、导出结果列表、任务日志等入口。

验证情况：
- `corepack pnpm build` 已通过。

本次未修改：
1. 没有修改 Rust / FFmpeg 后端逻辑。
2. 没有新增真实字幕、TTS、会员、API Key、画中画或视频效果处理能力。
3. 没有复制竞品品牌资源。

## 2026-06-14 补充记录：UI v3 主工作台 + 二级设置窗口

状态：已开发，待用户手动验收。

完成：
1. 主窗口保持三栏结构：左侧素材和输出目录，中间视频预览和核心操作，右侧功能入口列表。
2. 右侧不再展开堆叠全部参数，改为“混剪设置、画布设置、视频效果、平滑转场、画中画、镜像旋转、视频变速、字幕设置、导出设置”等入口。
3. 点击入口后打开二级设置弹窗，参数区域内部滚动，不挤压主页面。
4. 任务日志、导出结果、批量生成结果改为底部浮层抽屉，需要时展开，不再占用固定底部高度。
5. 拆分前端组件：HomePage、MaterialPanel、PreviewPanel、RightToolPanel、ToolSettingModal、ExportResultDrawer、TaskLogDrawer、BatchResultDrawer。
6. 保留导入视频、导入文件夹、视频预览、切片、随机抽取、拼接抽中片段、批量生成、画布比例、模糊背景、平滑混剪、输出目录、导出结果列表和任务日志入口。

验证情况：
- `corepack pnpm build` 已通过。

本次未修改：
1. 没有修改 Rust / FFmpeg 后端逻辑。
2. 没有新增真实字幕、TTS、会员、API Key 或画中画处理能力。

## 2026-06-14 补充记录：V0.3.1 平滑混剪第一版

状态：已开发，待用户手动验收。

完成：
1. “拼接抽中片段”支持平滑混剪开关。
2. “批量生成混剪”支持平滑混剪开关。
3. 开启平滑混剪后，VideoEngine 会过滤小于 1.5 秒的过短片段。
4. 开启平滑混剪后，每个有效片段会先生成临时平滑片段，画面首尾各 0.2 秒淡入淡出。
5. 有原声音频的片段会同步做 0.2 秒音频淡入淡出。
6. 原有画布比例、模糊背景、水平镜像、变速和导出结果列表链路继续保留。

验证情况：
- `corepack pnpm build` 已通过。
- `cargo check` 已通过。

待人工验收：
1. 关闭平滑混剪时，原有拼接和批量生成仍正常。
2. 开启平滑混剪时，片段切换有淡入淡出效果。
3. 开启平滑混剪时，小于 1.5 秒的片段会被过滤，日志显示过滤数量。
4. 16:9 横屏 + 模糊背景仍正常。
5. 导出成功后仍进入导出结果列表。

## 2026-06-14 补充记录：V0.3 阶段开发顺序已重排

当前阶段：
V0.3 混剪质量增强与视频效果处理阶段。

当前已完成：
1. V0.1 基础视频闭环。
2. V0.2 基础混剪闭环。
3. UI v2 暗色专业工作台。
4. 画布比例 / 裁剪 / 模糊背景。
5. 16:9 横屏 + 模糊背景修复。
6. 导出结果列表管理。
7. CodeGraph MCP 已可用。

V0.3 开发顺序：
1. V0.3.1 淡入淡出 / 平滑混剪第一版。
2. V0.3.2 基础视频效果第一版。
3. V0.3.3 关键帧 / 封面帧 / 片段预览图第一版。
4. V0.3.4 分类混剪第一版。
5. V0.3.5 画中画第一版。
6. V0.3.6 V0.3 阶段验收与整理。

下一步最高优先级：
V0.3.1 淡入淡出 / 平滑混剪第一版。

原因：
分类混剪、批量生成、后续视频效果都会依赖底层拼接导出流程，必须先把成片切换质量做好。

本次只更新文档：
1. 没有修改 UI。
2. 没有修改视频处理逻辑。
3. 没有修改 Rust / FFmpeg 后端。

## 2026-06-14 补充记录：UI v2 主链路手动验收通过

状态：用户已确认通过。
验收结果：
1. UI v2 右侧参数栏遮挡混剪入口的问题已解决。
2. 导入、预览、切片、随机抽取、拼接、批量生成、导出主链路可以跑通。
3. 当前可以继续进入下一步功能开发或调试。

## 2026-06-14 补充记录：修复 UI v2 右侧参数栏遮挡混剪入口

状态：已修复，待用户手动验收。
完成：
1. 压缩右侧参数栏卡片间距、标题间距、输入框高度和按钮上边距。
2. 保留原有功能按钮尺寸，没有压缩混剪、批量生成、导出等关键操作按钮的可点击空间。
3. 本次只修改前端样式，没有修改 Rust、FFmpeg、VideoEngine 或视频处理 Service。
验证情况：
- `corepack pnpm build` 已通过。

## 历史版本记录

v0.2.0-dev

## 项目定位

本地桌面端 AI 视频批量混剪工作台。

## 历史阶段

阶段二：基础混剪能力开发中。

当前核心目标：

```text
视频切片 → 随机片段抽取 → 片段拼接 → 基础混剪导出
```

## 已完成

- [x] 首页 UI
- [x] 视频导入按钮
- [x] 基础页面布局
- [x] 项目整体开发框架规划
- [x] FFmpeg环境检测
- [x] 单视频/多视频文件导入
- [x] 视频信息读取
- [x] 文件夹导入
- [x] 视频预览
- [x] 输出目录选择
- [x] 基础导出任务
- [x] 基础导出日志
- [x] V0.1 主链路人工验收通过
- [x] 固定时长视频切片
- [x] 随机片段抽取
- [x] 片段拼接导出
- [x] 批量生成混剪结果
- [x] 基础水平镜像
- [x] 桌面软件工作台 UI 框架升级
- [x] 基础变速

## 历史阻塞

暂无。

## V0.2 阶段历史限制（不再作为当前范围）

- 不允许更换 Tauri
- 不允许重做首页
- 不允许新增会员系统
- 不允许开发 AI 内容理解
- 不允许开发 API Key 系统
- 不允许开发 TTS
- 不允许开发 ASR 字幕识别
- 不允许进行 UI 大重构

## 2026-06-12 开发记录：基础变速

日期：2026-06-12
完成：实现基础变速。用户可以在混剪参数中设置 0.5x 到 2.0x 的变速倍数，单次片段拼接和批量生成都会通过 VideoEngine 应用对应变速。
涉及文件：
- src-tauri/src/video_engine/mix.rs
- src-tauri/src/lib.rs
- src/services/videoMixService.ts
- src/App.vue
- PROJECT_STATUS.md
- TODO_NEXT.md
验证情况：`corepack pnpm build` 已通过；设置 `CARGO_HTTP_CHECK_REVOKE=false` 后 `cargo check` 已通过；已检查变速参数由页面传入，FFmpeg 滤镜仍只在 VideoEngine 中拼接。
人工验收：用户已确认基础变速测试完美通过。
遗留问题：暂无。

## 下一步建议

1. 继续开发 V0.2 裁剪比例
2. 裁剪比例先支持原画、9:16 竖屏、1:1 方屏、16:9 横屏
3. 保持裁剪逻辑通过 VideoEngine 封装，不让页面组件直接拼接 FFmpeg 命令
---

## 2026-06-12 补充记录：视频比例画布适配

状态：已开发，待用户人工验收。

本次实际实现的产品逻辑：

1. 不是自由裁剪。
2. 不是拖拽缩放裁剪框。
3. 选择 9:16、1:1、16:9 时，保留完整原视频画面。
4. 目标比例外的空白区域用黑边或模糊背景补齐。
5. 预览区使用目标比例画布展示，前景播放器仍保留原生 controls。
6. 基础导出、片段拼接导出、批量生成混剪结果都接入同一套画布参数。

涉及文件：

- src/App.vue
- src/styles.css
- src/services/videoMixService.ts
- src/services/videoRenderService.ts
- src-tauri/src/lib.rs
- src-tauri/src/video_engine/canvas.rs
- src-tauri/src/video_engine/render.rs
- src-tauri/src/video_engine/mix.rs

验证情况：

- `corepack pnpm build` 已通过。
- `cargo check` 已通过。
- `corepack pnpm tauri dev` 已启动；首次启动时发现旧开发进程占用 1420 端口，已结束旧进程后重新启动。

待人工验收：

1. 原画模式预览和导出不改变比例。
2. 9:16 黑边模式保留完整画面，导出为竖屏画布。
3. 9:16 模糊背景模式保留完整画面，导出为竖屏画布。
4. 1:1 和 16:9 模式导出比例正确。
5. 播放器底部播放、暂停、时长条仍可用。
6. 画布参数能与水平镜像、基础变速一起使用。

## 2026-06-12 补充记录：修复原画预览比例异常

状态：已修复，待用户复验。

问题：
导入 1080 x 1920 竖屏原始视频后，预览区没有按原视频比例居中显示，而是被横向大画布撑满，导致原画预览看起来被放大和裁切。

修复：

1. 为预览画布增加明确的比例数字变量。
2. 预览画布根据容器宽高自动计算最大可显示尺寸。
3. 原画模式使用原视频宽高比例。
4. 9:16、1:1、16:9 模式继续使用目标画布比例。
5. 未修改导出引擎和 FFmpeg 逻辑。

涉及文件：

- src/App.vue
- src/styles.css

验证情况：

- `corepack pnpm build` 已通过。

待复验：

1. 竖屏原画导入后，预览区按竖屏比例居中显示。
2. 横屏原画导入后，预览区按横屏比例显示。
3. 9:16、1:1、16:9 切换仍然正常。
4. 播放器底部 controls 仍然可用。

## 2026-06-12 补充记录：修复混剪导出模糊背景未生效

状态：已修复，用户人工测试已通过。

问题：
选择 16:9 横屏和模糊背景后，点击“拼接抽中片段”导出的视频仍然像竖屏居中黑边，模糊背景没有生效。

修复：

1. 已确认页面会把画布比例和背景方式传给 `VideoMixService`。
2. 已确认“拼接抽中片段”会调用混剪导出流程，不是基础导出流程。
3. 混剪导出已使用 `libx264 + aac` 重新编码，可应用 FFmpeg 滤镜。
4. 修复 VideoEngine 画布滤镜：模糊背景模式下，前景主体不再先 pad 成黑底整画布，避免覆盖背景层。
5. 混剪导出日志增加输出比例、输出分辨率、背景方式、是否应用到混剪导出。

涉及文件：

- src/App.vue
- src/services/videoMixService.ts
- src-tauri/src/video_engine/canvas.rs
- src-tauri/src/video_engine/mix.rs
- PROJECT_STATUS.md
- TODO_NEXT.md

验证情况：

- `corepack pnpm build` 已通过。
- `cargo check` 已通过。
- 用户已确认 16:9 横屏 + 模糊背景的“拼接抽中片段”导出完美通过人工测试。

## 下一步建议

1. 开发导出结果列表管理。
2. 支持从页面打开输出目录。
3. 只整理导出结果查看和目录打开能力，不新增字幕、TTS、会员、API Key 或 AI 功能。

## 2026-06-12 补充记录：导出结果列表管理第一版

状态：已开发，用户截图确认可用。

完成：

1. 基础导出成功后加入导出结果列表。
2. 拼接导出成功后加入导出结果列表。
3. 批量生成每条成功结果加入导出结果列表。
4. 结果区统一展示导出类型、文件名、时间和完整路径。

验证情况：

- `corepack pnpm build` 已通过。

## 2026-06-13 补充记录：UI v2 暗色专业工作台重构

状态：已开发，待用户验收。

完成：

1. 增加首页 / 功能入口页。
2. 首页保留项目名称，不使用竞品品牌名、Logo 和文案。
3. 已实现模块可以进入真实工作台，未实现模块显示“敬请期待”。
4. 工作台调整为深色三栏布局：左侧素材区、中间预览和结果区、右侧参数区、底部任务区。
5. 保留导入、预览、切片、随机抽取、拼接、批量生成、画布设置、输出目录、结果列表和任务日志能力。
6. 未修改 Rust、FFmpeg 或视频处理核心逻辑。

验证情况：

- `corepack pnpm build` 已通过。

## 2026-06-12 补充记录：打开输出目录和结果位置

状态：已开发，待用户验收。

完成：

1. 输出设置区增加“打开输出目录”。
2. 导出结果列表每条结果增加“打开位置”。
3. 后端通过 Tauri Command 调用系统文件管理器。

验证情况：

- `corepack pnpm build` 已通过。
- `cargo check` 已通过。

## 2026-06-12 补充记录：修复任务日志区域过短

状态：已修复，待用户验收。

完成：

1. 底部任务日志区域高度从固定短高度改为更大的自适应高度。
2. 日志列表取消过短的最大高度限制，改为占满任务日志面板可用空间。

验证情况：

- `corepack pnpm build` 已通过。
## 2026-07-12 补充记录：AI 主流程工作台 UI 优化

状态：界面开发和自动检查已完成，等待用户人工验收。

本次完成：
1. 工作台收敛为左侧素材、中间预览与文案分镜、右侧成片设置、底部单一主操作。
2. AI 文案分镜成为第一屏核心区域，随机抽取、分类混剪和随机批量生成移入折叠区。
3. 左侧素材库增加“原始素材 / 视频片段”标签页，切片完成后自动切换到片段列表。
4. 右侧只常驻画布、效果、平滑混剪、画中画和 BGM，未完成入口不再干扰主流程。
5. 底部主按钮会根据进度提示下一步，并补充 1180px 和 720px 窄屏适配。
6. 本次没有修改 Rust、FFmpeg、AI 接口或 VideoEngine 业务逻辑。
7. 已根据 2K 实机截图补充大屏专用可读性布局，解决片段和文字过小的问题。
8. 已修复真实验收中发现的方舟请求发送超时兼容问题，等待用户重新启动后复测。
9. 原一次发送全部图片的方案已改为分批画面理解和纯文字分镜规划；当前切片会话复用画面描述，等待真实方舟端到端验收。
10. 第一版未接入 TTS，输入文案只用于匹配和排列画面，成片保留原片段声音。
11. 多视频导入现在会统一批量切片并合并为同一个 AI 候选片段池，不再只处理当前选中视频。

验证：
- `corepack pnpm build` 通过。
- 已使用本地页面检查空状态、三栏布局、第一屏 AI 区域和底部主按钮。
## 2026-07-20：智剪 SmartCut 第一版品牌与UI改版

状态：正式代码、前端生产构建、Tauri Debug构建、真实桌面视觉检查和2026-07-21用户人工验收全部完成；已纳入本地Git稳定基线，未推送GitHub。

1. 产品品牌统一为“智剪 / SmartCut”，新增SVG页面图标和Windows ICO应用图标。
2. 首页重做为三个快速入口、最近项目、本机能力状态和右侧三步引导，不再使用六张功能卡片墙。
3. 首次启动教程只自动显示一次，之后可从首页和顶部按钮重新打开；教程网页暂保留入口。
4. 工作台使用中性黑灰、细分割线、小圆角和青蓝强调色，已有三栏功能结构和业务事件保持不变。
5. AI配音当前卡片下增加4款常用音色，点击直接更新原有speaker；完整25款音色库仍支持筛选和官方样音试听。
6. 未修改Rust、FFmpeg、VideoEngine、AI/TTS接口、SQLite和项目快照结构。
7. 前端151个模块构建通过；Tauri Debug应用构建通过；1100×760真实桌面窗口检查正常。

## 2026-08-07 当前进展

AI 成片中间工作区已完成竞品式画布与文案区重排：顶部直接控制画布，底部以文案为主，画面匹配放入后台折叠流程。当前版本已经通过前端构建和本地浏览器多尺寸、双主题检查，等待用户验收，不代表已达到百分之百复刻。

2026-08-07 追加：已修复内联裁剪/文本工具触发的预览区空白布局问题，并完成双窗口尺寸点击回归。

2026-08-07 追加：比例虚框、裁剪可调实框和画布内直接文字编辑已完成开发，等待桌面版真实视频素材验收。

## 2026-08-07 画布交互边界修复

- 文字输入已改为稳定的非受控画布编辑，避免每次输入触发重绘导致光标异常。
- 比例虚框已按实际画布边界绘制，修复顶部缺线和四边不完整。
- 视频预览底部增加控制条安全区，等待用户在 1280×720 桌面窗口复验。

## 2026-08-08 当前进展

画面处理区域的四个竞品式直接操作模块已完成校正：去除水印、图片/视频水印、HSL 调色和动态缩放均已接入现有状态与 Rust 视频处理链路。修正了 HSL 亮度中性值显示错误，默认值现在为 `100%`。

本轮自动验证已通过：前端构建、Rust 格式检查、编译、142 项测试、严格 Clippy 和差异检查；浏览器已验证四个模块的开关、区域数量、色相和动态缩放模式交互。当前仍等待用户在浏览器集中验收，本轮不提交 Git。

## 2026-08-08 图片/视频水印画面内编辑

图片/视频水印已从“相对整个输出画布定位”改为“相对视频实际可见区域定位”。新增画面内编辑框、拖动、四角缩放以及可持久化的 X/Y 归一化坐标，并接入 FFmpeg 导出。前端构建、Rust 严格检查和 143 项测试通过，等待用户使用真实素材验收。

## 2026-08-08 素材栏第二轮可读性修复

状态：开发完成，等待用户验收，未提交 Git。

- 在上一轮基础上继续放大素材栏文字和缩略图，重点解决用户反馈的“仍然看不清”。
- 左栏宽度、三栏工作台和齿轮设置交互保持不变，仅调整信息识别层级与对比度。
- 待完成：用户导入真实文件夹后确认文件夹名、文件名、时长和选中态是否清晰。

## 2026-08-08 视频效果前三项二级面板

音频设置、视频封面和入场效果已完成第一轮竞品式二级面板对齐。三个入口均保持独立组件和现有父级状态边界，没有修改 AI 智能混剪模块；待用户集中验收后继续拆解后续视频效果。

根据用户新的竞品截图，三个二级面板已再次紧凑化：封面面板约 430px 宽，入场效果面板约 760px 宽，参数和动态演示未删减。
# 2026-08-08 本轮状态

视频效果处理的七个二级参数入口已完成 UI 与交互接线：视频帧操作、画中画、画面调整、像素融合、旋转换像、视频变速、动态缩放。浏览器验证通过，当前状态为“待用户验收，未提交 Git”。

## 2026-08-08 智能配置与视频效果处理收尾

状态：开发完成，等待用户验收，未提交 Git。

1. 智能配置会统一点亮 10 个视频效果卡，卡片仍可打开对应二级参数面板继续调整。
2. 开始处理会按导入顺序逐条复用现有导出服务，保留原有视频处理链路边界。
3. 左侧素材列表展示每条视频的处理状态和进度，底部输出栏展示素材总数、完成数和总进度。
4. 缺少素材、输出目录或效果配置时，开始处理按钮保持禁用。
5. 已通过 `corepack pnpm build`、`git diff --check`、1280×720、1440×900、浅色/深色主题和浏览器控制台检查。
6. 待用户导入真实素材确认逐条处理、失败项显示和导出结果；通过后再按用户指令提交 Git。
## 2026-08-08 贴画与水印面板对齐

状态：开发完成，等待用户验收，本轮未提交 Git。

- 右侧“贴画与水印”已由入口列表改为竞品式四张内联卡片。
- 添加贴画复用画中画素材选择、透明度、大小和位置。
- 水印和花字复用已有文字文案、字号、透明度、颜色和位置。
- 图片/视频水印复用已有素材、类型、透明度、大小和轨迹。
- 去除水印/字幕复用已有手动框选区域数和字幕位置/大小。
- `corepack pnpm build` 通过；1440×900、1280×720、浅色/深色主题和完整刷新控制台检查通过。

## 2026-08-08 整体 UI 规范化

本轮完成现有产品外壳的 UI 规范化，不改变已验收的视频处理业务逻辑：

1. 统一 Windows 中文桌面字体优先的字体栈和信息层级。
2. 首页入口、视频效果工作台、效果卡、空状态和底部输出栏完成可读性收敛。
3. 浅色/深色主题均覆盖主工作台和底部任务处理区。
4. 1440×900 与 1280×720 浏览器检查通过，页面无新增错误或警告。
5. 当前等待用户集中验收，未提交 Git。

## 2026-08-08 竞品图标与封面状态修复

本轮只修复视频效果处理工作台的视觉残缺和封面误显示问题，不改变已完成的视频处理逻辑：

1. 新增 `src/components/ToolIcon.vue`，统一提供本地 SVG 线性图标。
2. 右侧效果卡、导入按钮、输出路径、空状态和设置面板素材按钮均移除 Unicode 字符占位符。
3. 封面默认状态恢复为“未选择封面图片”，左侧视频缩略图缓存不再写入用户封面路径。
4. 封面图片文件和文件夹入口分别触发文件选择与目录选择。
5. 已通过构建、差异检查、Impeccable 检测、1440×900/1280×720 和浅色/深色主题检查；等待用户验收，未提交 Git。

## 2026-08-08 视频混剪主界面第一轮对齐

状态：开发完成，等待用户验收，未提交 Git。

- 视频混剪页面已按参考拆解补齐五步使用指引。
- 左侧改为文件夹素材库，齿轮进入三模式混剪规则面板。
- 中间接入真实预览、比例、填充、裁剪和文本预览。
- 右侧复用基础设置与画面处理入口，不新增独立视频处理链路。
- 已通过构建、差异检查、双窗口尺寸、双主题和浏览器错误检查。

## 2026-08-09 视频混剪右侧工具栏统一

状态：开发完成，等待用户验收，未提交 Git。

- 视频混剪右侧不再使用简化仿制栏。
- 已直接复用 AI 智能混剪使用的 `RightToolPanel.vue`。
- 基础设置、配音头像、BGM、字幕样式和画面处理参数与 AI 智能混剪共用同一组件和状态。
- 已通过前端构建，等待用户人工对比截图验收。
