# ROADMAP.md

## 2026-07-17 当前里程碑

AI 文案智能混剪主链路已支持批量差异成片。当前链路为：多视频导入 → 全部素材切片 → 成功片段保留与失败片段跳过 → 预览图画面理解 → 本地文案短句 → AI固定短句画面匹配 → 人工调整 → 1～10条差异方案 → TTS一次生成并复用/无配音 → 顺序VideoEngine导出。

前端渐进式整理、TTS逐句配音、原声保留、自动字幕、素材完整封面、AI分镜稳定性、多条差异视频和软件内API密钥安全保存均已完成。下一阶段先由用户填写真实密钥并进行多组素材稳定性测试；音频输入驱动、素材库持久化、项目保存与失败恢复、普通用户安装包、ASR和复杂时间轴仍按用户确认顺序推进。

## UI v3 最终结构

方向：
后续 UI 和功能路线参考专业视频处理软件的产品结构、页面层级和交互流程，但只对标结构、尺寸感和操作路径，不复制竞品软件名、Logo、品牌文案、图标素材。

首页：
1. 首页采用模块入口卡片结构。
2. 顶部分类为创作中心、效率工具、自动化。
3. 模块入口包括 AI 智能混剪、视频效果处理、视频混剪、分类混剪、文案改写、视频内容提炼。
4. 已实现模块进入工作台，未实现模块显示敬请期待或禁用状态。
5. 首次进入可显示欢迎弹窗，但必须可关闭。

工作台：
1. 左侧素材区：导入视频、导入文件夹、素材列表、文件夹树、输出目录。
2. 中间主预览区：视频预览、当前视频信息、当前模式操作区、片段列表或结果区。
3. 右侧参数区：只显示功能入口或分组卡片，不长期堆全部参数。
4. 底部任务区：输出格式、分辨率、帧率、命名规则、线程、进度、开始处理按钮，日志和导出结果可折叠查看。

二级设置面板：
1. 复杂功能进入二级面板、弹窗或抽屉。
2. 面板宽度控制在 480px 到 640px。
3. 参数过多时面板内部滚动。
4. 不挤压主页面。
5. 有关闭、重置、应用按钮。

后续功能路线：
1. V0.3.1 平滑混剪：画面淡入淡出、音频淡入淡出、过滤短片段。
2. V0.3.2 基础视频效果：水平镜像、垂直镜像、旋转、变速、亮度、对比度、饱和度、轻微缩放。
3. V0.3.3 画中画：主视频、叠加视频或图片、位置、大小、透明度、边距。
4. V0.3.4 关键帧 / 封面帧 / 片段预览：抽取封面帧、生成片段预览图、选择封面帧。
5. V0.3.5 分类混剪：开头钩子、产品展示、使用过程、细节特写、效果展示、结尾引导。
6. V0.4 音频与 BGM。
7. V0.5 字幕 / 文案模式。
8. V0.6 TTS 语音合成。
9. V0.7 API Key 管理。
10. V0.8 会员卡密。
11. V0.9 打包发布。

## 路线图原则

本项目先做可运行 MVP，再逐步扩展。

当前优先级：

```text
先打通本地视频处理闭环
再做切片和混剪
再做字幕、TTS、AI、会员和 API Key
```

禁止为了追竞品功能一次性开发多个大模块。

## V0.1 基础可运行版

目标：
软件可以导入视频、读取信息、预览视频、选择输出目录，并调用 FFmpeg 导出一个新 mp4。

功能范围：

1. FFmpeg / FFprobe 环境检测。
2. 单视频导入。
3. 文件夹导入。
4. 视频列表。
5. 视频信息读取。
6. 视频预览。
7. 选择输出目录。
8. 基础导出。
9. 任务进度和日志。

验收标准：

1. 用户导入一个 mp4 后，可以在列表中看到它。
2. 软件可以读取视频时长、分辨率、帧率、是否有音频。
3. 用户点击视频后，可以看到视频预览。
4. 用户选择输出目录后，可以导出一个新 mp4。
5. 导出过程中可以看到任务状态。
6. 导出失败时有明确错误提示。

不做：

1. 会员系统。
2. API Key 系统。
3. AI 内容理解。
4. 文案改写。
5. TTS。
6. ASR 字幕识别。
7. 去水印。
8. 复杂画中画。
9. 随机混剪。
10. UI 大重构。

## V0.2 基础混剪版

目标：
多个视频可以随机切片、拼接，并导出混剪结果。

功能范围：

1. 固定时长切片。
2. 随机片段抽取。
3. 片段拼接。
4. 批量导出。
5. 基础镜像。
6. 基础变速。
7. 裁剪比例。

验收标准：

1. 用户导入多个视频后，可以生成一个随机拼接的新视频。
2. 用户可以设置片段时长。
3. 用户可以批量生成多个结果。
4. 每个导出任务都有状态和日志。

不做：

1. AI 内容理解。
2. 分类混剪。
3. TTS。
4. ASR 字幕识别。
5. 会员系统。

## V0.3 混剪质量增强与视频效果处理阶段

目标：
在 V0.1 基础视频闭环、V0.2 基础混剪闭环、UI v2 暗色专业工作台、画布比例 / 模糊背景、导出结果列表管理都稳定之后，继续提升成片质量和常用视频处理能力。

开发原则：

1. 先提升底层拼接质量，再扩展视频效果。
2. 继续保持所有 FFmpeg 逻辑通过 VideoEngine 封装。
3. 不修改已经稳定的画布比例、模糊背景、输出目录和导出结果列表链路。
4. 不做字幕、TTS、会员、API Key、AI 内容理解。
5. 不做复杂专业剪辑软件能力，优先做可验收的小版本。

### V0.3.1 淡入淡出 / 平滑混剪第一版

优先级：
最高，先做。

目标：
让“拼接抽中片段”和“批量生成混剪”的片段切换更自然。

功能：

1. 平滑混剪模式。
2. 每个片段开头 0.2 秒画面淡入。
3. 每个片段结尾 0.2 秒画面淡出。
4. 原声音频 0.2 秒淡入淡出。
5. 过滤小于 1.5 秒的过短片段。
6. 不破坏画布比例、模糊背景、导出结果列表。

原因：
分类混剪、批量生成、后续视频效果都会依赖底层拼接导出流程，必须先把成片切换质量做好。

### V0.3.2 基础视频效果第一版

目标：
增加常用视频效果处理能力，为去重和素材处理做准备。

功能：

1. 水平镜像。
2. 垂直镜像。
3. 视频变速。
4. 亮度调整。
5. 对比度调整。
6. 饱和度调整。
7. 轻微缩放。

限制：
不做复杂特效库，不做 AI 效果。

### V0.3.3 画中画第一版

目标：
支持基础画中画叠加能力。

功能：

1. 选择主视频。
2. 选择叠加视频或图片。
3. 设置画中画位置。
4. 设置画中画大小比例。
5. 设置透明度。
6. 设置边距。
7. 导出结果加入导出结果列表。

限制：
不做复杂多轨时间轴，不做高级动画。

### V0.3.4 关键帧 / 封面帧 / 片段预览图第一版

目标：
为素材预览、片段管理、封面选择、后续 AI 分析做基础。

功能：

1. 从视频中抽取封面帧。
2. 从片段中生成预览图。
3. 支持选择某一帧作为封面。
4. 关键帧信息用于 UI 展示，不做复杂时间轴动画。

限制：
本阶段不做专业剪辑软件那种关键帧动画。

### V0.3.5 分类混剪第一版

目标：
把现有随机混剪升级为按镜头类型顺序混剪，让生成视频更像正常带货视频结构。

功能：

1. 给片段增加分类字段。
2. 支持分类：开头钩子、产品展示、使用过程、细节特写、效果展示、结尾引导。
3. 用户可以手动给片段选择分类。
4. 增加“分类混剪”模式。
5. 分类混剪按顺序抽取片段：开头钩子 → 产品展示 → 使用过程 → 细节特写 → 效果展示 → 结尾引导。
6. 每个分类默认抽取 1 个片段。
7. 空分类自动跳过。
8. 导出后加入导出结果列表。
9. 继续保留画布比例、模糊背景、输出目录等已有设置。

限制：
不做 AI 自动分类。

### V0.3.6 V0.3 阶段验收与整理

目标：
检查 V0.3 所有新增能力是否稳定。

验收点：

1. 随机混剪正常。
2. 平滑混剪正常。
3. 画布比例 / 模糊背景没有被破坏。
4. 导出结果列表正常。
5. 基础视频效果正常。
6. 关键帧 / 封面帧正常。
7. 分类混剪正常。
8. 画中画正常。
9. `corepack pnpm build` 通过。
10. 项目文档更新完整。

## V0.4 音频与 BGM 第一版

说明：
V0.3.6 已经完成人工验收并提交 Git。原 V0.4 分类混剪能力已经前移到 V0.3.5，因此 V0.4 重新确定为音频与 BGM 阶段。

目标：
让用户可以给混剪导出结果添加本地背景音乐，并保留原视频声音。

功能范围：

1. 选择本地音频文件作为 BGM。
2. 开启 / 关闭 BGM。
3. 调节原视频音量。
4. 调节 BGM 音量。
5. BGM 自动适配导出视频长度。
6. 拼接抽中片段支持 BGM。
7. 分类混剪支持 BGM。
8. 批量生成支持 BGM。
9. 任务日志显示 BGM 是否启用。
10. 导出结果继续进入导出结果列表。

验收标准：

1. 不开启 BGM 时，原有混剪结果不受影响。
2. 开启 BGM 后，导出视频可以听到背景音乐。
3. 原视频声音不会被意外完全覆盖。
4. 原视频音量和 BGM 音量设置生效。
5. BGM 比视频短时不会中途静音。
6. BGM 比视频长时不会让导出时长变长。
7. 分类混剪带 BGM 可以正常导出。
8. 批量生成带 BGM 可以正常导出多条结果。
9. `corepack pnpm build` 通过。
10. 修改 Rust / VideoEngine 时，`cargo check` 通过。

不做：

1. AI 自动配乐。
2. 在线音乐库。
3. 歌词识别。
4. 节拍自动卡点。
5. 复杂多轨时间线。
6. 字幕识别。
7. TTS 配音。
8. API Key 系统。
9. 会员卡密系统。

开发原则：

1. 所有 FFmpeg 音频处理继续通过 VideoEngine 封装。
2. 前端只收集 BGM 参数，不直接拼接 FFmpeg 命令。
3. 第一版优先保证稳定和可验收，不追求专业音频工作站能力。

## V0.5 会员 / API Key 版

目标：
加入商业化和外部服务配置能力。

功能范围：

1. API Key 安全保存。
2. API Key 可用性检测。
3. 会员卡密远程校验。
4. 会员状态展示。
5. 功能开关。

验收标准：

1. API Key 不明文保存。
2. 会员状态不能只靠本地判断。
3. 离线状态有合理提示。
4. 会员功能开关清晰可控。

不做：

1. 纯本地卡密校验。
2. 复杂支付系统。
3. 绕过远程校验的永久会员判断。

## V0.6 AI 内容理解版

目标：
让软件能理解素材内容，辅助用户自动混剪。

功能范围：

1. 视频内容提炼。
2. ASR 字幕识别。
3. OCR 画面文字识别。
4. 文案改写。
5. 素材智能推荐。
6. 镜头内容标签。

验收标准：

1. 用户导入视频后，系统可以提炼基础内容。
2. 系统可以根据文案辅助选择素材片段。
3. 系统可以给素材生成基础标签。

不做：

1. 承诺自动生成爆款。
2. 不可控的大而全 AI 系统。
3. 没有人工确认的自动发布流程。
