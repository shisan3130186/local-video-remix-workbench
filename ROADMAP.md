# ROADMAP.md

## 路线图原则

本项目先做可运行 MVP，再逐步扩展。

当前优先级：

```text
先打通本地视频处理闭环
再做切片和混剪
再做字幕、TTS、AI、会员和 API Key
```

禁止为了追竞品功能一次性开发多个大模块。

## V0.1 基础可运行版

目标：
软件可以导入视频、读取信息、预览视频、选择输出目录，并调用 FFmpeg 导出一个新 mp4。

功能范围：

1. FFmpeg / FFprobe 环境检测。
2. 单视频导入。
3. 文件夹导入。
4. 视频列表。
5. 视频信息读取。
6. 视频预览。
7. 选择输出目录。
8. 基础导出。
9. 任务进度和日志。

验收标准：

1. 用户导入一个 mp4 后，可以在列表中看到它。
2. 软件可以读取视频时长、分辨率、帧率、是否有音频。
3. 用户点击视频后，可以看到视频预览。
4. 用户选择输出目录后，可以导出一个新 mp4。
5. 导出过程中可以看到任务状态。
6. 导出失败时有明确错误提示。

不做：

1. 会员系统。
2. API Key 系统。
3. AI 内容理解。
4. 文案改写。
5. TTS。
6. ASR 字幕识别。
7. 去水印。
8. 复杂画中画。
9. 随机混剪。
10. UI 大重构。

## V0.2 基础混剪版

目标：
多个视频可以随机切片、拼接，并导出混剪结果。

功能范围：

1. 固定时长切片。
2. 随机片段抽取。
3. 片段拼接。
4. 批量导出。
5. 基础镜像。
6. 基础变速。
7. 裁剪比例。

验收标准：

1. 用户导入多个视频后，可以生成一个随机拼接的新视频。
2. 用户可以设置片段时长。
3. 用户可以批量生成多个结果。
4. 每个导出任务都有状态和日志。

不做：

1. AI 内容理解。
2. 分类混剪。
3. TTS。
4. ASR 字幕识别。
5. 会员系统。

## V0.3 文案 / TTS / 字幕版

目标：
输入文案后，可以生成语音和字幕，并与视频进行基础匹配。

功能范围：

1. 文案输入。
2. TTS 语音生成。
3. 基础字幕生成。
4. 字幕样式设置。
5. 视频时长与语音时长基础匹配。

验收标准：

1. 用户输入文案后，可以生成语音。
2. 导出视频中可以包含语音。
3. 导出视频中可以包含字幕。
4. 字幕可以设置基础样式。

不做：

1. 高级文案改写。
2. 动态字幕模板。
3. 声音克隆。
4. AI 自动选片。

## V0.4 分类混剪版

目标：
用户可以把素材按类别组织，并按类别规则生成混剪视频。

功能范围：

1. 素材分类。
2. 分类标签。
3. 按类别抽取片段。
4. 分类混剪模板。
5. 批量生成分类混剪结果。

验收标准：

1. 用户可以手动给素材分类。
2. 系统可以按照分类规则抽取素材。
3. 系统可以生成符合分类规则的新视频。

不做：

1. 自动内容识别分类。
2. 复杂 AI 推荐。
3. 云端素材库。

## V0.5 会员 / API Key 版

目标：
加入商业化和外部服务配置能力。

功能范围：

1. API Key 安全保存。
2. API Key 可用性检测。
3. 会员卡密远程校验。
4. 会员状态展示。
5. 功能开关。

验收标准：

1. API Key 不明文保存。
2. 会员状态不能只靠本地判断。
3. 离线状态有合理提示。
4. 会员功能开关清晰可控。

不做：

1. 纯本地卡密校验。
2. 复杂支付系统。
3. 绕过远程校验的永久会员判断。

## V0.6 AI 内容理解版

目标：
让软件能理解素材内容，辅助用户自动混剪。

功能范围：

1. 视频内容提炼。
2. ASR 字幕识别。
3. OCR 画面文字识别。
4. 文案改写。
5. 素材智能推荐。
6. 镜头内容标签。

验收标准：

1. 用户导入视频后，系统可以提炼基础内容。
2. 系统可以根据文案辅助选择素材片段。
3. 系统可以给素材生成基础标签。

不做：

1. 承诺自动生成爆款。
2. 不可控的大而全 AI 系统。
3. 没有人工确认的自动发布流程。
