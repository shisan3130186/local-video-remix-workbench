# TODO_NEXT.md

## 当前唯一下一步：AI 文案智能混剪 MVP 人工验收

状态：
AI 文案智能混剪第一版已开发，下一步使用真实火山引擎方舟环境变量完成端到端验收。

验收顺序：
1. 在启动软件的同一终端配置 `AI_API_KEY`、`AI_BASE_URL`、`AI_MODEL`。
2. 导入视频、选择输出目录并完成切片。
3. 确认所有片段出现预览图和真实时长。
4. 输入文案并请求 AI 排序。
5. 测试上移、下移、删除和重新排序。
6. 点击“生成 AI 混剪视频”，确认成片顺序与列表一致。
7. 回归画布、平滑混剪、视频效果、画中画、BGM 和导出结果列表。
8. 回归随机混剪、分类混剪和批量生成。

失败重点检查：
1. 三个环境变量是否都在 Tauri 进程环境中。
2. `AI_BASE_URL` 是否为方舟 `/api/v3` 基地址或完整 `/chat/completions` 地址。
3. `AI_MODEL` 是否为支持图片理解的模型或推理接入点。
4. AI 是否严格返回只包含 `orderedSegmentIds` 的 JSON。

## 保留待验收：V0.4.1 / V0.4.2 本地 BGM 联合人工验收

状态：
V0.4.1 本地 BGM 第一版和 V0.4.2 BGM 淡入淡出第一版已经开发完成，正在等待用户一起人工验收。

目标：
确认 BGM、音量、淡入淡出可用，并确认原有拼接、分类混剪、批量生成、画布、视频效果、平滑混剪、画中画和结果列表没有被破坏。

## BGM 验收保留事项

1. 按清单人工验收 V0.4.1 / V0.4.2 BGM。
2. 发现问题后只做小步修复。
3. 修复后重新运行必要检查。
4. 更新 `PROJECT_STATUS.md`、`TODO_NEXT.md`、`CHANGELOG.md`。
5. 重要决策写入 `DECISIONS.md`。

## 继续不做的范围

1. 不做 AI 自动配乐。
2. 不做在线音乐库。
3. 不做歌词识别。
4. 不做音乐节拍自动卡点。
5. 不做复杂多轨时间线。
6. 不做字幕识别。
7. 不做 TTS 配音。
8. 不做 API Key 系统。
9. 不做会员卡密系统。
10. 不做新的大 UI 重构。
11. 不绕过 `VideoEngine` 直接拼接 FFmpeg 命令。

## V0.4.1 验收标准

1. 不开启 BGM 时，原有混剪导出结果不受影响。
2. 开启 BGM 后，导出视频可以听到背景音乐。
3. 原视频声音不会被意外完全覆盖。
4. 原视频音量和 BGM 音量设置生效。
5. BGM 比视频短时，导出不会中途静音。
6. BGM 比视频长时，导出不会超过视频长度。
7. 分类混剪可以带 BGM 导出。
8. 批量生成可以带 BGM 导出。
9. 任务日志能看懂 BGM 是否启用。
10. 导出结果继续进入导出结果列表。

## V0.4.2 验收标准

1. BGM 淡入秒数默认为 0.5 秒。
2. BGM 淡出秒数默认为 0.5 秒。
3. 开头 BGM 不会突然进入。
4. 结尾 BGM 不会突然停止。
5. 淡入淡出秒数设为 0 时，不做渐变。
6. 淡入淡出秒数只能在 0 到 10 秒之间。
7. 分类混剪带 BGM 淡入淡出可以正常导出。
8. 批量生成带 BGM 淡入淡出可以正常导出。

## 验收启动方式

双击项目根目录中的：

```text
E:\Workspace\Project_03_本地短视频批量混剪工作台\启动验收.bat
```

也可以命令行运行：

```text
cd E:\Workspace\Project_03_本地短视频批量混剪工作台
corepack pnpm tauri dev
```
