# TODO_NEXT.md

## 当前最高优先级

1. 开发 V0.2 裁剪比例
2. 支持原画、9:16 竖屏、1:1 方屏、16:9 横屏
3. 单次片段拼接和批量生成都可以应用裁剪比例
4. 裁剪比例必须通过 VideoEngine 封装，页面组件不能直接拼接 FFmpeg 命令

## 开发前置阻塞

暂无。

人工测试记录：

1. 2026-06-12：片段拼接导出已通过用户测试。
2. 测试素材为 2 分 47 秒视频。
3. 固定切片秒数为 10 秒，生成 17 个片段。
4. 随机抽取 5 个片段。
5. 最终成功拼接出约 46 秒的新 mp4 视频。
6. 2026-06-12：批量生成混剪结果已通过用户测试。
7. 2026-06-12：桌面软件工作台 UI 框架用户反馈勉强可用，后续再继续优化。
8. 2026-06-12：基础变速已通过用户测试。

启动检查：

1. `corepack pnpm build` 已通过。
2. `corepack pnpm tauri dev` 已通过启动检查。
3. Vite 已排除 `src-tauri/target` 和 `target` 目录，避免监听 Rust 编译产物。
4. `cargo check` 已通过。

## 本轮只允许开发

当前只允许开发 V0.2 裁剪比例。未经确认，不开发字幕、TTS、会员、API Key 或 AI 功能。

## V0.1 推荐任务拆分

### 任务 1：FFmpeg / FFprobe 环境检测

状态：
已完成。

目标：
检测本机是否可以正常调用 ffmpeg 和 ffprobe。

验收标准：

1. 可以检测 ffmpeg 是否存在。
2. 可以检测 ffprobe 是否存在。
3. 可以显示版本号。
4. 不存在时前端有明确提示。

可单独提交：
是。

### 任务 2：建立 VideoEngine 基础入口

状态：
已完成基础入口，后续随视频信息读取继续补充。

目标：
所有视频处理能力从统一入口调用，避免页面组件直接拼 FFmpeg 命令。

验收标准：

1. 环境检测能力通过 VideoEngine 调用。
2. 页面组件不直接拼接 FFmpeg / FFprobe 命令。
3. 返回结果格式清晰，包含成功状态和错误信息。

可单独提交：
是。

### 任务 3：单视频导入

状态：
已完成。

目标：
用户可以选择一个视频文件导入。

验收标准：

1. 可以导入单个 mp4。
2. 导入后列表显示文件名。
3. 可以保存文件路径用于后续读取。

可单独提交：
是。

### 任务 4：文件夹导入

状态：
已完成。

目标：
用户可以选择一个文件夹，批量导入其中的视频文件。

验收标准：

1. 可以选择文件夹。
2. 可以过滤非视频文件。
3. 导入后素材列表显示多个视频。

可单独提交：
是。

### 任务 5：视频信息读取

状态：
已完成。

目标：
使用 ffprobe 读取视频基础信息。

验收标准：

1. 能读取视频时长。
2. 能读取分辨率。
3. 能读取帧率。
4. 能读取是否有音频。

可单独提交：
是。

### 任务 6：视频预览

状态：
已完成。

目标：
用户点击素材后可以预览视频。

验收标准：

1. 点击素材后可以播放。
2. 可以暂停。
3. 切换视频不会崩溃。

可单独提交：
是。

### 任务 7：输出目录选择

状态：
已完成。

目标：
用户可以选择导出结果保存位置。

验收标准：

1. 可以选择输出目录。
2. 页面显示当前输出目录。
3. 后续导出任务可以使用该目录。

可单独提交：
是。

### 任务 8：基础导出

状态：
已完成。

目标：
用户可以把一个视频导出成一个新的 mp4。

验收标准：

1. 可以选择一个导入视频。
2. 可以点击导出。
3. 输出目录中生成新 mp4。
4. 导出失败时有错误提示。

可单独提交：
是。

### 任务 9：任务进度展示

状态：
已完成。

目标：
导出任务有基础状态展示。

验收标准：

1. 可以看到任务开始。
2. 可以看到任务进行中。
3. 可以看到任务完成。
4. 可以看到任务失败。

可单独提交：
是。

### 任务 10：日志和错误提示整理

状态：
已完成。

目标：
用户可以看懂任务结果和失败原因。

验收标准：

1. 任务开始有日志。
2. 任务完成有输出路径。
3. 任务失败有错误原因。
4. 日志不泄露 API Key 等敏感信息。

可单独提交：
是。

### 下一步：V0.1 主链路自检

状态：
已完成。用户已人工测试通过。

目标：
按测试验收清单检查当前基础闭环是否稳定。

验收标准：

1. FFmpeg / FFprobe 检测正常。
2. 单视频导入、文件夹导入、视频信息读取正常。
3. 视频预览和播放正常。
4. 输出目录选择和基础导出正常。
5. 导出日志能显示开始、进行中、成功输出路径或失败原因。
6. 页面组件没有直接拼接 FFmpeg / FFprobe 命令。

可单独提交：
是。

## V0.2 推荐任务拆分

### 任务 1：固定时长视频切片

状态：
已完成。

目标：
用户选择一个已导入视频后，可以按固定秒数切成多个片段，片段保存到输出目录。

验收标准：

1. 可以输入或选择切片秒数。
2. 可以对当前选中的视频执行切片。
3. 输出目录中生成多个片段文件。
4. 切片任务有开始、进行中、完成或失败日志。
5. 切片逻辑必须走 VideoEngine，页面组件不能直接拼接 FFmpeg 命令。

暂时不做：

1. 随机抽取片段。
2. 多视频批量切片。
3. 片段拼接。
4. 混剪导出。
5. 百分比进度。

可单独提交：
是。

### 任务 2：随机片段抽取

状态：
已完成。

目标：
在固定切片能力稳定后，从片段中随机选择素材。

验收标准：

1. 可以从当前切片结果中随机选择片段。
2. 可以设置随机选择片段数量。
3. 随机结果能在页面展示文件名或路径。
4. 随机抽取只处理片段列表，不直接拼接视频。
5. 不修改原始片段文件。

可单独提交：
是。

### 任务 3：片段拼接导出

状态：
已完成。

目标：
把多个片段拼接成一个新的 mp4。

验收标准：

1. 可以使用随机抽取结果作为拼接输入。
2. 可以点击按钮生成一个新的 mp4。
3. 拼接结果保存到输出目录。
4. 拼接任务有开始、进行中、完成或失败日志。
5. 拼接逻辑必须走 VideoEngine，页面组件不能直接拼接 FFmpeg 命令。

暂时不做：

1. 批量生成多个混剪结果。
2. 镜像、变速、裁剪比例。
3. 字幕、TTS、AI 内容理解。

可单独提交：
是。

### 任务 4：批量生成混剪结果

状态：
已完成。

目标：
基于切片、随机抽取和片段拼接能力，一次生成多个混剪 mp4。

暂时不做：

1. 镜像、变速、裁剪比例。
2. 字幕、TTS、AI 内容理解。
3. 多任务并发。

可单独提交：
是。

### 任务 5：基础镜像

状态：
已开发，待人工测试。

目标：
在混剪导出时支持水平镜像处理。

验收标准：

1. 页面有一个“水平镜像”开关。
2. 开启后，单次片段拼接导出的视频为水平镜像效果。
3. 开启后，批量生成的混剪视频也应用水平镜像。
4. 关闭后，导出结果保持正常方向。
5. 镜像处理必须走 VideoEngine，页面组件不能直接拼接 FFmpeg 命令。

暂时不做：

1. 变速。
2. 裁剪比例。
3. 字幕、TTS、AI 内容理解。

可单独提交：
是。

### 任务 6：桌面软件工作台 UI 框架升级

状态：
已完成基础验收，后续继续优化。

目标：
把工程测试版页面整理为适合长期扩展的本地桌面端视频混剪工作台。

验收标准：

1. 顶部显示软件名称、当前版本、当前模式和 FFmpeg 状态。
2. 左侧集中展示素材列表、片段列表、导入按钮。
3. 中间集中展示视频预览、当前视频信息、混剪结果。
4. 右侧集中展示切片设置、混剪设置、导出设置。
5. 底部集中展示任务日志和输出目录。
6. 不改 FFmpeg / VideoEngine / Service 核心逻辑。
7. 原有功能入口仍然可用。

暂时不做：

1. 新视频处理功能。
2. UI 主题系统。
3. 多窗口布局。
4. 复杂拖拽排序。

可单独提交：
是。

### 任务 7：基础变速

状态：
已完成，用户已人工测试通过。

目标：
在混剪导出时支持基础播放速度调整。

验收标准：

1. 页面可以设置变速倍数。
2. 默认 1.0x 时保持原速。
3. 设置大于 1.0x 时，导出视频时长变短。
4. 设置小于 1.0x 时，导出视频时长变长。
5. 单次片段拼接和批量生成都支持变速。
6. 变速逻辑必须走 VideoEngine，页面组件不能直接拼接 FFmpeg 命令。

暂时不做：

1. 裁剪比例。
2. 复杂变速曲线。
3. 分段变速。
4. 字幕、TTS、AI 内容理解。

可单独提交：
是。

### 任务 8：裁剪比例

状态：
待开发。

目标：
在混剪导出时支持基础画面比例处理。

验收标准：

1. 页面可以选择原画、9:16 竖屏、1:1 方屏、16:9 横屏。
2. 原画模式保持原始画面比例。
3. 9:16、1:1、16:9 模式可以导出对应比例的视频。
4. 单次片段拼接和批量生成都支持裁剪比例。
5. 裁剪比例可以和水平镜像、基础变速一起使用。
6. 裁剪逻辑必须走 VideoEngine，页面组件不能直接拼接 FFmpeg 命令。

暂时不做：

1. 自由裁剪框。
2. 拖拽裁剪位置。
3. 智能主体识别裁剪。
4. AI 自动构图。

可单独提交：
是。

## 不允许开发

- 会员系统
- API Key系统
- AI内容识别
- 文案改写
- TTS
- ASR字幕识别
- 高级去水印
- 复杂画中画
- 云端渲染
- 支付系统
- 首页重构
- UI大改
---

## 当前下一步：人工验收视频比例画布适配

状态：已开发，待测试。

本次功能只做画布适配，不做裁剪框：

1. 原画。
2. 9:16 竖屏。
3. 1:1 方屏。
4. 16:9 横屏。
5. 黑边背景。
6. 模糊背景。

人工验收顺序：

1. 启动软件。
2. 导入一个横屏视频和一个竖屏视频。
3. 分别切换原画、9:16、1:1、16:9。
4. 分别测试黑边和模糊背景。
5. 确认预览画面保留完整原视频内容。
6. 确认播放器底部播放、暂停、时长条仍然可用。
7. 分别测试基础导出、片段拼接导出、批量生成。
8. 用 ffprobe 或播放器属性确认导出比例正确。
9. 测试与水平镜像、基础变速一起使用。

通过后再继续下一个功能：导出结果列表管理 + 打开输出目录。

## 当前插入修复：原画预览比例异常

状态：已修复，待用户复验。

复验顺序：

1. 保持比例选择为“原画”。
2. 点击左侧 1080 x 1920 的竖屏素材。
3. 确认预览画布变成竖屏比例，而不是横向铺满。
4. 切换到 9:16、1:1、16:9，确认目标比例画布仍然正常。
5. 播放视频，确认播放、暂停、时长条仍然可用。

本次修复后，仍然优先完成“视频比例画布适配”的人工验收。

## 当前插入修复：混剪导出模糊背景未生效

状态：已修复，待用户验收。

复验顺序：

1. 视频比例选择“16:9 横屏”。
2. 背景方式选择“模糊背景”。
3. 点击“拼接抽中片段”导出混剪视频。
4. 确认导出视频为 1920x1080。
5. 确认竖屏主体居中，左右两侧是同视频放大模糊背景，不是黑边。
6. 查看日志中是否显示输出比例、输出分辨率、背景方式、已应用到混剪导出。
