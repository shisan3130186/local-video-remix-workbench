# TODO_NEXT.md

## 当前唯一下一步：规划前端重构第五阶段

状态：
前四个重构阶段已经完成代码迁移、自动检查和人工回归。`App.vue` 已从约2095行减少到约825行，AI混剪、素材切片、普通混剪和导出流程均已迁入独立功能目录。

第五阶段范围：
1. 将 `ToolSettingModal.vue` 按画布、视频效果、平滑混剪、画中画和BGM拆成小型设置组件。
2. 将 `styles.css` 按基础变量、整体布局、公共组件和功能模块渐进拆分。
3. 保持现有界面尺寸、颜色、按钮位置、默认设置和交互路径不变。
4. 不修改Rust、FFmpeg、AI请求、VideoEngine或实际视频生成结果。

开始开发前：先统计 `ToolSettingModal.vue` 和 `styles.css` 的结构，给出本阶段预计修改文件、拆分边界、验证方法和风险点，获得用户确认后再写代码。

第五阶段完成后需要回归：
1. 设置弹窗打开、关闭、应用和重置正常。
2. 画布、效果、平滑混剪、画中画和BGM设置保持原行为。
3. 主界面和弹窗在常用窗口尺寸下没有样式错位。
4. AI混剪、随机混剪、分类混剪和批量生成继续正常。

重构路线：`docs/13_前端渐进式重构路线.md`

本阶段继续不做：TTS、图片转视频、去水印、字幕、Rust VideoEngine拆分和安装包分发。

Git规则：第五阶段人工验收通过后单独提交，不push。
