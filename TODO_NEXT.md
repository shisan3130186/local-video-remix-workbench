# TODO_NEXT.md

## 当前唯一下一步：V0.4.1 / V0.4.2 本地 BGM 联合人工验收

状态：
V0.4.1 本地 BGM 第一版和 V0.4.2 BGM 淡入淡出第一版已经开发完成，正在等待用户一起人工验收。

目标：
确认 BGM、音量、淡入淡出可用，并确认原有拼接、分类混剪、批量生成、画布、视频效果、平滑混剪、画中画和结果列表没有被破坏。

## 本阶段只允许做什么

1. 按清单人工验收 V0.4.1 / V0.4.2 BGM。
2. 发现问题后只做小步修复。
3. 修复后重新运行必要检查。
4. 更新 `PROJECT_STATUS.md`、`TODO_NEXT.md`、`CHANGELOG.md`。
5. 重要决策写入 `DECISIONS.md`。

## 本阶段暂时不做什么

1. 不做 AI 自动配乐。
2. 不做在线音乐库。
3. 不做歌词识别。
4. 不做音乐节拍自动卡点。
5. 不做复杂多轨时间线。
6. 不做字幕识别。
7. 不做 TTS 配音。
8. 不做 API Key 系统。
9. 不做会员卡密系统。
10. 不做新的大 UI 重构。
11. 不绕过 `VideoEngine` 直接拼接 FFmpeg 命令。

## V0.4.1 验收标准

1. 不开启 BGM 时，原有混剪导出结果不受影响。
2. 开启 BGM 后，导出视频可以听到背景音乐。
3. 原视频声音不会被意外完全覆盖。
4. 原视频音量和 BGM 音量设置生效。
5. BGM 比视频短时，导出不会中途静音。
6. BGM 比视频长时，导出不会超过视频长度。
7. 分类混剪可以带 BGM 导出。
8. 批量生成可以带 BGM 导出。
9. 任务日志能看懂 BGM 是否启用。
10. 导出结果继续进入导出结果列表。

## V0.4.2 验收标准

1. BGM 淡入秒数默认为 0.5 秒。
2. BGM 淡出秒数默认为 0.5 秒。
3. 开头 BGM 不会突然进入。
4. 结尾 BGM 不会突然停止。
5. 淡入淡出秒数设为 0 时，不做渐变。
6. 淡入淡出秒数只能在 0 到 10 秒之间。
7. 分类混剪带 BGM 淡入淡出可以正常导出。
8. 批量生成带 BGM 淡入淡出可以正常导出。

## 验收启动方式

双击项目根目录中的：

```text
E:\Workspace\Project_03_本地短视频批量混剪工作台\启动验收.bat
```

也可以命令行运行：

```text
cd E:\Workspace\Project_03_本地短视频批量混剪工作台
corepack pnpm tauri dev
```
