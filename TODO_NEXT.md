# TODO_NEXT.md

## 当前唯一下一步：智能切片与多帧理解第一版

状态：等待用户确认后开始，当前不提前开发。

推荐目标：

1. 不再只按固定5秒机械切片，增加按画面变化寻找候选切点。
2. 每个候选片段抽取开头、中间和结尾等多帧，而不是只理解一张缩略图。
3. 输出主体、场景、动作变化和镜头连续性等结构化结果。
4. 将结果保存到现有SQLite AI缓存，避免重复分析。
5. 保留现有固定时长切片作为稳定模式，智能切片作为可选入口。

暂不包含：视频主题卖点提炼、镜头类型自动分类和音频ASR；这些建立在本项结果之上。

开始前仍需用户确认。
