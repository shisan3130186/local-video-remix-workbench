# TODO_NEXT.md

## 当前唯一下一步

按 `docs/17_深色混剪工作台UI与文案改良方案.md` 的A–C步骤先修正模式、指引、确认摘要与参数语义，再完成深色界面分组和真实素材回归。当前交付的是改良方案及少量CSS修正；下方全局人工验收仍是发布前必需环节。

## 发布前验收（保留）

打开 `docs/15_发布前全局人工验收方案.html` 完成全功能人工验收。每完成一项标记通过；有问题标记失败，并填写复现现象、截图路径和输出文件路径。全部完成后点击“导出 HTML 报告”，将导出的文件发回开发者集中处理。P0/P1 全部为零且真实输出文件检查通过后，再执行 Tauri release 打包、独立电脑安装和外部用户测试包验证。

信息流带货成片已经完成页面和真实调用接线。下一步只做最终人工验收：使用已激活会员账号导入
`E:\Workspace\测试素材库\Project_03_水印测试\水印测试源视频.mp4` 与 `竖屏预览测试.mp4`，选择
`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\信息流带货成片验收`，依次执行切片、分类修正、模板切换和三版本导出，检查 MP4 数量、时长、9:16 比例、字幕和源文件完整性。

若账号未激活，界面会按既有权限规则阻止真实处理，不得把权限弹窗记为导出成功。

## 历史唯一下一步

请用户先导入真实素材，分别点击一次切片、AI 分析、混剪和导出，确认未激活会员时可以停留在工作台但不会创建处理任务；再按 `docs/09_测试验收清单.md` 顶部的“2026-08-11 全部真实功能集中人工验收”逐项复验。用户确认通过后执行本地 Git 提交。

## 历史待办（不作为当前任务）

## 历史批量实施待办：ECutAuto 对标闭环（2026-08-07）

1. 用真实短视频验证普通混剪字幕开启、位置/字号、画布比例、水印和画中画组合导出。
2. 核对内容提炼结果卡片、分类建议和“精华成片”在空状态、任务中、成功、失败四类状态下的表现。
3. 补齐 AI 混剪文件夹规则、文案/音频模式、任务结果目录和失败重试的竞品级状态反馈。
4. 继续核对视频效果二级设置、封面、去水印、批量改名、图片转视频和公共结果抽屉。
5. 完成前端、Rust、测试、双尺寸和独立 Windows 主链路检查后，统一生成集中验收清单，再提交 Git。

## 历史待验收：AI 成片文案区与云端成片输出修复（2026-08-07）

1. 导入素材后确认底部“视频文案”区域足够大且始终可编辑。
2. 完成分镜后确认前端只显示后台匹配状态，不再出现画面缩略图和拥挤的逐句卡片。
3. 检查右侧当前音色头像在不同音色和头像资源失败时仍保持圆形渐变头像。
4. 使用云端模型开启 AI 配音，确认最终视频进入“视频成片”，配音文件进入“配音与字幕”。
5. 用户验收通过后，再检查 Git 差异并提交本轮改动；不推送 GitHub。

# 2026-08-06：启动入口已补齐

项目根目录现在可以直接双击 `启动智剪.bat` 启动 Tauri 开发版。当前脚本用于开发和人工验收，正式用户安装包仍使用安装程序创建的桌面快捷方式。

## 当前状态：新电脑测试安装包已生成，等待独立电脑验收

V0.5.0新电脑测试安装包已经包含新版六卡片首页、三模式主题、真实到期日期和卡密/AI/TTS权限门禁，并通过本机隔离安装检查。

## 上一状态：V0.5.0会员系统和卡密管理器已验收

独立卡密管理器已完成预设天数、自定义天数、备注、生成复制、列表筛选、数量统计、停用和恢复。管理员凭证继续由Windows加密保存，工具不进入普通用户安装包；2026-07-24用户要求提交本地Git，视为本阶段验收通过。

V0.5.0国内账号会员安装包、CloudBase服务和卡密管理器均已完成。本轮提交本地Git，不推送GitHub。

## 上一状态：水印阶段已完成人工验收

水印工具、移动水印关键帧跟踪、自动检查和真实视频验证全部完成，本轮提交本地Git，暂不推送GitHub。

## 当前唯一下一步

用户把安装包复制到独立新电脑，完成安装、新版首页、账号登录、到期状态、API/TTS配置、权限弹窗、最短视频导出和卸载测试；验收通过后只提交本轮会员权限与打包记录，不提交`.workbuddy/`和`docs/icon-candidates/`，不推送GitHub。

## 后续候选

1. 独立干净Windows电脑完整主链路与卸载测试。
2. Windows代码签名、自动更新和更新失败回滚。
3. 字幕逐字变色、卡拉OK和模板市场等高级增强。
4. 图片转视频的转场、配乐和批量模板增强。
5. 正式用户协议、隐私政策版本化和软件内“关于”页面。

正式开始商业化阶段前必须先找用户确认顺序。

## 本轮验收后

1. 用户验收顶部画布直接操作和多文案标签交互。
2. 验收通过后执行 `git status`、差异检查并提交本轮改动。
3. 下一轮再处理真实素材状态下的画布预览、文字叠加和批量文案生成回归。

4. 用户复验本轮内联工具布局；确认无误后再提交 Git。

5. 用户在桌面版导入真实视频，验收比例虚框、裁剪框拖动/缩放、文字直接编辑和文字拖动。

## 2026-08-07 待用户复验

- 导入真实竖屏视频后连续输入中文、删除、换行，确认画布文字光标不跳动。
- 切换 16:9、1:1、9:16，确认虚框顶部、底部、左右边界完整且不歪斜。
- 在 1280×720 与 1440×900 下确认视频底部控制条完整可见。
## 人工验收前置事项（2026-08-15）

本轮代码已完成，下一步不是继续堆功能，而是按验收清单验证真实导出：

1. 启动桌面端，分别使用 1440×900 和 1280×720。
2. 在浅色、深色主题各导入横屏和竖屏测试视频。
3. 点击“自动检测常见水印区域”，确认红框出现并可拖动。
4. 分别选择融合修复、区域模糊、马赛克、色块遮盖，导出后逐一确认画面发生对应变化。
5. 验收通过后再提交本轮 Git；若失败，只记录具体步骤、输入文件和输出文件，不以界面高亮代替结论。

## 人工验收前置事项（2026-08-16 信息流带货成片）

1. 在珍珠雾白主题下确认信息流页面背景、面板、表单、列表和按钮均为浅色，视频预览画布保持黑色。
2. 切换夜航墨色再切回珍珠雾白，确认颜色实时往返且不需要刷新页面。
3. 使用至少三条素材完成切片、分类确认和三版本导出，检查结果卡片可直接预览真实视频。
4. 制造单版本失败后点击“重试失败版本”，确认只重试失败版本；未激活账号时记录权限拦截，不把界面状态当作导出成功。
