# TODO_NEXT.md

## 当前唯一下一步：手动验收新手/高级双模式 UI 和 V0.3.5 分类混剪第一版

状态：
已开发，等待用户手动验收。

目标：
确认新版 UI 是否足够简洁，零基础用户是否能一眼看懂“导入素材 -> 预览视频 -> 开始处理 -> 查看结果”，同时确认 V0.3.5 分类混剪能力没有被破坏。

## 本次只允许开发

本次开发范围已完成，人工验收时只检查以下能力：

1. 默认新手模式界面极简。
2. 左侧素材区只保留清爽素材树和小型导入按钮。
3. 中间视频预览区最大化。
4. 右侧默认只放模式、流程、设置和结果入口。
5. 高级模式保留旧功能入口。
6. 底部只保留必要输出状态和开始处理入口。
7. 继续保留分类混剪、随机混剪、批量生成、画布比例、模糊背景、平滑混剪、基础视频效果、画中画、输出目录和任务日志。

## 本次不允许开发

1. 不做 AI 自动分类。
2. 不做字幕识别。
3. 不做 TTS 配音。
4. 不做 API Key 系统。
5. 不做会员卡密系统。
6. 不做多轨时间轴。
7. 不修改 Rust / FFmpeg / VideoEngine。
8. 不绕过 VideoEngine 直接拼接 FFmpeg 命令。

## 开发后检查

1. 确认 V0.1 基础视频闭环已可用。
2. 确认 V0.2 基础混剪闭环已可用。
3. 确认 V0.3.1 平滑混剪已可用。
4. 确认 V0.3.2 基础视频效果已可用。
5. 确认 V0.3.3 画中画已可用。
6. 确认 V0.3.4 封面帧 / 片段预览已可用。
7. `corepack pnpm build` 已通过。
8. 本次未修改 Rust，因此不强制运行 `cargo check`。

## 验收标准

1. 默认打开后不再看到一堆参数。
2. 左侧导入按钮不再巨大占位。
3. 中间预览区最大、最清楚。
4. 新手模式能看到四步流程。
5. 点击“一键混剪 / 开始处理”仍走现有批量生成流程。
6. 点击“高级设置”能打开二级设置面板。
7. 点击“进入高级”后，旧功能入口仍然存在。
8. 原有随机混剪、分类混剪、批量生成、画布比例、模糊背景、平滑混剪、基础视频效果和画中画不被破坏。
9. `corepack pnpm build` 通过。

## 验收启动方式

双击项目根目录中的：

```text
E:\Workspace\Project_03_本地短视频批量混剪工作台\启动验收.bat
```

也可以命令行运行：

```text
cd E:\Workspace\Project_03_本地短视频批量混剪工作台
corepack pnpm tauri dev
```

## 备注

历史验收记录不要继续堆在本文件里，统一沉淀到 `PROJECT_STATUS.md` 和 `docs/10_开发者验收清单.md`。
