# TODO_NEXT.md

## 当前唯一下一步：V0.3.6 阶段验收与整理

状态：
已确认进入 V0.3.6 阶段，先做验收整理，不直接开发新功能。

目标：
把 V0.3 已经完成的功能统一验收一遍，确认主链路稳定、旧功能没有被新 UI 和分类混剪破坏，再决定 V0.4 做什么。

## 本阶段只允许做什么

1. 整理 V0.3.6 验收清单。
2. 按清单人工验收现有功能。
3. 发现问题后，只做小步修复。
4. 修复后运行必要检查。
5. 更新 `PROJECT_STATUS.md`、`TODO_NEXT.md`、`CHANGELOG.md`。
6. 重要决策写入 `DECISIONS.md`。

## 本阶段暂时不做什么

1. 不做 AI 自动分类。
2. 不做字幕识别。
3. 不做 TTS 配音。
4. 不做 API Key 系统。
5. 不做会员卡密系统。
6. 不做复杂多轨时间线。
7. 不做新的大 UI 重构。
8. 不绕过 VideoEngine 直接拼接 FFmpeg 命令。

## V0.3.6 验收顺序

1. 启动和环境检查
   - `corepack pnpm build` 通过。
   - 软件可以正常启动。
   - FFmpeg / FFprobe 检测正常。

2. 基础素材链路
   - 可以导入单个视频。
   - 可以导入文件夹。
   - 非视频文件不会进入素材列表。
   - 点击素材后，中间预览区可以播放。

3. 切片和片段管理
   - 固定时长切片可以生成片段。
   - 片段列表显示缩略图。
   - 点击片段后，中间大预览区播放对应片段。
   - 点击原视频后，预览区回到原视频。

4. 分类混剪
   - 片段可以手动选择分类。
   - 分类包括：开头钩子、产品展示、使用过程、细节特写、效果展示、结尾引导。
   - 至少两个分类有片段时，可以生成分类混剪。
   - 空分类会自动跳过。
   - 任务日志能显示每个分类抽中了哪个片段。
   - 结果进入导出结果列表。

5. 随机混剪和批量生成
   - 随机抽取可以按数量抽出片段。
   - 拼接抽中片段可以生成 mp4。
   - 批量生成可以生成多条 mp4。
   - 任务日志能看到关键步骤。

6. 画布和视觉效果
   - 原画模式正常。
   - 9:16、1:1、16:9 画布正常。
   - 黑边背景正常。
   - 模糊背景正常。
   - 水平镜像、垂直镜像、旋转、变速、亮度、对比度、饱和度、轻微缩放正常。

7. 平滑混剪
   - 关闭平滑混剪时，原有混剪正常。
   - 开启平滑混剪时，片段有淡入淡出。
   - 小于 1.5 秒的片段会被过滤。
   - 平滑混剪可以和画布比例、基础效果一起使用。

8. 画中画
   - 不开启画中画时，原有混剪正常。
   - 可以选择视频或图片作为画中画素材。
   - 位置、大小、透明度、边距生效。
   - 画中画可以和画布比例、平滑混剪、基础效果一起使用。

9. 结果和日志
   - 输出目录可以选择。
   - 导出结果可以查看。
   - 可以打开输出目录。
   - 任务日志能看到关键过程。

10. UI 验收
    - 默认新手模式仍然简洁。
    - 高级功能入口仍然能找到。
    - 右侧不堆满参数。
    - 左侧素材区不再大面积占位。
    - 中间视频预览区仍然最大、最清晰。

## 验收通过标准

1. V0.1 基础视频闭环仍然可用。
2. V0.2 基础混剪闭环仍然可用。
3. V0.3.1 平滑混剪可用。
4. V0.3.2 基础视频效果可用。
5. V0.3.3 画中画可用。
6. V0.3.4 封面帧 / 片段预览可用。
7. V0.3.5 分类混剪可用。
8. 新手 / 高级双模式 UI 不影响旧功能。
9. `corepack pnpm build` 通过。

## 验收启动方式

双击项目根目录中的：

```text
E:\Workspace\Project_03_本地短视频批量混剪工作台\启动验收.bat
```

也可以命令行运行：

```text
cd E:\Workspace\Project_03_本地短视频批量混剪工作台
corepack pnpm tauri dev
```

## 本轮完成后的下一步

V0.3.6 验收稳定后，再重新规划 V0.4。V0.4 可能方向包括：BGM、字幕 / 文案模式、继续增强分类混剪，但必须等 V0.3.6 通过后再决定。
