# TODO_NEXT.md

## 当前唯一下一步：手动验收 V0.3.5 分类混剪第一版

状态：
已开发，等待用户手动验收。

目标：
把现有随机混剪升级为按镜头类型顺序混剪，让生成结果更像一条正常短视频，而不是单纯随机拼接。

## 本次只允许开发

本次开发范围已完成，人工验收时只检查以下能力：

1. 给片段增加手动分类能力。
2. 支持分类：开头钩子、产品展示、使用过程、细节特写、效果展示、结尾引导。
3. 支持用户给已切片片段选择分类。
4. 增加“分类混剪”模式。
5. 分类混剪按顺序抽取片段：开头钩子 -> 产品展示 -> 使用过程 -> 细节特写 -> 效果展示 -> 结尾引导。
6. 每个分类默认抽取 1 个片段。
7. 空分类自动跳过。
8. 导出后加入导出结果列表。
9. 继续保留画布比例、模糊背景、平滑混剪、基础视频效果、画中画、输出目录和任务日志。

## 本次不允许开发

1. 不做 AI 自动分类。
2. 不做字幕识别。
3. 不做 TTS 配音。
4. 不做 API Key 系统。
5. 不做会员卡密系统。
6. 不做多轨时间轴。
7. 不做 UI 大重构。
8. 不绕过 VideoEngine 直接拼接 FFmpeg 命令。

## 开发后检查

1. 确认 V0.1 基础视频闭环已可用。
2. 确认 V0.2 基础混剪闭环已可用。
3. 确认 V0.3.1 平滑混剪已可用。
4. 确认 V0.3.2 基础视频效果已可用。
5. 确认 V0.3.3 画中画已可用。
6. 确认 V0.3.4 封面帧 / 片段预览已可用。
7. `corepack pnpm build` 已通过。
8. `cargo check` 已通过。

## 验收标准

1. 切片后，用户可以给每个片段选择一个分类。
2. 分类信息在当前页面状态中稳定保存，切换片段时不丢失。
3. 点击分类混剪后，系统按固定顺序抽取片段。
4. 某个分类没有片段时，系统自动跳过，不报错。
5. 至少两个分类有可用片段时，可以导出一个 mp4。
6. 导出结果进入导出结果列表。
7. 任务日志能说明每个分类抽中了哪个片段。
8. 原有随机混剪、批量生成、画布比例、模糊背景、平滑混剪、基础视频效果和画中画不被破坏。
9. `corepack pnpm build` 通过。
10. `cargo check` 通过。

## 验收启动方式

双击项目根目录中的：

```text
E:\Workspace\Project_03_本地短视频批量混剪工作台\启动验收.bat
```

也可以命令行运行：

```text
cd E:\Workspace\Project_03_本地短视频批量混剪工作台
corepack pnpm tauri dev
```

## 备注

历史验收记录不要继续堆在本文件里，统一沉淀到 `PROJECT_STATUS.md` 和 `docs/10_开发者验收清单.md`。
