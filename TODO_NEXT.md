# TODO_NEXT.md

## 当前唯一下一步：规划TTS第二阶段逐句配音与画面同步

状态：
TTS第一阶段已于2026-07-15通过真实火山语音环境人工验收。当前软件可以把文案生成本地MP3并在软件内试听，但配音还不会自动加入视频。

第二阶段开始编码前必须先确定：

1. 原视频声音默认静音，还是降低音量后保留。
2. 配音比对应画面长时，采用冻结尾帧、循环画面还是轻微变速。
3. 配音比对应画面短时，裁剪画面还是保留短尾部。
4. 第二阶段是否同步生成字幕，还是继续只做配音和画面同步。
5. 第一版是否只生成一个音色版本，不做批量多音色。

建议第二阶段最小链路：

```text
每句AI分镜文案
→ 分句生成配音
→ 按配音时长调整对应画面
→ 调整原视频声音
→ 叠加现有BGM
→ 通过VideoEngine输出完整配音视频
```

边界：第二阶段仍不得在前端拼接FFmpeg；音频混合、镜头时长调整和最终编码必须进入Rust VideoEngine。

暂不做：声音克隆、批量多音色、图片转视频、去水印、会员、复杂时间轴或自动发布。

Git规则：TTS第一阶段验收通过后单独提交，不push；第二阶段未经用户确认方案前不直接编码。
