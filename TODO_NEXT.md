# TODO_NEXT.md

## 当前最高优先级

1. 完成文件夹导入
2. 完成导入视频后的预览
3. 完成输出目录选择
4. 完成基础导出

## 开发前置阻塞

暂无。

启动检查：

1. `corepack pnpm build` 已通过。
2. `corepack pnpm tauri dev` 已通过启动检查。
3. Vite 已排除 `src-tauri/target` 和 `target` 目录，避免监听 Rust 编译产物。
4. `cargo check` 已通过。

## 本轮只允许开发

文件夹导入模块。

## V0.1 推荐任务拆分

### 任务 1：FFmpeg / FFprobe 环境检测

状态：
已完成。

目标：
检测本机是否可以正常调用 ffmpeg 和 ffprobe。

验收标准：

1. 可以检测 ffmpeg 是否存在。
2. 可以检测 ffprobe 是否存在。
3. 可以显示版本号。
4. 不存在时前端有明确提示。

可单独提交：
是。

### 任务 2：建立 VideoEngine 基础入口

状态：
已完成基础入口，后续随视频信息读取继续补充。

目标：
所有视频处理能力从统一入口调用，避免页面组件直接拼 FFmpeg 命令。

验收标准：

1. 环境检测能力通过 VideoEngine 调用。
2. 页面组件不直接拼接 FFmpeg / FFprobe 命令。
3. 返回结果格式清晰，包含成功状态和错误信息。

可单独提交：
是。

### 任务 3：单视频导入

状态：
已完成。

目标：
用户可以选择一个视频文件导入。

验收标准：

1. 可以导入单个 mp4。
2. 导入后列表显示文件名。
3. 可以保存文件路径用于后续读取。

可单独提交：
是。

### 任务 4：文件夹导入

目标：
用户可以选择一个文件夹，批量导入其中的视频文件。

验收标准：

1. 可以选择文件夹。
2. 可以过滤非视频文件。
3. 导入后素材列表显示多个视频。

可单独提交：
是。

### 任务 5：视频信息读取

状态：
已完成。

目标：
使用 ffprobe 读取视频基础信息。

验收标准：

1. 能读取视频时长。
2. 能读取分辨率。
3. 能读取帧率。
4. 能读取是否有音频。

可单独提交：
是。

### 任务 6：视频预览

目标：
用户点击素材后可以预览视频。

验收标准：

1. 点击素材后可以播放。
2. 可以暂停。
3. 切换视频不会崩溃。

可单独提交：
是。

### 任务 7：输出目录选择

目标：
用户可以选择导出结果保存位置。

验收标准：

1. 可以选择输出目录。
2. 页面显示当前输出目录。
3. 后续导出任务可以使用该目录。

可单独提交：
是。

### 任务 8：基础导出

目标：
用户可以把一个视频导出成一个新的 mp4。

验收标准：

1. 可以选择一个导入视频。
2. 可以点击导出。
3. 输出目录中生成新 mp4。
4. 导出失败时有错误提示。

可单独提交：
是。

### 任务 9：任务进度展示

目标：
导出任务有基础状态展示。

验收标准：

1. 可以看到任务开始。
2. 可以看到任务进行中。
3. 可以看到任务完成。
4. 可以看到任务失败。

可单独提交：
是。

### 任务 10：日志和错误提示整理

目标：
用户可以看懂任务结果和失败原因。

验收标准：

1. 任务开始有日志。
2. 任务完成有输出路径。
3. 任务失败有错误原因。
4. 日志不泄露 API Key 等敏感信息。

可单独提交：
是。

## 不允许开发

- 会员系统
- API Key系统
- AI内容识别
- 文案改写
- TTS
- ASR字幕识别
- 高级去水印
- 复杂画中画
- 云端渲染
- 支付系统
- 首页重构
- UI大改
