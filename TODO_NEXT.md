# TODO_NEXT.md

## 当前唯一下一步：V0.4 音频与 BGM 第一版

状态：
V0.3.6 阶段已经由用户人工验收完成，并已提交 Git。当前进入 V0.4 开阶段规划，下一步优先开发本地 BGM 第一版。

目标：
在不破坏现有导入、预览、切片、随机混剪、分类混剪、批量生成、画布、视频效果、平滑混剪、画中画和结果列表的前提下，让用户可以给导出的混剪视频添加本地背景音乐。

## 本阶段只允许做什么

1. 做本地 BGM 第一版。
2. 支持选择本地音频文件作为 BGM。
3. 支持开启 / 关闭 BGM。
4. 支持调节原视频音量和 BGM 音量。
5. BGM 自动适配导出视频长度：视频更长时循环或延长，视频更短时截断。
6. 优先接入拼接抽中片段、分类混剪和批量生成。
7. 所有 FFmpeg 音频处理继续通过 Rust `VideoEngine` 封装。
8. 修复过程中只做小步修改，并更新 `PROJECT_STATUS.md`、`TODO_NEXT.md`、`CHANGELOG.md`。

## 本阶段暂时不做什么

1. 不做 AI 自动配乐。
2. 不做在线音乐库。
3. 不做歌词识别。
4. 不做音乐节拍自动卡点。
5. 不做复杂多轨时间线。
6. 不做字幕识别。
7. 不做 TTS 配音。
8. 不做 API Key 系统。
9. 不做会员卡密系统。
10. 不做新的大 UI 重构。
11. 不绕过 `VideoEngine` 直接拼接 FFmpeg 命令。

## V0.4.1 开发顺序

1. 读取现有混剪导出链路
   - 确认拼接抽中片段、分类混剪和批量生成分别如何调用导出。
   - 确认现有音频处理、平滑混剪和画中画参数在哪里组装。

2. 设计 BGM 参数结构
   - 是否启用 BGM。
   - BGM 文件路径。
   - 原视频音量。
   - BGM 音量。
   - BGM 适配方式先固定为自动适配视频长度。

3. 接入前端入口
   - 在高级模式的音频或 BGM 设置入口中放入参数。
   - 新手模式暂不展开复杂音频参数。
   - 参数提示要面向零基础用户。

4. 接入 Service 层
   - 把 BGM 参数传给现有混剪导出方法。
   - 保持随机混剪、分类混剪和批量生成调用路径一致。

5. 接入 Rust `VideoEngine`
   - 在统一导出链路中处理 BGM。
   - 使用 FFmpeg 音频混合能力。
   - 保留原视频音频，并按音量参数混合 BGM。
   - BGM 文件路径必须支持中文和空格。

6. 验证
   - `corepack pnpm build` 通过。
   - 修改 Rust 时运行 `cargo check`。
   - 手动验收无 BGM、开启 BGM、调节音量、分类混剪、批量生成。

## V0.4.1 验收标准

1. 不开启 BGM 时，原有混剪导出结果不受影响。
2. 开启 BGM 后，导出视频可以听到背景音乐。
3. 原视频声音不会被意外完全覆盖。
4. 原视频音量和 BGM 音量设置生效。
5. BGM 比视频短时，导出不会中途静音。
6. BGM 比视频长时，导出不会超过视频长度。
7. 分类混剪可以带 BGM 导出。
8. 批量生成可以带 BGM 导出。
9. 任务日志能看懂 BGM 是否启用。
10. 导出结果继续进入导出结果列表。

## 验收启动方式

双击项目根目录中的：

```text
E:\Workspace\Project_03_本地短视频批量混剪工作台\启动验收.bat
```

也可以命令行运行：

```text
cd E:\Workspace\Project_03_本地短视频批量混剪工作台
corepack pnpm tauri dev
```
