# TODO_NEXT.md

## 当前唯一下一步：确认是否开发完整输出设置第一版

状态：
任务取消、FFmpeg真实进度、批量失败项单独重试和临时文件清理已经全部通过用户人工验收，准备提交本地Git，不推GitHub。

推荐下一功能：完整输出设置第一版。

1. 输出格式：第一版保留MP4，预留后续格式扩展结构。
2. 分辨率：原画、720p、1080p及跟随画布比例的常用尺寸。
3. 帧率：跟随源视频、24、25、30、50和60fps。
4. 视频码率或质量档位：省空间、标准、高清。
5. 编码方式：优先稳定的H.264 CPU编码，并检测可用GPU编码器。
6. 全部导出链路统一复用设置对象和VideoEngine。

自动检查结果：

- 前端正式构建通过。
- Rust格式检查通过。
- Rust编译和严格Clippy通过。
- Rust 81项测试通过、0项失败。

开始前仍需用户确认具体范围，不直接开发。
