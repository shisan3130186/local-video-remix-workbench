# 2026-07-24

## 工作台 UI 重设计（方案阶段，未动代码）

- 用户诉求：现工作台太复杂杂乱、缺设计感，要布局级重设计（不只是换色），小白一看就会用；功能一律不动。
- 已诊断现状问题：同屏入口 20+、流程拆散在三栏、无主按钮层级。
- 出了 3 个布局方案（A 分步向导 / B 三栏精简 / C 预览优先卡带），用户已选定 **方案 A · 分步向导式**。
- 已完成 5 个步骤页的页面级设计稿：导入素材 / 智能切片 / 文案分镜（核心页）/ 配音字幕 / 生成导出。
- 设计规格文档：`docs/16_向导式工作台UI重设计方案.md`（含现有功能→新位置映射表，承诺一个功能不删；实施分 5 批，每批可独立验收）。
- 待定：用户最终确认设计稿后才动代码；配色主题可后续单独再选（此前出过深岩/紫核/明澈三版配色稿）。

## 用户确认并开始实施（同日晚）

- 用户确认方案 A，要求 UI 做成"简约大气、让人有付费欲望"，目标客群：电商商家/短视频达人/电商运营；参考全网同类软件。
- 调研结论：该客群熟悉即创/抖店/可画的浅色 SaaS 风；最终视觉方向=浅色主界面+深色预览区+品牌青主色（第5批落地）。
- 实施分 5 批（任务清单 #1-#5）。**第1批向导骨架已完成**：新增 src/features/wizard/（步骤导航/底栏/步骤2操作卡/步骤5面板/composable/wizard.css）；MaterialPanel 加 section 属性；App.vue AI 工作区改 5 步向导，批量/工具工作区不变。vue-tsc + vite build 均通过，待用户人工验收。
- 骨架期过渡形态：步骤3 仍是旧 PreviewPanel（含旧步骤条）、步骤4 暂用原右栏面板、仍是深色主题。
- 构建注意：本机 corepack pnpm 在 managed node 下路径损坏；用 `./node_modules/.bin/vue-tsc --noEmit` 和 `./node_modules/.bin/vite build` 替代。

## 第1批已被用户否决并完整回滚（同日 20:30）

- 用户验收后表示不喜欢这版向导骨架，决定重新思考设计方向，要求回滚。
- 已完整回滚：App.vue / styles.css / MaterialPanel.vue 用 git checkout 恢复；删除 src/features/wizard/ 与 docs/16 设计文档；CURRENT_TASK.md 恢复为 V0.5.0 沙盒回归等待状态；任务清单 #1-#5 已删除。
- 回滚后 vue-tsc + vite build 通过，工作区回到 V0.5.0 基线（仅剩 .workbuddy/ 与 icon-candidates 未跟踪）。
- 仍有效、可复用的结论：目标客群（电商商家/达人/运营）偏好浅色 SaaS 高级感；现状 UI 问题诊断（入口过多、流程跨栏、无主按钮层级）依然成立。下次重谈设计时，先搞清楚用户不喜欢的是"向导式"这个交互模型，还是骨架期的粗糙观感——不要直接重做同一方案。
