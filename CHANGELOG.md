# CHANGELOG

## 2026-07-18：ASR与本地文案库精简联动

- 语音识别结果新增“保存到文案库”和“保存并使用到当前文案”。
- 文案与画面分镜工具栏新增“文案库”入口。
- 文案库第一版只提供载入和删除，暂不增加搜索、标签、分类和复杂编辑。
- 文案以独立版本化JSON保存到应用数据目录，不修改SQLite，不复制音视频文件。
- 相同来源文件和相同文字自动去重。
- 保存记录保留来源文件、完整文字、音视频时长和句子时间轴。
- 当前文案已有内容时，使用文案库内容前会要求用户确认替换。
- 超过4000字的文案可以保存，但不会直接用于AI分镜。
- 前端正式构建和Rust严格检查通过；104项Rust测试通过、0项失败。
- 2026-07-18用户人工验收通过：ASR结果可保存、载入、去重、重启恢复并联动当前AI文案。


## 2026-07-18：音频输入与火山云端ASR第一版

- 右侧常用调整新增独立“语音识别”入口，支持音频和带声音的视频。
- Rust VideoEngine新增ASR音频标准化，输出16kHz单声道压缩MP3，并检查声音轨、2小时时长和上传体积。
- 接入火山大模型极速语音识别接口，解析完整文字和句子级毫秒时间轴。
- 火山接口状态优先读取响应头，区分静音、服务繁忙、额度不足、鉴权错误和临时网络故障。
- 复用现有Windows加密保存的豆包语音密钥，界面统一为“TTS / ASR语音密钥”。
- 新增应用数据目录版本化JSON缓存，同一未修改文件不重复调用云端接口，不修改SQLite表结构。
- 语音识别接入现有任务中心、取消流程、FFmpeg真实进度和临时失败受控重试。
- 结果面板新增完整文字、复制、句子时间轴、音频时长、上传体积和缓存命中提示。
- `App.vue`只增加功能接线，ASR状态、服务和界面均位于`src/features/asr`。
- `corepack pnpm build`、Rust格式、编译、严格Clippy全部通过；102项Rust测试通过、0项失败。
- 2026-07-18用户人工验收通过：真实语音识别、完整文字和句子时间轴显示正常。


## 2026-07-17：视频内容提炼与AI自动分类第一版

- 视频片段页签新增“AI提炼全部片段”入口、完成数量、运行进度和错误状态。
- 复用智能切片已有的多帧画面描述，缓存存在时不重复上传图片。
- 新增主题、可见卖点、主体动作和内容标签提取。
- 新增开头钩子、产品展示、使用过程、细节特写、效果展示、结尾引导、人物口播和环境镜头8类自动分类。
- 当前片段的提炼结果支持人工修改并自动保存。
- 结构化结果以版本化JSON写入SQLite现有description字段，旧缓存保持兼容，数据库结构不变。
- Rust新增严格JSON解析和字段校验，内容提炼失败支持现有受控重试并保留部分成功结果。
- 前端正式构建、Rust格式、编译、严格Clippy和96项测试通过。
- 2026-07-17用户人工验收通过：内容提炼、自动分类、人工修改和缓存恢复符合预期。

## 2026-07-17：智能切片与多帧理解第一版

- 混剪设置新增智能切片与固定时长切片双模式，智能模式默认推荐。
- 智能切片通过FFmpeg画面变化分数寻找候选转场点，提供稳健、均衡、灵敏三档。
- 新增最短/最长片段保护，减少闪屏碎片，并对连续长镜头自动补切。
- 无明显转场的单个短视频在条件允许时自动均分为两个片段。
- 每个片段最多抽取开头、中间、结尾3帧，AI综合描述主体、场景、动作及动作变化。
- 片段代表图和AI描述继续复用现有SQLite素材库缓存，数据库结构保持不变。
- FFmpeg分段增加0.05秒关键帧容差，修复新版FFmpeg指定切点可能只输出一个文件的问题。
- 前端正式构建、Rust格式、严格Clippy和94项测试通过；真实合成3场景视频准确识别3秒、6秒转场并输出3段。
- 修复精简模式找不到功能入口的问题：右侧“常用调整”新增一级“智能切片”按钮，不再要求先开启高级模式。
- 2026-07-17用户人工验收通过：智能切片入口、场景切割、固定模式和多帧理解符合预期。

## 2026-07-17：素材库持久化第一版

- 新增内置SQLite素材库，普通用户无需另外安装数据库。
- 自动保存原视频元数据、封面、切片分类、缩略图和AI画面描述。
- 最近项目快照和长期素材库分开处理；空白项目只提示可用素材库，不再自动载入。
- 失效文件不会阻止其他素材恢复，原视频可从界面重新定位。
- 素材库增加读取、保存、恢复数量、失效数量和错误状态提示。
- SQLite采用版本化建表和事务替换，未来可继续迁移升级。
- 前端正式构建、Rust格式、编译、严格Clippy和90项测试全部通过。
- 修复“放弃上次记录”后SQLite立即恢复相同内容的问题；现在“新建空白项目”真正保持空白，素材库改为手动载入。
- 2026-07-17用户人工验收通过：SQLite保存、空白项目、手动载入、AI缓存和失效素材处理符合预期。

## 2026-07-17：完整输出设置第一版

- 导出设置新增MP4格式说明、分辨率、帧率、质量档位和编码器选择。
- 右侧“成片设置”新增可直接打开“导出设置”的入口，避免功能完成后无法从界面进入。
- 新增CPU、NVIDIA、Intel、AMD实际编码检测和自动推荐。
- 不可用GPU编码器会明确显示并禁止选择。
- 基础导出和所有随机/分类/AI/TTS最终成片统一应用输出设置。
- 输出日志新增实际分辨率、帧率、质量、编码器和目标码率。
- 旧项目快照自动使用安全默认值，新项目保存完整输出设置。
- 横竖屏720p、固定25fps、三档质量文件大小和NVIDIA GPU真实FFmpeg测试通过。
- 前端正式构建、Rust格式、编译、严格Clippy和87项测试全部通过。
- 2026-07-17用户人工验收通过：导出设置入口、分辨率、帧率、质量档位和GPU编码可正常使用。

## 2026-07-17：统一任务取消、真实进度、失败重试和临时清理

- 新增前端独立任务中心和Rust任务运行时。
- FFmpeg任务使用真实输出时间更新0～100%进度，底部显示当前阶段。
- 新增“取消任务”，真正终止当前FFmpeg，而不是只停止界面状态。
- 取消或失败后删除切片目录、导出半成品和混剪半成品。
- 随机批量、AI差异成片和AI配音成片支持只重试失败条目。
- 平滑片段、配音分镜和拼接清单统一进入可控临时目录并自动清理。
- 启动和手动清理只处理超过24小时的本项目遗留目录，保护活动任务和用户文件。
- 新增低Token启动入口，避免每次全文读取大型历史文档。
- 前端正式构建、Rust格式、编译、严格Clippy和81项Rust测试全部通过。
- 修复首轮人工验收发现的清理反馈不可见：清理结果现在直接显示在底部任务栏，不再只写入隐藏日志。
- 2026-07-17用户最终验收通过：真实进度、任务取消、失败项重试和临时清理反馈全部符合预期。

## 2026-07-17：项目保存与失败恢复第一版

- 新增最近项目自动保存，覆盖素材、切片、文案、AI描述、人工分镜和主要成片设置。
- 软件启动时显示恢复或放弃提示，防止关闭软件后工作进度全部丢失。
- 恢复前检查全部本地路径，缺失素材自动跳过并给出明确说明。
- 失效分镜不会静默恢复；失效BGM、画中画和输出目录会安全关闭或要求重选。
- 项目快照使用版本化JSON保存在Windows应用数据目录，限制2MB和最多10000条路径。
- Rust强制禁止API Key、密码、凭证和secret字段进入项目快照。
- 新增顶部自动保存状态、加载错误、损坏快照和缺失素材状态。
- 修复首轮验收中Vue响应式Proxy无法被 `structuredClone` 复制导致恢复中断的问题，改用JSON安全复制。
- 前端正式构建、Rust格式、编译、严格Clippy和76项测试全部通过。
- 2026-07-17用户人工复验通过：关闭并重新打开软件后可以成功恢复上次项目。

## 2026-07-17：AI与TTS临时失败自动重试

- AI片段画面理解、固定短句画面匹配、TTS试听和逐句配音新增受控自动重试。
- 网络连接失败、请求超时、HTTP 408/425/429/5xx 等临时故障最多尝试3次，等待时间逐步增加。
- API Key错误、模型配置错误、权限错误和明确的额度/余额耗尽不会盲目重试。
- 任务日志显示重试对象、等待时间、当前次数和服务返回的安全错误说明。
- 单句TTS失败只重试当前句，已经成功的句子不会重新生成；片段画面理解同样保留已成功结果。
- 前端正式构建、Rust格式、编译、严格Clippy和72项Rust测试全部通过。
- 2026-07-17用户人工验收通过：AI/TTS自动重试功能符合使用预期。

## 2026-07-17：软件内API密钥安全保存

- 右侧常用设置新增“API 密钥”入口和独立配置页。
- 支持方舟AI与火山TTS密钥、模型接入点、连接地址、资源ID和音色ID。
- 使用Windows当前用户数据保护机制加密保存，不再要求每次从终端启动配置。
- 完整密钥不会从Rust重新返回前端，也不会进入任务日志。
- 支持密码显隐、保存状态、环境变量备用和二次确认删除。
- AI画面理解、AI分镜、TTS试听和逐句配音统一读取软件内配置，保存后立即生效。
- 前端构建、Rust格式、编译、Clippy和67项测试通过；Windows加密及密文文件往返测试通过。
- 2026-07-17用户人工验收通过：软件内保存后无需每次从终端填写AI/TTS配置。

## 2026-07-17：一次生成多条AI差异视频

- AI分镜新增1～10条差异方案生成器，默认3条。
- 第一条保留当前人工确认方案，后续方案轮换各句备选画面，不改变文案和分镜顺序。
- 每条视频内部禁止重复片段，重复组合自动去除；备选不足时返回实际唯一方案数。
- 无配音链路改为顺序批量导出，单条失败后继续生成剩余版本。
- AI配音链路只生成一次逐句TTS音频，并复用到全部差异版本，避免重复请求和付费。
- 界面新增生成数量输入、备选不足说明、当前进度、成功/失败数和最终汇总。
- 新增Rust数量边界和候选冲突测试；前端正式构建、Rust格式、编译、Clippy和61项测试全部通过。
- 2026-07-17用户人工验收通过：一次生成多条AI差异视频主流程可用；多组稳定性回归因测试素材不足仍需继续。

## 2026-07-16：允许跳过失败片段继续文案分镜

- 修复切片已经完成但中间仍停留在“切片全部素材”的状态不同步问题。
- AI片段准备不再要求全部片段100%成功，成功片段会立即保留。
- 少数片段预览图或时长失败时自动跳过，只要剩余不少于2个即可继续。
- 部分成功结果在文案区域显示绿色提醒；不足2个时显示明确红色错误。
- 删除生成分镜前“准备数量必须等于全部切片数量”的重复限制。
- 更新AI配音说明，不再显示已经废弃的“长配音冻结尾帧”。
- `corepack pnpm build`通过。

## 2026-07-16：稳定长文案分镜匹配

- 长文案改为Rust本地自动断句，AI不再承担不稳定的文案拆分工作。
- 软件按标点和长度生成固定短句，通常6～14个有效字符，特殊尾句最多16个。
- AI新协议只返回 `shotIndex`、主片段和备选片段，不能改写或遗漏用户文案。
- AI返回顺序可自动恢复；重复分镜序号、遗漏分镜、未知片段和重复主片段仍会被拒绝。
- 备选片段与其他分镜主片段冲突时自动删除，不再让整份分镜失败。
- 更新任务进度文字为“固定短句画面匹配”。
- `corepack pnpm build`、`cargo fmt`、`cargo check`、`cargo clippy --all-targets -- -D warnings`通过；Rust 55项测试全部通过。

## 2026-07-16：限制长配音画面定格

- 修复长配音分镜使用短画面时连续复制最后一帧数秒的问题。
- AI分镜现在必须完整保留原文，目标每句6～14个有效字符，最长16个。
- AI规划增加片段时长匹配，并返回可用于补足时长的较长备选画面。
- TTS混剪把主片段、时长和备选片段一起交给Rust VideoEngine。
- VideoEngine按真实配音时长自动换用合适备选画面，只允许最多8%轻微减速和约0.8秒短补帧。
- 无法安全匹配时返回明确提示，不再输出长时间定格的坏成片。
- 保留原声时，原声音轨会同步应用轻微速度调整。
- `corepack pnpm build`、`cargo fmt -- --check`、`cargo check`、`cargo clippy --all-targets -- -D warnings`通过；当前Rust 55项测试全部通过。

## 2026-07-16：修复素材封面裁切和切片无反应

- 原始素材封面新增后端完整画面模式，竖屏视频会生成带留黑的320×240封面。
- AI片段预览图继续保持源比例，不改变火山图片理解输入。
- 切片前检查输出目录是否真实存在，不再只检查路径文字。
- 未选择目录或旧目录失效时，点击切片会自动要求重新选择并继续处理。
- 切片错误直接显示在核心流程区，不再只藏在任务日志或设置弹窗。
- 真实竖屏封面、真实FFmpeg切片、前端构建、Rust严格检查和全部47项测试通过。
- 2026-07-16人工验收通过：素材完整显示、失效目录重新选择和全部素材切片正常。

## 2026-07-15：自动字幕第一阶段

- AI配音设置新增“自动生成字幕”开关，默认开启。
- 新增底部、中间、顶部三种字幕位置和小、标准、大三种字号。
- 新增字幕样式预览，设置变化会立即反映在预览区域。
- AI分镜文案会按标点自然拆成短字幕，没有标点的长文案会按可读长度继续拆分。
- 多条短字幕会按文字长度分配该句TTS时长并依次显示，不再把整段文字一次堆在画面底部。
- 新增Rust `video_engine/subtitle.rs`，集中ASS字幕生成和安全转义。
- 默认字幕字号和半透明背景块收紧，短句通常保持单行，特别长的文字仍会自动缩小字号。
- 原始素材缩略图改为完整缩放，竖屏封面不再被横向框裁掉上下内容。
- FFmpeg缺少 `ass` 滤镜时返回明确提示，不继续生成无字幕视频。
- 临时字幕文件随配音临时目录自动清理。
- 首轮人工验收确认其他功能正常；针对字幕拥挤和素材封面裁切完成修复，前端构建、Rust严格检查、45项测试和竖屏真实字幕视频验证通过。
- 当前阶段不做ASR、逐字变色、卡拉OK或复杂字幕模板。

## 2026-07-15：TTS逐句配音视频第一版

- 新增“保留原视频声音”傻瓜式开关，默认关闭；开启后可用滑块在0%～100%范围调节，默认15%。
- 保留的是完整原声音轨，界面明确提示可能包含原人声、环境声和背景音乐，不伪装成只提取BGM。
- Rust配音混剪引擎会检测每个源片段是否存在音轨：存在时与AI配音混合，不存在时自动退回纯AI配音。
- AI配音、可选原声和现有外部BGM可以继续组合使用。
- 新增4项原声混合单元测试，总计35项Rust测试通过；真实FFmpeg混音和无音轨回退测试通过。
- 设置面板已做本地视觉检查，开关关闭时滑块自动禁用，打开后显示15%并允许调节。
- 2026-07-15人工验收通过：TTS逐句配音视频、画面时长同步、原声开关和音量调节均可正常使用。
- AI配音设置新增“生成AI视频时加入逐句配音”开关。
- 按人工调整后的AI分镜顺序逐句调用火山TTS，并显示第X/Y句进度。
- 新增Rust `narrated_mix` VideoEngine模块，将每句配音与对应画面合成为临时分镜。
- 配音比画面长时冻结尾帧，配音比画面短时裁短画面；原片段声音默认静音。
- 配音模式固定1.0x，不生成字幕，只使用当前单一音色。
- 带配音视频继续支持画布、效果、画中画、平滑混剪、BGM、任务日志和导出结果列表。
- 临时MP3和临时视频在系统临时目录生成，并在任务结束后清理。
- 前端生产构建、Rust严格检查和31项Rust测试通过。
- 本地FFmpeg测试确认冻结和裁短规则生效，随后已完成真实火山语音环境人工验收。

## 2026-07-14：火山引擎TTS语音合成第一阶段

- 新增 `src/features/tts` 独立功能模块。
- 右侧设置新增“AI配音”入口，支持编辑当前混剪文案和音色ID。
- 新增TTS配置状态、生成进度、错误提示、MP3试听、时长和输出路径展示。
- Rust新增火山引擎单向流式语音合成HTTP调用，分块音频合并后保存本地MP3。
- `TTS_API_KEY`只在Rust环境变量中读取，不进入前端、日志或Git。
- 支持 `TTS_RESOURCE_ID` 和 `TTS_SPEAKER` 环境变量，默认使用 `seed-tts-2.0` 和官方示例中文女声音色。
- 保留字词时间信息，为后续字幕和逐句配音同步预留数据。
- 修复火山正式成功码 `20000000` 被误判为服务拒绝的问题，同时继续兼容 `code = 0` 的响应。
- 本阶段不把配音加入视频，不修改原声、BGM、字幕、FFmpeg或VideoEngine。
- 前端生产构建和Rust严格检查通过，Rust 26项单元测试全部通过，其中TTS 8项。
- 2026-07-15真实火山语音环境人工验收通过：中文文案可生成MP3、软件内试听正常，并正确显示时长和输出路径。

## 2026-07-14：前端渐进式重构第五阶段

- 设置弹窗拆分为混剪、画布、平滑转场、画中画、音频、封面、视频效果和导出面板。
- `ToolSettingModal.vue` 继续保持原有参数和事件接口，从约654行减少到约174行。
- `styles.css` 从3148行减少为7条导入，原样式按原顺序拆入 `src/styles`。
- 拆分前后生产构建生成的CSS文件指纹一致，页面视觉规则没有变化。
- 设置弹窗增加标题关联，不改变可见界面和操作路径。
- 页面布局、设置默认值、AI请求、Rust、FFmpeg和VideoEngine行为没有变化。
- 前端构建、Rust Clippy和18项Rust单元测试全部通过。
- 第五阶段已由用户完成人工验收，设置弹窗、窗口布局和混剪生成回归通过。

## 2026-07-13：前端渐进式重构第四阶段

- 新增 `src/features/remix-export`，集中基础导出、随机混剪、分类混剪和批量生成。
- 普通混剪状态、错误、日志清理和生成结果记录从 `App.vue` 迁出。
- 随机抽取和分类抽取规则从共享视频Service迁入普通混剪功能目录。
- 基础导出Service和批量结果抽屉收拢到普通混剪功能目录。
- AI混剪继续复用普通混剪模块的生成状态和结果记录接口。
- `App.vue` 从约1154行减少到约825行。
- 页面布局、CSS、成片设置、Rust命令、FFmpeg和VideoEngine行为没有变化。
- 前端构建、Rust Clippy和18项Rust单元测试全部通过。
- 第四阶段已由用户完成人工验收，基础导出、普通混剪、批量生成、AI生成和状态清理回归通过。

## 2026-07-13：前端渐进式重构第三阶段

- 新增 `src/features/materials`，集中素材组件、素材状态、封面管理、导入和切片工作流。
- 单视频导入、文件夹导入、素材元数据读取和素材选择从 `App.vue` 迁出。
- 多视频依次切片、失败素材跳过、片段分类初始化和缩略图衔接从 `App.vue` 迁出。
- `MaterialPanel.vue`、视频导入 Service 和视频切片 Service 收拢到素材功能目录。
- `App.vue` 从约 1381 行减少到约 1154 行。
- 页面布局、CSS、Rust命令、FFmpeg、AI请求和VideoEngine行为没有变化。
- 前端构建、Rust Clippy 和18项Rust单元测试全部通过。
- 第三阶段已由用户完成人工验收，素材、切片、封面、AI衔接和旧混剪功能回归通过。

## 2026-07-13：前端渐进式重构第二阶段

- 第一阶段人工回归通过。
- 新增 `src/features/ai-remix`，集中 AI 混剪组件、类型、Service、片段工作流和状态控制。
- 片段预览图与时长准备、画面描述缓存、分批图片理解、文案分镜规划和人工调整从 `App.vue` 迁出。
- AI 视频生成继续复用现有 `concatSelectedSegments`、成片设置、VideoEngine、任务日志和导出结果列表。
- `App.vue` 从 1895 行减少到约 1381 行。
- 页面布局、CSS、火山引擎请求、提示词和 VideoEngine 行为没有变化。
- 前端构建、Rust Clippy 和 18 项 Rust 单元测试全部通过。
- 第二阶段已由用户完成人工验收，AI 主链路和旧混剪功能回归通过。

## 2026-07-13：前端渐进式重构第一阶段

- 新增 `src/types/workbench.ts`，统一工作台工具、日志、模块卡片和导出结果类型。
- 新增 `src/constants/workbench.ts`，集中首页模块卡片和片段分类选项。
- 新增 `useTaskLogs`，统一管理各类任务日志和导出结果。
- 新增 `useRemixSettings`，统一管理画布、效果、画中画、BGM 和导出设置。
- 多个组件移除重复类型定义，改用共享类型。
- `App.vue` 从 2095 行减少到 1895 行。
- 本次没有修改页面布局、视觉样式、AI 请求或 VideoEngine 行为。
- 前端构建、Rust Clippy、AI 测试和 FFmpeg 工具路径测试全部通过。

## 2026-07-13：代码质量与安装包准备

- 完成代码质量审计，确认项目无需重写，但 `App.vue`、`styles.css` 和 `mix.rs` 是后续小步治理重点。
- 新增统一 FFmpeg / FFprobe 路径解析，支持显式路径、内置资源目录和系统 PATH。
- 新增 3 项工具路径单元测试。
- 混剪导出参数收拢为统一设置对象，`cargo clippy --all-targets -- -D warnings` 通过。
- 补齐 `assets`、`tasks` 和 FFmpeg 打包资源预留目录。
- 新增安装包检查脚本、发布准备清单、基础功能分类和代码质量审计报告。
- 成功生成 Windows NSIS 安装器。
- 当前安装包未内置 FFmpeg / FFprobe、未签名，AI 仍依赖环境变量，只适合内部技术测试。

## 2026-07-14

- 真实火山环境人工验收通过：原 13 个片段完成图片理解和纯文字分镜规划，AI 分镜可以正常生成。
- AI 智能混剪 MVP 核心链路正式打通，项目进入第一轮稳定性和分镜质量测试。
- 确认最新超时发生在纯文字分镜规划阶段，不是图片画面理解失败。
- 火山方舟请求统一增加 `thinking.type = disabled`，关闭 `doubao-seed-2-1-pro` 默认深度思考。
- 纯文字分镜最大输出从 1600 调整为 800 tokens，超时从 120 秒调整为 45 秒；图片理解仍保留 180 秒。
- 新增关闭深度思考的请求体单元测试，AI 单元测试增加到 15 项并全部通过。
- AI 图片理解由每批 2 张预览图调整为每批 1 张，最多同时处理 2 张，降低单请求超时概率。
- 图片理解单请求超时调整为 180 秒，并与纯文字分镜规划使用独立超时。
- AI 网络错误现在明确标注“片段画面理解阶段”或“纯文字分镜规划阶段”。
- 任务日志新增当前片段编号和批次范围，且不记录 API Key、图片 Base64、完整请求体或本地图片路径。
- `corepack pnpm build`、`cargo check` 通过，`cargo fmt` 已执行，AI 单元测试 15 项全部通过。
- 本轮人工验收已通过，相关文件已经保存 Git，未 push。

## 2026-07-13

- 人工验收确认多视频导入可用。
- 人工验收确认“切片全部素材”可处理所有导入视频，并将不同来源片段合并到统一素材池。
- 13 个片段生成 AI 分镜失败，界面最终显示 0 个分镜，并提示等待火山引擎响应超时（`operation timed out`）。
- 本次只记录验收结果，没有宣称 AI 分镜通过，没有继续修改业务代码，也没有提交 Git 或 push。

## 2026-07-12

- 工作台优化为素材、预览与 AI 分镜、成片设置、底部任务操作四个清晰区域。
- AI 文案分镜成为第一屏主流程，新增四步进度引导和明确空状态。
- 素材库新增原始素材与视频片段标签页，切片后自动进入片段列表。
- 随机抽取、分类混剪和随机批量生成移入“其他混剪方式”折叠区。
- 右侧只常驻画布、画面效果、平滑混剪、画中画和背景音乐。
- 底部改为单一主操作按钮，并删除没有真实接线的导出参数控件。
- 新增 1180px 与 720px 响应式布局适配。
- 本次未修改 Rust、FFmpeg、AI 接口或 VideoEngine。
- 修正 2K 大屏下左右栏过窄、预览高度不足、片段缩略图和文字难以辨认的问题。
- 大屏布局参考竞品的信息比例调整为更宽素材栏、更高视频预览和适中的右侧设置栏。
- 左侧片段缩略图放大到 120×72，AI 主画面预览放大到 180×104，候选画面同步放大。
- AI 火山引擎请求改用 HTTP/1.1 兼容模式，并增加 15 秒连接超时。
- AI 网络错误现在会明确区分连接失败、连接超时和等待模型响应超时，不再只显示笼统的发送失败。
- AI 分镜改为两阶段火山引擎流程：每批最多 2 张预览图生成画面描述，再用文案、编号、时长和描述生成纯文字分镜。
- 当前切片会话会缓存已成功生成的画面描述；只修改文案或重新生成分镜时不再重复上传图片。
- 画面理解按两组请求并行执行，界面显示“正在理解片段画面 X/Y”和“正在生成分镜”。
- 文案输入区明确说明第一版不会自动朗读文案，导出视频继续保留所选原片段声音。
- AI 模块单元测试增加到 13 项。
- “切片当前视频”升级为“切片全部素材”：按顺序处理所有已导入视频，并把成功生成的片段合并为同一个 AI 素材池。
- 单个视频切片失败时保留其他视频的成功片段，并在任务日志中标明失败素材。


## 2026-07-11

- AI 文案混剪从“全部片段排序”升级为“逐句分镜匹配”。
- AI 可以舍弃无关素材，每个分镜返回一个主片段和最多 3 个备选片段。
- 新增分镜结果严格校验，拒绝空文案、重复主片段、未知片段和过多备选片段。
- 前端新增逐句分镜卡片，支持切换备选画面、上移、下移和删除。
- 当前分镜继续复用现有 VideoEngine、画布、平滑混剪、效果、画中画和 BGM 导出链路。
- `corepack pnpm build` 通过，`cargo fmt` 已执行；Rust 编译测试被当前 Windows 对新生成构建脚本的执行限制阻塞。

- 新增 ECuTAuto 竞品与第一版 MVP 差距分析报告，覆盖竞品定位、目标用户、核心链路、11 张界面说明、功能差距矩阵、48 小时实施顺序和验收标准。
- 研究结论：当前项目按既定第一版 MVP 约完成 90%，按竞品完整 AI 智能混剪核心体验约达到 60%～65%；下一阶段应优先验证真实主链路，再做文案拆句和逐句画面匹配。
- 修复 Windows `CRYPT_E_NO_REVOCATION_CHECK` 导致方舟 AI 请求在 HTTPS 握手阶段失败的问题。
- Rust AI 客户端改用 Mozilla 内置可信根证书，保留证书签名和域名验证，不关闭 HTTPS 安全校验。
- 新增内置根证书客户端构建测试。
- AI 模块 8 项单元测试、`cargo check` 和 `corepack pnpm build` 已通过。
- 新增 AI 文案智能混剪 MVP：切片后准备片段编号、真实时长和预览图。
- 新增 Rust 方舟多模态 AI 调用，配置只从 `AI_API_KEY`、`AI_BASE_URL`、`AI_MODEL` 环境变量读取。
- AI 请求只发送文案、片段编号、时长和预览图，不发送完整视频或本地文件路径。
- 严格校验 `orderedSegmentIds`，拒绝非 JSON、重复、遗漏或未知片段编号。
- 新增排序结果列表，支持片段上移、下移、删除和重新排序。
- AI 排序结果继续复用现有 `concatSelectedSegments`、VideoEngine、画布、平滑混剪、视频效果、画中画和 BGM 导出链路。
- AI 混剪结果进入现有导出结果列表。
- 保留已有字幕接口草稿，并在调用点统一传递禁用字幕配置；未开发字幕 UI 或字幕烧录。
- 新增 Rust 排序结果校验单元测试。
- `corepack pnpm build` 已通过。
- `cargo fmt` 已执行。
- `cargo check` 已通过。
- AI 排序校验单元测试 7 项全部通过。

## 2026-06-22

- 开发 V0.4.2 BGM 淡入淡出第一版：BGM 设置弹窗新增淡入秒数和淡出秒数。
- 淡入 / 淡出默认都是 0.5 秒，允许设置 0 到 10 秒；设置为 0 时不做渐变。
- 前端会在开始混剪前检查 BGM 淡入 / 淡出参数，避免非法参数传到后端。
- Rust VideoEngine 会根据最终导出视频时长计算 BGM 淡出起点，并通过 FFmpeg `afade` 只处理背景音乐。
- 本次不做网络音乐素材、在线音乐库、节拍卡点、复杂多轨时间线或 AI 自动配乐。
- `corepack pnpm build` 已通过。
- `cargo fmt` 已执行。
- `cargo check` 已通过。
- 开发 V0.4.1 本地 BGM 第一版：高级模式新增“背景音乐”入口。
- BGM 设置支持开启 / 关闭、选择本地音频文件、调节原视频音量、调节 BGM 音量。
- 拼接抽中片段、分类混剪、批量生成都会把 BGM 参数传给 Rust VideoEngine。
- VideoEngine 统一封装 FFmpeg 音频混合：BGM 短于视频时循环，BGM 长于视频时随视频结束。
- 开启 BGM 但未选择音频文件时，前端会直接提示错误，避免按钮看起来无反应。
- 本次未做 AI 自动配乐、在线音乐库、歌词识别、节拍卡点、字幕识别、TTS、API Key、会员卡密或复杂多轨时间线。
- `corepack pnpm build` 已通过。
- `cargo fmt` 已执行。
- `cargo check` 已通过。
- 已用本地预览页面检查高级模式入口和 BGM 设置弹窗，确认“背景音乐”“启用 BGM”“选择本地音乐”“原视频音量”“BGM 音量”等界面元素可见。
- V0.3.6 已完成人工验收并提交 Git，项目进入 V0.4 音频与 BGM 第一版规划。
- 更新 `TODO_NEXT.md`，把当前唯一下一步切换为 V0.4.1 本地 BGM 第一版。
- 更新 `PROJECT_STATUS.md`、`ROADMAP.md`、`DECISIONS.md`、`README.md`、`docs/10_开发者验收清单.md` 和 `docs/05_视频处理引擎设计.md`，统一说明 V0.4 先做本地 BGM，不做 AI 自动配乐、在线音乐库、字幕识别、TTS、API Key、会员卡密或复杂多轨时间线。
- 本次只更新文档，不修改 Vue、Rust、FFmpeg 或 VideoEngine 代码。
- V0.3.6 修复项已完成人工验收：画中画参数提示、分类混剪、批量生成和平滑混剪入口均已由用户确认可用。
- 本次补充记录只更新文档，不修改前端、Rust、FFmpeg 或 VideoEngine。
- 补回高级模式右侧“平滑混剪”入口，点击后打开原有平滑转场二级设置。
- 本次只恢复 UI 入口，没有修改平滑混剪导出逻辑，没有修改 Rust / FFmpeg / VideoEngine。
- 修复 V0.3.6 验收中发现的画中画参数问题：透明度手动输入 `5` 时，后端会拒绝生成，但前端提示不明显，表现为分类混剪和批量生成看似无反应。
- 画中画大小比例、透明度、边距输入现在会在前端自动限制到合法范围。
- 分类混剪、拼接抽中片段和批量生成在执行前会检查画中画参数，失败时直接在中间流程区显示错误。
- 本次只修改 Vue 前端校验和提示，没有修改 Rust / FFmpeg / VideoEngine。

## 2026-06-21

- 进入 V0.3.6 阶段验收与整理：下一步不直接开发新功能，先统一验收 V0.3 已完成能力。
- 重写 `TODO_NEXT.md`，把当前唯一任务整理为 V0.3.6 验收清单，覆盖启动、导入、预览、切片、片段点击预览、分类混剪、随机混剪、批量生成、画布、平滑混剪、基础效果、画中画、结果和日志、UI 等检查项。
- 更新 `PROJECT_STATUS.md`，明确当前阶段原则：先验收主链路，再验收新增能力，最后检查旧功能是否被破坏。
- 新增片段分类点击预览：切片后点击左侧 `segment_000.mp4` 等片段，中间大视频预览区会直接播放该片段。
- 片段列表新增选中高亮，方便人工判断每个片段应该归为开头钩子、产品展示、使用过程、细节特写、效果展示或结尾引导。
- 分类下拉框继续保留，点击下拉框只修改分类，不误触发片段预览切换。
- 点击左侧原视频素材后，预览区会回到原视频。
- 本次只调整 Vue 前端状态联动和 CSS，没有修改 Rust / FFmpeg / VideoEngine，也没有修改混剪生成逻辑。
- `corepack pnpm build` 已通过。

- 新增新手/高级双模式 UI：默认进入新手工作台，只突出导入素材、预览视频、开始处理、查看结果。
- 左侧素材区改为清爽素材树，导入视频和导入文件夹保留为顶部小按钮，避免大面积占位。
- 中间预览区继续作为最大视觉中心，新手流程区压缩为简洁状态条。
- 右侧默认只显示本地引擎状态、模式切换、新手流程、高级设置入口和结果入口。
- 高级模式继续保留混剪设置、画布设置、视频效果、画中画、封面、字幕、音频、BGM、TTS、水印等入口。
- 底部任务栏在新手模式只保留输出目录、处理状态和开始处理按钮；高级模式继续显示更多导出参数。
- 本次只调整 Vue 组件结构和 CSS，没有修改 Rust / FFmpeg / VideoEngine 视频处理逻辑。
- `corepack pnpm build` 已通过。
- 开发 V0.3.5 分类混剪第一版：切片片段现在可以手动选择分类。
- 新增 6 个固定分类：开头钩子、产品展示、使用过程、细节特写、效果展示、结尾引导。
- 新增“分类混剪”按钮，会按固定顺序每类随机抽取 1 个片段，空分类自动跳过。
- 分类混剪继续复用现有 `concatSelectedSegments` 和 VideoEngine 拼接导出能力，没有在页面里拼接 FFmpeg 命令。
- 分类混剪成功后会进入导出结果列表，任务日志会显示每个分类抽中的片段和跳过的空分类。
- 分类混剪继续保留画布比例、模糊背景、平滑混剪、基础视频效果、画中画、输出目录和任务日志链路。
- `corepack pnpm build` 已通过。
- `cargo check` 已通过。
- 修复 `启动验收.bat` 中文内容在 Windows `cmd` 中被解析成乱码命令的问题，脚本内部改为纯英文 ASCII 输出。
- 新增 `启动验收.bat`，用于双击启动本地 Tauri 验收环境。
- 在 `README.md` 中补充最简单启动方式，方便每次人工验收直接打开软件。
- 统一 Tauri / Rust / 前端版本号，`package.json`、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json` 均对齐到 `0.3.4`。
- 修正项目技术栈说明，将本地视频处理从 `Python + FFmpeg` 对齐为当前实际使用的 `Rust + FFmpeg`。
- 补齐 `README.md` 的开发启动、构建检查、Rust 检查和本地打包命令。
- 修正 `ROADMAP.md` 中 V0.3.3 / V0.3.4 / V0.3.5 的功能顺序，让路线图和当前状态一致。
- 将 `.codegraph/` 纳入 `.gitignore`，避免代码索引缓存进入 Git。
- 清理 `TODO_NEXT.md`，移除过期历史任务，只保留当前唯一下一步：V0.3.5 分类混剪第一版。
- 将 `package.json` 版本号从 `0.1.0` 统一更新为 `0.3.4`，匹配当前已完成能力阶段。
- 在 `PROJECT_STATUS.md` 顶部新增当前状态摘要，明确现有主链路已通过多轮人工验收，下一步进入分类混剪。
- 新增 `docs/10_开发者验收清单.md`，沉淀每次开发后的固定自检顺序。
- 本次只整理文档和版本信息，没有修改 Vue 页面、Rust 后端、FFmpeg / VideoEngine 视频处理逻辑。

## 2026-06-14

- UI v3 架构定版：主工作台改为左侧素材、中间预览和核心操作、右侧功能入口。
- 新增二级设置弹窗和底部信息抽屉，复杂参数、任务日志、导出结果不再挤压主页面。
- 拆分前端组件，降低 `App.vue` 模板堆叠。
- 新增 V0.3.1 平滑混剪第一版：拼接抽中片段和批量生成混剪支持平滑模式。
- 平滑混剪会过滤小于 1.5 秒的过短片段，并对有效片段画面和原声音频做 0.2 秒淡入淡出。
- 保留画布比例、模糊背景、导出结果列表、水平镜像和变速链路。
- 修复 UI v2 右侧参数栏占位过高的问题：压缩参数卡片间距和输入区高度，让混剪、批量生成、导出按钮在较矮窗口里更容易露出。
- 本次只调整 `src/styles.css`，没有修改 Rust、FFmpeg、VideoEngine 或视频处理功能逻辑。

## 2026-06-12

- 修复原画预览比例异常：竖屏原始视频现在会按真实比例居中显示，不再被横向预览区域撑满。
- 本次只调整前端预览画布尺寸计算，没有修改 VideoEngine 和 FFmpeg 导出逻辑。
- 修复混剪导出模糊背景未生效：16:9 模糊背景混剪导出现在会生成横屏画布、模糊背景和居中主体。
- 混剪导出日志增加输出比例、输出分辨率、背景方式和是否应用到混剪导出。
- 新增导出结果列表第一版：基础导出、拼接导出、批量生成成功后会统一进入结果列表。
- 修复任务日志区域过短：底部日志面板加高，日志列表占满可用空间。
- UI v2 暗色专业工作台重构：增加首页功能入口页，并将工作台调整为深色三栏布局。
- 新增打开目录能力：支持打开当前输出目录和导出结果所在位置。
