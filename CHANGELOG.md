# CHANGELOG

## 2026-07-11

- 修复 Windows `CRYPT_E_NO_REVOCATION_CHECK` 导致方舟 AI 请求在 HTTPS 握手阶段失败的问题。
- Rust AI 客户端改用 Mozilla 内置可信根证书，保留证书签名和域名验证，不关闭 HTTPS 安全校验。
- 新增内置根证书客户端构建测试。
- AI 模块 8 项单元测试、`cargo check` 和 `corepack pnpm build` 已通过。
- 新增 AI 文案智能混剪 MVP：切片后准备片段编号、真实时长和预览图。
- 新增 Rust 方舟多模态 AI 调用，配置只从 `AI_API_KEY`、`AI_BASE_URL`、`AI_MODEL` 环境变量读取。
- AI 请求只发送文案、片段编号、时长和预览图，不发送完整视频或本地文件路径。
- 严格校验 `orderedSegmentIds`，拒绝非 JSON、重复、遗漏或未知片段编号。
- 新增排序结果列表，支持片段上移、下移、删除和重新排序。
- AI 排序结果继续复用现有 `concatSelectedSegments`、VideoEngine、画布、平滑混剪、视频效果、画中画和 BGM 导出链路。
- AI 混剪结果进入现有导出结果列表。
- 保留已有字幕接口草稿，并在调用点统一传递禁用字幕配置；未开发字幕 UI 或字幕烧录。
- 新增 Rust 排序结果校验单元测试。
- `corepack pnpm build` 已通过。
- `cargo fmt` 已执行。
- `cargo check` 已通过。
- AI 排序校验单元测试 7 项全部通过。

## 2026-06-22

- 开发 V0.4.2 BGM 淡入淡出第一版：BGM 设置弹窗新增淡入秒数和淡出秒数。
- 淡入 / 淡出默认都是 0.5 秒，允许设置 0 到 10 秒；设置为 0 时不做渐变。
- 前端会在开始混剪前检查 BGM 淡入 / 淡出参数，避免非法参数传到后端。
- Rust VideoEngine 会根据最终导出视频时长计算 BGM 淡出起点，并通过 FFmpeg `afade` 只处理背景音乐。
- 本次不做网络音乐素材、在线音乐库、节拍卡点、复杂多轨时间线或 AI 自动配乐。
- `corepack pnpm build` 已通过。
- `cargo fmt` 已执行。
- `cargo check` 已通过。
- 开发 V0.4.1 本地 BGM 第一版：高级模式新增“背景音乐”入口。
- BGM 设置支持开启 / 关闭、选择本地音频文件、调节原视频音量、调节 BGM 音量。
- 拼接抽中片段、分类混剪、批量生成都会把 BGM 参数传给 Rust VideoEngine。
- VideoEngine 统一封装 FFmpeg 音频混合：BGM 短于视频时循环，BGM 长于视频时随视频结束。
- 开启 BGM 但未选择音频文件时，前端会直接提示错误，避免按钮看起来无反应。
- 本次未做 AI 自动配乐、在线音乐库、歌词识别、节拍卡点、字幕识别、TTS、API Key、会员卡密或复杂多轨时间线。
- `corepack pnpm build` 已通过。
- `cargo fmt` 已执行。
- `cargo check` 已通过。
- 已用本地预览页面检查高级模式入口和 BGM 设置弹窗，确认“背景音乐”“启用 BGM”“选择本地音乐”“原视频音量”“BGM 音量”等界面元素可见。
- V0.3.6 已完成人工验收并提交 Git，项目进入 V0.4 音频与 BGM 第一版规划。
- 更新 `TODO_NEXT.md`，把当前唯一下一步切换为 V0.4.1 本地 BGM 第一版。
- 更新 `PROJECT_STATUS.md`、`ROADMAP.md`、`DECISIONS.md`、`README.md`、`docs/10_开发者验收清单.md` 和 `docs/05_视频处理引擎设计.md`，统一说明 V0.4 先做本地 BGM，不做 AI 自动配乐、在线音乐库、字幕识别、TTS、API Key、会员卡密或复杂多轨时间线。
- 本次只更新文档，不修改 Vue、Rust、FFmpeg 或 VideoEngine 代码。
- V0.3.6 修复项已完成人工验收：画中画参数提示、分类混剪、批量生成和平滑混剪入口均已由用户确认可用。
- 本次补充记录只更新文档，不修改前端、Rust、FFmpeg 或 VideoEngine。
- 补回高级模式右侧“平滑混剪”入口，点击后打开原有平滑转场二级设置。
- 本次只恢复 UI 入口，没有修改平滑混剪导出逻辑，没有修改 Rust / FFmpeg / VideoEngine。
- 修复 V0.3.6 验收中发现的画中画参数问题：透明度手动输入 `5` 时，后端会拒绝生成，但前端提示不明显，表现为分类混剪和批量生成看似无反应。
- 画中画大小比例、透明度、边距输入现在会在前端自动限制到合法范围。
- 分类混剪、拼接抽中片段和批量生成在执行前会检查画中画参数，失败时直接在中间流程区显示错误。
- 本次只修改 Vue 前端校验和提示，没有修改 Rust / FFmpeg / VideoEngine。

## 2026-06-21

- 进入 V0.3.6 阶段验收与整理：下一步不直接开发新功能，先统一验收 V0.3 已完成能力。
- 重写 `TODO_NEXT.md`，把当前唯一任务整理为 V0.3.6 验收清单，覆盖启动、导入、预览、切片、片段点击预览、分类混剪、随机混剪、批量生成、画布、平滑混剪、基础效果、画中画、结果和日志、UI 等检查项。
- 更新 `PROJECT_STATUS.md`，明确当前阶段原则：先验收主链路，再验收新增能力，最后检查旧功能是否被破坏。
- 新增片段分类点击预览：切片后点击左侧 `segment_000.mp4` 等片段，中间大视频预览区会直接播放该片段。
- 片段列表新增选中高亮，方便人工判断每个片段应该归为开头钩子、产品展示、使用过程、细节特写、效果展示或结尾引导。
- 分类下拉框继续保留，点击下拉框只修改分类，不误触发片段预览切换。
- 点击左侧原视频素材后，预览区会回到原视频。
- 本次只调整 Vue 前端状态联动和 CSS，没有修改 Rust / FFmpeg / VideoEngine，也没有修改混剪生成逻辑。
- `corepack pnpm build` 已通过。

- 新增新手/高级双模式 UI：默认进入新手工作台，只突出导入素材、预览视频、开始处理、查看结果。
- 左侧素材区改为清爽素材树，导入视频和导入文件夹保留为顶部小按钮，避免大面积占位。
- 中间预览区继续作为最大视觉中心，新手流程区压缩为简洁状态条。
- 右侧默认只显示本地引擎状态、模式切换、新手流程、高级设置入口和结果入口。
- 高级模式继续保留混剪设置、画布设置、视频效果、画中画、封面、字幕、音频、BGM、TTS、水印等入口。
- 底部任务栏在新手模式只保留输出目录、处理状态和开始处理按钮；高级模式继续显示更多导出参数。
- 本次只调整 Vue 组件结构和 CSS，没有修改 Rust / FFmpeg / VideoEngine 视频处理逻辑。
- `corepack pnpm build` 已通过。
- 开发 V0.3.5 分类混剪第一版：切片片段现在可以手动选择分类。
- 新增 6 个固定分类：开头钩子、产品展示、使用过程、细节特写、效果展示、结尾引导。
- 新增“分类混剪”按钮，会按固定顺序每类随机抽取 1 个片段，空分类自动跳过。
- 分类混剪继续复用现有 `concatSelectedSegments` 和 VideoEngine 拼接导出能力，没有在页面里拼接 FFmpeg 命令。
- 分类混剪成功后会进入导出结果列表，任务日志会显示每个分类抽中的片段和跳过的空分类。
- 分类混剪继续保留画布比例、模糊背景、平滑混剪、基础视频效果、画中画、输出目录和任务日志链路。
- `corepack pnpm build` 已通过。
- `cargo check` 已通过。
- 修复 `启动验收.bat` 中文内容在 Windows `cmd` 中被解析成乱码命令的问题，脚本内部改为纯英文 ASCII 输出。
- 新增 `启动验收.bat`，用于双击启动本地 Tauri 验收环境。
- 在 `README.md` 中补充最简单启动方式，方便每次人工验收直接打开软件。
- 统一 Tauri / Rust / 前端版本号，`package.json`、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json` 均对齐到 `0.3.4`。
- 修正项目技术栈说明，将本地视频处理从 `Python + FFmpeg` 对齐为当前实际使用的 `Rust + FFmpeg`。
- 补齐 `README.md` 的开发启动、构建检查、Rust 检查和本地打包命令。
- 修正 `ROADMAP.md` 中 V0.3.3 / V0.3.4 / V0.3.5 的功能顺序，让路线图和当前状态一致。
- 将 `.codegraph/` 纳入 `.gitignore`，避免代码索引缓存进入 Git。
- 清理 `TODO_NEXT.md`，移除过期历史任务，只保留当前唯一下一步：V0.3.5 分类混剪第一版。
- 将 `package.json` 版本号从 `0.1.0` 统一更新为 `0.3.4`，匹配当前已完成能力阶段。
- 在 `PROJECT_STATUS.md` 顶部新增当前状态摘要，明确现有主链路已通过多轮人工验收，下一步进入分类混剪。
- 新增 `docs/10_开发者验收清单.md`，沉淀每次开发后的固定自检顺序。
- 本次只整理文档和版本信息，没有修改 Vue 页面、Rust 后端、FFmpeg / VideoEngine 视频处理逻辑。

## 2026-06-14

- UI v3 架构定版：主工作台改为左侧素材、中间预览和核心操作、右侧功能入口。
- 新增二级设置弹窗和底部信息抽屉，复杂参数、任务日志、导出结果不再挤压主页面。
- 拆分前端组件，降低 `App.vue` 模板堆叠。
- 新增 V0.3.1 平滑混剪第一版：拼接抽中片段和批量生成混剪支持平滑模式。
- 平滑混剪会过滤小于 1.5 秒的过短片段，并对有效片段画面和原声音频做 0.2 秒淡入淡出。
- 保留画布比例、模糊背景、导出结果列表、水平镜像和变速链路。
- 修复 UI v2 右侧参数栏占位过高的问题：压缩参数卡片间距和输入区高度，让混剪、批量生成、导出按钮在较矮窗口里更容易露出。
- 本次只调整 `src/styles.css`，没有修改 Rust、FFmpeg、VideoEngine 或视频处理功能逻辑。

## 2026-06-12

- 修复原画预览比例异常：竖屏原始视频现在会按真实比例居中显示，不再被横向预览区域撑满。
- 本次只调整前端预览画布尺寸计算，没有修改 VideoEngine 和 FFmpeg 导出逻辑。
- 修复混剪导出模糊背景未生效：16:9 模糊背景混剪导出现在会生成横屏画布、模糊背景和居中主体。
- 混剪导出日志增加输出比例、输出分辨率、背景方式和是否应用到混剪导出。
- 新增导出结果列表第一版：基础导出、拼接导出、批量生成成功后会统一进入结果列表。
- 修复任务日志区域过短：底部日志面板加高，日志列表占满可用空间。
- UI v2 暗色专业工作台重构：增加首页功能入口页，并将工作台调整为深色三栏布局。
- 新增打开目录能力：支持打开当前输出目录和导出结果所在位置。
