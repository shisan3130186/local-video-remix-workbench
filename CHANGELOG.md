# CHANGELOG

## 2026-06-21

- 修复 `启动验收.bat` 中文内容在 Windows `cmd` 中被解析成乱码命令的问题，脚本内部改为纯英文 ASCII 输出。
- 新增 `启动验收.bat`，用于双击启动本地 Tauri 验收环境。
- 在 `README.md` 中补充最简单启动方式，方便每次人工验收直接打开软件。
- 统一 Tauri / Rust / 前端版本号，`package.json`、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json` 均对齐到 `0.3.4`。
- 修正项目技术栈说明，将本地视频处理从 `Python + FFmpeg` 对齐为当前实际使用的 `Rust + FFmpeg`。
- 补齐 `README.md` 的开发启动、构建检查、Rust 检查和本地打包命令。
- 修正 `ROADMAP.md` 中 V0.3.3 / V0.3.4 / V0.3.5 的功能顺序，让路线图和当前状态一致。
- 将 `.codegraph/` 纳入 `.gitignore`，避免代码索引缓存进入 Git。
- 清理 `TODO_NEXT.md`，移除过期历史任务，只保留当前唯一下一步：V0.3.5 分类混剪第一版。
- 将 `package.json` 版本号从 `0.1.0` 统一更新为 `0.3.4`，匹配当前已完成能力阶段。
- 在 `PROJECT_STATUS.md` 顶部新增当前状态摘要，明确现有主链路已通过多轮人工验收，下一步进入分类混剪。
- 新增 `docs/10_开发者验收清单.md`，沉淀每次开发后的固定自检顺序。
- 本次只整理文档和版本信息，没有修改 Vue 页面、Rust 后端、FFmpeg / VideoEngine 视频处理逻辑。

## 2026-06-14

- UI v3 架构定版：主工作台改为左侧素材、中间预览和核心操作、右侧功能入口。
- 新增二级设置弹窗和底部信息抽屉，复杂参数、任务日志、导出结果不再挤压主页面。
- 拆分前端组件，降低 `App.vue` 模板堆叠。
- 新增 V0.3.1 平滑混剪第一版：拼接抽中片段和批量生成混剪支持平滑模式。
- 平滑混剪会过滤小于 1.5 秒的过短片段，并对有效片段画面和原声音频做 0.2 秒淡入淡出。
- 保留画布比例、模糊背景、导出结果列表、水平镜像和变速链路。
- 修复 UI v2 右侧参数栏占位过高的问题：压缩参数卡片间距和输入区高度，让混剪、批量生成、导出按钮在较矮窗口里更容易露出。
- 本次只调整 `src/styles.css`，没有修改 Rust、FFmpeg、VideoEngine 或视频处理功能逻辑。

## 2026-06-12

- 修复原画预览比例异常：竖屏原始视频现在会按真实比例居中显示，不再被横向预览区域撑满。
- 本次只调整前端预览画布尺寸计算，没有修改 VideoEngine 和 FFmpeg 导出逻辑。
- 修复混剪导出模糊背景未生效：16:9 模糊背景混剪导出现在会生成横屏画布、模糊背景和居中主体。
- 混剪导出日志增加输出比例、输出分辨率、背景方式和是否应用到混剪导出。
- 新增导出结果列表第一版：基础导出、拼接导出、批量生成成功后会统一进入结果列表。
- 修复任务日志区域过短：底部日志面板加高，日志列表占满可用空间。
- UI v2 暗色专业工作台重构：增加首页功能入口页，并将工作台调整为深色三栏布局。
- 新增打开目录能力：支持打开当前输出目录和导出结果所在位置。
