# DECISIONS.md

## 2026-06-22：V0.4 先做本地 BGM 第一版，不直接进入字幕、TTS 或 AI

决定：
V0.4 第一优先级确定为本地 BGM 第一版。先支持用户选择本地音频文件，设置是否启用 BGM，调节原视频音量和 BGM 音量，并让 BGM 自动适配导出视频长度。第一版优先接入拼接抽中片段、分类混剪和批量生成。

原因：
V0.3 已经完成并验收了混剪画面主链路，包括画布、平滑混剪、基础效果、画中画、封面帧和分类混剪。下一步最自然的增强是补齐成片声音体验。BGM 范围比字幕、TTS、AI 自动分类、在线音乐库和多轨时间线更清晰，也更容易验收。

范围：
1. 支持选择本地音频文件作为 BGM。
2. 支持开启 / 关闭 BGM。
3. 支持原视频音量和 BGM 音量。
4. BGM 比视频短时自动适配视频长度。
5. BGM 比视频长时按导出视频长度截断。
6. 先接入拼接抽中片段、分类混剪和批量生成。
7. 任务日志要能说明 BGM 是否启用。

限制：
1. 不做 AI 自动配乐。
2. 不做在线音乐库。
3. 不做歌词识别。
4. 不做节拍自动卡点。
5. 不做复杂多轨时间线。
6. 不做字幕识别。
7. 不做 TTS 配音。
8. 不做 API Key 或会员卡密系统。
9. 不让前端页面组件直接拼接 FFmpeg 命令。

技术边界：
所有 BGM 混音、循环、截断和音量处理必须继续通过 Rust `VideoEngine` 统一封装。前端只负责收集参数，Service 层只负责传递结构化参数。

## 2026-06-21：默认采用新手/高级双模式，不把全部功能摊在主界面

决定：
工作台默认进入新手模式，只展示“导入素材 -> 预览视频 -> 开始处理 -> 查看结果”的主流程。高级功能通过“进入高级”和“高级设置”进入二级入口，保留现有能力但不在主界面全部展开。

原因：
本项目面向零基础用户，主界面如果同时展示切片、随机抽取、分类混剪、批量生成、画布、效果、画中画、封面、字幕、音频、BGM、TTS、水印、日志和结果，会让用户第一眼不知道该点哪里。参考视频和截图的可学习点是“左侧素材树、中间大预览、右侧少量入口、底部任务状态”，不是复制品牌资源或视觉素材。

范围：
1. 左侧保留清爽素材树和导入入口。
2. 中间优先保留最大视频预览区。
3. 右侧新手模式只保留引擎状态、模式切换、流程和结果入口。
4. 高级模式继续保留混剪、画布、效果、画中画、封面、字幕、音频、BGM、TTS、水印等入口。
5. 底部新手模式只保留输出目录、处理状态和开始处理。

边界：
1. 不复制竞品软件名称、Logo、图标素材和文案。
2. 不修改 Rust / FFmpeg / VideoEngine。
3. 不新增真实字幕、TTS、AI 自动分类或会员能力。
4. 不让页面组件直接拼接 FFmpeg 命令。

## 2026-06-21：V0.3.5 分类混剪先做手动分类，不做 AI 自动分类

决定：
分类混剪第一版只做“用户手动给片段分类 + 按固定分类顺序抽取 + 复用现有拼接导出”。分类抽取规则放在前端 Service 层，最终导出继续调用现有 `concatSelectedSegments`，由 Rust VideoEngine 统一处理 FFmpeg。

原因：
当前目标是先验证分类混剪的基础工作流，让用户能按短视频结构组织片段。AI 自动分类、字幕识别、TTS、API Key 和多轨时间轴都会明显扩大范围，暂不进入 V0.3.5。

范围：
1. 支持开头钩子、产品展示、使用过程、细节特写、效果展示、结尾引导 6 个固定分类。
2. 每个分类默认随机抽取 1 个片段。
3. 空分类自动跳过。
4. 至少 2 个分类有片段时才导出。
5. 导出后进入导出结果列表。
6. 任务日志显示每个分类抽中的片段。

限制：
1. 不做 AI 自动分类。
2. 不做字幕识别。
3. 不做 TTS。
4. 不做 API Key 系统。
5. 不做会员卡密系统。
6. 不做多轨时间轴。
7. 不让页面组件直接拼接 FFmpeg 命令。

## 2026-06-14：V0.3.4 关键帧能力先做静态抽帧，不做动画时间轴

决定：关键帧 / 封面帧 / 片段预览第一版只做静态抽帧能力。视频封面帧、片段预览图都通过后端 VideoEngine 调用 FFmpeg 生成 jpg，不在前端拼接 FFmpeg 命令。

原因：当前阶段目标是让素材预览、片段管理和封面选择先可用，为后续分类混剪和内容管理打基础。专业剪辑软件那种关键帧动画、曲线和时间轴会明显扩大范围，暂不进入 V0.3.4。

范围：
1. 导入视频后生成素材封面帧。
2. 切片后生成片段预览图。
3. 支持按秒数重新抽取某一帧作为当前封面。
4. 缩略图失败不阻塞切片、混剪和导出主链路。

限制：
1. 不做关键帧动画。
2. 不做复杂时间轴。
3. 不做 AI 画面分析。
4. 不做字幕、TTS、会员、API Key。

## 2026-06-14：V0.3.3 画中画先作为混剪导出叠加层

决定：
画中画第一版先接入拼接抽中片段和批量生成混剪，不开发多轨时间轴，不做拖拽定位，不做动画。

原因：
当前稳定链路是混剪导出链路。画中画先作为最终画面的第二路叠加输入，可以复用已有画布比例、模糊背景、平滑混剪、视频效果和导出结果列表。

范围：
1. 主视频使用当前混剪导出的主画面。
2. 叠加素材支持视频或图片。
3. 支持位置、大小比例、透明度、边距。
4. 后端通过 VideoEngine 封装 FFmpeg overlay。

限制：
1. 不做复杂多轨时间轴。
2. 不做多个画中画。
3. 不做拖拽定位。
4. 不做画中画动画。
5. 不让前端直接拼接 FFmpeg 命令。

## 2026-06-14：V0.3.2 基础视频效果继续挂在混剪导出链路

决定：
基础视频效果第一版先作为拼接抽中片段和批量生成混剪的导出参数，不单独开发复杂特效库，也不新建独立多轨时间轴。

原因：
当前稳定链路是切片、随机抽取、拼接、批量生成、画布比例、模糊背景、平滑混剪和导出结果列表。基础效果先复用这条链路，风险最小，也方便用户马上验收。

范围：
1. 水平镜像。
2. 垂直镜像。
3. 90° / 180° 旋转。
4. 视频变速。
5. 亮度、对比度、饱和度。
6. 轻微缩放。

限制：
1. 不做复杂特效库。
2. 不做 AI 效果。
3. 不做多轨时间轴。
4. 不让前端直接拼接 FFmpeg 命令。

## 2026-06-14：UI v3 高还原只还原结构，不复制品牌资产

决定：
本次 UI v3 按用户提供的参考图高还原桌面视频处理软件的结构、尺寸感和工作流，但软件名称继续使用当前项目名称。

边界：
1. 可以参考首页模块卡片、三栏工作台、右侧功能入口、底部导出栏、二级弹窗等交互结构。
2. 不使用参考软件名称。
3. 不复制对方 Logo、品牌文案、图标素材。
4. 不修改 Rust / FFmpeg / VideoEngine。
5. 不借 UI 重构开发新视频处理能力。

原因：
后续还会加入视频效果、画中画、关键帧、字幕、TTS、BGM、API Key 等入口，必须先把主页面信息架构定稳，避免继续把参数堆在一个页面里。

## 2026-06-14：UI v3 按专业视频处理软件结构对标，但保持原创

决定：
UI v3 采用“首页模块入口 + 三栏工作台 + 底部任务区 + 二级设置窗口”的最终结构。首页用于模块分流，工作台用于实际处理，复杂参数进入二级窗口，日志和结果进入抽屉。

原因：
后续还要加入视频效果、画中画、关键帧、字幕、TTS、BGM、API Key 等入口。如果继续把所有参数堆在一个主页面，预览区、结果区和任务日志会互相挤压。

尺寸原则：
1. 工作台左栏约 340px。
2. 工作台右栏约 320px。
3. 底部任务条约 104px。
4. 二级设置窗口宽度控制在 480px 到 640px。
5. 主预览区优先保留最大空间。

边界：
1. 只对标结构、比例、层级和交互流程。
2. 不复制竞品软件名、Logo、品牌文案、图标素材。
3. 不修改 Rust / FFmpeg / VideoEngine。
4. 不开发本次任务以外的新功能。

## 2026-06-14：UI v3 采用主工作台 + 二级设置窗口

决定：
UI v3 将主页面收敛为三栏主工作台，左侧放素材和输出目录，中间放预览和核心操作，右侧只放功能入口。复杂参数统一放入二级弹窗，任务日志和导出结果统一放入浮层抽屉。

原因：
后续还会增加平滑转场、画中画、关键帧、字幕、TTS、BGM、API Key 等入口，如果继续把所有参数展开在主页面，会持续挤压预览区和核心操作区。

影响：
App.vue 不再承载全部 UI 模板，拆分为多个前端组件。业务调用仍由 App.vue 组织，视频处理仍走现有 Service 和 VideoEngine。

禁止：
本次 UI v3 只做界面结构定版，不开发新的真实视频处理功能，不修改 Rust / FFmpeg 后端。

## 2026-06-14：V0.3.1 平滑混剪先采用临时平滑片段方案

决定：
平滑混剪第一版开启后，VideoEngine 先过滤小于 1.5 秒的片段，再为每个有效片段生成带 0.2 秒画面和音频淡入淡出的临时片段，最后复用已有片段拼接、画布比例、模糊背景和导出结果链路。

原因：
现有拼接链路已经稳定支持画布比例、模糊背景、水平镜像、变速和导出结果列表。第一版优先降低改动风险，不直接重写成复杂多输入滤镜图。

影响：
开启平滑混剪时会多一次临时片段编码，导出速度会比普通拼接慢一些，但更容易保证现有导出链路稳定。

禁止：
本步骤不做画中画、关键帧动画、字幕、TTS、会员、API Key 或 AI 内容理解。

## 2026-06-11：桌面框架选择

决定：
使用 Tauri + Vue3，不使用 Electron。

原因：
Tauri更适合轻量本地桌面软件。

影响：
后续所有桌面功能围绕 Tauri Command 和前端 Vue 组件开发。

禁止：
未经确认，不允许改成 Electron。

---

## 2026-06-11：视频处理方式

决定：
所有视频处理统一通过 VideoEngine 封装 FFmpeg。

原因：
避免 FFmpeg 命令散落在不同模块，后期难维护。

禁止：
任何页面组件直接拼接 FFmpeg 命令。

---

## 2026-06-11：V0.1 范围收敛

决定：
V0.1 只做基础可运行闭环，不做会员、API Key、AI 内容理解、TTS、ASR、复杂混剪和 UI 大重构。

原因：
当前最重要的是先打通本地视频处理链路。如果过早开发高级功能，会导致基础导入、预览、导出迟迟跑不通。

影响：
后续开发必须先完成：

1. FFmpeg / FFprobe 环境检测。
2. 单视频导入。
3. 文件夹导入。
4. 视频信息读取。
5. 视频预览。
6. 选择输出目录。
7. 基础导出。
8. 任务进度和日志。

禁止：
未经确认，不允许在 V0.1 开发会员、API Key、AI 内容理解、TTS、ASR 字幕识别、复杂画中画、云端渲染、支付系统。

---

## 2026-06-11：任务队列先做基础版

决定：
V0.1 的任务队列先做单任务顺序执行，不做复杂并发、暂停恢复、失败自动重试。

原因：
视频导出属于耗时任务，必须有任务状态和日志。但 V0.1 不需要复杂调度，先保证稳定可用。

影响：
任务对象至少应该包含：

1. 任务 ID。
2. 任务类型。
3. 任务状态。
4. 输入文件。
5. 输出文件。
6. 进度。
7. 日志。
8. 错误信息。
9. 开始时间。
10. 结束时间。

禁止：
不要把任务进度写死在某个按钮或页面组件里。

---

## 2026-06-11：API Key 和会员安全原则

决定：
API Key 后续不能明文保存，会员卡密后续不能纯本地判断。

原因：
API Key 明文保存容易泄露。会员卡密如果只靠本地判断，容易被修改配置或代码绕过。

影响：
API Key 后续优先使用系统安全存储。会员卡密后续必须设计远程校验、有效期校验、设备绑定或服务端签名。

禁止：
禁止把 API Key 写入普通 JSON、日志、数据库明文字段。
禁止把会员状态写成完全离线、完全本地的最终判断。

---

## 2026-06-12：固定时长切片策略

决定：
V0.2 固定时长切片使用 VideoEngine 封装 FFmpeg，并采用重编码切片方式生成 mp4 片段。

原因：
直接复制流切片速度更快，但容易受关键帧影响，片段时长不稳定，也不利于后续拼接和混剪。当前阶段优先保证片段兼容性和后续可处理性。

影响：
切片速度会比直接复制流慢一些，但生成片段更适合作为后续随机抽取和拼接的基础素材。

禁止：
页面组件不得直接拼接 FFmpeg 切片命令。
未经确认，不在本阶段加入复杂 GPU 加速、场景识别切片或批量切片。

---

## 2026-06-12：片段拼接策略

决定：
V0.2 片段拼接使用 VideoEngine 封装 FFmpeg concat demuxer，并对拼接结果重新编码为 H.264 + AAC 的 mp4。

原因：
固定时长切片已经统一生成兼容片段，concat 列表可以按随机抽取顺序组织输入；重新编码比直接复制流更慢，但对后续不同片段来源更稳。

影响：
拼接会生成一个新的 `remix_时间戳.mp4` 文件。拼接过程中会临时生成 concat 列表文件，任务结束后删除。

注意：
FFmpeg concat 列表文件不能带 UTF-8 BOM，否则会出现 `unknown keyword '﻿file'`。项目代码使用 Rust `fs::write` 生成列表文件，避免 BOM。

禁止：
页面组件不得直接拼接 FFmpeg 拼接命令。
未经确认，不在本阶段加入批量并发拼接、字幕、TTS 或复杂滤镜。

---

## 2026-06-12：批量生成先采用顺序执行

决定：
V0.2 批量生成混剪结果先采用前端顺序调度，每条结果重新随机抽取片段，并复用 VideoEngine 的片段拼接能力生成 mp4。

原因：
当前目标是先跑通“多条混剪结果生成”闭环。并发生成会同时启动多个 FFmpeg 进程，容易占满 CPU、抢占磁盘 IO，也会让日志和错误处理变复杂。

影响：
批量生成速度不是最快，但更稳定，日志更容易看懂。后续如果需要性能优化，再设计真正的任务队列和并发限制。

禁止：
未经确认，不在本阶段加入多任务并发、暂停恢复、失败自动重试或后台队列持久化。

---

## 2026-06-12：基础镜像先作为混剪导出选项

决定：
V0.2 基础镜像先只支持水平镜像，并作为片段拼接和批量生成混剪时的导出选项。

原因：
水平镜像是短视频混剪中常用的基础变化手段，范围清晰，适合作为 V0.2 第一个视频效果能力。当前不做复杂滤镜组合，避免影响已经跑通的切片、随机抽取、拼接和批量生成链路。

影响：
前端页面只保存“是否开启水平镜像”的开关值，并通过 Service 传给 Tauri Command；真正的 FFmpeg `hflip` 滤镜仍然封装在 VideoEngine 中。

禁止：
页面组件不得直接拼接 FFmpeg 镜像命令。未经确认，不在本步骤加入变速、裁剪比例、滤镜模板、字幕、TTS 或 AI 内容理解。

---

## 2026-06-12：UI 先升级为桌面工作台框架

决定：
当前 UI 从工程测试版整理为桌面软件工作台结构，采用顶部状态栏、左侧素材/片段区、中间预览/结果区、右侧参数设置区、底部任务日志区。

原因：
项目已经完成导入、预览、导出、切片、随机抽取、拼接、批量生成和基础镜像，原来的纵向测试页面不适合继续承载变速、裁剪、字幕、TTS 等后续能力。先建立稳定的信息架构，可以减少后续功能堆叠导致的页面混乱。

影响：
本次只调整 UI 布局和样式，不重写 FFmpeg、VideoEngine、Service 或核心业务逻辑。后续新增参数应优先放入右侧参数面板，任务反馈优先进入底部任务日志区。

禁止：
未经确认，不借 UI 升级机会重写核心业务逻辑、引入大型 UI 库、开发新视频处理功能或进行品牌营销式首页改造。

---

## 2026-06-12：基础变速先限制在 0.5x 到 2.0x

决定：
V0.2 基础变速先只支持 0.5x 到 2.0x，并只作用于片段拼接导出和批量生成混剪结果。

原因：
FFmpeg 的音频 `atempo` 滤镜在 0.5 到 2.0 范围内最直接稳定。当前目标是先让用户能做常见的快放和慢放，不做复杂变速曲线、分段变速或更大倍率链式滤镜。

影响：
页面只提供一个“变速倍数”数值输入，默认 1.0x。VideoEngine 根据该参数组合视频 `setpts` 和音频 `atempo` 滤镜。基础导出、视频预览和切片本身不受变速影响。

禁止：
页面组件不得直接拼接 FFmpeg 变速命令。未经确认，不在本步骤加入裁剪比例、分段变速、字幕、TTS 或 AI 内容理解。
---

## 2026-06-12：视频比例采用画布适配，不做裁剪框

决定：
V0.2 的视频比例功能采用“目标比例画布 + 原视频完整保留 + 背景补齐”的方式实现。

支持范围：

1. 原画。
2. 9:16 竖屏画布。
3. 1:1 方屏画布。
4. 16:9 横屏画布。
5. 黑边背景。
6. 模糊背景。

原因：
用户的核心需求不是自由裁剪，也不是拖动裁剪框，而是把不同横竖比例的视频放进目标比例画布中，保证原视频内容完整可见。短视频平台常见的横转竖、竖转横场景更适合用黑边或模糊背景补齐。

影响：

1. 预览区必须尽量呈现最终导出效果。
2. 前景视频播放器必须保留原生播放、暂停、时长条等控件。
3. 基础导出、片段拼接导出、批量生成必须使用同一套画布参数。
4. FFmpeg 的 scale、pad、blur、overlay 等滤镜必须继续封装在 VideoEngine 内。

禁止：

1. 本阶段不做自由裁剪框。
2. 本阶段不做拖拽调整主体位置。
3. 本阶段不做 AI 智能构图。
4. 页面组件不得直接拼接 FFmpeg 命令。
