# DECISIONS.md

## 2026-07-17：智能切片采用本地场景检测与时长保护，多帧结果复用现有缓存

决定：
智能切片由Rust VideoEngine调用FFmpeg场景变化检测，前端只选择灵敏度、最短片段和最长片段。切点先过滤过短镜头，再为过长连续镜头补切。每个切片抽取开头、中间、结尾最多3帧供AI综合理解；SQLite第一版仍只保存代表缩略图和最终画面描述，不修改数据库结构。

原因：
场景边界属于本地确定性视频处理，不需要每次调用AI，能降低费用和等待；单纯按转场切割容易产生闪屏碎片或超长镜头，因此必须同时设置时长保护。多帧比单张首帧更能反映动作变化，但把全部分析帧写入数据库会扩大迁移范围，第一版保存最终描述已经能实现主要缓存价值。

技术边界：
1. 三档灵敏度映射为固定场景变化阈值，第一版不开放专业数值输入。
2. 最短片段范围0.5～10秒，最长片段范围2～60秒，最长至少比最短多0.5秒。
3. 无明显转场时优先按最长时长补切；短视频满足两个最短片段时至少二等分。
4. FFmpeg分段使用关键帧切点并允许0.05秒时间容差，避免不同版本因时间基误差漏切。
5. AI每次最多并行理解2个片段，每个片段最多发送3帧，控制请求大小和成本。
6. 第一版不做语义事件边界、人物跟踪、光流模型或本地视觉大模型。

## 2026-07-17：素材库使用版本化SQLite索引并保留最近项目JSON快照

决定：
素材库第一版使用Rust和内置SQLite保存原视频、切片、分类、缩略图路径和AI画面描述。数据库只保存轻量索引，不复制视频、切片或成片。现有最近项目JSON快照继续负责恢复最后一次完整工作状态。SQLite启动时只检查并展示可载入数量，不再自动应用到当前项目；用户点击“载入素材库”后才恢复。

原因：
最近项目快照适合恢复一次中断，但不适合长期积累素材和AI缓存。首轮实现虽然设置了快照优先级，但用户放弃快照后SQLite立刻载入同一批内容，使“放弃”失去真实效果。改为用户主动载入后，空白项目、项目恢复和长期素材库三个概念才不会互相覆盖。

技术边界：
1. SQLite通过Rust `rusqlite`访问，前端只传结构化数据，不能直接执行SQL。
2. 数据库使用 `PRAGMA user_version` 管理结构版本，保存使用单个事务整体替换当前素材库状态。
3. 文件失效只标记索引，不自动删除用户记录；原视频允许重新定位，切片需要重新生成。
4. API Key、TTS Key、密码和会员凭证禁止写入素材库。
5. 第一版不做云同步、多项目素材空间、文件夹监控、内容哈希和复杂搜索。
6. “新建空白项目”不能自动载入SQLite；素材库必须通过独立可见按钮由用户主动载入。

## 2026-07-17：完整输出设置采用质量档位和实际GPU编码测试

决定：
输出格式第一版固定MP4/H.264/AAC。用户选择分辨率、帧率、三档质量和编码方式；质量档位由Rust转换为目标码率。GPU检测必须使用FFmpeg完成极小实际编码测试，自动模式只选择实际成功的编码器。用户输出设置只作用于最终成片，中间临时片段继续使用CPU。

原因：
直接展示CRF、GOP、VBV等专业参数不适合零基础用户；只读取 `ffmpeg -encoders` 会把“编译时存在但显卡或驱动不可用”的编码器误判为可用。临时片段使用GPU会扩大字幕、配音、平滑混剪等链路的兼容风险。

技术边界：
1. 第一版不支持MOV、MKV、WebM、H.265、AV1或任意自定义参数。
2. 720p标准目标码率约4500kbps，1080p/跟随画布标准约8000kbps；省空间和高清档位分别降低或提高。
3. 固定画布按横屏、竖屏和方屏映射标准尺寸；原画按源方向保持比例缩放并保证偶数尺寸。
4. NVIDIA使用 `h264_nvenc`，Intel使用 `h264_qsv`，AMD使用 `h264_amf`，CPU使用 `libx264`。
5. 不可用编码器必须禁选；恢复旧项目或硬件状态变化时安全回退到自动模式。
6. 所有FFmpeg参数继续由VideoEngine生成，前端只传结构化设置。

## 2026-07-17：任务取消和真实进度由Rust统一管理

决定：
耗时视频任务使用前端生成的任务编号注册到Rust任务中心。VideoEngine调用FFmpeg时统一加入 `-progress pipe:1`，按真实输出时间更新当前阶段的百分比；取消请求由Rust共享状态通知当前FFmpeg子进程并终止。前端只负责展示、轮询和组织多条任务，不直接管理系统进程。

原因：
只在Vue里把按钮恢复为可点击并不能停止FFmpeg，会继续占用CPU并留下半成品；写死进度或只按完成文件数计算也无法反映单条长视频的真实处理状态。进程、进度和清理必须与VideoEngine处于同一边界。

技术边界：
1. 第一版只支持取消，不做暂停、续跑、并发队列或重启后恢复运行中任务。
2. 多阶段混剪把每个真实FFmpeg阶段映射到总进度区间，不使用固定自动增长动画。
3. AI/TTS网络请求不强杀底层HTTP连接，取消后在当前请求返回时停止后续步骤。
4. 批量失败重试保存原失败条目的输入，只重做失败条目。
5. 临时文件统一进入系统临时目录下的 `local-video-remix-workbench`；生命周期结束立即删除，启动清理只处理超过24小时的遗留目录。
6. 输出半成品和取消中的切片目录必须删除，用户原素材和已经成功生成的结果禁止删除。

## 2026-07-17：项目默认使用两份短入口恢复上下文

决定：
每次开发默认只读取 `CODEX_START_HERE.md`、`CURRENT_TASK.md`、Git状态和本次相关代码。大型状态、决策、路线和架构文档只通过标题与关键词读取相关章节。

原因：
历史文档已经超过百KB，每次全文读取会造成明显等待和Token浪费，也会让真正的当前任务被历史流水账淹没。

边界：
短入口必须就地覆盖更新，不能继续追加成新历史日志；功能完成时仍需同步正式状态、任务和变更文档。

## 2026-07-17：项目恢复第一版采用单个版本化JSON快照，不提前引入SQLite项目表

决定：
项目恢复第一版只保存“最近一个项目”，由Rust把版本化JSON快照写入当前Windows用户应用数据目录。前端使用约1.2秒延迟合并连续变化，启动时必须先让用户选择恢复或放弃。Rust加载时检查记录中的文件和目录路径，前端按缺失项安全降级。

原因：
当前项目尚未形成多项目列表、搜索、排序和长期素材索引，直接引入SQLite项目表会同时扩大数据库迁移、数据关系和界面范围。单个快照已经能解决“关闭软件后全部重做”的核心问题，并能先验证真正需要长期保存的字段。等多项目管理或素材库持久化开始时，再把已经稳定的快照结构迁入SQLite。

技术边界：
1. 第一版只保存最近一个项目，不做项目列表、另存为、云同步或多人协作。
2. 快照保存素材路径和状态，不复制原始视频、切片、音频或成片。
3. API Key、密码、凭证和secret字段禁止进入项目快照，继续使用Windows加密配置。
4. 快照限制2MB、10000条路径，并带版本号；损坏或版本不兼容时不得自动套用。
5. 启动恢复决策完成前暂停自动保存，禁止空白工作台覆盖旧快照。
6. 缺失素材自动跳过；分镜引用失效时清空旧分镜；失效BGM和画中画自动关闭；输出目录失效时要求重选。
7. 当前不保存正在执行的FFmpeg或云端请求，任务中断后恢复到最近稳定编辑状态，而不是从命令中间续跑。

## 2026-07-17：API密钥使用Windows当前用户加密保存

决定：
AI与TTS密钥由Rust接收后，通过Windows数据保护机制加密整个配置文件，保存在当前用户应用数据目录。运行时优先读取本机保存配置，环境变量作为开发备用。前端只接收配置状态和非敏感参数，不允许后端回传已保存的完整密钥。

原因：
普通用户每次打开PowerShell填写环境变量既麻烦又容易出错；普通JSON、localStorage或SQLite明文字段不适合保存API Key。Windows数据保护机制不需要用户再记一个主密码，同时能把密文绑定到当前Windows用户。

技术边界：
1. 不使用localStorage、普通JSON、明文SQLite或日志保存Key。
2. 密钥输入框留空表示保留旧Key，删除必须使用独立操作并二次确认。
3. 配置状态只能返回是否配置、来源和非敏感连接参数。
4. AI调用和TTS调用必须复用同一个配置解析模块。
5. 环境变量仍可用于开发和故障排查，但本机保存值优先。
6. 当前安全存储针对Windows桌面版；未来支持其他系统时必须接入对应系统安全存储，不能降级为明文。

## 2026-07-17：多条差异视频采用确定性备选轮换和顺序导出

决定：
第一条视频保留用户当前确认的主分镜；后续视频只轮换每句已有备选画面。文案和分镜顺序不变，每条视频内部不得重复片段，完全相同的组合不得重复输出。备选不足时只返回实际唯一版本。所有视频顺序导出，单条失败后继续。TTS逐句音频只生成一次并由所有版本复用。

原因：
随机打乱分镜会破坏用户已经确认的叙事结构，也可能生成内容相同但文件名不同的伪差异视频。多个FFmpeg任务并发会明显增加本地电脑资源压力；每个版本重复调用TTS还会增加等待时间和费用。

技术边界：
1. 数量范围固定为1～10，默认3。
2. Rust负责生成和校验差异组合，Vue只负责组织任务、显示进度和结果。
3. 继续复用现有VideoEngine，不在前端拼接FFmpeg命令。
4. 无法组成请求数量时不得用重复视频补足。
5. 单条失败必须保留错误原因并继续后续版本。
6. 多版本共享同一句文案的TTS音频、字幕文字和时序。

## 2026-07-16：素材封面完整性由后端产物和前端共同保证

决定：
原始素材列表使用独立的 `contain` 缩略图模式。Rust直接把源画面完整缩放到320×240画布并补深色留白，前端继续使用完整缩放显示。AI片段分析缩略图继续保持源比例。

原因：
只修改前端 `object-fit` 容易受到旧窗口、缓存或后续样式覆盖影响；素材库的核心目标是辨认完整画面。后端产物本身不裁切后，即使界面意外使用铺满规则，也只会裁掉少量留白，不会裁掉人物主体。

边界：
1. 不修改原视频文件。
2. 不给AI图片理解输入增加固定横屏留黑，避免缩小有效画面。
3. 不引入新图片依赖，继续通过VideoEngine与FFmpeg生成JPEG。

## 2026-07-16：切片前必须验证输出目录并在主流程展示错误

决定：
切片操作开始前检查输出目录是否真实存在。未选择或目录失效时自动要求用户重新选择；失败信息必须显示在核心流程区。

原因：
桌面软件可能长期保持旧路径，目录随后被移动或删除。仅检查字符串不为空会让所有视频立即失败，而隐藏错误会让用户误判按钮失效。

边界：
目录检查由Rust执行，前端只组织选择和提示；不修改切片编码、时长、顺序或VideoEngine边界。

## 2026-07-15：自动字幕第一阶段采用分镜内自然短句ASS烧录

决定：
自动字幕第一阶段只服务AI逐句配音视频。每个AI分镜使用自己的文案，优先按标点和自然停顿拆成短字幕；没有标点的长句按可读长度继续拆分。短字幕在该句配音时长内依次显示。默认开启、底部居中、标准字号、白字和半透明黑底。

原因：
当前每个分镜已经拥有明确文案和独立TTS音频，不需要重新调用ASR识别。首轮人工验收证明整段字幕会在短视频画面中显得拥挤；分镜内再按停顿拆短，并按有效字符数分配该句配音时长，可以在不增加网络请求和复杂时间轴的情况下提升可读性。

技术边界：
1. ASS文件生成、中文转义、换行、样式和FFmpeg滤镜路径统一放在Rust VideoEngine。
2. 前端只传递开关、位置、字号和每个分镜的字幕文字，不拼FFmpeg命令。
3. 字幕先烧录到临时配音分镜，再复用现有混剪导出链路。
4. 生成前检查FFmpeg是否包含 `ass` 滤镜，不能静默忽略字幕。
5. 临时ASS文件不进入输出结果列表、日志或Git。
6. 每条短字幕尽量保持单行，显示时间在总时长允许时不少于0.7秒；第一版不依赖尚未验证单位的词级时间戳。

暂不实现：
原视频语音ASR、逐字变色、卡拉OK、字幕动画、自由字体颜色、字幕模板市场和复杂时间轴。

## 2026-07-15：原视频声音采用可选保留与低音量混合

决定：
AI逐句配音视频继续默认关闭原片段声音，但在AI配音设置中增加“保留原视频声音”开关。开关默认关闭；开启后原声音量默认15%，允许在0%～100%之间调整。

原因：
部分素材的环境声和背景音乐有保留价值，低音量混合可以减少用户重新准备BGM的步骤。默认关闭可以避免原视频人声与AI配音互相打架，15%默认值可以让AI配音保持主导。

技术边界：
1. 当前保留的是完整原声音轨，可能同时包含原人声、环境声和背景音乐。
2. 不做人声分离，不宣称能够只提取原视频BGM。
3. 无音轨片段必须自动退回纯AI配音，不能因为用户打开开关而失败。
4. 原声与AI配音在Rust VideoEngine中混合，前端只传递开关和音量。
5. 现有外部BGM继续可用；三层声音同时开启时由用户自行调整或关闭其中一层。

## 2026-07-15：TTS第二阶段采用原声静音与配音时长驱动画面

决定：
TTS第二阶段按AI逐句分镜依次生成语音，并以每句配音时长作为对应画面的最终时长。原片段声音默认静音并允许用户主动开启低音量保留；配音比画面长时冻结尾帧，配音比画面短时裁短画面。配音视频固定使用1.0x速度和单一音色，本阶段不生成字幕。

原因：
第一版优先保证文案、配音和画面严格对应。保留原声会与新配音互相干扰；循环画面容易产生明显跳动；自动变速会改变画面节奏并可能造成不自然。冻结尾帧与裁短画面是最容易理解和稳定验收的规则。

技术边界：
1. 前端只组织分镜、逐句TTS请求和进度，不拼接FFmpeg命令。
2. 画面裁剪、尾帧冻结、原声静音、配音轨道和最终编码统一放在Rust VideoEngine。
3. 临时MP3和临时视频不得进入Git或用户输出结果列表，任务结束后必须清理。
4. 最终继续复用现有画布、视频效果、画中画、平滑混剪和BGM链路。
5. 非1.0x速度必须明确拒绝，不能静默改变配音速度。

暂不实现：
字幕、声音克隆、人声与背景音乐分离、批量多音色、复杂时间轴、图片转视频和去水印。

## 2026-07-14：TTS第一阶段使用火山语音独立API Key并只生成MP3

决定：
TTS第一阶段使用火山引擎单向流式语音合成HTTP接口 `https://openspeech.bytedance.com/api/v3/tts/unidirectional`。默认资源为 `seed-tts-2.0`，输出MP3和24000Hz采样率。第一阶段只完成“当前混剪文案 → 本地MP3 → 软件内试听”，不把配音自动加入视频。

原因：
火山语音的 `X-Api-Key` 与方舟大模型 `AI_API_KEY` 属于不同产品凭证。先独立验证语音请求、音色、音频保存和播放，可以避免同时排查TTS网络请求和FFmpeg音视频同步。

安全边界：
1. `TTS_API_KEY` 只在Rust环境变量中读取，不传给Vue前端。
2. `TTS_RESOURCE_ID` 和 `TTS_SPEAKER`允许环境变量覆盖，前端只接收非敏感配置状态。
3. 日志不记录Key、完整请求体、完整文案、服务原始响应或音频Base64。
4. HTTP错误只提取服务返回的安全错误说明，不回显完整响应体。

后续边界：
1. 火山返回的字词时间信息保留在结构化结果中，为字幕和逐句同步预留。
2. 配音混入视频、原声音量、镜头时长匹配和BGM混音必须在后续阶段通过VideoEngine实现。
3. 本阶段不开发声音克隆、字幕、批量多音色或完整时间轴。

## 2026-07-13：大文件采用按功能渐进式拆分

决定：
不重写 `App.vue`、`styles.css` 和 `mix.rs`。前端先提取共享基础层，再依次迁出 AI 混剪、素材切片、普通混剪和设置模块。未来 TTS、图片转视频、去水印和视频工具从一开始进入 `src/features/<feature-name>`。

原因：
现有主链路已经人工验收，推倒重写会同时破坏大量稳定功能。渐进式迁移可以让每一步都能构建、人工回归和独立提交。

边界：
1. `App.vue` 只保留页面组合和跨功能编排。
2. 功能状态和业务流程放 composable，外部调用放 service。
3. 每阶段不改变界面、默认参数和视频处理结果。
4. 人工回归通过后才进入下一阶段。

## 2026-07-13：安装包优先支持内置 FFmpeg，但第三方二进制不提交 Git

决定：
VideoEngine 统一通过工具路径解析器寻找 FFmpeg / FFprobe。顺序为显式环境变量、程序内置资源目录、程序同级目录、开发资源目录和系统 PATH。FFmpeg 大型二进制不直接提交 Git，待确认合法来源和许可证后由发布流程准备。

原因：
开发电脑可以依赖 PATH，但普通测试用户的干净电脑通常没有 FFmpeg。安装包必须能够内置工具，同时项目仓库不能直接混入未经确认来源、许可证和体积的大型第三方文件。

边界：
1. 不下载或分发来源不明的 FFmpeg 构建。
2. 内置前必须保留许可证和来源说明。
3. 环境变量和系统 PATH 继续作为开发与排障后备方案。
4. 没有内置 FFmpeg 时，不得宣称安装包可在干净电脑开箱即用。

## 2026-07-13：混剪导出参数统一使用设置对象

决定：
前端和 Rust 的混剪导出入口使用一个 `RemixSettings` / `RemixExportSettings` 对象传递画布、变速、效果、画中画、BGM 和字幕开关，不再使用 10 个独立函数参数。

原因：
独立参数数量已经触发 Rust 严格静态检查警告。继续增加字幕、TTS 或水印会提高漏传、错传和前后端不一致风险。

边界：
1. 本次只收拢参数，不改变 VideoEngine 的 FFmpeg 行为。
2. 当前字幕仍未实现，若错误开启，Rust 必须明确拒绝。
3. 后续新增成片设置优先扩展设置对象，不继续增加顶层参数。

## 2026-07-13：外部用户安装包不能内置开发者公共 AI Key

决定：
当前环境变量方案只允许内部技术测试。发普通外部用户前，必须选择“用户自己的 Key + 系统安全存储”或“自建服务端代理”之一，禁止把开发者公共 Key 写入前端、安装包或普通配置文件。

原因：
桌面安装包中的公共 Key 可以被提取和滥用，无法安全控制额度、用户身份和成本。

## 2026-07-14：纯文字分镜关闭深度思考

决定：
火山方舟的图片理解和纯文字分镜请求统一发送 `thinking.type = disabled`。纯文字分镜超时设为 45 秒，最大输出设为 800 tokens；图片理解超时继续保持 180 秒。

原因：
真实验收已经证明图片画面描述可以生成，最终超时发生在纯文字分镜规划阶段。当前模型 `doubao-seed-2-1-pro` 默认开启深度思考，但该阶段只需把文案与已有画面描述进行匹配并返回严格 JSON，深度推理会增加等待时间，不会带来与成本相称的价值。

边界：
1. 不通过无限增加超时时间掩盖模型选择或请求参数问题。
2. 关闭思考只影响 AI 请求速度，不改变分镜 JSON 校验规则。
3. API Key、请求体、图片 Base64 和本地路径继续不进入前端或日志。
4. 如果关闭思考后仍超时，再考虑为纯文字规划切换同平台的 Flash 模型或增加本地匹配兜底。

## 2026-07-14：图片理解使用单图请求，分镜规划使用独立超时

决定：
AI 图片理解每次只发送 1 张片段预览图，前端最多同时执行 2 个单图请求。图片理解单请求超时为 180 秒，纯文字分镜规划超时为 120 秒。

原因：
真实验收中 13 个片段按双图批次请求仍出现 `operation timed out`。已有在线测试表明单张图片约需 25 秒；双图理解、模型处理和并发排队叠加后更容易超过统一的 120 秒上限。单图请求能降低单次负担，同时保留 2 路并发可以控制总等待时间。

边界：
1. 不把所有图片放进一次请求。
2. 不无限增加整体超时掩盖问题。
3. 已成功返回的画面描述继续在当前切片会话缓存。
4. 日志只记录阶段、片段编号、批次和结果，不记录 API Key、Base64、完整请求体或本地图片路径。
5. 如果单图仍超时，再根据明确的阶段日志决定是否将并发降为 1 或调整模型设置。

## 2026-07-12：全部已导入视频统一进入一个切片素材池

决定：
第一版点击切片后依次处理所有已导入视频，而不是只处理当前选中的预览视频。所有成功片段按照素材导入顺序合并，再统一生成编号、预览图、时长和 AI 画面描述。

原因：
核心使用方式是导入多个素材视频，让 AI 在全部素材中为文案寻找合适画面。只切当前视频虽然界面允许多选导入，但实际无法形成跨视频混剪素材池。

边界：
1. 当前选中视频只决定中间预览，不决定唯一切片来源。
2. 单个视频失败时记录日志并继续处理其他视频。
3. 成功片段总数不足 2 个时停止 AI 混剪。
4. 本次不增加复杂素材勾选器，默认使用全部已导入视频。


## 2026-07-12：只使用火山引擎，并把图片理解与分镜规划拆成两阶段

决定：
第一版只接入火山引擎，不保留 DeepSeek 或其他模型供应商接口。AI 先分批理解片段预览图，再使用片段文字描述和用户文案生成分镜。

原因：
真实验收中单张图片在线理解约需 25 秒，一次发送 7 张图片超过 120 秒整体超时。片段描述可以在同一次切片会话中复用，而文案可能反复修改，拆分后能降低重复图片请求、等待时间和费用。

边界：
1. 单次画面理解最多发送 2 张预览图。
2. 前端最多并行执行 2 个画面理解批次。
3. 画面描述只在当前切片会话缓存，重新切片后重新识别。
4. 分镜规划阶段不再发送图片，只发送文案、片段编号、时长和画面描述。
5. 不增加平台选择，不接入 DeepSeek。

## 2026-07-12：第一版文案只用于画面匹配，成片保留原片段声音

决定：
在 TTS 尚未开发时，用户输入的成片文案不会自动变成语音。VideoEngine 继续保留被选中原片段的音频，并允许叠加现有 BGM。

原因：
文字变成新配音必须依赖 TTS。当前阶段的核心目标是先验证导入、切片、画面理解、文案匹配和视频生成，不能让用户误以为输入文字会自动朗读。

边界：
1. 不根据输入文案生成语音。
2. 不生成字幕时间轴。
3. 原片段有人声或环境声时继续随片段进入成片。
4. 后续 TTS 接入后再根据逐句配音时长调整画面和字幕。


## 2026-07-11：AI 混剪升级为逐句分镜和相关片段子集

决定：
第一阶段不再要求 AI 使用全部切片，而是把文案拆成分镜句子，为每个句子选择一个不重复的主片段，并提供最多 3 个备选片段。

原因：
竞品核心价值是“文案与画面匹配”，强制使用全部素材会把无关画面带入成片。逐句分镜能让用户直接看懂每句话对应哪个画面，也为后续 TTS、字幕和镜头时长同步建立稳定数据结构。

边界：
1. 主片段必须来自本次输入素材，且不同分镜不得重复。
2. 备选片段必须来自本次输入素材，每个分镜最多 3 个。
3. 第一阶段只决定画面顺序，不改变片段时长。
4. 导出继续复用 VideoEngine，不在前端拼接 FFmpeg。
5. 本阶段不同时开发 TTS、字幕、ASR 或多版本生成。

## 2026-07-11：方舟 HTTPS 请求使用内置可信根证书

决定：
Rust AI 客户端使用 Mozilla 内置可信根证书进行 HTTPS 证书签名和域名校验，不再依赖 Windows 在线证书吊销查询。

原因：
当前 Windows 环境访问方舟时无法检查证书吊销状态，系统返回 `CRYPT_E_NO_REVOCATION_CHECK`，导致请求在收到 HTTP 响应前失败。方舟域名、DNS 和接口本身均已验证正常。

安全边界：
1. 不关闭证书验证。
2. 不允许无效证书或域名不匹配。
3. 只绕开当前不可用的 Windows 在线吊销查询。
4. API Key 读取和日志安全规则保持不变。

## 2026-07-11：AI 文案智能混剪第一版使用方舟多模态排序并复用现有导出链路

决定：
AI 第一版只负责根据用户文案、片段编号、真实时长和预览图返回完整片段顺序。Rust 后端调用火山引擎方舟 OpenAI 兼容的 `/chat/completions` 接口，前端只展示和调整排序，最终视频继续由现有 `concatSelectedSegments` 和 `VideoEngine` 生成。

原因：
现有项目已经稳定具备切片、预览图、画布、平滑混剪、视频效果、画中画、BGM 和导出结果管理。让 AI 只做排序可以用最小改动验证内容理解价值，也能避免把完整视频上传、时间轴或 FFmpeg 逻辑复制到新模块。

接口约定：
1. 环境变量为 `AI_API_KEY`、`AI_BASE_URL`、`AI_MODEL`。
2. `AI_BASE_URL` 可以是方舟 `/api/v3` 基地址，也可以是完整 `/chat/completions` 地址。
3. 远端请求不包含本地文件路径或完整视频。
4. AI 必须返回 `{"orderedSegmentIds":[...]}`，并覆盖所有片段且不重复。

安全边界：
1. API Key 只在 Rust 中读取，不传给 Vue 前端。
2. API Key、图片 Base64 和完整请求体不写日志。
3. 非法 AI 结果不覆盖当前排序。

范围限制：
不做字幕烧录、TTS、会员、复杂时间轴、完整视频上传、AI 自动发布或无关重构。

## 2026-07-11：已有字幕接口草稿只做禁用兼容接线

决定：
保留工作区已有 `SubtitleSettings` 类型和 `concatSelectedSegments` 参数，在现有调用点统一传入 `enabled: false` 的默认配置。本次不增加字幕 UI，也不修改 VideoEngine 生成字幕。

原因：
已有未提交草稿把 Service 函数从 10 个参数改为 11 个参数，导致项目基线构建失败。仅补禁用接线可以恢复类型检查，同时不扩大 AI MVP 范围。

## 2026-06-22：V0.4.2 BGM 淡入淡出只做基础渐变，不做节拍卡点

决定：
V0.4.2 在本地 BGM 第一版基础上补充淡入和淡出秒数。前端只收集秒数并做范围检查，真正的音频渐变继续放在 Rust `VideoEngine` 里统一交给 FFmpeg 处理。

原因：
BGM 如果直接从第一秒满音量进入、最后一秒突然停止，成片听感会比较生硬。淡入淡出是最小但很实用的声音优化，比节拍卡点、歌词识别、在线音乐库和多轨时间线更容易稳定验收。

范围：
1. 支持 BGM 淡入秒数。
2. 支持 BGM 淡出秒数。
3. 默认淡入 / 淡出都是 0.5 秒。
4. 允许范围是 0 到 10 秒。
5. 设置为 0 时不做对应渐变。
6. 淡出起点根据最终导出视频时长计算。

限制：
1. 不做网络音乐素材。
2. 不做在线音乐库。
3. 不做节拍自动卡点。
4. 不做复杂多轨时间线。
5. 不做 AI 自动配乐。
6. 不让前端页面组件直接拼接 FFmpeg 命令。

## 2026-06-22：V0.4 先做本地 BGM 第一版，不直接进入字幕、TTS 或 AI

决定：
V0.4 第一优先级确定为本地 BGM 第一版。先支持用户选择本地音频文件，设置是否启用 BGM，调节原视频音量和 BGM 音量，并让 BGM 自动适配导出视频长度。第一版优先接入拼接抽中片段、分类混剪和批量生成。

原因：
V0.3 已经完成并验收了混剪画面主链路，包括画布、平滑混剪、基础效果、画中画、封面帧和分类混剪。下一步最自然的增强是补齐成片声音体验。BGM 范围比字幕、TTS、AI 自动分类、在线音乐库和多轨时间线更清晰，也更容易验收。

范围：
1. 支持选择本地音频文件作为 BGM。
2. 支持开启 / 关闭 BGM。
3. 支持原视频音量和 BGM 音量。
4. BGM 比视频短时自动适配视频长度。
5. BGM 比视频长时按导出视频长度截断。
6. 先接入拼接抽中片段、分类混剪和批量生成。
7. 任务日志要能说明 BGM 是否启用。

限制：
1. 不做 AI 自动配乐。
2. 不做在线音乐库。
3. 不做歌词识别。
4. 不做节拍自动卡点。
5. 不做复杂多轨时间线。
6. 不做字幕识别。
7. 不做 TTS 配音。
8. 不做 API Key 或会员卡密系统。
9. 不让前端页面组件直接拼接 FFmpeg 命令。

技术边界：
所有 BGM 混音、循环、截断和音量处理必须继续通过 Rust `VideoEngine` 统一封装。前端只负责收集参数，Service 层只负责传递结构化参数。

## 2026-06-21：默认采用新手/高级双模式，不把全部功能摊在主界面

决定：
工作台默认进入新手模式，只展示“导入素材 -> 预览视频 -> 开始处理 -> 查看结果”的主流程。高级功能通过“进入高级”和“高级设置”进入二级入口，保留现有能力但不在主界面全部展开。

原因：
本项目面向零基础用户，主界面如果同时展示切片、随机抽取、分类混剪、批量生成、画布、效果、画中画、封面、字幕、音频、BGM、TTS、水印、日志和结果，会让用户第一眼不知道该点哪里。参考视频和截图的可学习点是“左侧素材树、中间大预览、右侧少量入口、底部任务状态”，不是复制品牌资源或视觉素材。

范围：
1. 左侧保留清爽素材树和导入入口。
2. 中间优先保留最大视频预览区。
3. 右侧新手模式只保留引擎状态、模式切换、流程和结果入口。
4. 高级模式继续保留混剪、画布、效果、画中画、封面、字幕、音频、BGM、TTS、水印等入口。
5. 底部新手模式只保留输出目录、处理状态和开始处理。

边界：
1. 不复制竞品软件名称、Logo、图标素材和文案。
2. 不修改 Rust / FFmpeg / VideoEngine。
3. 不新增真实字幕、TTS、AI 自动分类或会员能力。
4. 不让页面组件直接拼接 FFmpeg 命令。

## 2026-06-21：V0.3.5 分类混剪先做手动分类，不做 AI 自动分类

决定：
分类混剪第一版只做“用户手动给片段分类 + 按固定分类顺序抽取 + 复用现有拼接导出”。分类抽取规则放在前端 Service 层，最终导出继续调用现有 `concatSelectedSegments`，由 Rust VideoEngine 统一处理 FFmpeg。

原因：
当前目标是先验证分类混剪的基础工作流，让用户能按短视频结构组织片段。AI 自动分类、字幕识别、TTS、API Key 和多轨时间轴都会明显扩大范围，暂不进入 V0.3.5。

范围：
1. 支持开头钩子、产品展示、使用过程、细节特写、效果展示、结尾引导 6 个固定分类。
2. 每个分类默认随机抽取 1 个片段。
3. 空分类自动跳过。
4. 至少 2 个分类有片段时才导出。
5. 导出后进入导出结果列表。
6. 任务日志显示每个分类抽中的片段。

限制：
1. 不做 AI 自动分类。
2. 不做字幕识别。
3. 不做 TTS。
4. 不做 API Key 系统。
5. 不做会员卡密系统。
6. 不做多轨时间轴。
7. 不让页面组件直接拼接 FFmpeg 命令。

## 2026-06-14：V0.3.4 关键帧能力先做静态抽帧，不做动画时间轴

决定：关键帧 / 封面帧 / 片段预览第一版只做静态抽帧能力。视频封面帧、片段预览图都通过后端 VideoEngine 调用 FFmpeg 生成 jpg，不在前端拼接 FFmpeg 命令。

原因：当前阶段目标是让素材预览、片段管理和封面选择先可用，为后续分类混剪和内容管理打基础。专业剪辑软件那种关键帧动画、曲线和时间轴会明显扩大范围，暂不进入 V0.3.4。

范围：
1. 导入视频后生成素材封面帧。
2. 切片后生成片段预览图。
3. 支持按秒数重新抽取某一帧作为当前封面。
4. 缩略图失败不阻塞切片、混剪和导出主链路。

限制：
1. 不做关键帧动画。
2. 不做复杂时间轴。
3. 不做 AI 画面分析。
4. 不做字幕、TTS、会员、API Key。

## 2026-06-14：V0.3.3 画中画先作为混剪导出叠加层

决定：
画中画第一版先接入拼接抽中片段和批量生成混剪，不开发多轨时间轴，不做拖拽定位，不做动画。

原因：
当前稳定链路是混剪导出链路。画中画先作为最终画面的第二路叠加输入，可以复用已有画布比例、模糊背景、平滑混剪、视频效果和导出结果列表。

范围：
1. 主视频使用当前混剪导出的主画面。
2. 叠加素材支持视频或图片。
3. 支持位置、大小比例、透明度、边距。
4. 后端通过 VideoEngine 封装 FFmpeg overlay。

限制：
1. 不做复杂多轨时间轴。
2. 不做多个画中画。
3. 不做拖拽定位。
4. 不做画中画动画。
5. 不让前端直接拼接 FFmpeg 命令。

## 2026-06-14：V0.3.2 基础视频效果继续挂在混剪导出链路

决定：
基础视频效果第一版先作为拼接抽中片段和批量生成混剪的导出参数，不单独开发复杂特效库，也不新建独立多轨时间轴。

原因：
当前稳定链路是切片、随机抽取、拼接、批量生成、画布比例、模糊背景、平滑混剪和导出结果列表。基础效果先复用这条链路，风险最小，也方便用户马上验收。

范围：
1. 水平镜像。
2. 垂直镜像。
3. 90° / 180° 旋转。
4. 视频变速。
5. 亮度、对比度、饱和度。
6. 轻微缩放。

限制：
1. 不做复杂特效库。
2. 不做 AI 效果。
3. 不做多轨时间轴。
4. 不让前端直接拼接 FFmpeg 命令。

## 2026-06-14：UI v3 高还原只还原结构，不复制品牌资产

决定：
本次 UI v3 按用户提供的参考图高还原桌面视频处理软件的结构、尺寸感和工作流，但软件名称继续使用当前项目名称。

边界：
1. 可以参考首页模块卡片、三栏工作台、右侧功能入口、底部导出栏、二级弹窗等交互结构。
2. 不使用参考软件名称。
3. 不复制对方 Logo、品牌文案、图标素材。
4. 不修改 Rust / FFmpeg / VideoEngine。
5. 不借 UI 重构开发新视频处理能力。

原因：
后续还会加入视频效果、画中画、关键帧、字幕、TTS、BGM、API Key 等入口，必须先把主页面信息架构定稳，避免继续把参数堆在一个页面里。

## 2026-06-14：UI v3 按专业视频处理软件结构对标，但保持原创

决定：
UI v3 采用“首页模块入口 + 三栏工作台 + 底部任务区 + 二级设置窗口”的最终结构。首页用于模块分流，工作台用于实际处理，复杂参数进入二级窗口，日志和结果进入抽屉。

原因：
后续还要加入视频效果、画中画、关键帧、字幕、TTS、BGM、API Key 等入口。如果继续把所有参数堆在一个主页面，预览区、结果区和任务日志会互相挤压。

尺寸原则：
1. 工作台左栏约 340px。
2. 工作台右栏约 320px。
3. 底部任务条约 104px。
4. 二级设置窗口宽度控制在 480px 到 640px。
5. 主预览区优先保留最大空间。

边界：
1. 只对标结构、比例、层级和交互流程。
2. 不复制竞品软件名、Logo、品牌文案、图标素材。
3. 不修改 Rust / FFmpeg / VideoEngine。
4. 不开发本次任务以外的新功能。

## 2026-06-14：UI v3 采用主工作台 + 二级设置窗口

决定：
UI v3 将主页面收敛为三栏主工作台，左侧放素材和输出目录，中间放预览和核心操作，右侧只放功能入口。复杂参数统一放入二级弹窗，任务日志和导出结果统一放入浮层抽屉。

原因：
后续还会增加平滑转场、画中画、关键帧、字幕、TTS、BGM、API Key 等入口，如果继续把所有参数展开在主页面，会持续挤压预览区和核心操作区。

影响：
App.vue 不再承载全部 UI 模板，拆分为多个前端组件。业务调用仍由 App.vue 组织，视频处理仍走现有 Service 和 VideoEngine。

禁止：
本次 UI v3 只做界面结构定版，不开发新的真实视频处理功能，不修改 Rust / FFmpeg 后端。

## 2026-06-14：V0.3.1 平滑混剪先采用临时平滑片段方案

决定：
平滑混剪第一版开启后，VideoEngine 先过滤小于 1.5 秒的片段，再为每个有效片段生成带 0.2 秒画面和音频淡入淡出的临时片段，最后复用已有片段拼接、画布比例、模糊背景和导出结果链路。

原因：
现有拼接链路已经稳定支持画布比例、模糊背景、水平镜像、变速和导出结果列表。第一版优先降低改动风险，不直接重写成复杂多输入滤镜图。

影响：
开启平滑混剪时会多一次临时片段编码，导出速度会比普通拼接慢一些，但更容易保证现有导出链路稳定。

禁止：
本步骤不做画中画、关键帧动画、字幕、TTS、会员、API Key 或 AI 内容理解。

## 2026-06-11：桌面框架选择

决定：
使用 Tauri + Vue3，不使用 Electron。

原因：
Tauri更适合轻量本地桌面软件。

影响：
后续所有桌面功能围绕 Tauri Command 和前端 Vue 组件开发。

禁止：
未经确认，不允许改成 Electron。

---

## 2026-06-11：视频处理方式

决定：
所有视频处理统一通过 VideoEngine 封装 FFmpeg。

原因：
避免 FFmpeg 命令散落在不同模块，后期难维护。

禁止：
任何页面组件直接拼接 FFmpeg 命令。

---

## 2026-06-11：V0.1 范围收敛

决定：
V0.1 只做基础可运行闭环，不做会员、API Key、AI 内容理解、TTS、ASR、复杂混剪和 UI 大重构。

原因：
当前最重要的是先打通本地视频处理链路。如果过早开发高级功能，会导致基础导入、预览、导出迟迟跑不通。

影响：
后续开发必须先完成：

1. FFmpeg / FFprobe 环境检测。
2. 单视频导入。
3. 文件夹导入。
4. 视频信息读取。
5. 视频预览。
6. 选择输出目录。
7. 基础导出。
8. 任务进度和日志。

禁止：
未经确认，不允许在 V0.1 开发会员、API Key、AI 内容理解、TTS、ASR 字幕识别、复杂画中画、云端渲染、支付系统。

---

## 2026-06-11：任务队列先做基础版

决定：
V0.1 的任务队列先做单任务顺序执行，不做复杂并发、暂停恢复、失败自动重试。

原因：
视频导出属于耗时任务，必须有任务状态和日志。但 V0.1 不需要复杂调度，先保证稳定可用。

影响：
任务对象至少应该包含：

1. 任务 ID。
2. 任务类型。
3. 任务状态。
4. 输入文件。
5. 输出文件。
6. 进度。
7. 日志。
8. 错误信息。
9. 开始时间。
10. 结束时间。

禁止：
不要把任务进度写死在某个按钮或页面组件里。

---

## 2026-06-11：API Key 和会员安全原则

决定：
API Key 后续不能明文保存，会员卡密后续不能纯本地判断。

原因：
API Key 明文保存容易泄露。会员卡密如果只靠本地判断，容易被修改配置或代码绕过。

影响：
API Key 后续优先使用系统安全存储。会员卡密后续必须设计远程校验、有效期校验、设备绑定或服务端签名。

禁止：
禁止把 API Key 写入普通 JSON、日志、数据库明文字段。
禁止把会员状态写成完全离线、完全本地的最终判断。

---

## 2026-06-12：固定时长切片策略

决定：
V0.2 固定时长切片使用 VideoEngine 封装 FFmpeg，并采用重编码切片方式生成 mp4 片段。

原因：
直接复制流切片速度更快，但容易受关键帧影响，片段时长不稳定，也不利于后续拼接和混剪。当前阶段优先保证片段兼容性和后续可处理性。

影响：
切片速度会比直接复制流慢一些，但生成片段更适合作为后续随机抽取和拼接的基础素材。

禁止：
页面组件不得直接拼接 FFmpeg 切片命令。
未经确认，不在本阶段加入复杂 GPU 加速、场景识别切片或批量切片。

---

## 2026-06-12：片段拼接策略

决定：
V0.2 片段拼接使用 VideoEngine 封装 FFmpeg concat demuxer，并对拼接结果重新编码为 H.264 + AAC 的 mp4。

原因：
固定时长切片已经统一生成兼容片段，concat 列表可以按随机抽取顺序组织输入；重新编码比直接复制流更慢，但对后续不同片段来源更稳。

影响：
拼接会生成一个新的 `remix_时间戳.mp4` 文件。拼接过程中会临时生成 concat 列表文件，任务结束后删除。

注意：
FFmpeg concat 列表文件不能带 UTF-8 BOM，否则会出现 `unknown keyword '﻿file'`。项目代码使用 Rust `fs::write` 生成列表文件，避免 BOM。

禁止：
页面组件不得直接拼接 FFmpeg 拼接命令。
未经确认，不在本阶段加入批量并发拼接、字幕、TTS 或复杂滤镜。

---

## 2026-06-12：批量生成先采用顺序执行

决定：
V0.2 批量生成混剪结果先采用前端顺序调度，每条结果重新随机抽取片段，并复用 VideoEngine 的片段拼接能力生成 mp4。

原因：
当前目标是先跑通“多条混剪结果生成”闭环。并发生成会同时启动多个 FFmpeg 进程，容易占满 CPU、抢占磁盘 IO，也会让日志和错误处理变复杂。

影响：
批量生成速度不是最快，但更稳定，日志更容易看懂。后续如果需要性能优化，再设计真正的任务队列和并发限制。

禁止：
未经确认，不在本阶段加入多任务并发、暂停恢复、失败自动重试或后台队列持久化。

---

## 2026-06-12：基础镜像先作为混剪导出选项

决定：
V0.2 基础镜像先只支持水平镜像，并作为片段拼接和批量生成混剪时的导出选项。

原因：
水平镜像是短视频混剪中常用的基础变化手段，范围清晰，适合作为 V0.2 第一个视频效果能力。当前不做复杂滤镜组合，避免影响已经跑通的切片、随机抽取、拼接和批量生成链路。

影响：
前端页面只保存“是否开启水平镜像”的开关值，并通过 Service 传给 Tauri Command；真正的 FFmpeg `hflip` 滤镜仍然封装在 VideoEngine 中。

禁止：
页面组件不得直接拼接 FFmpeg 镜像命令。未经确认，不在本步骤加入变速、裁剪比例、滤镜模板、字幕、TTS 或 AI 内容理解。

---

## 2026-06-12：UI 先升级为桌面工作台框架

决定：
当前 UI 从工程测试版整理为桌面软件工作台结构，采用顶部状态栏、左侧素材/片段区、中间预览/结果区、右侧参数设置区、底部任务日志区。

原因：
项目已经完成导入、预览、导出、切片、随机抽取、拼接、批量生成和基础镜像，原来的纵向测试页面不适合继续承载变速、裁剪、字幕、TTS 等后续能力。先建立稳定的信息架构，可以减少后续功能堆叠导致的页面混乱。

影响：
本次只调整 UI 布局和样式，不重写 FFmpeg、VideoEngine、Service 或核心业务逻辑。后续新增参数应优先放入右侧参数面板，任务反馈优先进入底部任务日志区。

禁止：
未经确认，不借 UI 升级机会重写核心业务逻辑、引入大型 UI 库、开发新视频处理功能或进行品牌营销式首页改造。

---

## 2026-06-12：基础变速先限制在 0.5x 到 2.0x

决定：
V0.2 基础变速先只支持 0.5x 到 2.0x，并只作用于片段拼接导出和批量生成混剪结果。

原因：
FFmpeg 的音频 `atempo` 滤镜在 0.5 到 2.0 范围内最直接稳定。当前目标是先让用户能做常见的快放和慢放，不做复杂变速曲线、分段变速或更大倍率链式滤镜。

影响：
页面只提供一个“变速倍数”数值输入，默认 1.0x。VideoEngine 根据该参数组合视频 `setpts` 和音频 `atempo` 滤镜。基础导出、视频预览和切片本身不受变速影响。

禁止：
页面组件不得直接拼接 FFmpeg 变速命令。未经确认，不在本步骤加入裁剪比例、分段变速、字幕、TTS 或 AI 内容理解。
---

## 2026-06-12：视频比例采用画布适配，不做裁剪框

决定：
V0.2 的视频比例功能采用“目标比例画布 + 原视频完整保留 + 背景补齐”的方式实现。

支持范围：

1. 原画。
2. 9:16 竖屏画布。
3. 1:1 方屏画布。
4. 16:9 横屏画布。
5. 黑边背景。
6. 模糊背景。

原因：
用户的核心需求不是自由裁剪，也不是拖动裁剪框，而是把不同横竖比例的视频放进目标比例画布中，保证原视频内容完整可见。短视频平台常见的横转竖、竖转横场景更适合用黑边或模糊背景补齐。

影响：

1. 预览区必须尽量呈现最终导出效果。
2. 前景视频播放器必须保留原生播放、暂停、时长条等控件。
3. 基础导出、片段拼接导出、批量生成必须使用同一套画布参数。
4. FFmpeg 的 scale、pad、blur、overlay 等滤镜必须继续封装在 VideoEngine 内。

禁止：

1. 本阶段不做自由裁剪框。
2. 本阶段不做拖拽调整主体位置。
3. 本阶段不做 AI 智能构图。
4. 页面组件不得直接拼接 FFmpeg 命令。
## 2026-07-12：工作台默认只突出 AI 文案混剪主流程

决定：
默认界面采用“左侧素材 / 中间预览与文案分镜 / 右侧常用成片设置 / 底部单一主操作”的结构。

原因：
现有能力较多，但同级展示会让零基础用户无法判断第一步和下一步。第一版必须优先让用户完成导入、切片、文案匹配和生成视频。

影响：
随机抽取、分类混剪、随机批量生成保留在折叠区；字幕、TTS、去水印等未完成入口暂不展示；底部不再展示未接线的格式、分辨率、帧率、命名和线程选项。

边界：
本次只调整前端信息架构、组件和样式，不修改 Rust、AI API、FFmpeg 或 VideoEngine。

---

## 2026-07-16：配音画面同步采用短分镜、备选画面和有限补偿

决定：
Rust先在本地把原文拆成通常6～14个有效字符的固定短句，特殊尾句最长16个；AI只按 `shotIndex` 为固定短句匹配画面，不再负责断句或返回文案。VideoEngine按真实配音时长选择主画面或较长备选画面，只允许最多8%的轻微减速和约0.8秒尾帧补齐。

原因：
旧方案在配音长于画面时无限复制最后一帧，真实成片出现约4.1秒人物静止但配音继续；同时AI偶尔不遵守分镜字数或把其他主片段作为备选，严格校验会让用户反复重试。可确定的断句和可自动处理的备选冲突不应交给模型碰运气。

影响：
软件本地断句保证原文和短句长度；AI只返回分镜编号与片段编号。备选片段如果被其他分镜作为主片段会自动删除，主片段重复、未知编号、遗漏分镜仍严格拒绝。TTS链路传递主片段和剩余备选片段，VideoEngine使用真实音频时长做最终判断。任何候选画面都无法安全承载配音时，任务直接给出可理解错误，不生成坏视频。

边界：
本阶段不做极端慢放、循环画面、倒放补时、裁掉配音、ASR或复杂时间轴。
