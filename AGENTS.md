# AGENTS.md

## 项目身份

本项目是一个本地桌面端 AI 视频批量混剪工作台。
目标不是复制竞品，而是蒸馏其核心工作流：
素材导入 → 视频分析 → 切片 → 混剪 → 字幕/TTS → 批量导出。

## 技术栈固定

桌面端：Tauri + Vue3 + TypeScript
本地视频处理：Rust + FFmpeg
数据库：SQLite
AI能力：优先 API，后续接本地模型
包管理：pnpm / cargo，按项目实际配置执行

未经用户确认，不允许更换技术栈。

## 每次任务开始前必须读取

默认只读取：

1. `CODEX_START_HERE.md`
2. `CURRENT_TASK.md`
3. `git status` 和本次任务直接相关的代码

详细文档按需读取，不允许每次整份通读：

- 涉及架构边界时，读取 `docs/02_技术架构说明书.md` 的相关章节。
- 涉及FFmpeg或视频处理时，读取 `docs/05_视频处理引擎设计.md` 的相关章节。
- 查历史决定时，用关键词定位 `DECISIONS.md` 的相关标题。
- 查完成历史时，用关键词定位 `PROJECT_STATUS.md`，不从头通读。
- 涉及路线、发布或安装包时，再读取对应 `ROADMAP.md` 或发布文档。

然后先输出：
- 当前项目目标
- 当前已完成内容
- 本次任务范围
- 预计修改文件
- 风险点

用户已经明确确认当前任务时，不需要重复确认；范围发生实质变化时必须再次确认。

## 禁止事项

- 禁止重写整个项目
- 禁止删除已有功能
- 禁止绕过 VideoEngine 直接散写 FFmpeg 命令
- 禁止明文保存 API Key
- 禁止把会员卡密校验写成纯本地逻辑
- 禁止一次性开发多个大模块
- 禁止擅自修改数据库结构
- 禁止引入大型依赖，除非说明理由并获得确认
- 禁止复制竞品 UI 资源、品牌名、图标和文案

## 开发原则

- 一个任务只做一个功能
- 一个功能必须有可验收结果
- 开发过程中持续更新 `CURRENT_TASK.md`
- 功能完成后更新 `PROJECT_STATUS.md`、`TODO_NEXT.md`、`TASK.md` 和 `CHANGELOG.md`
- 重要技术选择必须记录到 DECISIONS.md
- 每次提交前必须说明改动范围

## 低 Token 工作规则

- 优先用 `rg` 搜索文件、标题和符号，只读取命中位置附近内容。
- 禁止把超过约300行的文档整份输出到上下文，除非用户明确要求全文审阅。
- 已经在当前任务中读取且未变化的规则，不重复读取。
- `CURRENT_TASK.md` 必须记录中断点、未提交改动和下一动作，保证恢复时无需重扫项目。
- 每个阶段结束时压缩短入口内容，删除已经过期的过程描述。

## 前端代码组织规则

- `App.vue` 只负责页面组合、跨功能编排和少量全局状态，禁止继续堆入完整新功能。
- TTS、图片转视频、去水印和视频工具等新业务优先放入 `src/features/<feature-name>`。
- 功能状态和流程放 composable，Tauri / API 调用放 service，展示逻辑放组件。
- 多功能共享类型和常量才允许放入 `src/types` 与 `src/constants`。
- 大文件采用渐进式拆分，每次只迁移一个可独立验收的功能，不做一次性重写。
