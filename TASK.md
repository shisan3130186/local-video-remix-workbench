# TASK

## 2026-08-05：修复浅色主题残留黑色输入条

状态：代码修复、浏览器视觉复查和本地Git提交完成；不推送GitHub。

完成内容：
1. 提高浅色主题表单控件覆盖优先级，处理旧版全局深色强制样式。
2. 统一修复文本框、数字框、下拉框、文本域和文件路径栏。
3. 补齐画中画等二级工具弹窗中的文件选择按钮浅色状态。
4. 保留视频预览、大字报设计画布和模板预览的深色工作区。

验证：浏览器实际检查视频混剪、文件批量改名、大字报设计和画中画弹窗；1280×720下输入区均为浅色，视频与设计预览继续保持黑底。`corepack pnpm build`、暂存差异检查和敏感信息扫描通过。

## 2026-08-04：新增跟随系统、浅色和深色主题切换

状态：代码、主题记忆、双尺寸视觉检查和自动构建完成，等待用户统一人工验收；验收前不提交Git。

完成内容：
1. 顶栏新增主题入口，提供“跟随系统 / 浅色 / 深色”三个模式。
2. 用户选择写入本机存储；跟随系统模式会读取并监听Windows外观变化。
3. 首页、AI工作台、通用工作台、二级设置弹窗、语音音色模块和个人中心统一接入主题。
4. 浅色主题采用柔和灰白与低饱和青绿强调色；视频预览画布继续保持黑色，避免影响画面判断。
5. 主题弹窗支持关闭、Escape、焦点返回、键盘方向切换和当前选中状态提示。

验证：`corepack pnpm build`与`git diff --check`通过；1440×900和1280×720下检查系统主题、浅色/深色首页、AI工作台、二级设置弹窗和个人中心，刷新后主题选择仍保留。截图位于`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-08-04_主题切换验收`。

## 2026-08-04：修复四个视频工作台的竖屏预览裁切

状态：代码和双尺寸真实素材验证完成，等待用户人工验收；验收前不提交Git。

完成内容：
1. 视频混剪、分类混剪、视频效果处理和视频内容提炼共用预览统一读取素材真实宽高。
2. 预览元素按横屏/竖屏方向采用不同缩放基准，竖屏按可用高度缩放，横屏按可用宽度缩放。
3. 预览视频改为在容器内绝对居中，避免CSS Grid中视频原始像素高度突破预览区后被隐藏裁切。

验证：使用用户截图中的`测试2.mp4`（1080×1920）复测；1280×720下视频显示239.81×426.34，1440×900下显示308.09×547.73，均保持9:16且完整落在预览区。前端生产构建和差异检查通过。测试图位于`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-08-04_红框显示修复`。

## 2026-08-04：竞品逐页高保真工作台重构

状态：开发、双尺寸视觉对照和完整自动检查完成，等待用户统一人工验收；验收前不提交Git。

完成内容：
1. 删除AI页顶部步骤导航、工作区切换条和通用底部任务条，改为竞品式48px顶栏与左348px、中间弹性、右304px三栏。
2. AI页左侧改为文件夹素材与匹配模式，中间改为大型视频预览和视频文案，右侧直接展示音量、BGM、语音、字幕和画面处理参数。
3. 视频混剪和分类混剪删除五步向导，分别改为三栏直达工作台，并保留导入、切片、随机混剪、分类混剪和批量生成能力。
4. 视频效果处理改为左素材、中预览与分割、右效果参数、底部输出规格；视频内容提炼改为左素材、中预览与片段、右分析维度。
5. 个人中心对齐约640×460双栏结构，API页主视图只保留平台、Ark API Key、TTS API Key和保存，高级连接项折叠收纳。
6. 首页与五项效率工具统一紧凑桌面工作台视觉；大字报模板由6套扩展到20套。

验证：1440×900和1280×720逐页检查通过；1280×720下AI预览仍为674×430，页面无横向或纵向溢出。前端生产构建、Rust格式、编译、严格Clippy、138项测试和差异检查全部通过。

## 2026-08-04：竞品式V3工作台与五项新增工具完整交付

状态：开发、真实桌面回归和完整自动检查完成，等待用户统一人工验收；验收前不提交Git。

完成内容：
1. 首页重构为“创作中心＋效率工具”，保留智剪品牌和暖黑桌面工作台风格。
2. AI智能成片成为主入口，视频混剪、分类混剪、视频效果处理和视频内容提炼进入任务列表。
3. 字幕识别、AI配音、水印处理和导出转换支持从首页直达现有功能。
4. 顶部统一为“AI成片 / 批量混剪 / 效率工具”三个工作区入口。
5. 新增左右双栏文案改写工作台，支持真实AI改写、多版本复制、保存和直接用于AI成片。
6. 新增三栏批量改名工作台，支持前缀、后缀、查找替换和顺序编号，并提供改名前预览与冲突保护。
7. 新增字幕编辑工作台，支持真实视频预览、ASR时间轴编辑和SRT/TXT导出。
8. 新增大字报设计工作台，支持模板、文案、字体、配色、对齐和1080×1920 PNG导出。
9. 新增图片转视频工作台，支持图片预览、时长/画布设置和批量生成竖屏MP4。
10. 保留现有视频、AI、会员、素材库、项目恢复和导出逻辑，并补充未导入素材时可见的AI成片文案草稿区。

验证：前端生产构建、Vue/TypeScript、Rust格式、编译、严格Clippy、138项Rust测试和差异检查全部通过。真实桌面完成文案改写串联、3文件批量改名、7条中文字幕编辑与SRT/TXT导出、1080×1920大字报导出、两条1080×1920/30fps/5秒图片视频生成；1440×900与1280×720布局检查通过。

## 2026-07-24：制作可视化智剪卡密管理器

状态：代码、自动回归和用户确认已完成；提交本地Git，不推送GitHub。

完成内容：
1. 新增独立深色WPF管理界面，保持智剪简约风格。
2. 支持1/7/30/90/365天预设和自定义会员天数。
3. 支持备注、生成兑换码、自动复制和再次复制。
4. 支持卡密列表、状态筛选、数量统计、停用和恢复。
5. 管理员令牌继续使用Windows当前用户加密文件，不进入界面和源码。
6. 新增双击无终端启动入口和零基础使用说明。

验证：XAML与控件加载、CloudBase管理员接口连接、生成测试卡密、停用测试卡密和列表状态检查全部通过。

人工验收结果：2026-07-24用户要求提交本地Git，本阶段验收通过。

## 2026-07-24：V0.5.0国内账号会员系统第一版

状态：代码开发、CloudBase部署、国内公网回归、完整自动检查、正式打包和用户确认已完成；提交本地Git，不推送GitHub。

完成内容：
1. 邮箱注册、登录、退出和修改密码。
2. 一次性兑换码创建、查看、停用、恢复、兑换和重复兑换拦截。
3. 会员有效期绑定账号，同一账号换电脑登录可恢复会员。
4. 服务端ECDSA P-256签名，桌面端公钥验签和72小时离线宽限。
5. Windows当前用户加密保存会话和签名缓存，不保存密码和已兑换卡密。
6. 个人中心显示邮箱、会员状态、有效期、兑换码和API Key页签。
7. 国内正式服务部署到腾讯云开发环境`smartcut-membership-d8bfdff54769`。
8. V0.5.0正式安装包内置国内服务地址和验签公钥，无需终端配置。

自动验证：
1. 国内公网会员全流程11项全部通过。
2. 服务端8项单元测试通过。
3. 前端生产构建通过。
4. Rust格式、编译、严格Clippy和132项测试通过。
5. 安装包大小115.95MB，SHA256为`5b24f3cdd443260ac4b412bf5bb8662a66ccc8c4e13d4beb3e620c7c17a2b6eb`。

人工验收重点：安装V0.5.0后注册新账号，登录，输入测试兑换码，确认个人中心会员状态和有效期，再退出并重新登录确认会员仍然存在。

## 2026-07-24：修复后台FFmpeg/FFprobe反复弹终端

状态：代码开发、自动检查、正式打包、隔离安装、真实项目恢复验证和用户人工验收全部完成；提交本地Git，不推送GitHub。

完成内容：
1. 新增Windows后台视频命令统一启动器。
2. 所有FFmpeg/FFprobe调用统一隐藏控制台窗口。
3. 覆盖导入、缩略图、切片、字幕、GPU检测、混剪和导出。
4. 保留任务进度、取消与错误读取能力。
5. 版本升级为V0.4.1。
6. 129项Rust测试通过。
7. V0.4.1安装包隔离安装和项目恢复检查通过。

人工验收重点：打开软件、导入视频、执行切片和导出时不再出现黑色终端窗口。

人工验收结果：2026-07-24用户确认通过，打开软件不再出现黑色终端窗口。

## 2026-07-21：第四阶段普通用户测试包第一版

状态：代码开发、自动检查、本机隔离安装、真实界面验证和用户人工验收全部完成；提交本地Git，不推送GitHub。

完成内容：
1. V0.4.0 NSIS安装包内置FFmpeg 8.1.1和FFprobe。
2. 新增四步首次启动向导和可重复打开的“新手教程/诊断”入口。
3. 新增AI、TTS、ASR和本地视频引擎真实配置状态检测。
4. 新增启动日志、Rust崩溃日志和不含密钥、视频、文案正文的诊断报告。
5. 新增隐私说明、FFmpeg GPL-3.0许可证和第三方组件说明。
6. 新增FFmpeg资源准备脚本和用户测试包自动检查脚本。
7. 修复Windows PowerShell 5.1读取无BOM中文脚本失败的问题。
8. 修复安装后程序同级`ffmpeg`目录未被标记为内置版本的问题。
9. 最终安装包大小115.84MB，SHA256为`c9e61b72df85c0dec495ab5f7deb2755007795487e26a01db502870d07c433de`。
10. 前端生产构建、Rust格式/编译/严格Clippy和128项测试通过。
11. 已在标准测试结果库完成静默安装，确认主程序、FFmpeg、隐私文件存在并成功启动。
12. 已真实检查向导四页、配置状态、隐私说明、诊断报告和顶部诊断入口。
13. 修复正式安装版启动时附带黑色终端窗口，安装后主程序确认为Windows GUI子系统。
14. 修复项目恢复弹窗“新建空白项目”按钮白底白字，真实安装版显示正常。

人工验收重点：
1. 双击安装包后是否能正常安装并打开智剪。
2. 首次启动向导是否一眼看懂，FFmpeg是否显示“安装包内置”。
3. AI/TTS/ASR配置状态是否与当前设置一致。
4. “生成诊断报告”是否成功，报告中是否没有密钥和视频/文案正文。
5. 导入一个测试视频后，预览、切片和基础导出是否正常。

当前限制：安装包尚未代码签名；真正独立的干净Windows电脑整条流程测试留给用户测试阶段。

人工验收结果：2026-07-21用户确认通过，安装版无需终端运行，项目恢复弹窗按钮显示正常。

## 2026-07-20：智剪 SmartCut 第一版品牌与UI改版

状态：品牌、首页和工作区真实分流已完成代码开发、自动构建、真实桌面视觉检查和用户人工验收；提交本地Git，不推送GitHub。

完成内容：
1. 正式品牌改为“智剪 / SmartCut”，应用图标、窗口标题和网页标题同步更新。
2. 重做首页，删除六张功能卡片墙，改为三个主要入口和最近项目区域。
3. 新增右侧三步新手引导、首次启动教程和教程网页预留入口。
4. 工作台统一为中性深色编辑器风格，减少绿色、大圆角、重卡片和视觉噪音。
5. AI配音新增4款常用音色卡片，选择结果继续复用原有`speaker`字段。
6. 保留完整25款音色库、多语种筛选、场景筛选和官方样音试听。
7. `corepack pnpm build`通过。
8. `corepack pnpm tauri build --debug --no-bundle`通过。
9. 真实桌面检查：首页、图标、教程、工作台和AI配音弹窗均正常显示。
10. 首页三个入口改为真实工作区分流，不再全部打开同一套工作台。
11. 新增批量混剪独立页面和视频工具中心，AI成片继续保留四步主流程。
12. 右侧功能按工作区筛选，顶部支持AI、批量、工具三类工作区直接切换。
13. 新版分流通过1100×760左右真实Tauri桌面窗口检查。
14. 默认窗口改为居中1320×860，最小1100×720，启动时不再默认全屏。
15. AI工作区重新分配三栏和上下区域比例，中间预览明显增大，文案区内部滚动。
16. AI右侧设置改为三个任务分组；批量和工具预览改为完整画面，工具卡常用窗口下三列显示。
17. 根据竖屏预览截图反馈，按竞品结构重排AI三栏宽度和上下区域：竖屏舞台使用约1.37:1比例随预览高度缩放，文案区固定为紧凑高度，其余空间优先给视频。
18. 真实1320像素宽Tauri窗口复查中，黑色舞台约643×480像素，竖屏视频明显放大，左右留黑与竞品缩放比例基本一致。

人工验收重点：
1. 首次进入是否能一眼看懂三个主要入口。
2. 关闭教程后，再从顶部和首页能否重新打开。
3. 工作台是否比旧版更简约、清晰且不显得冰冷。
4. AI配音常用卡片和完整音色库是否容易理解。
5. 原有导入、预览、设置和任务入口是否仍能正常使用。
6. 三个首页入口点开后，中心页面、步骤和右侧功能是否明显不同。
7. 默认窗口是否协调，竖屏视频是否能完整、清楚地预览。
8. 最大化窗口后，竖屏预览两侧黑边是否已明显缩小并接近竞品比例。

人工验收结果：2026-07-21用户确认通过，同意提交本地Git。

## 2026-07-20：MoneyPrinterPlus 竞品分析

状态：研究、源码核对、功能对比、Markdown 报告和 PDF 视觉检查全部完成；未修改业务代码。

完成内容：
1. 核对 `ddean2009/MoneyPrinterPlus` 的 README、仓库结构、核心视频逻辑、自动发布、配置保存、依赖、提交、Release 和公开问题。
2. 与当前项目的素材、切片、AI 分镜、TTS/ASR、字幕、批量导出、任务控制、项目恢复、密钥安全和水印能力逐项对比。
3. 形成结论：核心功能相似度约 65% - 75%；MoneyPrinterPlus 的平台接入和供应商覆盖更宽，我们的智能剪辑、可靠性和工程底座更深。
4. 明确建议借鉴资源采集、provider 抽象和独立发布队列，不直接复制 Streamlit 状态层、明文 YAML 密钥和固定 XPath 发布实现。
5. 生成 `docs/MoneyPrinterPlus_竞品分析.md` 和 `output/pdf/MoneyPrinterPlus_竞品分析报告.pdf`。
6. PDF 共 4 页，已逐页检查，无截断、重叠、乱码和表格溢出。

## 2026-07-20：水印工具第一版

状态：代码开发、自动检查、真实FFmpeg验证和用户人工验收全部通过；提交本地Git，不推GitHub。

完成内容：
1. 新增文字水印和图片水印设置面板及一级入口。
2. 支持五个位置、透明度、边距、文字字号/颜色和图片大小。
3. 新增`src/features/watermark`，状态、类型、校验、组件和样式独立组织。
4. 新增Rust `video_engine/watermark.rs`，集中字体、中文文字文件、路径转义和滤镜生成。
5. 水印接入基础导出、普通/分类/批量混剪、AI成片和AI配音成片。
6. 图片水印与画中画、BGM可以同时使用，输入流编号按实际启用功能动态计算。
7. 项目快照保存水印设置，旧快照自动补默认值，失效图片自动关闭。
8. 添加水印阶段前端构建、Rust格式、编译、严格Clippy和110项测试全部通过。
9. 三组真实FFmpeg视频已生成并目视确认。
10. 新增边缘裁剪、融合修复、区域模糊、马赛克和色块遮盖。
11. 处理原水印和添加新水印可同时开启，顺序固定为先处理旧区域、再叠加新水印。
12. 最终前端构建、Rust格式、编译、严格Clippy和122项测试全部通过。
13. 横屏顶部裁剪、竖屏顶部裁剪和“裁剪+新水印”组合视频已生成并目视确认。
14. 新增完整视频预览和移动处理框，可保存2～8个关键位置并显示轨迹。
15. 新增关键位置载入、更新、删除、清空和相邻位置线性移动。
16. 动态模糊、马赛克、色块遮盖及“移动处理+添加新水印”已通过真实视频验证。
17. 移动模式只接入原画画布的当前完整视频导出，不进入时间线会重排的混剪链路。
18. 最终前端151个模块构建成功，Rust 125项测试通过、0项失败。

人工验收结果：2026-07-20用户确认通过，同意提交本地Git。

## 2026-07-18：可视化AI音色库第一版

状态：代码开发、自动检查和用户确认完成；已提交本地Git `4438287`，不推GitHub。

完成内容：
1. 新增25款真实火山TTS 2.0音色目录和可视化音色卡片。
2. 覆盖中文、英语、日语、西班牙语和印尼语，并提供多语种快捷筛选。
3. 支持搜索、语言筛选、场景筛选、推荐标识、男女声和风格说明。
4. 每款音色支持官方样音试听，不消耗用户TTS生成额度。
5. 选择结果复用原有`speaker`字段和现有MP3、逐句配音视频流程。
6. 保留高级手动音色ID，兼容用户账号中已开通的其他声音。
7. 增加键盘焦点约束、Esc关闭、焦点返回、空结果和试听错误反馈。
8. 前端构建、Rust格式、编译、严格Clippy和104项测试全部通过。

人工验收重点：搜索、语言/场景筛选、多语种筛选、试听/停止、选择音色、真实MP3生成和窄窗口布局。

最终确认：2026-07-18用户要求提交并继续下一步，音色库已进入本地Git基线。

## 2026-07-18：ASR与本地文案库精简联动

状态：代码开发、自动检查和用户人工验收全部通过；提交本地Git，不推GitHub。

完成内容：
1. ASR识别结果增加保存到文案库和保存后直接使用两个按钮。
2. 文案分镜区域增加文案库入口，列表支持使用和删除。
3. 文案使用独立版本化JSON持久化，不修改SQLite结构。
4. 保存来源文件名、完整文字和句子时间轴，重启软件仍可读取。
5. 相同来源和相同文字不会重复保存。
6. 替换当前文案前进行确认，超过4000字时阻止直接用于AI分镜。
7. 前端构建、Rust格式、编译、严格Clippy和104项测试全部通过。

人工验收重点：保存、载入、保存并使用、重复保存、重启恢复和删除。

最终人工验收：2026-07-18用户确认ASR识别、文案库保存与当前文案联动验收成功。


## 2026-07-18：音频输入与火山云端ASR第一版

状态：代码开发、自动检查和用户人工验收全部通过；与文案库联动一并提交本地Git，不推GitHub。

完成内容：
1. 右侧新增独立“语音识别”入口，支持选择本地音频和带声音的视频。
2. VideoEngine统一检查声音轨和2小时时长限制，并转换为16kHz单声道压缩音频。
3. 接入火山大模型极速语音识别，返回完整文字和句子级开始/结束时间。
4. 复用现有Windows加密保存的语音密钥，API设置文案升级为“TTS / ASR语音密钥”。
5. 识别结果使用应用数据目录中的版本化JSON缓存，不修改SQLite结构。
6. 缓存键包含源文件路径、大小、修改时间和模型版本，同一未修改文件不重复付费识别。
7. 接入现有任务中心、FFmpeg真实进度、任务取消和临时网络错误受控重试。
8. 结果面板支持完整文字复制、句子时间轴、时长、上传体积和缓存状态展示。
9. 前端构建、Rust格式、编译、严格Clippy和102项测试全部通过。

人工验收重点：
1. “API 密钥”中确认语音密钥文案同时包含TTS和ASR。
2. “语音识别”选择有清晰人声的音频或视频并开始识别。
3. 确认完整文字和每句起止时间可见。
4. 重复识别同一文件，确认显示“本地缓存”。
5. 识别中点击底部“取消任务”，确认安全停止。


## 2026-07-17：视频内容提炼与AI自动分类第一版

状态：代码开发、自动检查和用户人工验收全部通过；提交本地Git，不推GitHub。

完成内容：
1. 视频片段页签新增明确的“AI提炼全部片段”入口和完成数量提示。
2. 提炼前复用多帧画面描述，仅对缺失描述的片段补充画面理解。
3. 提取内容主题、最多3个可见卖点、主体动作和最多5个标签。
4. 自动判断8类镜头：钩子、产品展示、使用过程、细节特写、效果展示、结尾引导、人物口播和环境镜头。
5. 选中片段后可查看并人工修改结构化结果和镜头类型。
6. 使用SQLite现有description字段保存版本化JSON，旧纯文字缓存继续兼容，不修改数据库表结构。
7. AI返回值在Rust端严格校验片段编号、字段长度、标签数量和镜头类型。
8. 前端构建、Rust格式、编译、严格Clippy和96项测试全部通过。

人工验收重点：
1. 完成切片后进入左侧“视频片段”，确认顶部能看到“AI提炼全部片段”。
2. 点击后确认进度先显示画面理解，再显示内容提炼，完成后出现主题、卖点、动作、标签和镜头类型。
3. 切换不同片段，确认结果各不相同且分类会自动写入。
4. 修改任一字段并保存，切换片段再切回，确认修改仍保留。
5. 重启软件并载入素材库，确认提炼结果和镜头类型仍存在。

最终人工验收：2026-07-17用户确认内容提炼、自动分类、人工修改和缓存恢复验收成功。

## 2026-07-17：智能切片与多帧理解第一版

状态：代码开发、自动检查、真实FFmpeg验证和用户人工验收全部通过；提交本地Git，不推GitHub。

完成内容：
1. 混剪设置新增“智能切片（推荐）”与“固定时长”两种模式。
2. 智能切片支持稳健、均衡、灵敏三档画面变化灵敏度。
3. 支持最短片段与最长片段保护，过滤闪屏碎片，并为无明显转场的长镜头自动补切。
4. 单个连续短视频在时长允许时自动均分为两个可用片段，避免只生成一个片段无法继续AI混剪。
5. 每个片段抽取开头、中间、结尾最多3帧，AI综合理解主体、场景、动作与动作变化。
6. 代表缩略图与AI描述继续进入现有SQLite素材库缓存，不修改数据库结构。
7. 新增FFmpeg关键帧切点容差，兼容新版FFmpeg，并同步增强原固定时长切片。
8. 前端正式构建、Rust格式、严格Clippy和94项测试通过；合成三场景视频真实切片为3段。

首轮人工验收反馈：智能切片切换入口被放在“显示高级 → 更多高级设置”内，精简模式完全不可见。已将它提升为右侧“常用调整”中的一级“智能切片”按钮，并把弹窗标题改为“智能切片与批量”，等待复验。

最终人工验收：2026-07-17用户确认入口修复、智能切片和多帧理解验收成功。

人工验收重点：
1. 打开右侧“混剪设置”，确认默认显示“智能切片”和“推荐”。
2. 选择均衡、最短2秒、最长10秒，导入带多个转场的视频并开始智能切片。
3. 确认底部进度先显示扫描画面变化，再显示生成智能切片和多帧预览。
4. 确认左侧片段不是机械等长，明显转场处能分开，且没有大量不足2秒的碎片。
5. 切换到固定时长模式，确认原有按秒切片仍可用。
6. 输入文案生成AI分镜，确认多帧理解能正常完成；再次生成时可复用SQLite中的画面描述。

## 2026-07-17：素材库持久化第一版

状态：代码开发、自动检查和用户人工验收全部通过；提交本地Git，不推GitHub。

完成内容：
1. 新增内置SQLite素材库和版本化数据库结构。
2. 自动保存原视频、封面、切片、分类、缩略图和AI画面描述。
3. 软件启动时只检查本机素材库，不自动覆盖空白项目；用户点击“载入素材库”后才恢复。
4. 失效原视频显示原路径并支持重新定位，失效切片提示重新切片。
5. 最近项目恢复优先级保持不变，现有JSON快照不会被SQLite覆盖。
6. 前端构建、Rust格式、编译、严格Clippy和90项测试全部通过。

人工验收重点：
1. 导入视频后确认素材库顶部显示“素材库已自动保存”。
2. 切片并完成一次AI画面理解，关闭软件后重新打开。
3. 如果出现最近项目恢复弹窗，先选择“清除记录”；随后应自动载入SQLite素材库。
4. 确认原视频、切片、分类、缩略图和AI缓存仍在。
5. 临时移动一个原视频并重启，确认显示失效素材和原路径。
6. 点击“重新定位”选择移动后的文件，确认素材恢复。
7. 回归最近项目恢复、导出、AI分镜、配音、字幕和任务取消。

首轮人工验收反馈：点击“放弃上次记录”后SQLite又自动载入相同素材，导致按钮看起来无效。已将项目恢复和素材库分离：按钮改为“新建空白项目”，点击后保持空白；本机素材库通过左侧“载入素材库”手动恢复，等待复验。

最终人工验收：2026-07-17用户确认复验成功，素材库持久化第一版全部完成。

## 2026-07-17：完整输出设置第一版

状态：代码开发、自动检查、真实FFmpeg验证和用户人工验收全部通过；提交本地Git，不推GitHub。

完成内容：
1. 新增完整输出设置界面和GPU编码状态列表。
2. 分辨率支持跟随画布、720p和1080p。
3. 帧率支持跟随源、24、25、30、50和60fps。
4. 质量支持省空间、标准和高清。
5. 编码支持自动、CPU、NVIDIA、Intel和AMD，GPU使用实际编码检测。
6. 全部最终成片链路统一生效，项目保存和恢复同步支持。
7. 前端构建、Rust严格检查和87项测试全部通过。
8. 补齐右侧“成片设置”区域的“导出设置”入口。

人工验收结果：2026-07-17用户确认完整输出设置验收成功。

人工验收重点：
1. 打开“导出设置”，确认格式、分辨率、帧率、质量和编码方式清楚可见。
2. 确认当前电脑显示CPU、NVIDIA可用，Intel、AMD不可用。
3. 分别导出720p和1080p，检查尺寸与横竖屏比例。
4. 选择25或30fps，检查成片信息。
5. 用三个质量档位导出相同内容，检查文件大小差异。
6. 选择NVIDIA GPU导出，确认成功且日志显示NVIDIA编码。
7. 回归AI配音/字幕/BGM/画布/任务取消是否正常。

## 2026-07-17：统一任务控制第一版

状态：代码开发、自动检查和用户人工验收全部通过；按用户要求提交本地Git，不推GitHub。

完成内容：
1. 底部任务栏显示真实百分比、当前处理阶段、取消任务、重试失败项和清理临时文件。
2. 切片、基础导出、普通混剪、AI差异视频和AI配音视频接入统一Rust任务中心。
3. 取消FFmpeg后删除半成品和本次任务临时目录。
4. 随机批量、AI无配音和AI配音的失败条目可以单独重试。
5. 启动时自动清理超过24小时的本项目遗留临时目录；活动目录、素材和成片不会被清理。
6. 前端构建、Rust格式、编译、严格Clippy和81项测试全部通过。

人工验收重点：
1. 使用稍长视频切片或生成成片，观察底部百分比持续变化。
2. 处理中点击“取消任务”，确认任务停止并可重新开始。
3. 查看输出目录，确认没有取消产生的半成品文件或空切片目录。
4. 批量任务出现失败时点击“重试失败项”，确认成功条目没有重复生成。
5. 空闲时点击“清理临时文件”，确认有明确结果提示且不影响素材、成片和项目恢复。

首轮人工验收：前4项通过；第5项发现清理结果只进入隐藏日志。已改为底部任务栏直接显示清理反馈，等待用户复验。

最终人工验收：清理反馈复验通过，本任务全部完成。

## 2026-07-17：项目保存与失败恢复第一版

状态：代码开发、自动检查和用户人工验收全部通过；按用户要求提交本地Git，不推GitHub。

完成内容：
1. 新增独立 `src/features/project-recovery`，负责自动保存状态、恢复流程、失效路径降级和恢复对话框。
2. Rust新增项目快照模块，保存最近一个项目并限制版本、大小、路径数量和敏感字段。
3. 自动保存素材、切片、分类、文案、AI描述、分镜、生成数量、TTS、字幕、原声、画布、效果、画中画和BGM设置。
4. 启动时必须先选择恢复或放弃，避免空白工作台自动覆盖上次项目。
5. 缺失视频和切片自动跳过；失效输出目录要求重选；失效BGM和画中画自动关闭。
6. 顶部新增轻量自动保存状态，不改变现有三栏工作流和设置入口。
7. 前端构建、Rust格式、编译、严格Clippy和76项测试全部通过。

人工验收重点：
1. 导入素材、填写文案并调整几个设置，等待顶部显示“已自动保存”。
2. 关闭软件再重新打开，确认出现恢复提示。
3. 点击恢复后检查素材、输出目录、文案、AI分镜和主要设置。
4. 再次打开后选择“放弃上次记录”，确认进入空白工作台且不再重复提示。
5. 可选：临时移动一个测试素材后恢复，确认软件明确提示并跳过失效项。

首轮人工验收反馈：恢复窗口正确显示3个原视频和47个切片，但点击恢复出现 `structuredClone` 报错。已改用JSON安全复制并通过完整自动检查，原快照未被删除，等待用户直接重新点击恢复复验。

最终人工验收结果：2026-07-17用户确认恢复成功。

下一步：推荐做“普通用户安装包与首次启动配置第一版”，等待用户确认后再开发。

## 2026-07-17：AI与TTS临时失败自动重试

状态：代码开发、自动检查和用户人工验收全部通过；按用户要求提交本地Git，不推GitHub。

完成内容：
1. AI画面理解、AI分镜匹配、TTS试听和逐句TTS配音统一接入最多3次的受控重试。
2. 临时网络错误、超时、429限流和常见5xx服务错误可自动恢复。
3. Key错误、权限错误、模型配置错误和明确额度耗尽不做无意义重试。
4. 日志显示第几次尝试、等待秒数和最终失败原因。
5. 已成功的AI片段描述和TTS句子继续保留，只补失败项目。
6. 前端构建、Rust格式、编译、严格Clippy和72项测试全部通过。

人工验收重点：
1. 正常网络下AI分镜和逐句配音行为应与之前一致。
2. 出现429、超时或临时服务错误时，日志应显示自动重试次数。
3. Key错误或额度耗尽时应直接显示原因，不应等待3轮。
4. 某一句配音失败时，之前成功的句子不应重新生成。

人工验收结果：2026-07-17用户确认验收通过。

下一步：推荐开发“项目保存与失败恢复第一版”，先保存素材路径、输出目录、文案和AI分镜，软件重启后可以继续；等待用户确认后再开发。

## 2026-07-17：软件内安全保存AI与TTS密钥

状态：开发、自动检查、桌面界面检查和用户人工验收全部完成。

完成内容：
1. 右侧常用设置新增“API 密钥”入口。
2. 支持填写方舟API Key、AI模型接入点ID、AI Base URL、TTS API Key、资源ID和默认音色ID。
3. 密钥使用Windows当前用户的数据保护机制加密保存，软件重启后自动读取。
4. 已保存密钥不会重新返回前端；界面只显示“已安全保存、使用环境变量、尚未配置”。
5. 密钥输入支持显示和隐藏，留空保存不会覆盖已有密钥。
6. 支持分别删除本机AI与TTS密钥，删除操作需要二次点击确认。
7. AI与TTS运行时优先使用软件内保存配置，环境变量继续作为开发备用。
8. TTS设置页不再要求用户通过PowerShell配置。

验证：
- `corepack pnpm build`通过。
- `cargo fmt -- --check`、`cargo check`、`cargo clippy --all-targets -- -D warnings`通过。
- Rust共67项测试通过，其中6项覆盖配置默认值、密钥保留、模型校验、状态脱敏、Windows加密往返和密文文件往返。
- 真实桌面窗口视觉检查通过：入口、内部滚动、AI/TTS分区、密码显隐、可访问名称和保存按钮正常。
- 测试期间未写入真实用户密钥配置文件。
- 2026-07-17用户人工验收通过：软件内AI/TTS密钥入口可用，重启后无需终端重复填写。
- 自动测试报告：`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-07-17_API密钥安全保存自动测试\自动测试报告.md`。

Git状态：用户已确认人工验收通过，准备提交本地Git；按用户要求暂不操作GitHub。

下一步：继续做多组真实素材稳定性回归，重点观察额度、限流、失败重试和导出稳定性。

## 2026-07-17：一次生成多条AI差异视频

状态：代码开发、完整自动检查和用户人工验收已完成，继续保持测试中。

完成内容：
1. AI分镜支持一次生成1～10条差异视频，默认3条。
2. 第一条保留当前人工确认分镜，后续版本轮换各句备选画面。
3. 文案和分镜顺序保持不变；每条视频内部片段不重复，重复方案自动去除。
4. 备选画面不足时只生成实际可组成的不重复版本，并在界面提示。
5. 无配音视频按顺序导出，单条失败记录后继续剩余版本。
6. AI配音只逐句合成一次，同一批音频复用到所有差异版本，避免重复调用和付费。
7. 界面新增生成数量、当前第几条、成功数、失败数和最终汇总。

自动验证：
- `corepack pnpm build`通过。
- `cargo fmt -- --check`、`cargo check`、`cargo clippy --all-targets -- -D warnings`通过。
- 2026-07-17稳定性回归基线复测通过：前端构建、Rust格式、编译、Clippy和67项Rust测试全部通过。
- 真实桌面验收入口可启动到干净窗口，FFmpeg显示就绪。
- 真实素材端到端回归暂未完成：Codex自动化无法稳定操作Windows系统“选择文件夹”弹窗，未能完成素材目录导入。
- 本轮回归报告：`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-07-17_多条差异视频真实稳定性回归\真实稳定性回归报告.md`。
- Rust 61项测试全部通过，覆盖数量边界、主方案保留、差异唯一、单条内不重复、无备选和候选冲突。
- 2026-07-17用户人工验收通过：一次生成多条AI差异视频的主流程符合预期。
- 自动测试报告：`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-07-17_多条差异视频自动测试\自动测试报告.md`。

真实验收限制：当前环境未配置AI和TTS环境变量；测试素材库只有2个小样本，尚不满足至少3组、每组3～7个视频的稳定性回归要求。

Git状态：功能已提交为 `ef82055 feat(ai): 支持一次生成多条差异视频`。首次推送被GitHub以“远端包含新提交”拒绝；随后多次获取远端均因 `github.com:443` 连接失败而无法安全合并。未使用强制推送，等待网络恢复后先获取远端再补推。

下一步：配置真实AI/TTS密钥并补齐测试素材后，完成多条无配音和带配音视频的人工回归。

## 2026-07-16：修复切片后无法进入文案步骤

状态：开发、前端构建检查和人工验收全部完成。

完成内容：
1. AI片段准备从全有或全无改为保留成功片段。
2. 失败片段自动跳过，至少2个成功片段即可继续。
3. 移除生成前“数量必须与全部切片完全一致”的错误限制。
4. 核心流程显示阻断错误，文案区域显示可继续的部分成功提醒。
5. 更新过期的配音时长说明。

验证：`corepack pnpm build`通过。

## 2026-07-16：修复AI断句和备选片段冲突

状态：开发、自动检查和人工验收全部完成。

完成内容：
1. 长文案改由Rust本地按标点和长度自动断句。
2. AI只接收固定短句编号并匹配画面，不再返回或修改分镜文案。
3. AI返回顺序变化时按分镜编号恢复原顺序。
4. 备选片段与其他主片段冲突时自动删除备选项。
5. 保留未知编号、遗漏分镜、重复主画面等真正错误的严格检查。

自动检查：前端构建通过；Rust格式、编译、Clippy通过；55项Rust测试全部通过。

下一步：使用刚才失败的长文案重新生成分镜，确认两类错误消失，再继续配音成片验收。

## 2026-07-16：解决长配音导致画面定格

状态：开发、自动检查和人工验收全部完成。

完成内容：
1. AI分镜增加原文完整性、单句长度和预计时长校验。
2. 主画面过短时，TTS混剪会自动尝试较长的相关备选画面。
3. VideoEngine只允许轻微减速和最多约0.8秒短补帧。
4. 画面仍不足时直接报错，不再连续复制最后一帧数秒。
5. 保留原声时同步调整原声音轨速度。

自动检查：前端构建通过；Rust格式、编译、Clippy通过；当前55项Rust测试全部通过。

下一步：用户用真实长文案生成一次AI配音视频，重点检查每个分镜结尾。验收通过后再提交Git，不push。

## 2026-07-16：配音长于画面问题分析

状态：根因定位完成，等待方案确认，尚未修改代码。

完成内容：
1. 检查用户提供的15秒真实成片。
2. 确认底层画面从约10.8秒静止到结尾，约4.1秒。
3. 确认字幕切换正常，卡住的是分镜画面，不是字幕渲染。
4. 定位到 `narrated_mix.rs` 的尾帧复制策略。
5. 推荐改为时长感知的短文案多分镜方案，并只保留极短误差兜底。

下一步：用户确认推荐方案后再开发，不直接修改稳定链路。

## 2026-07-16：素材显示与切片入口第二轮修复

状态：开发、自动验证和人工复验全部通过。

完成内容：
1. 原始素材封面由Rust直接生成完整画面版本，不再只依赖CSS避免裁切。
2. AI图片理解使用的片段预览图保持原始比例，不受素材列表封面模式影响。
3. 新增输出目录存在性检查；目录缺失时切片按钮自动要求重新选择。
4. 有效目录选好后自动继续切片。
5. 切片错误在核心流程区直接显示。
6. 真实竖屏封面和真实FFmpeg切片验证通过。

人工验收结果：素材完整封面、输出目录重新选择和全部素材切片均通过。

## 2026-07-15：自动字幕第一阶段

状态：首轮人工验收主要功能通过；两项界面反馈已修复，等待复验。

完成内容：
1. AI配音面板新增默认开启的自动字幕设置卡片。
2. 支持顶部、中间、底部位置和小、标准、大字号，并提供实时预览。
3. 每个AI分镜携带对应字幕文字，按标点和可读长度拆成短句，并在该句配音时长内依次显示。
4. 新增独立Rust ASS字幕模块，不把字幕逻辑继续堆入配音混剪文件。
5. 支持中文、标点、特殊字符、自然分句、无标点长文案拆分和动态字号收缩。
6. 自动检查FFmpeg `ass`滤镜，临时字幕任务结束后自动清理。
7. 前端构建、Rust编译、Clippy、修复后45项测试和真实FFmpeg字幕烧录通过。
8. 修复首轮验收发现的整段字幕拥挤问题，默认字号和字幕背景块更紧凑。
9. 修复原始素材竖屏封面被缩略框裁切的问题，完整画面优先显示。

下一步：按 `TODO_NEXT.md` 只复验字幕短句切换和原始素材完整缩略图；通过前不提交Git。

## 2026-07-15：TTS第二阶段逐句配音与画面同步

状态：代码开发、自动检查、真实FFmpeg测试和人工验收全部通过。

完成内容：
1. AI分镜支持开启逐句配音视频模式，关闭时旧链路不变。
2. 按分镜顺序逐句生成临时MP3，并显示生成进度。
3. 新增独立Rust配音混剪模块：原声默认静音、短画面冻结尾帧、长画面按配音裁短。
4. 配音模式固定1.0x，继续支持画布、效果、画中画、平滑混剪和BGM。
5. 临时MP3和临时视频使用安全目录并自动清理。
6. 前端构建、Rust严格检查和31项Rust测试通过。
7. 本地FFmpeg真实滤镜测试通过，结果已保存到标准测试结果库。
8. 新增“保留原视频声音”开关和0%～100%音量滑块，默认关闭、默认15%。
9. 有音轨时混合完整原声，无音轨时自动退回纯AI配音；继续兼容现有外部BGM。
10. 原声增强后前端构建、Rust严格检查、35项测试和真实FFmpeg混音测试通过。

人工验收结果：逐句配音视频、画面同步、原声开关和音量调节均通过。下一步按 `TODO_NEXT.md` 确认新的单功能阶段。

## 2026-07-14：火山引擎TTS语音合成第一阶段

状态：代码开发、自动检查和真实火山语音环境人工验收全部通过。

完成内容：
1. 新增独立TTS前端功能模块和AI配音设置入口。
2. TTS文案与当前AI混剪文案同步，音色ID可以调整。
3. 新增TTS配置状态、生成状态、错误提示、MP3试听和时长展示。
4. Rust调用火山语音 `seed-tts-2.0` HTTP接口并保存MP3。
5. TTS Key只在Rust读取，日志不记录Key、请求体、完整文案或音频Base64。
6. 兼容火山正式成功码 `20000000` 和旧响应成功码 `0`，避免把 `message = OK` 的成功响应误判为拒绝。
7. 新增8项TTS单元测试，前端构建、Rust严格检查和全部26项Rust测试通过。
8. 2026-07-15人工验收确认：中文文案生成MP3、软件内试听、时长、输出路径和重新生成均正常。

后续：TTS第一阶段已提交且未push；项目已进入上方记录的逐句配音与视频画面同步阶段。

## 2026-07-14：前端渐进式重构第五阶段

状态：代码拆分、自动检查和人工回归全部通过。

完成内容：
1. 将设置弹窗拆成8个独立设置面板和共享输入工具。
2. 保持 `ToolSettingModal.vue` 对 `App.vue` 的全部参数和事件接口不变。
3. 将3148行全局样式按原始顺序拆成7个样式文件。
4. 设置弹窗减少到约174行，`styles.css` 减少到7行导入入口。
5. 拆分前后生产CSS指纹一致，页面最终样式规则没有变化。
6. 前端构建、Rust严格检查和18项Rust单元测试通过。

人工验收结果：所有设置面板、窗口布局和混剪生成链路回归通过。

下一步：按 `TODO_NEXT.md` 确认首个新增业务功能，未经确认不直接开发。

## 2026-07-13：前端渐进式重构第四阶段

状态：代码迁移、自动检查和人工回归全部通过。

完成内容：
1. 新增 `src/features/remix-export` 功能模块。
2. 基础导出、随机抽取、片段拼接、分类混剪和批量生成从 `App.vue` 迁出。
3. 普通混剪状态、错误、日志清理和结果记录迁入独立composable。
4. 批量结果抽屉和基础导出Tauri Service移入功能目录。
5. AI混剪继续复用同一混剪状态和结果接口。
6. `App.vue` 减少到约825行。
7. 前端构建、Rust严格检查和18项Rust单元测试通过。

人工验收结果：基础导出、随机混剪、分类混剪、批量生成、批量结果抽屉、成片设置、AI分镜与生成、任务日志和重新导入后的状态清理全部通过。

下一步：按 `TODO_NEXT.md` 规划第五阶段设置弹窗与样式拆分，未经确认不直接开发。

## 2026-07-13：前端渐进式重构第三阶段

状态：代码迁移、自动检查和人工回归全部通过。

完成内容：
1. 新增 `src/features/materials` 功能模块。
2. 素材面板、文件导入、文件夹导入、元数据读取和素材选择从公共目录迁出。
3. 视频封面生成和自定义封面状态迁入素材模块。
4. 全部素材切片、失败素材跳过、片段分类初始化和 AI 缩略图衔接迁入素材模块。
5. `App.vue` 减少到约 1154 行，继续只保留跨功能编排。
6. 前端构建、Rust严格检查和18项Rust单元测试通过。

人工验收结果：素材导入、切片、片段预览、分类、封面、AI衔接、重新导入状态清理和普通混剪回归全部通过。

下一步：先规划第四阶段普通混剪与导出模块，未经确认不直接开发。

## 2026-07-13：前端渐进式重构第二阶段

状态：代码迁移、自动检查和人工回归全部通过。

完成内容：
1. 第一阶段由用户人工验收通过。
2. 新增 `src/features/ai-remix` 功能模块。
3. AI 片段准备、画面理解、分镜规划、人工调整和生成状态从 `App.vue` 迁出。
4. AI 专用组件、类型和 Tauri Service 收拢到同一功能目录。
5. `App.vue` 降至约 1381 行，界面和业务行为保持不变。
6. 前端构建、Rust 严格检查和 18 项 Rust 单元测试通过。

人工验收结果：AI 主链路、缓存复用、人工调整、AI 视频生成、普通混剪和设置回归全部通过。

下一步：先规划第三阶段素材与切片模块，未经确认不直接开发。

## 2026-07-13：前端渐进式重构第一阶段

状态：开发、自动检查和人工回归全部通过。

完成内容：
1. 新增工作台共享类型和常量。
2. 新增日志与导出结果 composable。
3. 新增混剪设置 composable。
4. 清理多个组件的重复类型定义。
5. `App.vue` 减少 200 行，保持界面和业务行为不变。

人工验收结果：页面、导入切片、AI 分镜、AI 视频生成、普通混剪、设置、日志和结果列表均通过。

## 2026-07-13：代码质量与安装包可行性审计

状态：审计和低风险整改完成，自动检查与 NSIS 安装器构建通过。

完成内容：
1. 统计并确认代码热点，不进行推倒式重写。
2. 补齐 `assets`、`tasks` 和 FFmpeg 资源预留目录。
3. 新增统一 FFmpeg / FFprobe 路径解析和 3 项测试。
4. 将混剪导出参数收拢为设置对象，Rust Clippy 严格检查通过。
5. 新增安装包准备脚本、发布清单、基础功能分类和审计报告。
6. 成功生成 Windows NSIS 安装器。

未解决阻塞：安装包尚未内置 FFmpeg / FFprobe；普通用户 AI Key 方案未确定；安装包未签名。

下一步：用户确认测试对象和 AI 凭证方案后，再完成干净电脑可用的测试安装包。

## 2026-07-14：修复纯文字 AI 分镜规划超时

状态：代码修复、自动检查和真实火山环境人工验收全部完成。

完成内容：
1. 确认图片理解已经成功，失败点位于最后的纯文字分镜规划。
2. AI 请求统一增加 `thinking.type = disabled`，关闭模型深度思考。
3. 纯文字分镜超时从 120 秒缩短到 45 秒，最大输出从 1600 调整到 800 tokens。
4. 新增请求体测试；AI 单元测试共 15 项全部通过。
5. `corepack pnpm build`、`cargo fmt`、`cargo check` 全部通过。

人工验收结果：原 13 个片段已成功完成画面理解和纯文字分镜规划，AI 分镜超时问题解决，智能混剪 MVP 主链路通过。

下一步：本次 AI MVP 相关文件已提交且未 push；随后进入 10 轮不同素材和文案的稳定性测试。

## 2026-07-14：修复 AI 图片理解请求超时

状态：代码修复和自动检查完成，等待人工复测。

完成内容：
1. 图片理解改为单图批次，最多同时执行 2 个请求。
2. 图片理解和文字分镜使用不同超时。
3. 错误提示增加具体请求阶段。
4. 前端任务日志增加片段编号和批次范围。
5. 自动检查全部通过：前端构建、Rust 格式化、Rust 编译、AI 14 项单元测试。

下一步：使用原 13 个片段在真实火山环境重新生成 AI 分镜。人工验收通过前不提交 Git。

## 2026-07-13：AI 核心链路人工验收记录

状态：验收暂停，2026-07-14 继续。

验收结果：
1. 多视频导入：通过。
2. 切片全部素材：通过。
3. 多来源片段进入统一素材池：通过。
4. 生成 AI 分镜：失败。13 个片段请求后仍为 0 个分镜，火山引擎等待响应超时。

错误截图：`E:\AI\Temp\codex-clipboard-77874e0d-bbda-4311-8abd-35db15ff33b8.png`

下一步：先定位是画面理解批次超时还是纯文字分镜规划超时，再做最小修复。本次未继续排查，未提交 Git，未 push。

## 2026-07-12：AI 主流程工作台 UI 优化

状态：开发完成，等待人工验收。

完成内容：
1. 重构素材库、预览与分镜、常用设置和底部任务栏的信息层级。
2. 将 AI 文案分镜提升为默认核心工作区。
3. 将低频混剪方式收进折叠区，隐藏未完成入口。
4. 底部改为根据当前进度变化的单一主按钮。
5. 完成桌面布局和窄屏适配，并通过前端构建检查。
6. 根据真实 2K 屏幕截图和竞品比例，放大大屏素材栏、视频预览、片段缩略图、AI 分镜图和常用文字。
7. 修复火山引擎请求在发送阶段长时间等待的问题，增加 HTTP/1.1 兼容模式、连接超时和可理解的错误分类。
8. 将 AI 请求拆成分批画面理解和纯文字分镜规划，增加会话内描述缓存和阶段进度。
9. 明确第一版音频规则：输入文案不生成配音，导出继续使用原片段声音和可选 BGM。
10. 修复多视频只切当前选中项的问题：全部导入视频依次切片，成功片段统一进入 AI 素材池。


## 2026-07-11：AI 文案逐句分镜匹配第一版

状态：开发完成，等待人工验收。

本次目标：
1. 文案拆成逐句分镜。
2. 每个分镜匹配主画面和备选画面。
3. 支持替换、上移、下移和删除。
4. 复用现有 VideoEngine 生成视频。

本次不做：TTS、字幕、音频识别和批量多版本。

## 2026-07-11：ECuTAuto 竞品与第一版 MVP 差距研究

状态：已完成。

本次完成：
1. 读取竞品飞书教程，整理产品定位、目标用户和核心工作流。
2. 核对用户提供的 11 张竞品 UI 截图。
3. 对比当前项目已有能力和竞品完整 AI 智能混剪体验。
4. 明确第一版必做、暂缓功能、48 小时顺序和验收标准。
5. 生成 Markdown 与 PDF 研究报告。

本次未修改业务代码。

## 2026-07-11：修复 AI HTTPS 连接失败

状态：已修复且自动检查通过，等待用户重新启动验证真实方舟请求。

问题：Windows 无法完成证书吊销状态查询，导致 Rust AI 请求在 HTTPS 握手阶段失败。

处理：AI 客户端使用内置可信根证书完成证书和域名校验，不依赖 Windows 在线吊销查询。

## 2026-07-11：AI 文案智能混剪 MVP

状态：开发和自动检查已完成，等待真实火山引擎环境人工验收。

本次目标：
1. 切片后准备片段编号、真实时长和预览图。
2. Rust 后端读取环境变量并调用火山引擎方舟多模态模型。
3. 严格校验 `orderedSegmentIds`。
4. 前端展示排序结果并支持上移、下移、删除。
5. 复用现有 `VideoEngine` 混剪导出链路生成视频。

本次不做：
1. 字幕烧录。
2. TTS。
3. 会员系统。
4. 复杂时间轴。
5. 完整视频上传。
6. AI 自动发布。

验收命令：
```text
corepack pnpm build
cargo fmt
cargo check
```

提交信息：
```text
feat(ai): add script based remix planning
```
