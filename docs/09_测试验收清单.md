# 测试验收清单

## 2026-08-11 全部真实功能集中人工验收

结论先说：自动化和真实输出已经通过。人工验收的目的，是确认你在桌面软件中看到的操作方式、画面效果和使用感受符合预期，而不是再次替开发代码兜底。

### 一、验收准备

1. 双击项目根目录的 `启动智剪.bat`。
2. 测试素材只使用 `E:\Workspace\测试素材库\Project_03_水印测试`：
   - 横屏：`水印测试源视频.mp4`
   - 竖屏：`竖屏预览测试.mp4`
   - BGM：`测试BGM.m4a`
   - 画中画：`画中画测试图.png`
3. 输出目录选择 `E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\人工验收`。
4. 分别在 1440×900 和 1280×720 窗口执行一次核心检查。

### 二、界面与主题

- [ ] 首页显示的“功能可用”模块都能进入真实页面；没有可点击但只有提示的假入口。
- [ ] AI 成片、视频效果处理、视频混剪和分类混剪的预览舞台都可见。
- [ ] 原比例、9:16、16:9、1:1 只改变内部画面/虚框，不改变播放器外壳。
- [ ] 珍珠雾白和夜航墨色往返切换后，右栏下拉框、数字框、复选框、按钮和二级弹窗均清晰可见。
- [ ] 1440×900 与 1280×720 都没有整页横向/纵向滚动条，底部任务栏没有遮挡预览。

### 三、切片真实结果

- [ ] 选择“自定义间隔”，设置 1.5 秒，能得到约 1.5、1.5、1 秒的三个片段。
- [ ] 设置去除片头/片尾后，片段内容和总时长同步变化。
- [ ] “文件名分类”在独立的源视频目录中生成 `segment_000.mp4` 等文件。
- [ ] “文件夹分类”按源文件父目录分组，文件名包含源视频名。
- [ ] “不分类”直接在所选根目录生成文件，多个源视频不会互相覆盖。

### 四、音频真实结果

- [ ] 主视频音量范围设置为固定 70%-70% 后，成片原声确实比 100% 小。
- [ ] 打开动态调整后可以正常导出，声音没有削波爆音。
- [ ] BGM“选择文件”能选择 `测试BGM.m4a`。
- [ ] BGM“选择文件夹”能从测试目录中随机选择支持的音频或视频。
- [ ] BGM 循环时，8 秒成片后半段仍有 BGM；不循环时成片不被截短，后半段只保留原声。
- [ ] 点击“清除”后路径消失、BGM 关闭，后续导出不再混入旧 BGM。

### 五、画中画与画面处理

- [ ] 外部素材模式选择 `画中画测试图.png` 后，图片真实出现在成片指定位置。
- [ ] 主视频模式选择 `竖屏预览测试.mp4` 后，背景铺满并虚化，主视频按设置大小和偏移覆盖。
- [ ] 拖动 AI 页裁剪框后导出，成片裁剪区域与界面一致。
- [ ] 文字拖动后会吸附到左上、右上、居中、左下或右下，成片位置与预览一致。
- [ ] 18 个入场效果逐项切换时预览表现不同，选择后能正常导出。

### 六、字幕与分段变速

- [ ] 字幕字体选择“黑体”、颜色选择橙色、透明度设置 65%，成片样式明显改变。
- [ ] 字幕顶部、居中、底部和小/中/大字号都能改变成片结果。
- [ ] 分段变速设置 0.65x-1.35x、每段 1-2 秒后，同一条成片内速度有变化，原声音画保持同步。
- [ ] 关闭字幕或关闭变速后再次导出，旧设置不会残留。

### 七、文件夹批量生成

- [ ] 多个文件夹的模式、输出分类或裂变数量冲突时，软件明确拒绝并说明哪项不一致。
- [ ] 文案模式按“文案数 × 每条裂变数”生成，不再取所有文件夹设置中的最大值。
- [ ] 独立拼接固定首素材不占用普通素材数量。
- [ ] 按文案分类时不同文案进入独立目录；不分类时直接进入根输出目录。
- [ ] 只有本批次所有裂变成片都成功，且成片文件真实存在时，才允许删除源文件。

### 八、自动验收证据

- [x] 前端生产构建通过。
- [x] Rust 格式、编译、严格 Clippy 通过。
- [x] Rust 162 项测试通过。
- [x] 真实分段变速、BGM 循环/不循环、两种画中画、裁剪字幕、横竖屏和三种切片分类已生成。
- [x] BGM 前 3 秒音频摘要一致；4.5 秒后的循环/不循环音频摘要不同，证明循环开关真实改变后半段结果。
- [x] 1440×900、1280×720、珍珠雾白、夜航墨色无页面溢出，浏览器控制台错误/警告为空。

自动结果目录：`E:\Workspace\测试结果库\Project_03_本地短视频批量混剪工作台\2026-08-11_真实功能闭环验收`。

### 九、最终验收结论

- [ ] 通过：所有项目符合预期，可以提交 Git。
- [ ] 不通过：记录具体页面、设置值、输入素材、输出文件和截图，再按单项问题修复。

## 2026-07-24 V0.4.1后台命令终端弹窗修复

- [x] FFmpeg/FFprobe调用已统一使用Windows隐藏窗口启动器。
- [x] Rust格式、编译、严格Clippy和129项测试通过。
- [x] V0.4.1正式安装包生成并隔离安装成功。
- [x] 恢复已有7个视频和16个片段时未出现终端窗口。
- [x] 导入新视频时不出现终端窗口。
- [x] 智能切片或固定切片时不出现终端窗口。
- [x] 导出视频时不出现终端窗口。
- [x] 2026-07-24用户人工验收通过。

## 2026-07-21 第四阶段用户测试包人工验收（通过）

自动检查已完成：

- [x] V0.4.0 NSIS安装包生成成功，大小115.84MB。
- [x] 安装包内置FFmpeg 8.1.1和FFprobe。
- [x] 本机隔离目录静默安装成功并真实启动。
- [x] 四步首次启动向导、配置状态、隐私说明和诊断报告真实显示正常。
- [x] 前端构建、Rust严格检查和128项测试通过。
- [x] 安装版主程序为Windows GUI子系统，启动不附带黑色终端窗口。
- [x] 项目恢复弹窗两个按钮文字和背景对比正常。

用户人工验收：

- [x] 双击安装包可以正常安装并打开“智剪 SmartCut”。
- [x] 首次启动向导四步清楚易懂，FFmpeg和FFprobe显示“安装包内置”。
- [x] AI、AI配音和语音识别状态与当前密钥配置一致。
- [x] 顶部“诊断”和首页“开始新手引导”都能重新打开向导。
- [x] 生成诊断报告成功，报告中没有API密钥、视频内容和文案正文。
- [x] 导入测试视频后，主要流程可正常使用。
- [x] 安装包没有要求手动安装FFmpeg或打开终端配置环境变量。
- [x] 正式版启动不附带黑色终端窗口，项目恢复弹窗按钮清晰可见。
- [x] 2026-07-21用户确认验收成功。

## 2026-07-18 云端ASR与本地文案库联动验收

- [x] 音频和带声音视频可以完成真实云端语音识别。
- [x] 完整文字和句子级开始/结束时间正常显示。
- [x] 相同未修改文件可以命中本地识别缓存。
- [x] ASR结果可以保存到本地文案库。
- [x] 文案库内容可以载入当前AI分镜文案。
- [x] 相同来源和相同文字不会重复保存。
- [x] 前端构建、Rust严格检查和104项测试通过。
- [x] 2026-07-18用户人工验收成功。

## 2026-07-16 AI配音主链路修复验收

- [x] 切片后至少2个片段准备成功即可显示文案输入框。
- [x] 少数片段预览图或时长失败时自动跳过，不清空其他成功片段。
- [x] 长文案由软件本地自动拆成固定短句，AI只匹配画面。
- [x] 备选片段与其他主片段冲突时自动清理，不让整份分镜失败。
- [x] 主画面过短时优先换用更长备选片段，只允许轻微减速和短补帧。
- [x] 无法安全适配配音时明确报错，不生成连续数秒定格的坏成片。

## 2026-07-15 自动字幕首轮反馈复验

- [ ] 含多个逗号的长文案会按停顿拆成多条短字幕依次出现。
- [ ] 没有标点的长文案不会一次挤满画面，会按可读长度拆分。
- [ ] 每条短字幕通常保持单行，默认字号和背景块观感协调。
- [ ] 短字幕顺序与AI配音内容基本一致，最后一条与该句配音同时结束。
- [ ] 原始素材中的竖屏封面完整可见，不再被横向缩略框裁切。
- [ ] 原声、BGM、画布、效果和最终导出仍正常。

## V0.1 基础可运行版

### V0.1 主链路人工验收记录

验收日期：2026-06-12

验收结论：
已通过基础闭环测试，可以进入 V0.2 基础混剪版。

已确认通过：

- [x] 可以导入视频
- [x] 可以浏览视频素材
- [x] 可以播放视频预览
- [x] 可以选择输出目录
- [x] 可以成功导出视频
- [x] 导出过程有基础日志

仍需后续边界测试：

- [ ] FFmpeg 路径包含空格或中文
- [ ] 视频路径包含空格或中文
- [ ] 输出目录包含空格或中文
- [ ] 磁盘空间不足
- [ ] 无音频视频
- [ ] 损坏视频文件

### 1. FFmpeg 环境检测

- [ ] 启动软件时可以检测 ffmpeg 是否存在
- [ ] 可以检测 ffprobe 是否存在
- [ ] 如果不存在，前端有明确提示
- [ ] 如果存在，能显示版本号
- [ ] ffmpeg 路径包含空格时可以正常检测
- [ ] ffmpeg 路径包含中文时可以正常检测

### 2. 视频导入

- [ ] 可以导入单个 mp4
- [ ] 可以导入文件夹
- [ ] 可以过滤非视频文件
- [ ] 导入后列表显示文件名
- [ ] 导入后能读取路径
- [ ] 文件名包含中文时可以正常导入
- [ ] 文件路径包含空格时可以正常导入
- [ ] 文件夹中混有非视频文件时不会崩溃

### 3. 视频信息读取

- [ ] 能读取视频时长
- [ ] 能读取分辨率
- [ ] 能读取帧率
- [ ] 能读取是否有音频
- [ ] 无音频视频可以正常识别
- [ ] 读取失败时有明确错误提示
- [ ] 读取失败不会导致软件崩溃

### 4. 视频预览

- [ ] 点击视频后可以预览
- [ ] 可以播放/暂停
- [ ] 切换视频不会崩溃
- [ ] 中文路径视频可以预览
- [ ] 空格路径视频可以预览
- [ ] 无法预览时有明确提示

### 5. 基础导出

- [ ] 可以选择输出目录
- [ ] 可以导出一个新 mp4
- [ ] 导出失败时有错误提示
- [ ] 导出完成后能打开输出目录
- [ ] 输出目录包含中文时可以正常导出
- [ ] 输出目录包含空格时可以正常导出
- [ ] 输出文件已存在时有处理策略
- [ ] 磁盘空间不足时有错误提示

### 6. 任务日志

- [ ] 任务开始有日志
- [ ] 任务进度能显示
- [ ] 任务失败能记录原因
- [ ] 任务完成能记录输出路径
- [ ] 日志不泄露 API Key 等敏感信息
- [ ] 连续执行任务时日志不会混乱
- [ ] 任务失败后软件仍可继续使用

## V0.1 禁止项检查

- [ ] 没有新增会员系统
- [ ] 没有新增 API Key 系统
- [ ] 没有新增 AI 内容理解
- [ ] 没有新增文案改写
- [ ] 没有新增 TTS
- [ ] 没有新增 ASR 字幕识别
- [ ] 没有新增高级去水印
- [ ] 没有新增复杂画中画
- [ ] 没有新增云端渲染
- [ ] 没有新增支付系统
- [ ] 没有进行 UI 大重构

## V0.1 架构检查

- [ ] 页面组件没有直接拼接 FFmpeg 命令
- [ ] 视频处理统一通过 VideoEngine
- [ ] 耗时任务通过任务队列管理
- [ ] 错误信息可以返回前端
- [ ] 任务日志可以定位失败原因
- [ ] 文件路径支持中文
- [ ] 文件路径支持空格

## 后续版本测试方向

### V0.2 基础混剪版

人工测试记录：

- [x] 2026-06-12：使用 2 分 47 秒视频，按 10 秒切片生成 17 个片段，随机抽取 5 个片段，最终成功拼接出约 46 秒的新 mp4 视频。
- [x] 2026-06-12：批量生成混剪结果已通过用户测试。
- [ ] 2026-06-12：基础水平镜像已开发，等待用户人工检查导出画面。
- [x] 2026-06-12：桌面软件工作台 UI 框架用户反馈勉强可用，后续继续优化。
- [x] 2026-06-12：基础变速已通过用户测试。

- [x] 可以固定时长切片
- [x] 可以随机抽取片段
- [x] 可以拼接片段
- [x] 可以批量导出
- [ ] 可以设置镜像
- [x] 可以设置变速
- [ ] 可以设置裁剪比例

### V0.2 UI 工作台框架

- [ ] 顶部可以看到软件名称、版本、模式和 FFmpeg 状态
- [ ] 左侧可以导入视频、导入文件夹、查看素材列表
- [ ] 左侧可以查看切片片段和随机抽取结果
- [ ] 中间可以预览当前视频
- [ ] 中间可以查看当前视频信息和导出结果
- [ ] 右侧可以设置切片秒数、抽取数量、批量生成数量、水平镜像
- [ ] 右侧可以选择输出目录、执行基础导出、切片、拼接和批量生成
- [ ] 底部可以查看输出目录和任务日志
- [x] UI 调整后原有功能入口基本可用，后续继续优化

### V0.2 基础变速

- [x] 页面可以设置变速倍数
- [x] 默认 1.0x 可以正常拼接导出
- [x] 1.2x 或 1.5x 可以导出更短的视频
- [x] 0.8x 可以导出更长的视频
- [x] 批量生成可以应用同一个变速倍数
- [ ] 小于 0.5 或大于 2.0 时有明确错误提示
- [x] 页面组件没有直接拼接 FFmpeg 变速命令

### V0.2 裁剪比例

- [ ] 页面可以选择原画、9:16 竖屏、1:1 方屏、16:9 横屏
- [ ] 原画模式保持原始画面比例
- [ ] 9:16 模式可以导出竖屏比例视频
- [ ] 1:1 模式可以导出方屏比例视频
- [ ] 16:9 模式可以导出横屏比例视频
- [ ] 单次片段拼接可以应用裁剪比例
- [ ] 批量生成可以应用同一个裁剪比例
- [ ] 裁剪比例可以和水平镜像、基础变速一起使用
- [ ] 页面组件没有直接拼接 FFmpeg 裁剪命令

### V0.3 文案 / TTS / 字幕版

- [x] 可以输入文案
- [x] 可以生成并试听本地MP3语音（TTS第一阶段，2026-07-15人工验收通过）
- [x] 可以按AI逐句分镜生成带配音视频（2026-07-15人工验收通过）
- [x] 配音比主画面长时优先换用更长备选片段，只允许轻微减速和短补帧
- [x] 配音比画面短时裁短画面
- [x] 配音视频原片段声音默认静音
- [x] 可以主动保留完整原视频声音并调节音量（2026-07-15人工验收通过）
- [x] 无音轨片段打开原声开关后仍能正常生成
- [ ] 配音视频可以叠加现有BGM
- [x] 可以为AI逐句配音视频生成字幕（主体功能首轮人工验收通过）
- [ ] 可以设置字幕顶部、中间和底部位置
- [ ] 可以设置小、标准和大字号
- [ ] 关闭自动字幕后成片不出现字幕
- [ ] 长文案会自然拆成短字幕依次显示，中文、数字和标点正常
- [ ] 临时ASS字幕文件自动清理
- [ ] 可以导出带语音和字幕的视频

### V0.4 分类混剪版

- [ ] 可以给素材分类
- [ ] 可以按分类抽取素材
- [ ] 可以按分类规则生成视频

### V0.5 会员 / API Key 版

- [x] API Key 不明文保存
- [x] 日志不输出完整 API Key
- [x] 会员卡密不是纯本地判断
- [x] 会员状态有远程校验和服务端签名
- [x] 同一账号换电脑登录可恢复会员
- [x] 修改密码会使其他设备旧会话失效
- [x] 管理员可视化工具可以生成、停用和恢复卡密

### V0.6 AI 内容理解版

- [ ] 可以提炼视频内容
- [ ] 可以识别字幕或语音内容
- [ ] 可以生成素材标签
- [ ] 可以根据文案辅助匹配素材

### 2026-07-13 AI 核心链路人工验收

- [x] 可以一次导入多个视频。
- [x] 可以切片全部已导入素材。
- [x] 多个来源视频的片段可以合并进入统一素材池。
- [x] 13 个片段可以成功生成 AI 分镜；关闭模型深度思考后，纯文字规划不再超时。
- [x] AI 分镜可以展示并进入人工调整和 VideoEngine 导出链路。
- [x] 2026-07-14：图片理解已改为单图批次，并通过前端构建、Rust 编译和 AI 单元测试。
- [x] 2026-07-14：使用原 13 个片段重新生成 AI 分镜，人工验收通过。
---

## V0.2 视频比例画布适配

状态：已开发，待人工验收。

### 基础预览检查

- [ ] 原画模式下，预览比例跟随原视频。
- [ ] 9:16 模式下，预览区变成竖屏画布。
- [ ] 1:1 模式下，预览区变成方屏画布。
- [ ] 16:9 模式下，预览区变成横屏画布。
- [ ] 切换比例时，原视频完整保留，不裁掉主体内容。
- [ ] 选择黑边背景时，空白区域为黑色。
- [ ] 选择模糊背景时，背景为同视频放大模糊效果。
- [ ] 播放器底部播放、暂停、时长条仍然可用。

### 导出检查

- [ ] 基础导出可以应用当前比例和背景方式。
- [ ] 片段拼接导出可以应用当前比例和背景方式。
- [ ] 批量生成可以应用当前比例和背景方式。
- [ ] 9:16 导出结果实际为竖屏比例。
- [ ] 1:1 导出结果实际为方屏比例。
- [ ] 16:9 导出结果实际为横屏比例。
- [ ] 黑边导出效果和预览逻辑一致。
- [ ] 模糊背景导出效果和预览逻辑一致。

### 组合检查

- [ ] 视频比例可以和水平镜像一起使用。
- [ ] 视频比例可以和基础变速一起使用。
- [ ] 原画模式不影响已有导出链路。
- [ ] 页面组件没有直接拼接 FFmpeg 命令。
