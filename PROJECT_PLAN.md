# PROJECT_PLAN

## 项目目标

开发一个面向零基础用户的本地桌面端 AI 视频批量混剪工作台，核心流程为：

```text
导入素材 → 切片 → AI 理解画面 → 文案逐句匹配 → 人工调整 → VideoEngine 导出
```

## 当前阶段

稳定生产基础、智能切片、多帧理解、内容提炼、AI自动分类、云端ASR、可视化音色库、水印工具、SmartCut第一版UI和第四阶段普通用户测试包均已于2026-07-21通过人工验收。

## 开发顺序

1. 整理共享类型、日志和成片设置。
2. 整理 AI 混剪功能模块。
3. 整理素材导入和切片模块。
4. 整理普通混剪和导出模块。
5. 拆分设置弹窗和全局样式。
6. 按用户确认顺序开发 TTS、图片转视频和视频基础工具。
7. 普通用户测试包、FFmpeg内置、首次启动向导和诊断能力。（已完成并验收）
8. 商业阶段再处理会员远程验证、用户中心、代码签名和自动更新。

## 固定边界

1. 不重写整个项目。
2. 不绕过 Rust VideoEngine 直接拼接 FFmpeg 命令。
3. API Key 不进入前端、日志或 Git。
4. 一个阶段只治理一个功能，自动检查和人工验收通过后再继续。
5. 当前唯一下一步以 `TODO_NEXT.md` 为准。
